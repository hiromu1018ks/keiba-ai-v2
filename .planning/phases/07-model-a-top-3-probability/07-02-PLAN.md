---
phase: 07-model-a-top-3-probability
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/ml/data_loader.py
  - tests/ml/test_data_loader.py
autonomous: true
requirements: [MODA-01]

must_haves:
  truths:
    - "data_loader.load_features(feature_path, train_window, holdout_window) が features_train.parquet を読み込み学習窓・ホールドアウトに分割して返す"
    - "7つの string dtype カラム(course_name/surface/direction/weather/track_condition/sex/grade)が category dtype に変換される"
    - "horse_race_id が race_id + horse_number から derive され、standard/entry.parquet の horse_race_id と join 整合する"
    - "grade の NaN が category 化後も保持される(95% NaN が code -1 で維持される)"
    - "学習窓(2018-2024, exclude_from_training=False) = 322,510 行 / 23,288 レース・ホールドアウト(2025-01〜2026-05, exclude=False) = 66,343 行 / 4,740 レースが expected_counts=None(デフォルト) の時のみ assert される（Codex HIGH #4 fix: 小規模 fixture では expected_counts=[] で assert を bypass）"
    - "audit_leakage([RaceSchema, EntrySchema]) を呼び出し post-race 列混入(popularity/win_odds)を検出する"
  artifacts:
    - path: "src/ml/data_loader.py"
      provides: "load_features(feature_path, train_window, holdout_window, expected_counts=None) -> dict[train, holdout, metadata] と categorical/dtype 整備・horse_race_id derive・audit_leakage 再実行。expected_counts=None で本番行数 assert・expected_counts=[] で assert bypass（Codex HIGH #4 fix）"
      exports: ["load_features"]
      min_lines: 85
    - path: "tests/ml/test_data_loader.py"
      provides: "MODA-01 data loading テスト(categorical 変換・horse_race_id derive・leakage audit・window 行数・grade NaN)"
  key_links:
    - from: "src/ml/data_loader.py"
      to: "src/pipeline/feature_generator.py"
      via: "from src.pipeline.feature_generator import FEATURE_COLUMNS, CATEGORICAL_COLUMNS"
      pattern: "from src.pipeline.feature_generator import"
    - from: "src/ml/data_loader.py"
      to: "src/schemas/audit.py"
      via: "audit_leakage([RaceSchema, EntrySchema], df, 'phase7 feature load')"
      pattern: "audit_leakage"
    - from: "src/ml/data_loader.py"
      to: "data/feature/features_train.parquet"
      via: "pd.read_parquet(feature_path, engine='pyarrow')"
      pattern: "read_parquet"
---

<objective>
features_train.parquet を読み込み、Phase 7 学習に必要な dtype 整備・horse_race_id derive・window 分割・リーク監査を行う data_loader モジュールを実装する。RESEARCH.md Pitfall #2/#3/#4/#7 に対応する実測ベリファイドの修正を全てエンコードする。

**Codex HIGH #4 fix (fixed-count assert breaks hermetic E2E):** 旧 load_features は `assert len(train_df) == 322510` / `assert len(holdout_df) == 66343` を無条件で実行したため、07-07 の小規模 fixture（10-15レース）を使った hermetic E2E テストが必ず AssertionError で失敗した。新仕様: `expected_counts` パラメータを追加し、`expected_counts=None`（デフォルト）の時のみ本番行数を assert・`expected_counts=[]` の時は assert を bypass する。これで本番実行（07-08 phase gate）では行数検証を維持しつつ、hermetic テスト（07-07）では小規模 fixture が通る。

Purpose: LightGBM 学習(trainer)・ベースライン(baseline)・オーケストレーション(run_train)が共通して使う feature 読込境界。string dtype の7カラムを category 化し(Pitfall #3)、horse_race_id を derive して D-15 成果物 parquet の必須カラムを満たし(Pitfall #2)、学習/ホールドアウト窓を行数 assert で固定する(Pitfall #7・ただし expected_counts で bypass 可能・Codex HIGH #4)。
Output: src/ml/data_loader.py (load_features 関数・expected_counts パラメータ付き) + tests/ml/test_data_loader.py (5テスト GREEN)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-model-a-top-3-probability/07-CONTEXT.md
@.planning/phases/07-model-a-top-3-probability/07-RESEARCH.md
@.planning/phases/07-model-a-top-3-probability/07-PATTERNS.md
@.planning/phases/07-model-a-top-3-probability/07-REVIEWS.md
@.planning/phases/07-model-a-top-3-probability/07-01-SUMMARY.md
@src/pipeline/feature_generator.py
@src/schemas/audit.py
@src/schemas/race.py
@src/schemas/entry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/ml/data_loader.py 実装 — load_features (expected_counts パラメータ付き・Codex HIGH #4 fix) + dtype 整備 + horse_race_id derive + audit_leakage + window 分割</name>
  <files>src/ml/data_loader.py</files>
  <read_first>
    - .planning/phases/07-model-a-top-3-probability/07-REVIEWS.md (§ Concerns HIGH #4 — load_features の固定行数 assert が 07-07 hermetic E2E を破る・「make expected_counts optional, or only verify fixed counts under the production config」fix)
    - src/pipeline/feature_generator.py (lines 37-107 — CATEGORICAL_COLUMNS, FEATURE_COLUMNS, ENTITY_KEY の定義。data_loader が import して再利用。lines 690-717 convert_to_categorical の object 条件が string を見逃す問題箇所。lines 757-820 load_and_merge の Parquet I/O + Path validation パターン。lines 994-1003 audit_leakage 再実行パターン)
    - src/schemas/audit.py (lines 45-84 — audit_leakage(model_classes, df, context) -> list[str] シグネチャ・D-12 warning のみで raise しない挙動)
    - src/schemas/race.py, src/schemas/entry.py (RaceSchema, EntrySchema の model_fields・pre_race 分類・get_post_race_columns が popularity/win_odds を post-race と判定することを確認)
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Common Pitfalls #2/#3/#4/#7 — VERIFIED コード/データ実測に基づく修正要件)
    - .planning/phases/07-model-a-top-3-probability/07-PATTERNS.md (§ src/ml/data_loader.py — analog 割当と CRITICAL な dtype 実測・horse_race_id derive パターン)
  </read_first>
  <behavior>
    - Test test_categorical_conversion: features_train の course_name/surface/direction/weather/track_condition/sex/grade が load_features 後に category dtype になる（実測: 7列は string・jockey/trainer のみ既に category）
    - Test test_horse_race_id_derive: load_features が horse_race_id 列を追加し、形式は f"{race_id}{horse_number:02d}"（アンダースコアなし・ゼロパディング2桁）。standard/entry.parquet の horse_race_id と完全一致
    - Test test_leakage_audit: features_train に popularity/win_odds が含まれない場合 audit_leakage が空リストを返す。意図的に popularity 列を混入した fixture では audit_leakage が ["popularity"] を返す
    - Test test_train_holdout_window_counts: 学習窓(2018-01-01..2024-12-31, exclude_from_training=False) = 322,510 行 / 23,288 レース・ホールドアウト窓(2025-01-01..2026-05-31, exclude=False) = 66,343 行 / 4,740 レース。**expected_counts=None（デフォルト）の時のみ assert・expected_counts=[] で bypass（Codex HIGH #4 fix）**
    - Test test_expected_counts_bypass (Codex HIGH #4): 小規模 fixture（例: 10レース）で load_features(expected_counts=[]) を呼び、行数 assert が bypass されて正常に戻り値が返ることを assert。expected_counts=None で小規模 fixture を渡すと AssertionError になることも確認（本番行数と不一致のため）。
    - Test test_grade_nan_preserved: grade 列の NaN カウントが category 化前後で同一（features_train では 95% NaN・約506,349件）
  </behavior>
  <action>
src/ml/data_loader.py を作成。loguru logger と絶対 import（from src.xxx import ...）を使用。以下の関数と定数を実装:

(1) import: `from pathlib import Path`, `import pandas as pd`, `import numpy as np`, `from loguru import logger`, `from src.pipeline.feature_generator import FEATURE_COLUMNS, CATEGORICAL_COLUMNS`, `from src.schemas.audit import audit_leakage`, `from src.schemas.entry import EntrySchema`, `from src.schemas.race import RaceSchema`。

(2) `load_features(feature_path: str | Path = "data/feature/features_train.parquet", train_window: tuple[str, str] = ("2018-01-01", "2024-12-31"), holdout_window: tuple[str, str] = ("2025-01-01", "2026-05-31"), entry_path: str | Path = "data/standard/entry.parquet", expected_counts: dict | None = None) -> dict`:
  - **Codex HIGH #4 fix**: expected_counts パラメータを追加。None（デフォルト）= 本番行数 {train_rows: 322510, train_races: 23288, holdout_rows: 66343, holdout_races: 4740} を assert。空 dict {} = assert bypass（hermetic テスト用）。dict with specific keys = その値で assert（柔軟性）。
  - feature_path を Path() でラップ、`is_file()` で存在確認（T-03-01 パターン・analog load_and_merge lines 780-803）。FileNotFoundError を raise。
  - `pd.read_parquet(feature_path, engine="pyarrow")` で読込。logger.info で読込元と行数を出力（per-table ログのみ・per-row 禁止・MEMORY.md scraper-logging-no-per-item）。
  - race_date を datetime に変換: `df["race_date"] = pd.to_datetime(df["race_date"])`（features_train は string dtype・実測確認済み）。
  - audit_leakage 再実行（D-12・Pitfall post-race 混入検出）: `leaked = audit_leakage([RaceSchema, EntrySchema], df, "phase7 feature load")`。ResultSchema は除外（race_id が post-race 扱いになるため・PATTERNS.md 指示）。`if leaked: logger.warning(f"Leakage detected: {leaked}")` で warning のみ・raise しない（D-12 互換）。
  - CATEGORICAL_COLUMNS 全列を `astype("category")` で無条件変換（Pitfall #3・analog convert_to_categorical の `dtype == object` 条件は string を見逃すため無条件化）。jockey/trainer は既に category のため再変換は冪等。logger.info で変換列数を出力。
  - grade NaN 保持検証（Pitfall #4）: 変換前後で `pd.isna(df["grade"]).sum()` が一致することを内部 assert。不一致なら logger.warning。
  - horse_race_id derive（Pitfall #2・CRITICAL CORRECTION）: `df["horse_race_id"] = df["race_id"].astype(str) + df["horse_number"].astype(int).astype(str).str.zfill(2)`。**RESEARCH.md A5 は f"{race_id}_{horse_number:02d}"（アンダースコア付き）と仮定したが、data/standard/entry.parquet の実測確認では形式はアンダースコアなし f"{race_id}{horse_number:02d}"（例: race_id=201501010101, horse_number=2 → "20150101010102"）。EntrySchema docstring の "{race_id}_{horse_number:02d}" は誤り。実データ優先でアンダースコアなし形式を採用。**
  - join 整合性 assert（Open Question #1 推奨）: entry.parquet の horse_race_id と derive 結果を merge し、一致率を logger.info 出力。entry_path が存在しない場合は warning のみで継続（テスト環境配慮）。
  - window 分割: train_df = df[(race_date >= train_window[0]) & (race_date <= train_window[1]) & (~exclude_from_training)]、holdout_df = df[(race_date >= holdout_window[0]) & (race_date <= holdout_window[1]) & (~exclude_from_training)]。**exclude_from_training=True（取消/除外馬・target_top3=0・result_status in {removed,scratched}）は学習・評価双方から除外（実測: 学習窓 1,240件除外 → 322,510 有効行・ホールドアウト 231件除外 → 66,343 有効行）。RESEARCH.md の「66,574」は除外含む全行・「66,343」が評価有効行。**
  - **行数 assert（Pitfall #7・D-01/D-02・Codex HIGH #4 fix）**: expected_counts is None の時のみ本番固定値を assert（PRODUCTION_COUNTS = {"train_rows": 322510, "train_races": 23288, "holdout_rows": 66343, "holdout_races": 4740}）。expected_counts == {} の時は assert bypass（hermetic テスト用・logger.info で "expected_counts bypassed" を出力）。expected_counts が特定キーを持つ dict の時はその値で assert。不一致は AssertionError。logger.info で境界値・行数・レース数・expected_counts モード（本番/bypass/カスタム）を出力。
  - 戻り値 dict: {"train": train_df, "holdout": holdout_df, "metadata": {"train_rows": len(train_df), "train_races": int(train_df["race_id"].nunique()), "holdout_rows": len(holdout_df), "holdout_races": int(holdout_df["race_id"].nunique()), "leaked_columns": leaked, "feature_columns": FEATURE_COLUMNS}}。
  </action>
  <verify>
    <automated>python -m pytest tests/ml/test_data_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/test_data_loader.py -x -q` が6テスト全て GREEN（test_categorical_conversion, test_horse_race_id_derive, test_leakage_audit, test_train_holdout_window_counts, test_expected_counts_bypass, test_grade_nan_preserved）
    - `python -c "from src.ml.data_loader import load_features; r = load_features(); print(r['metadata'])"` が train_rows=322510, train_races=23288, holdout_rows=66343, holdout_races=4740 を出力する（本番データ・expected_counts=None デフォルト）
    - `grep -c "astype.*category" src/ml/data_loader.py` が 1 以上（CATEGORICAL_COLUMNS の category 変換実装）
    - `grep -c "horse_race_id" src/ml/data_loader.py` が 2 以上（derive + join 整合性）
    - `grep -c "audit_leakage" src/ml/data_loader.py` が 1 以上（リーク監査呼び出し）
    - `grep -c "expected_counts" src/ml/data_loader.py` が 2 以上（パラメータ定義 + assert 制御・Codex HIGH #4 fix）
    - `grep -c "322510\|23288\|66343\|4740" src/ml/data_loader.py` が 1 以上（PRODUCTION_COUNTS 定数）
    - features_train に popularity/win_odds は含まれない（audit_leakage が空リストを返す・純粋予測×EV 構図 D-15/03-CONTEXT D-15 準拠）
    - **test_expected_counts_bypass が load_features(expected_counts=[]) で小規模 fixture が AssertionError なく通ることを assert（Codex HIGH #4 fix）**
  </acceptance_criteria>
  <done>load_features が expected_counts パラメータで本番行数 assert と hermetic bypass を切り替えられ、dtype 整備・horse_race_id derive・window 分割を全て行い、6テストが GREEN になる</done>
</task>

<task type="auto">
  <name>Task 2: tests/ml/test_data_loader.py の skip を外し、6テストを GREEN にする（test_expected_counts_bypass 含む）</name>
  <files>tests/ml/test_data_loader.py</files>
  <read_first>
    - tests/ml/conftest.py (07-01 で作成された sample_feature_df / sample_entry_df / tmp_ml_output_dir / ml_config fixture の定義を確認)
    - tests/pipeline/test_feature_generator.py::TestLoadMerge (lines 95-145 — プロパティベース検証パターン。assert の書き方・docstring の "Test N:" 形式)
    - src/ml/data_loader.py (Task 1 で実装した load_features のシグネチャと戻り値 dict 構造・expected_counts パラメータ)
  </read_first>
  <action>
tests/ml/test_data_loader.py の TestFeatureLoad クラスに定義された6テスト（07-01 で skip 状態・test_expected_counts_bypass を追加）の skip を外し、実装する。各テストは conftest.py の hermetic fixture を使う（外部データ依存なし・tests/pipeline パターン）。ただし test_train_holdout_window_counts と test_grade_nan_preserved は features_train.parquet の実データに依存するため @pytest.mark.gated を付け、RUN_GATED=1 のみ実行（デフォルト skip・CI では走らない）。test_categorical_conversion / test_horse_race_id_derive / test_leakage_audit / test_expected_counts_bypass は conftest の sample_feature_df で hermetic 実行。

- test_categorical_conversion: sample_feature_df を load_features 相当の dtype 整備で処理し、CATEGORICAL_COLUMNS 全列が category dtype になることを assert。`assert df["course_name"].dtype.name == "category"` 等を7列分行う。
- test_horse_race_id_derive: sample_feature_df で derive された horse_race_id が sample_entry_df の horse_race_id と一致することを assert。形式 f"{race_id}{horse_number:02d}"（アンダースコアなし）。`assert (derived == entry["horse_race_id"]).all()`。
- test_leakage_audit: sample_feature_df に popularity 列が含まれないことを確認。意図的に `df["popularity"] = 1.0` を追加した fixture で audit_leakage が ["popularity"] を返すことを assert（post-race 混入検出の健全性）。
- test_train_holdout_window_counts (@pytest.mark.gated): load_features() を実データで呼び出し、metadata の train_rows=322510 / train_races=23288 / holdout_rows=66343 / holdout_races=4740 を assert。expected_counts=None（デフォルト）で本番行数が assert されることを確認。
- test_expected_counts_bypass (Codex HIGH #4・hermetic): conftest の小規模 sample_feature_df を tmp_path の parquet に書き出し、load_features(feature_path=tmp_parquet, expected_counts=[]) を呼び、行数 assert が bypass されて正常に戻り値 dict が返ることを assert。len(result["train"]) + len(result["holdout"]) が小規模 fixture の行数（本番 322510+66343 ではない）と一致することを確認。さらに expected_counts=None（デフォルト）で同 fixture を渡すと AssertionError になることを assert（本番行数と不一致のため・bypass が必要であることの証明）。
- test_grade_nan_preserved (@pytest.mark.gated): features_train の grade NaN カウントが category 化前後で一致することを assert。

各テストの docstring に対応する MODA-01 / Pitfall #X / D-XX / Codex HIGH #4 を引用（analog パターン）。クラス docstring に「MODA-01: feature loading with dtype safety, horse_race_id derivation, leakage audit. expected_counts parameter enables hermetic E2E (Codex HIGH #4 fix)」を明記。
  </action>
  <verify>
    <automated>python -m pytest tests/ml/test_data_loader.py -x -q && python -m pytest tests/ml/test_data_loader.py -k "test_train_holdout_window_counts or test_grade_nan_preserved" --run-gated 2>/dev/null || echo "gated tests require RUN_GATED=1"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/test_data_loader.py -x -q` が hermetic 4テスト（test_categorical_conversion, test_horse_race_id_derive, test_leakage_audit, test_expected_counts_bypass）GREEN・gated 2テスト skip で完走
    - `python -m pytest tests/ml/ -q` が 0 failed（他の tests/ml/ は still skip）
    - test_categorical_conversion の assert が course_name/surface/direction/weather/track_condition/sex/grade 全列をカバー
    - test_horse_race_id_derive が アンダースコアなし形式 `f"{race_id}{horse_number:02d}"` で一致することを検証（RESEARCH A5 仮定のアンダースコア形式ではない）
    - **test_expected_counts_bypass が load_features(expected_counts=[]) で小規模 fixture が通ることと、expected_counts=None で AssertionError になることの両方を assert（Codex HIGH #4 fix）**
    - `python -m pytest tests/ml/test_data_loader.py -k test_train_holdout_window_counts --run-gated -x` が RUN_GATED=1 環境で GREEN（322510 / 23288 / 66343 / 4740）
  </acceptance_criteria>
  <done>test_data_loader.py の6テストが hermetic 4件 GREEN + gated 2件 skip（RUN_GATED=1 で GREEN）になる。expected_counts bypass が検証される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/feature/features_train.parquet → src/ml/ | feature 層 parquet が data_loader に入る（post-race 混入リスク） |
| data/standard/entry.parquet → src/ml/ | entry（horse_race_id 整合性確認用・popularity は post-race） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering (Data leakage) | data_loader: post-race 列混入 | mitigate | audit_leakage([RaceSchema, EntrySchema]) を load_features 内で呼び出し popularity/win_odds 等の post-race 列を検出（D-12・PATTERNS § audit_leakage 再実行パターン）。ResultSchema は除外（race_id post-race 誤検出回避）。warning のみ・D-12 準拠 |
| T-07-02-02 | Tampering (Pitfall #2 horse_race_id) | data_loader: horse_race_id derive | mitigate | race_id + horse_number から derive し entry.parquet と join 整合性を assert。RESEARCH A5 仮定のアンダースコア形式ではなく実データのアンダースコアなし形式を採用（VERIFIED） |
| T-07-02-03 | Tampering (Pitfall #3 dtype) | data_loader: categorical 変換 | mitigate | CATEGORICAL_COLUMNS 全列を無条件 astype("category")。feature_generator の dtype==object 条件は string を見逃すため（VERIFIED） |
| T-07-02-04 | Tampering (Pitfall #7 window) | data_loader: train/holdout window | mitigate | 行数・レース数を assert で固定（PRODUCTION_COUNTS = 322510/23288/66343/4740・VERIFIED）。D-01(2018-2024)/D-02(2025-2026/5) 境界を明示。exclude_from_training=True は両窓から除外（取消/除外馬・target=0・poisoning 回避）。**expected_counts パラメータで本番 assert と hermetic bypass を切り替え（Codex HIGH #4 fix）** |
| T-07-02-05 | Tampering (Codex HIGH #4 hermetic E2E 破壊) | data_loader: 固定行数 assert | mitigate | **expected_counts=None（デフォルト）= 本番行数 assert・expected_counts=[] = bypass。07-07 hermetic E2E は expected_counts=[] で小規模 fixture が通り、07-08 phase gate は expected_counts=None で本番行数を検証（Codex HIGH #4 fix）** |
</threat_model>

<verification>
- load_features が features_train.parquet を読み込み dtype 整備・horse_race_id derive・window 分割を行う
- audit_leakage が popularity/win_odds 混入を検出する（純粋予測×EV 構図 D-15）
- expected_counts パラメータで本番行数 assert と hermetic bypass を切り替え（Codex HIGH #4 fix）
- hermetic 4テスト GREEN + gated 2テスト skip で pytest 完走
- 既存テストスイート破壊なし
</verification>

<success_criteria>
1. load_features が MODA-01 の feature 読込要件を満たす（dtype・horse_race_id・window・leakage）
2. 6テストが意図通り hermetic GREEN + gated skip で動作
3. RESEARCH Pitfall #2/#3/#4/#7 の全てが実装に反映され VERIFIED データと一致
4. 純粋予測×EV 構図（popularity/win_odds 除外）が audit_leakage で担保される
5. Codex HIGH #4（固定行数 assert が hermetic E2E を破る）が expected_counts パラメータで解決される
</success_criteria>

<output>
Create `.planning/phases/07-model-a-top-3-probability/07-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New public symbols created by this plan (consumed by Wave 1 sibling plans + Wave 2 run_train):
- Function: `load_features(feature_path, train_window, holdout_window, entry_path, expected_counts=None) -> dict` in `src/ml/data_loader.py`
  - Returns: `{"train": pd.DataFrame, "holdout": pd.DataFrame, "metadata": dict}`
  - expected_counts=None (default): asserts production row counts (322510/23288/66343/4740). expected_counts=[]: bypasses assertion for hermetic fixtures (Codex HIGH #4 fix).
  - Constant: `PRODUCTION_COUNTS = {"train_rows": 322510, "train_races": 23288, "holdout_rows": 66343, "holdout_races": 4740}`
  - Imports re-used (not created): `FEATURE_COLUMNS`, `CATEGORICAL_COLUMNS` from `src.pipeline.feature_generator`; `audit_leakage` from `src.schemas.audit`; `RaceSchema`, `EntrySchema`
- New columns produced on output DataFrames: `horse_race_id` (derived, str format `f"{race_id}{horse_number:02d}"` no underscore)
- Test class: `TestFeatureLoad` in `tests/ml/test_data_loader.py` with methods: test_categorical_conversion, test_horse_race_id_derive, test_leakage_audit, test_train_holdout_window_counts (gated), test_expected_counts_bypass (Codex HIGH #4), test_grade_nan_preserved (gated)

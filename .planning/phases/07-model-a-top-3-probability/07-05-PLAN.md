---
phase: 07-model-a-top-3-probability
plan: 05
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/ml/calibrator.py
  - tests/ml/test_calibrator.py
autonomous: true
requirements: [MODA-04]

must_haves:
  truths:
    - "fit_calibrator(oof_raw, y_oof) が IsotonicRegression(y_min=0, y_max=1, out_of_bounds='clip') を返す"
    - "キャリブレーターは OOF のみで fit し holdout は predict のみ（D-10・Pitfall #5 リーク防止）"
    - "apply_calibrator の出力が [0, 1] 範囲に制限され単調非減少である"
    - "calibrator が joblib.dump で .joblib 形式で保存可能（D-15）"
  artifacts:
    - path: "src/ml/calibrator.py"
      provides: "fit_calibrator, apply_calibrator, save_calibrator, load_calibrator — Isotonic OOF→holdout リーク防止パターン"
      exports: ["fit_calibrator", "apply_calibrator", "save_calibrator", "load_calibrator"]
      min_lines: 50
    - path: "tests/ml/test_calibrator.py"
      provides: "MODA-04 キャリブレーションテスト（リーク防止・[0,1] 範囲・単調非減少・保存/読込）"
  key_links:
    - from: "src/ml/calibrator.py"
      to: "sklearn.isotonic.IsotonicRegression"
      via: "IsotonicRegression(y_min=0.0, y_max=1.0, out_of_bounds='clip').fit(oof_raw, y_oof)"
      pattern: "IsotonicRegression"
    - from: "src/ml/calibrator.py"
      to: "joblib"
      via: "joblib.dump(iso, path) / joblib.load(path)"
      pattern: "joblib"
---

<objective>
OOF 予測で IsotonicRegression を学習し、ホールドアウト raw 予測に適用するキャリブレーターモジュールを実装する。RESEARCH.md Pattern 2（Isotonic キャリブレーション・リーク防止標準パターン）と Pitfall #5（holdout でキャリブレーターを再 fit するとリーク）をエンコードする。

Purpose: MODA-04「OOF 予測による確率キャリブレーション」の中核。OOF は各 fold モデルが学習していない馬の予測（GroupTimeSeriesSplit 保証）のため、キャリブレーターは「未知データ上の予測分布」を学習しリークなし。Phase 8 Harville EV 計算の精度に直接効く安全側設計（D-10）。
Output: src/ml/calibrator.py (4関数) + tests/ml/test_calibrator.py (4テスト GREEN)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-model-a-top-3-probability/07-CONTEXT.md
@.planning/phases/07-model-a-top-3-probability/07-RESEARCH.md
@.planning/phases/07-model-a-top-3-probability/07-PATTERNS.md
@.planning/phases/07-model-a-top-3-probability/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/ml/calibrator.py 実装 — fit_calibrator + apply_calibrator + save/load（リーク防止 Isotonic）</name>
  <files>src/ml/calibrator.py</files>
  <read_first>
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Architecture Patterns Pattern 2 lines 335-375 — Isotonic キャリブレーションのフルコード例。§ Common Pitfalls #5 lines 429-434 — holdout でキャリブレーターを再 fit するとリーク。§ Code Examples lines 340-375 — fit_calibrator/apply_calibrator VERIFIED コード)
    - .planning/phases/07-model-a-top-3-probability/07-PATTERNS.md (§ src/ml/calibrator.py — analog compute_finish_time_zscore の race-boundary リーク防止 docstring パターン)
    - .planning/phases/07-model-a-top-3-probability/07-CONTEXT.md (D-10 Isotonic OOF→holdout・D-12 calibrator 保存 + p_top3_raw 併存・D-11 ECE<0.02 成功基準)
  </read_first>
  <behavior>
    - Test test_leak_free_calibration: OOF 予測 + y_oof で fit_calibrator し、holdout raw 予測に apply_calibrator を適用。キャリブレーターが holdout のラベルを見ていないことを検証（holdout の正解ラベルを渡す経路がない・Pitfall #5）。holdout ECE が OOF ECE より極端に良くない（0.0 に近すぎない）ことを assert。
    - Test test_isotonic_output_in_01_range: apply_calibrator の出力が全て [0.0, 1.0] に収まることを assert（y_min=0, y_max=1, out_of_bounds='clip'）。
    - Test test_isotonic_monotonic_non_decreasing: ソート済み raw 予測を apply_calibrator にかけると出力も単調非減少であることを assert（IsotonicRegression の保証）。
    - Test test_save_load_roundtrip: save_calibrator で .joblib 保存し load_calibrator で読込んだ後、apply_calibrator の結果が保存前と同一であることを assert（D-15 .joblib 形式・joblib.dump/load）。
  </behavior>
  <action>
src/ml/calibrator.py を作成。RESEARCH.md Pattern 2（lines 340-375）の VERIFIED コードをベースにする。loguru logger・絶対 import。以下の4関数を実装:

(1) import: `import joblib`, `import numpy as np`, `from pathlib import Path`, `from loguru import logger`, `from sklearn.isotonic import IsotonicRegression`。

(2) `fit_calibrator(oof_raw: np.ndarray, y_oof: np.ndarray) -> IsotonicRegression`:
  - RESEARCH Pattern 2 lines 348-365 の VERIFIED コードを採用。
  - **docstring にリーク防止理由を明記**（analog compute_finish_time_zscore の race-boundary safety docstring パターン踏襲）: 「D-10/D-12 リーク防止: oof_raw は各 fold モデルが学習していない馬の予測（GroupTimeSeriesSplit で保証）。したがってキャリブレーターは未知データ上の予測分布を学習。holdout で再 fit してはならない（Pitfall #5）」。
  - iso = IsotonicRegression(y_min=0.0, y_max=1.0, out_of_bounds="clip")。
  - iso.fit(oof_raw, y_oof)。
  - logger.info で fit 行数・入力予測範囲を出力。
  - return iso。

(3) `apply_calibrator(iso: IsotonicRegression, raw_preds: np.ndarray) -> np.ndarray`:
  - RESEARCH Pattern 2 lines 368-371 準拠。iso.predict(raw_preds) を返す。
  - docstring に「holdout にも使用可能・リークなし（fit は OOF のみ・D-10）」を明記。

(4) `save_calibrator(iso: IsotonicRegression, path: str | Path) -> Path`:
  - path を Path() でラップ・path.parent.mkdir(parents=True, exist_ok=True)。
  - joblib.dump(iso, path)。**D-15 が .joblib 形式を指定**（pickle ではない）。
  - logger.info で保存先を出力。return path。

(5) `load_calibrator(path: str | Path) -> IsotonicRegression`:
  - path 存在確認・joblib.load(path)。return iso。
  </action>
  <verify>
    <automated>python -m pytest tests/ml/test_calibrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/test_calibrator.py -x -q` が4テスト GREEN
    - `grep -c "IsotonicRegression" src/ml/calibrator.py` が 1 以上
    - `grep -c "y_min=0.0\|y_max=1.0\|out_of_bounds" src/ml/calibrator.py` が 1 以上（D-10 [0,1] clip 設定）
    - `grep -c "joblib.dump\|joblib.load" src/ml/calibrator.py` が 2 以上（save/load 実装・D-15 .joblib）
    - `python -c "from src.ml.calibrator import fit_calibrator, apply_calibrator, save_calibrator, load_calibrator; print('ok')"` が ok を出力
    - fit_calibrator の docstring に OOF/holdout/リーク防止（Pitfall #5）の言及がある（grep -c "OOF\|holdout\|リーク\|leak" が 1 以上）
    - test_isotonic_output_in_01_range が (result >= 0.0).all() and (result <= 1.0).all() を assert
    - test_isotonic_monotonic_non_decreasing が np.diff(result) >= 0 を assert
  </acceptance_criteria>
  <done>calibrator.py の4関数が Isotonic OOF→holdout リーク防止パターンで動作し、4テストが GREEN になる</done>
</task>

<task type="auto">
  <name>Task 2: tests/ml/test_calibrator.py の skip を外し、4テストを GREEN にする</name>
  <files>tests/ml/test_calibrator.py</files>
  <read_first>
    - tests/ml/conftest.py (sample_feature_df / tmp_ml_output_dir / ml_config fixture 確認)
    - tests/pipeline/test_feature_generator.py::TestFinishTimeZscore (lines 1013-1297 — リーク防止テスト設計パターン。手動で構築した小規模データで期待値を計算して比較する手法)
    - src/ml/calibrator.py (Task 1 で実装した4関数のシグネチャ)
  </read_first>
  <action>
tests/ml/test_calibrator.py の TestCalibrator クラスに定義された4テスト（07-01 で skip 状態）の skip を外し実装する。手動で構築した小規模データ（analog TestFinishTimeZscore パターン）で期待値を計算して比較する。

- test_leak_free_calibration: OOF raw 予測（例: np.array([0.1, 0.3, 0.5, 0.7, 0.9])）と y_oof（例: np.array([0, 0, 1, 1, 1])）で fit_calibrator。holdout raw（例: np.array([0.2, 0.4, 0.6, 0.8])）に apply_calibrator を適用。キャリブレーター関数が holdout の正解ラベルを引数に取らないことを検証（シグネチャ確認）。apply_calibrator(iso, holdout_raw) のみで holdout y は渡さない。
- test_isotonic_output_in_01_range: 極端な入力（負値や 1.0 超を含む raw 予測）を apply_calibrator にかけ、出力が全て [0.0, 1.0] に clip されることを assert。out_of_bounds='clip' の検証。
- test_isotonic_monotonic_non_decreasing: ソート済み raw 予測 np.array([0.1, 0.2, 0.3, 0.5, 0.8]) を apply_calibrator にかけ、np.diff(result) >= 0 を assert（IsotonicRegression の単調非減少保証）。
- test_save_load_roundtrip: tmp_ml_output_dir に save_calibrator で .joblib 保存・load_calibrator で読込・apply_calibrator 結果が保存前と np.allclose で一致することを assert（D-15 .joblib ラウンドトリップ健全性）。

各テスト docstring に「Test N: MODA-04 ... (D-10 Isotonic OOF→holdout / D-12 calibrator save / Pitfall #5 holdout recalibration forbidden)」を明記。クラス docstring に「MODA-04: leak-free isotonic calibration — fit on OOF only, predict on holdout」を記載。
  </action>
  <verify>
    <automated>python -m pytest tests/ml/test_calibrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/test_calibrator.py -x -q` が4テスト GREEN
    - test_leak_free_calibration が apply_calibrator のシグネチャに holdout y が含まれないことを検証
    - test_isotonic_output_in_01_range が (result >= 0.0).all() and (result <= 1.0).all() を assert
    - test_isotonic_monotonic_non_decreasing が np.diff(result) >= 0 を assert
    - test_save_load_roundtrip が .joblib ファイルを tmp_path に作成し、ラウンドトリップ一致を assert
    - `python -m pytest tests/ml/ -q` が 0 failed（07-02/07-03/07-04 と並存）
  </acceptance_criteria>
  <done>calibrator.py の4テストが GREEN になり、リーク防止・[0,1] 範囲・単調非減少・保存/読込が検証される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OOF 予測 → calibrator fit | OOF raw が IsotonicRegression の学習データ（fold モデル未学習馬の予測） |
| holdout raw → apply_calibrator | holdout は predict のみ（fit に使うとリーク・Pitfall #5） |
| calibrator → .joblib ファイル | モデル直列化（改竄リスク・gitignore で保護） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering (Calibration leakage・Pitfall #5) | calibrator: holdout で再 fit | mitigate | fit_calibrator は OOF のみ（D-10）。apply_calibrator は predict のみ・holdout の正解ラベルを引数に取らない。docstring + test_leak_free_calibration でリーク防止を検証。holdout ECE が OOF ECE より極端に良くないことで検出 |
| T-07-05-02 | Tampering (D-12 契約) | calibrator: 保存形式 | mitigate | joblib.dump で .joblib 形式（D-15 指定・pickle ではない）。save/load ラウンドトリップテストで健全性担保 |
| T-07-05-03 | Tampering (範囲違反) | apply_calibrator: 出力範囲 | mitigate | IsotonicRegression(y_min=0.0, y_max=1.0, out_of_bounds='clip') で [0,1] 保証。test_isotonic_output_in_01_range で検証 |
</threat_model>

<verification>
- fit_calibrator が OOF のみで IsotonicRegression を学習し apply_calibrator が holdout に適用可能
- 出力が [0,1] 範囲・単調非減少
- .joblib 保存/読込がラウンドトリップ一致
- 4テスト GREEN
</verification>

<success_criteria>
1. MODA-04「OOF 予測による確率キャリブレーション」のキャリブレーター中核が完成
2. RESEARCH Pitfall #5（holdout 再 fit リーク）が docstring + テストで構造的に防止される
3. D-10（OOF→holdout）・D-12（calibrator save + p_top3_raw 併存）・D-15（.joblib 形式）契約を満たす
4. Phase 8 Harville EV 計算の精度に直結する安全側設計
</success_criteria>

<output>
Create `.planning/phases/07-model-a-top-3-probability/07-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New public symbols created by this plan (consumed by 07-06 evaluator + 07-07 run_train):
- Function: `fit_calibrator(oof_raw, y_oof) -> IsotonicRegression` in `src/ml/calibrator.py`
- Function: `apply_calibrator(iso, raw_preds) -> np.ndarray` in `src/ml/calibrator.py`
- Function: `save_calibrator(iso, path) -> Path` in `src/ml/calibrator.py`
- Function: `load_calibrator(path) -> IsotonicRegression` in `src/ml/calibrator.py`
- Test class: `TestCalibrator` in `tests/ml/test_calibrator.py` with methods: test_leak_free_calibration, test_isotonic_output_in_01_range, test_isotonic_monotonic_non_decreasing, test_save_load_roundtrip

---
phase: 07-model-a-top-3-probability
plan: 08
type: execute
wave: 3
depends_on: ["07-07"]
files_modified: []
autonomous: false
requirements: [MODA-01, MODA-02, MODA-03, MODA-04]

must_haves:
  truths:
    - "python -m src.ml.run_train が実データ features_train.parquet（53万行）で完走し、D-15 の6成果物を生成する"
    - "ホールドアウト AUC（目安0.75以上）と ECE（<0.02）が metrics.json に記録され、人間が確認できる"
    - "人気ベースライン AUC が参考情報として報告される（D-07/D-08 必須成功条件ではない）"
    - "reliability_diagram.png が目視可能な品質で生成される"
    - "Phase 7 成功判定（D-07 + D-11）が人間の verify で判定される"
    - "CONTEXT D-05 LOCKS the train window to 2018-2024 (D-01) and holdout to 2025-01〜2026-05 (D-02). ROADMAP 成功基準#1 文言「trained on 2015-2023」更新は Phase 9 繰越（D-05）。Phase 7 成功判定は D-01/D-02 で行い、stale な ROADMAP「2015-2023」文言ではなく D-01（2018-2024 学習窓）+ D-02（ホールドアウト窓）が正しい契約。ROADMAP-contract divergence（2015-2023 → 2018-2024 学習）は 07-08-SUMMARY.md に文書化する（Phase 6 D-07 と同パターン）。"
  artifacts:
    - path: "models/phase7/model_a.lgb.txt"
      provides: "訓練済み LightGBM モデル（Phase 8 Harville EV に引き渡す）"
    - path: "models/phase7/isotonic_calibrator.joblib"
      provides: "Isotonic キャリブレーター（Phase 8/9 が p_top3_calibrated を利用）"
    - path: "data/model/oof/oof_predictions.parquet"
      provides: "OOF 予測（race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold）"
    - path: "data/model/oof/holdout_predictions.parquet"
      provides: "ホールドアウト予測（fold='holdout'・Phase 9 walk-forward の基準）"
    - path: "reports/phase7/metrics.json"
      provides: "全評価指標（auc_calibrated, ece_calibrated, brier, logloss, baseline_auc 等）"
    - path: "reports/phase7/reliability_diagram.png"
      provides: "reliability diagram 可視化（D-11）"
    - path: "reports/phase7/evaluation_report.md"
      provides: "評価レポート（指標表 + D-08 純粋予測×EV 構図注記）"
  key_links:
    - from: "python -m src.ml.run_train"
      to: "config/phase7_model_a.yaml + 全 src/ml/ モジュール"
      via: "run_train(config_path) が Wave 1+2 の全コンポーネントを orchestrate"
      pattern: "run_train"
    - from: "reports/phase7/metrics.json"
      to: "人間 verify（D-07 AUC目安0.75 + D-11 ECE<0.02）"
      via: "metrics.json の holdout_auc / holdout_ece を確認"
      pattern: "auc_calibrated|ece_calibrated"
---

<objective>
Phase 7 の最終検証: 実データ features_train.parquet（534,953行×78列）で run_train を実行し、D-15 の全6成果物を生成、ホールドアウト AUC（D-07 目安0.75以上）と ECE（D-11 <0.02）で Phase 7 成功判定を人間が確認する phase gate プラン。

Purpose: hermetic テスト（07-07）で検証済みの run_train を本番データで完走させ、Phase 8（Harville EV）/ Phase 9（walk-forward backtest）に引き渡す成果物を実証する。D-07（AUC目安0.75 + キャリブレーション成功）と D-11（ECE<0.02）の成功判定を実データで評価し、reliability diagram を目視確認する。
Output: 全6成果物（実データ生成）+ 人間 verify による Phase 7 成功判定。ソースファイル変更なし（実行・検証のみ）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-model-a-top-3-probability/07-CONTEXT.md
@.planning/phases/07-model-a-top-3-probability/07-RESEARCH.md
@.planning/phases/07-model-a-top-3-probability/07-VALIDATION.md
@.planning/phases/07-model-a-top-3-probability/07-07-SUMMARY.md
@config/phase7_model_a.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 実データ features_train.parquet で python -m src.ml.run_train を実行し6成果物を生成</name>
  <files>(実行のみ・ソース変更なし。成果物は gitignored ディレクトリに生成)</files>
  <read_first>
    - config/phase7_model_a.yaml（07-07 で作成。feature_path/train_window/holdout_window/artifacts パスを確認・本番パス data/feature/features_train.parquet を指す）
    - reports/phase7/ と models/phase7/ と data/model/oof/ が存在しないか空であることを確認（前回実行の残骸がないか）
    - src/ml/run_train.py（07-07 で実装。run_train 関数の戻り値 dict 構造を確認）
  </read_first>
  <action>
config/phase7_model_a.yaml の data.feature_path が data/feature/features_train.parquet を指すことを確認（07-07 で設定済み）。その後 `python -m src.ml.run_train` を実行（setuptools・`poetry run` 禁止・MEMORY.md）。

実行は Mac 単体で数分以内に完了する想定（RESEARCH § State of the Art・53万行×78列は CPU で十分・LightGBM native categorical で8x高速）。実行中は loguru ログで各ステップ（load_features / OOF 収集 / キャリブレーター fit / final model 学習 / holdout 予測 / 評価 / 成果物保存）の進行を確認。

実行完了後、以下の6成果物が生成されたことを確認:
1. models/phase7/model_a.lgb.txt（LightGBM .txt 形式）
2. models/phase7/isotonic_calibrator.joblib（joblib 形式）
3. data/model/oof/oof_predictions.parquet（race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold 列）
4. data/model/oof/holdout_predictions.parquet（同構造・fold="holdout"）
5. reports/phase7/metrics.json（auc_calibrated, ece_calibrated, brier, logloss, baseline_auc 等）
6. reports/phase7/evaluation_report.md + reliability_diagram.png

OOF parquet の行数が学習窓有効行数（322,510）に一致すること・holdout parquet の行数がホールドアウト有効行数（66,343）に一致することを確認。
  </action>
  <verify>
    <automated>python -m src.ml.run_train 2>&1 | tail -20 && python -c "
import json, os
from pathlib import Path
artifacts = [
    'models/phase7/model_a.lgb.txt',
    'models/phase7/isotonic_calibrator.joblib',
    'data/model/oof/oof_predictions.parquet',
    'data/model/oof/holdout_predictions.parquet',
    'reports/phase7/metrics.json',
    'reports/phase7/evaluation_report.md',
    'reports/phase7/reliability_diagram.png',
]
for a in artifacts:
    p = Path(a)
    assert p.exists() and p.stat().st_size > 0, f'MISSING or EMPTY: {a}'
    print(f'OK {a} ({p.stat().st_size} bytes)')
import pandas as pd
oof = pd.read_parquet('data/model/oof/oof_predictions.parquet')
hold = pd.read_parquet('data/model/oof/holdout_predictions.parquet')
print('OOF rows:', len(oof), 'holdout rows:', len(hold))
print('OOF cols:', list(oof.columns))
m = json.load(open('reports/phase7/metrics.json'))
print('metrics keys:', list(m.keys()))
print('holdout_auc_calibrated:', m.get('auc_calibrated'))
print('holdout_ece_calibrated:', m.get('ece_calibrated'))
print('baseline_auc:', m.get('baseline_auc'))
"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m src.ml.run_train` がエラーなく完走し、loguru ログで全7ステップの完了が出力される
    - 7成果物ファイル（model .txt, calibrator .joblib, oof parquet, holdout parquet, metrics.json, evaluation_report.md, reliability_diagram.png）が全て存在し size > 0
    - OOF parquet 行数が 322,510（学習窓有効行・D-01）に一致
    - holdout parquet 行数が 66,343（ホールドアウト有効行・D-02）に一致
    - OOF/holdout parquet 列が {race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold} に完全一致（D-15）
    - holdout parquet の fold 列が全て "holdout" である
    - metrics.json に auc_calibrated / ece_calibrated / baseline_auc が含まれる
    - reliability_diagram.png が目視可能（size > 10KB 程度・空画像でない）
  </acceptance_criteria>
  <done>run_train が実データで完走し、D-15 の6成果物が正しい行数・schema で生成される</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 7 成功判定 — ホールドアウト AUC（D-07 目安0.75以上）と ECE（D-11 <0.02）と reliability diagram を人間が確認</name>
  <files>(検証のみ・ソース変更なし。reports/phase7/metrics.json + reports/phase7/reliability_diagram.png + reports/phase7/evaluation_report.md を確認)</files>
  <action>Phase 7 Model A が実データで学習・評価され、D-07（ホールドアウト AUC 目安0.75以上）と D-11（ECE<0.02）の成功判定を人間が確認する phase gate。人気ベースライン AUC は参考情報（D-08 で必須条件ではない・純粋予測×EV 構図）。</action>
  <what-built>
Phase 7 Model A（LightGBM 3着内確率）が実データ features_train.parquet（534,953行×78列・2015-2026/5）で学習・評価され、以下が生成された:
1. 訓練済みモデル（2018-2024 全量再学習・単一 .txt）と Isotonic キャリブレーター（.joblib）
2. OOF 予渓（322,510行）とホールドアウト予測（66,343行・2025-01〜2026-05）
3. 評価レポート: metrics.json + evaluation_report.md + reliability_diagram.png
成功判定は D-07（ホールドアウト AUC 目安0.75以上 + キャリブレーション成功）と D-11（ECE<0.02）。人気ベースライン AUC は参考情報（D-08 で必須条件ではない・純粋予測×EV 構図）。
  </what-built>
  <how-to-verify>
1. metrics.json の確認:
   `cat reports/phase7/metrics.json` を実行し、以下を確認:
   - `auc_calibrated` が 0.75 以上であること（D-07 目安・厳密値は実測次第で discretion・Claude's Discretion 領域）
   - `ece_calibrated` が 0.02 未満であること（D-11 成功基準・厳格）
   - `baseline_auc`（人気ベースライン）との比較を確認（D-08: 純粋モデルが人気を超えるのは困難・参考情報）

2. reliability diagram の目視:
   `open reports/phase7/reliability_diagram.png`（Mac）で画像を開き、以下を確認:
   - 各 bin の棒（観測 top-3 率）が点線（完全校正線）に近いこと
   - 極端に外れた bin がないこと（ECE<0.02 と整合）

3. evaluation_report.md の確認:
   `cat reports/phase7/evaluation_report.md` を実行し、指標表と D-08 純粋予測×EV 構図の注記を確認。

4. OOF/holdout 予渓の健全性（オプション）:
   `python -c "import pandas as pd; oof = pd.read_parquet('data/model/oof/oof_predictions.parquet'); print(oof['p_top3_calibrated'].describe()); print('fold values:', oof['fold'].unique())"` で p_top3_calibrated の分布が [0,1] で妥当（平均が陽性率21.7%に近い）であることを確認。
  </how-to-verify>
  <resume-signal>Type "approved" と入力（成功判定 OK の場合）。もしくは metrics.json の具体的な問題点（AUC が低い・ECE が高い等）を記述。問題がある場合は Phase 7 の再調整（config ハイパラの sensible defaults 調整・D-13 discretion 領域）を協議する。</resume-signal>
  <verify>
    <automated>python -c "import json; m=json.load(open('reports/phase7/metrics.json')); print('auc_calibrated=', m['auc_calibrated'], 'ece_calibrated=', m['ece_calibrated'], 'baseline_auc=', m['baseline_auc'])"</automated>
    <human-check>metrics.json の auc_calibrated / ece_calibrated / baseline_auc を確認 + reliability_diagram.png を目視</human-check>
  </verify>
  <done>人間が Phase 7 成功判定（D-07 AUC目安0.75以上 + D-11 ECE<0.02 + reliability diagram 品質）を approved する、または再調整の協議を開始する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 実データ features_train.parquet → run_train | 本番データで学習・評価（リークチェーンの最終検証） |
| 成果物 → ディスク | gitignored ディレクトリに保存（改竄リスク低・ローカルのみ） |
| 成果物 → 人間 verify | metrics.json と reliability_diagram.png を人間が評価（Phase gate） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering (リークチェーン最終検証) | run_train 実データ実行 | mitigate | 07-02..07-07 の各モジュールで検証済みのリーク防止（audit_leakage / GroupTimeSeriesSplit / Isotonic OOF-only fit）を実データで最終確認。OOF 行数=322,510 / holdout 行数=66,343 が assert と一致 |
| T-07-08-02 | Information disclosure (D-07/D-11 成功判定) | metrics.json 評価 | mitigate | 人間 verify で AUC目安0.75 / ECE<0.02 を判定。ECE が OOF より異常に良い場合は Pitfall #5（キャリブレーター holdout 再 fit）のリーク疑いで確認（07-05 で構造的に防止済み・最終検証） |
| T-07-08-03 | Tampering (成果物改竄) | 成果物ファイル | accept | models/phase7/, data/model/oof/, reports/phase7/ を .gitignore で保護（07-07 で追記）。ローカル保存・外部公開なし・機密性低（馬名/騎手名は feature 層に既存） |
| T-07-08-04 | Information disclosure (Phase 9 引き渡し) | 成果物 → Phase 8/9 | accept | Phase 8 が model_a.lgb.txt + calibrator.joblib + holdout 予測を消費。Phase 9 が walk-forward でモデル・推論コード・キャリブレーター・GroupTimeSeriesSplit を再利用 |
</threat_model>

<verification>
- run_train が実データで完走し6成果物が生成される
- ホールドアウト AUC / ECE / baseline AUC が metrics.json に記録される
- reliability diagram が目視可能
- 人間が Phase 7 成功判定（D-07 + D-11）を確認
</verification>

<success_criteria>
1. python -m src.ml.run_train が実データで完走する
2. D-15 の6成果物が正しい行数・schema で生成される
3. ホールドアウト AUC（目安0.75以上）と ECE（<0.02）が人間 verify で判定される（D-07/D-11）
4. reliability diagram が品質良く生成される
5. Phase 8/9 が消費する成果物が全て揃う
6. Phase 7 成功（approved）または再調整が必要かが明確になる
</success_criteria>

<output>
Create `.planning/phases/07-model-a-top-3-probability/07-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan (Wave 3 phase gate) produces NO new source symbols — it executes the 07-07 run_train entry point and generates the runtime artifacts (all gitignored). The artifacts are the D-15 contract deliverables, produced by running `python -m src.ml.run_train`:
- Runtime files: `models/phase7/model_a.lgb.txt`, `models/phase7/isotonic_calibrator.joblib`, `data/model/oof/oof_predictions.parquet` (322,510 rows), `data/model/oof/holdout_predictions.parquet` (66,343 rows, fold="holdout"), `reports/phase7/metrics.json`, `reports/phase7/evaluation_report.md`, `reports/phase7/reliability_diagram.png`
- These artifacts are consumed by Phase 8 (Harville EV uses model + calibrator + p_top3_calibrated) and Phase 9 (walk-forward reuses model code, calibrator, GroupTimeSeriesSplit, holdout predictions as baseline)
- No new classes/functions/exports are created in this plan. It is a phase-gate execution + human-verify checkpoint.

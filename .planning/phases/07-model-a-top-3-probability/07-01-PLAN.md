---
phase: 07-model-a-top-3-probability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/ml/__init__.py
  - tests/ml/__init__.py
  - tests/ml/conftest.py
  - tests/ml/test_data_loader.py
  - tests/ml/test_group_timeseries_split.py
  - tests/ml/test_trainer.py
  - tests/ml/test_calibrator.py
  - tests/ml/test_evaluator.py
  - tests/ml/test_baseline.py
autonomous: false
requirements: [MODA-01, MODA-02, MODA-03, MODA-04]
user_setup:
  - service: libomp
    why: "LightGBM の lib_lightgbm.dylib が @rpath/libomp.dylib を要求（未導入で import lightgbm が OSError で失敗・実測確認済み）"
    env_vars: []
    dashboard_config:
      - task: "brew install libomp を実行（Mac Homebrew 必須）"
        location: "ターミナル: brew install libomp"

must_haves:
  truths:
    - "import lightgbm が OSError なしで成功する"
    - "import sklearn / import matplotlib / import joblib が成功する"
    - "python -m pytest tests/ml/ -q が collection エラーなく実行できる（テスト本体は skip または xfail）"
    - "tests/ml/conftest.py の hermetic fixture が feature 層のカラム構造（categorical mix・target_top3 含む）を再現する"
  artifacts:
    - path: "src/ml/__init__.py"
      provides: "src.ml パッケージマーカー（Phase 4 パターンの import-safe empty marker）"
      contains: "src.ml"
    - path: "tests/ml/__init__.py"
      provides: "tests.ml パッケージマーカー"
    - path: "tests/ml/conftest.py"
      provides: "Wave 1 テストが使う hermetic fixture（小規模 race データ・categorical mix・target_top3 含む）"
      min_lines: 60
    - path: "tests/ml/test_data_loader.py"
      provides: "MODA-01 テスト骨格（categorical 変換・horse_race_id derive・leakage audit）"
    - path: "tests/ml/test_group_timeseries_split.py"
      provides: "MODA-02 テスト骨格（同一 race 同一 fold・時系列順・境界割れ検出）"
    - path: "tests/ml/test_trainer.py"
      provides: "MODA-01 テスト骨格（学習実行・early stopping・OOF 収集）"
    - path: "tests/ml/test_calibrator.py"
      provides: "MODA-04 テスト骨格（リーク防止・[0,1] 範囲・単調非減少）"
    - path: "tests/ml/test_evaluator.py"
      provides: "MODA-04 テスト骨格（ECE 健全性・reliability diagram 生成）"
    - path: "tests/ml/test_baseline.py"
      provides: "MODA-03 テスト骨格（人気ベースライン AUC・NaN 処理）"
  key_links:
    - from: "pyproject.toml [project] dependencies"
      to: "lightgbm/scikit-learn/matplotlib/joblib"
      via: "pip install で pyproject.toml 宣言と一致"
      pattern: "(scikit-learn|matplotlib|joblib)"
    - from: "pyproject.toml [tool.pytest.ini_options] markers"
      to: "tests/ml/ gated 統合テスト"
      via: "@pytest.mark.gated + RUN_GATED=1 env var"
      pattern: "gated"
---

<objective>
Phase 7 の実行前提となる環境整備（libomp・scikit-learn・matplotlib・joblib）と、tests/ml/ テスト骨格（conftest fixture・6テストファイルの skipping 雛形・gated marker）を整える Wave 0 プラン。

Purpose: 後続 Wave 1 の src/ml/ モジュール実装が TDD サイクル（RED→GREEN）で進むよう、まず「テストが実行できる環境」と「hermetic fixture」を用意する。RESEARCH.md § Environment Availability で実測確認された4つの依存ギャップ（libomp import 失敗・sklearn/matplotlib/joblib 未導入）を解消し、VALIDATION.md § Wave 0 Requirements の全8項目を作成する。
Output: pyproject.toml（依存追記 + gated marker）、src/ml/__init__.py（empty marker）、tests/ml/ 配下8ファイル（conftest + 6テスト + __init__）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-model-a-top-3-probability/07-CONTEXT.md
@.planning/phases/07-model-a-top-3-probability/07-RESEARCH.md
@.planning/phases/07-model-a-top-3-probability/07-PATTERNS.md
@.planning/phases/07-model-a-top-3-probability/07-VALIDATION.md
@src/pipeline/feature_generator.py
@tests/pipeline/conftest.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: brew install libomp（Mac Homebrew・Claude 代替不可）</name>
  <files>(環境のみ・ファイル変更なし)</files>
  <read_first>
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Environment Availability — libomp 未導入で import lightgbm が OSError で失敗する実測記録)
  </read_first>
  <action>
RESEARCH.md § Environment Availability で実測確認済み: LightGBM の lib_lightgbm.dylib が @rpath/libomp.dylib を動的ロードし、libomp 未導入状態では `import lightgbm` が `OSError: dlopen(... Library not loaded: @rpath/libomp.dylib)` で失敗する。これは Mac の Homebrew 依存関係であり Claude が CLI で自動インストールできない（brew はユーザ権限・対話環境前提）。ユーザがターミナルで `brew install libomp` を実行すること。完了後、Claude が `python -c "import lightgbm"` で検証する。
  </action>
  <verify>
    <automated>python -c "import lightgbm; print('lightgbm', lightgbm.__version__)"</automated>
    <human-check>brew install libomp の実行（ユーザ環境でのみ可能）</human-check>
  </verify>
  <acceptance_criteria>
    - `python -c "import lightgbm; print(lightgbm.__version__)"` が OSError なしで "4.6.0" を出力する
    - `python -c "from lightgbm import early_stopping; print('ok')"` が成功する（callback API の存在確認・Pitfall #1 前提）
  </acceptance_criteria>
  <done>libomp 導入により import lightgbm が成功し、Wave 1 の trainer 実装が実行可能になる</done>
</task>

<task type="auto">
  <name>Task 2: pip install scikit-learn matplotlib joblib + pyproject.toml 依存追記 + gated marker 追加</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml（現在の [project] dependencies・[tool.pytest.ini_options] markers を確認・setuptools であり Poetry ではないことを再確認）
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Standard Stack・§ Environment Availability — sklearn/matplotlib/joblib の用途と未導入確認)
  </read_first>
  <action>
pyproject.toml の [project] dependencies に "scikit-learn>=1.9", "matplotlib>=3.10", "joblib>=1.4", "pyyaml>=6" を追記（setuptools・MEMORY.md「Repo uses setuptools, not Poetry」準拠・`poetry run` 禁止）。既存 dependencies（pydantic, pandas, numpy, lightgbm, loguru, tqdm, pyarrow, playwright, beautifulsoup4, lxml, click）は維持。pyyaml は既に pyproject.toml で依存宣言済みか確認し、未宣言の場合のみ追加（RESEARCH § Standard Stack で pyyaml 6.x が導入済みと記録されているため二重宣言を避ける）。その後 `pip install scikit-learn matplotlib joblib` を実行。

次に [tool.pytest.ini_options] markers リストに gated marker を追加（既存 `live` marker と同列）: `"gated: tests that require the real feature corpus / RUN_GATED=1 (skipped by default; requires RUN_GATED=1)"`。これは tests/ml/test_run_train.py（Wave 2）と tests/ml の gated 統合テスト用（PATTERNS.md § Test Structure・tests/scraper/test_end_to_end.py:713 の @pytest.mark.live パターン踏襲）。

パッケージ正当性（RESEARCH § Package Legitimacy Audit）: scikit-learn/scikit-learn_contrib, matplotlib/matplotlib, joblib/joblib は全て10年以上の正規権威ソース。seam は PyPI ダウンロード数を取得できず SUS 誤検知だが、CLAUDE.md 推奨スタック + pyproject.toml lightgbm>=4.6 既宣言済みと整合。blocking-human チェック不要。
  </action>
  <verify>
    <automated>python -c "import sklearn, matplotlib, joblib, yaml; print('sklearn', sklearn.__version__, 'matplotlib', matplotlib.__version__, 'joblib', joblib.__version__)" && grep -q "scikit-learn" pyproject.toml && grep -q "matplotlib" pyproject.toml && grep -q "joblib" pyproject.toml && grep -q "gated" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import sklearn, matplotlib, joblib, yaml"` が ModuleNotFoundError なしで成功する
    - `grep -c "scikit-learn" pyproject.toml` が 1 以上、`grep -c "matplotlib" pyproject.toml` が 1 以上、`grep -c "joblib" pyproject.toml` が 1 以上（[project] dependencies に追記済み）
    - `grep -c "gated" pyproject.toml` が 1 以上（[tool.pytest.ini_options] markers に gated 追加済み）
    - `python -m pytest tests/ -q --co 2>&1 | tail -3` が collection エラーなく既存 513 passed/1 skipped ベースを維持（markers 追記で既存テスト破壊なし）
  </acceptance_criteria>
  <done>scikit-learn/matplotlib/joblib/pyyaml が import 可能になり、gated marker が pytest 設定に登録される</done>
</task>

<task type="auto">
  <name>Task 3: tests/ml/ パッケージ + conftest hermetic fixture + 6テストファイル骨格（skip 版）作成</name>
  <files>src/ml/__init__.py, tests/ml/__init__.py, tests/ml/conftest.py, tests/ml/test_data_loader.py, tests/ml/test_group_timeseries_split.py, tests/ml/test_trainer.py, tests/ml/test_calibrator.py, tests/ml/test_evaluator.py, tests/ml/test_baseline.py</files>
  <read_first>
    - tests/pipeline/conftest.py（hermetic fixture パターン・sample_race_result_df / sample_standard_*_df の作り方・docstring の "Key design" セクション形式）
    - src/pipeline/feature_generator.py (lines 37-107 — CATEGORICAL_COLUMNS, RACE_FEATURES, HORSE_FEATURES, LAG_*, PERSON_FEATURES, DEBUT_FEATURE, ENTITY_KEY, FEATURE_COLUMNS の定義。conftest fixture が再現すべき feature 層カラム構造の権威)
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Validation Architecture Test Map — 各テストファイルのテストケース名と期待動作)
    - .planning/phases/07-model-a-top-3-probability/07-PATTERNS.md (各 tests/ml/test_*.py の analog 割当と "planner 指示")
  </read_first>
  <action>
(1) src/ml/__init__.py を Phase 4 パターン（src/scraper/__init__.py と同様）の import-safe empty marker として作成: モジュール docstring のみ、公開 re-export は空（Wave 2 の run_train plan で遷移）。内容は `"""ML training/evaluation package for Model A (top-3 probability). Public API re-exports are added in Plan 07-07."""` のみ。

(2) tests/ml/__init__.py を空パッケージマーカーとして作成。

(3) tests/ml/conftest.py を tests/pipeline/conftest.py パターンで作成。以下の hermetic fixture を定義（外部データ依存なし・tmp_path 使用・MEMORY.md scraper-logging-no-per-item 準拠）:
  - `sample_feature_df`: 小規模 feature 層再現 DataFrame。最低5レース・時系列順（race_date 昇順・2018-2024 窓を含む）・各レース3-5頭・categorical mix（course_name/surface/direction/weather/track_condition/sex は string dtype・jockey/trainer は category dtype・grade は一部 NaN 含む）。カラムは FEATURE_COLUMNS の主要列 + target_top3 + exclude_from_training + horse_number + race_id + race_date を含む。docstring の "Key design" セクションで「なぜこのサイズ・カーディナリティか」を明記（analog パターン）。
  - `sample_entry_df`: popularity/win_odds/horse_race_id/race_id/horse_number を含む小規模 entry。popularity に NaN を1-2行含め cancel/scratched 再現。
  - `tmp_ml_output_dir`: tmp_path 配下の出力ディレクトリ fixture。
  - `ml_config`: config/phase7_model_a.yaml の辞書版。RESEARCH § Code Examples "Sensible Defaults Config YAML" の構造（seed/data/cv/model/early_stopping/calibration/evaluation/artifacts）を最小 fixture 用に縮小。num_leaves=31, learning_rate=0.05, min_data_in_leaf=20（テスト用に小さく）, n_estimators=50, stopping_rounds=10。

(4) tests/ml/test_data_loader.py, test_group_timeseries_split.py, test_trainer.py, test_calibrator.py, test_evaluator.py, test_baseline.py の6ファイルを作成。各ファイルは RESEARCH § Validation Architecture Test Map と PATTERNS.md "planner 指示" に基づきテストケース名のみ定義し、本体は `pytest.skip("Wave 1 implements src/ml/ module first")` で skip 状態にする（RED ではなく skip・Wave 1 で src/ml/ モジュール実装後に skip を外して GREEN を目指す）。各テストクラスの docstring に対応する MODA-XX / D-XX / Pitfall #X を引用。クラス構造は analog 準拠（TestLoadMerge, TestGroupTimeSeriesSplit, TestEndToEnd, TestFinishTimeZscore, TestJockeyTrainerStats パターン）。

テストケース一覧（各ファイルに定義・skip 状態）:
- test_data_loader.py: TestFeatureLoad クラス — test_categorical_conversion (MODA-01, Pitfall #3), test_horse_race_id_derive (MODA-01, Pitfall #2), test_leakage_audit (T-data-leak), test_train_holdout_window_counts (MODA-01, D-01/D-02, Pitfall #7), test_grade_nan_preserved (Pitfall #4)
- test_group_timeseries_split.py: TestGroupTimeSeriesSplit クラス — test_same_race_same_fold (MODA-02, T-temporal-leak), test_temporal_order (MODA-02), test_no_boundary_split (MODA-02), test_get_n_splits (sklearn 互換)
- test_trainer.py: TestTrainer クラス — test_train_fold_model_returns_classifier (MODA-01), test_early_stopping_fires (Pitfall #1), test_collect_oof_predictions (MODA-01), test_train_final_model (D-15)
- test_calibrator.py: TestCalibrator クラス — test_leak_free_calibration (MODA-04, T-cali-leak, Pitfall #5), test_isotonic_output_in_01_range, test_isotonic_monotonic_non_decreasing
- test_evaluator.py: TestEvaluator クラス — test_ece_perfect_prediction (MODA-04), test_ece_worst_case (MODA-04), test_ece_bin_weighting, test_reliability_diagram_generates_file (D-11), test_evaluate_returns_metrics_dict (D-06)
- test_baseline.py: TestBaseline クラス — test_popularity_baseline_auc (MODA-03), test_popularity_nan_dropped (Pitfall #6), test_join_integrity
  </action>
  <verify>
    <automated>python -m pytest tests/ml/ -q --co 2>&1 | grep -c "test_" && python -m pytest tests/ml/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/ -q --co` が collection エラーなく全テストケースを列出する（skipped ではなく collection できる）
    - `python -m pytest tests/ml/ -q` の結果が "X skipped" で完了し、0 failed / 0 errors である（src/ml/ 未実装のため skip が正挙動）
    - `python -m pytest tests/ -q` が既存ベースライン（513 passed/1 skipped 相当）を維持し、tests/ml/ の skip が追加されるだけ（既存テスト破壊なし）
    - tests/ml/conftest.py に sample_feature_df fixture が定義され、`python -c "import pandas as pd; df = pd.DataFrame(); print('ok')"` が成功するレベルで import エラーがない
    - src/ml/__init__.py が `python -c "import src.ml; print('ok')"` で import できる（empty marker）
    - 各 test_*.py ファイルに対応するテストクラスとテストケース名が RESEARCH Test Map と一致する（grep で確認）
  </acceptance_criteria>
  <done>tests/ml/ 配下に conftest と6テストファイルが作成され、Wave 1 実装前に skip 状態で pytest が完走する。hermetic fixture が feature 層の categorical mix・target_top3・時系列順を再現する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI/pip → local env | scikit-learn/matplotlib/joblib パッケージが PyPI からローカル環境に入る |
| brew → local env | libomp バイナリが Homebrew からローカル環境に入る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | pip install scikit-learn/matplotlib/joblib | mitigate | RESEARCH § Package Legitimacy Audit で scikit-learn/matplotlib/joblib 全て正規権威ソース（10年以上・github.com/scikit-learn, matplotlib, joblib）と確認済み。seam SUS 判定は PyPI downloads 取得不能による誤検知。CLAUDE.md 推奨スタック + pyproject.toml lightgbm>=4.6 既宣言と整合。blocking-human 不要 |
| T-07-01-02 | Tampering | brew install libomp | accept | Homebrew は Mac デファクト標準パッケージマネージャ・libomp は LLVM 公式プロジェクト。pip 経由ではないが brew formula は review 済み |
| T-07-01-03 | Information disclosure | pyproject.toml への依存追記 | accept | 依存リストは公開情報・機密性なし |
</threat_model>

<verification>
- import lightgbm / sklearn / matplotlib / joblib / yaml が全て成功
- pyproject.toml に scikit-learn/matplotlib/joblib/gated marker が追記済み
- tests/ml/ が skip 状態で pytest 完走・既存 513 passed/1 skipped ベース維持
- conftest.py の hermetic fixture が feature 層構造を再現
</verification>

<success_criteria>
Wave 1 が着手可能な環境・テスト基盤が完成していること:
1. import lightgbm が OSError なしで成功（libomp 解決）
2. scikit-learn/matplotlib/joblib が import 可能・pyproject.toml 宣言済み
3. tests/ml/ に6テストファイル + conftest が作成され、skip 状態で pytest 完走
4. gated marker が [tool.pytest.ini_options] に登録済み
5. 既存テストスイート（513 passed/1 skipped）が破壊されていない
</success_criteria>

<output>
Create `.planning/phases/07-model-a-top-3-probability/07-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan (Wave 0) produces NO new public symbols consumed by downstream code. It produces test scaffolding and environment only:
- Package markers: `src.ml` (empty), `tests.ml`
- Test fixtures (tests/ml/conftest.py, consumed only by tests/ml/): `sample_feature_df`, `sample_entry_df`, `tmp_ml_output_dir`, `ml_config`
- Test skeletons (6 files, all skip-state): TestFeatureLoad, TestGroupTimeSeriesSplit, TestTrainer, TestCalibrator, TestEvaluator, TestBaseline and their test methods listed in Task 3
- pyproject.toml additions: dependencies `scikit-learn>=1.9`, `matplotlib>=3.10`, `joblib>=1.4`; pytest marker `gated`

No src/ml/ production symbols are created in this plan (Wave 1 creates them).

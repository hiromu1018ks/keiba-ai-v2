---
phase: 07-model-a-top-3-probability
plan: 07
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified:
  - src/ml/run_train.py
  - src/ml/__init__.py
  - config/phase7_model_a.yaml
  - tests/ml/test_run_train.py
  - .gitignore
autonomous: true
requirements: [MODA-01, MODA-02, MODA-03, MODA-04]

must_haves:
  truths:
    - "run_train(config_path) が data_loader→splitter→trainer→calibrator→evaluator→baseline→成果物保存の全ステップを orchestrate する"
    - "config/phase7_model_a.yaml が sensible defaults + seed + window 境界 + fold 設定 + feature_columns + 成果物パスを定義する（D-14・Codex HIGH #5 fix: feature_columns キー追加）"
    - "run_train が collect_oof_predictions と train_final_model に feature_columns を明示的引数として渡す（Codex HIGH #5 fix: config['data']['feature_columns'] 参照ではなく run_train で解決して引数渡し）"
    - "hermetic E2E テストが load_features(expected_counts=[]) で小規模 fixture で完走する（Codex HIGH #4 fix: 固定行数 assert bypass）"
    - "hermetic E2E テストで OOF parquet 行数 < 学習窓 fixture 全行数 であることを assert（Codex HIGH #2 fix: warm-up 除外）"
    - "run_train が metrics dict に oof_rows = int(len(oof_df)) を必ず書き込み metrics.json に永続化する（Cycle-2 HIGH #3 fix: producer/consumer contract を 07-08 と一致させる・key=\"oof_rows\"・型=int・計算=len(oof_df)）"
    - "run_train の expected_counts パラメータが UNIFIED sentinel（None=本番 / []=bypass / 非空 dict=カスタム・Cycle-2 HIGH #2 fix）で 07-02 load_features に転送される・{}（空 dict）は使わない"
    - "全6成果物（model .txt / calibrator .joblib / OOF parquet / holdout parquet / report md + metrics.json + reliability diagram / config YAML）が D-15 契約どおり保存される"
    - "src/ml/__init__.py が公開 API（run_train, GroupTimeSeriesSplit, train_fold_model, fit_calibrator, compute_ece, evaluate 等）を __all__ で再エクスポートする"
  artifacts:
    - path: "src/ml/run_train.py"
      provides: "run_train(config_path) -> dict オーケストレーション関数・python -m src.ml.run_train エントリポイント・feature_columns 明示的引数渡し"
      exports: ["run_train"]
      min_lines: 110
    - path: "config/phase7_model_a.yaml"
      provides: "sensible defaults ハイパラ・seed・window 境界・fold 設定・feature_columns・成果物パス（D-13/D-14/D-15・Codex HIGH #5 fix: feature_columns キー追加）"
    - path: "src/ml/__init__.py"
      provides: "src.ml パッケージ公開 API（__all__ 再エクスポート）"
    - path: "tests/ml/test_run_train.py"
      provides: "E2E hermetic テスト（学習→OOF→キャリブレーション→評価→成果物保存・expected_counts=[] と OOF<warm-up bypass 検証）"
    - path: ".gitignore"
      provides: "models/phase7/, data/model/oof/, reports/phase7/ の gitignore 追加"
  key_links:
    - from: "src/ml/run_train.py"
      to: "src/ml/data_loader.py"
      via: "from src.ml.data_loader import load_features"
      pattern: "load_features"
    - from: "src/ml/run_train.py"
      to: "src/ml/group_timeseries_split.py"
      via: "from src.ml.group_timeseries_split import GroupTimeSeriesSplit"
      pattern: "GroupTimeSeriesSplit"
    - from: "src/ml/run_train.py"
      to: "src/ml/trainer.py"
      via: "from src.ml.trainer import collect_oof_predictions, train_final_model — feature_columns を明示的引数として渡す（Codex HIGH #5 fix）"
      pattern: "collect_oof_predictions|train_final_model"
    - from: "src/ml/run_train.py"
      to: "src/ml/calibrator.py"
      via: "from src.ml.calibrator import fit_calibrator, apply_calibrator, save_calibrator"
      pattern: "fit_calibrator|apply_calibrator"
    - from: "src/ml/run_train.py"
      to: "src/ml/evaluator.py + baseline.py"
      via: "from src.ml.evaluator import evaluate, reliability_diagram; from src.ml.baseline import compute_popularity_baseline"
      pattern: "evaluate|compute_popularity_baseline"
---

<objective>
Phase 7 の全 Wave 1+2 モジュール（data_loader / GroupTimeSeriesSplit / trainer / calibrator / evaluator / baseline）を統合する run_train オーケストレーターと、sensible defaults を定義する config YAML、パッケージ公開 API を実装する。D-15 の6成果物を全て保存する。

**Codex HIGH #2 fix (OOF row count):** hermetic E2E テストで OOF parquet の行数が 学習窓 fixture 全行数 より小さいことを assert する（warm-up chunk 0 除外・val chunks のみ）。run_train が collect_oof_predictions の戻り値行数を logger.info で出力し、OOF < 学習窓全行であることを実行時に確認。

**Codex HIGH #4 fix (expected_counts bypass):** hermetic E2E テストは load_features(expected_counts=[]) で小規模 fixture を読み込み、固定行数 assert を bypass する。本番実行（07-08 phase gate）は expected_counts=None（デフォルト）で本番行数を assert。

**Codex HIGH #5 fix (feature_columns config key):** config/phase7_model_a.yaml に data.feature_columns キーを追加し、FEATURE_COLUMNS（src/pipeline/feature_generator.py から import）と一致するカラムリストを定義する。run_train が config から feature_columns を読み込み、collect_oof_predictions と train_final_model に明示的引数として渡す（config["data"]["feature_columns"] を trainer 内部で参照せず run_train で解決）。

**Codex HIGH #6 fix (train_final_model two-stage):** run_train は train_final_model（07-04 で二段階学習実装済み）を呼び出し、全量再学習された単一モデルを .txt で保存する。

**Cycle-2 HIGH #2 fix (expected_counts sentinel unification):** 07-07 の run_train 署名・forwarding・hermetic E2E call site 全てが 07-02 と同一の UNIFIED sentinel を使う。expected_counts=None（デフォルト）= 本番行数 assert・expected_counts=[]（空 list）= bypass・非空 dict = カスタム assert。{}（空 dict）は 07-02 側で TypeError 拒否されるため使わない。

**Cycle-2 HIGH #3 fix (oof_rows in metrics.json):** Codex Cycle 2 review は 07-07 Step 9 が baseline_auc のみを metrics に追加し、07-08 の automated verify が `oof_rows in m` を assert して失敗する cross-plan contract hole を指摘した。修正: run_train は `metrics["oof_rows"] = int(len(oof_df))` を必ず metrics dict に追加してから metrics.json を書き出す。producer/consumer contract: key="oof_rows"・型=int・計算=len(oof_df)。07-08 verify と完全一致。

**Cycle-2 HIGH #1 propagation (temporal-order assertion):** run_train は collect_oof_predictions（07-04）に feature_columns を渡すが、trainer が内部で splitter.split に dates=df["race_date"] を渡すことは 07-04 の責務。run_train 側は 07-02 load_features が返す train_df が race_date 列を保持していることを前提とする（07-02 で fix 済み・drop しない）。run_train が race_date を drop してはならない。

Purpose: MODA-01〜04 の全要件を単一のエントリポイント（`python -m src.ml.run_train`）に統合し、Phase 8/9 が消費する成果物（model .txt / calibrator .joblib / OOF parquet / holdout parquet / report / config）を一貫して生成する。
Output: src/ml/run_train.py + config/phase7_model_a.yaml（feature_columns キー追加）+ src/ml/__init__.py（re-exports 遷移）+ tests/ml/test_run_train.py（hermetic E2E・expected_counts bypass）+ .gitignore 追記
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-model-a-top-3-probability/07-CONTEXT.md
@.planning/phases/07-model-a-top-3-probability/07-RESEARCH.md
@.planning/phases/07-model-a-top-3-probability/07-PATTERNS.md
@.planning/phases/07-model-a-top-3-probability/07-REVIEWS.md
@.planning/phases/07-model-a-top-3-probability/07-02-SUMMARY.md
@.planning/phases/07-model-a-top-3-probability/07-03-SUMMARY.md
@.planning/phases/07-model-a-top-3-probability/07-04-SUMMARY.md
@.planning/phases/07-model-a-top-3-probability/07-05-SUMMARY.md
@.planning/phases/07-model-a-top-3-probability/07-06-SUMMARY.md
@src/pipeline/integration.py
@src/pipeline/feature_generator.py
@src/scraper/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: config/phase7_model_a.yaml (feature_columns キー追加・Codex HIGH #5 fix) + .gitignore 追記</name>
  <files>config/phase7_model_a.yaml, .gitignore</files>
  <read_first>
    - .planning/phases/07-model-a-top-3-probability/07-REVIEWS.md (§ Concerns HIGH #5 — config["data"]["feature_columns"] 欠落・KeyError・「make feature columns an explicit function argument, or persist a confirmed list in the config」fix)
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Code Examples lines 454-525 — Sensible Defaults Config YAML のフル定義。構造 seed/data/cv/model/early_stopping/calibration/evaluation/artifacts)
    - .planning/phases/07-model-a-top-3-probability/07-CONTEXT.md (D-13 sensible defaults + early stopping・D-14 YAML config + 固定 seed・D-15 成果物配置)
    - src/pipeline/feature_generator.py (lines 37-107 — FEATURE_COLUMNS の定義。config の data.feature_columns と一致させるための権威リスト)
    - .gitignore（現在の gitignore エントリ確認・data/ や models/ が既に ignore 対象か）
    - pyproject.toml（pyyaml が依存宣言済みか確認・setuptools なので poetry なし）
  </read_first>
  <action>
(1) config/phase7_model_a.yaml を作成。RESEARCH.md Code Examples（lines 454-525）の VERIFIED 定義を採用。構造: seed(42) / data(feature_path, train_window=["2018-01-01","2024-12-31"], holdout_window=["2025-01-01","2026-05-31"], target_column=target_top3, exclude_column=exclude_from_training, entry_path=data/standard/entry.parquet, **feature_columns: [FEATURE_COLUMNS の全カラム・Codex HIGH #5 fix]**, categorical_columns=9列, drop_columns=race_id/race_date/horse_entity_key/horse_name/result_status/is_dnf/horse_number) / cv(n_splits=5, group_column=race_id, sort_column=race_date, early_stopping_val_ratio=0.2) / model(objective=binary, metric=[binary_logloss,auc], num_leaves=31, learning_rate=0.05, min_data_in_leaf=100, feature_fraction=0.9, bagging_fraction=0.9, bagging_freq=5, max_depth=-1, lambda_l1=0.0, lambda_l2=0.0, min_gain_to_split=0.0, verbose=-1, n_estimators=1000) / early_stopping(stopping_rounds=50, verbose=false, first_metric_only=false) / calibration(method=isotonic, y_min=0.0, y_max=1.0, out_of_bounds=clip) / evaluation(ece_bins=10, ece_tolerance=0.02, reliability_diagram=true) / artifacts(model_dir=models/phase7, oof_dir=data/model/oof, report_dir=reports/phase7, model_filename=model_a.lgb.txt, calibrator_filename=isotonic_calibrator.joblib, oof_filename=oof_predictions.parquet, holdout_filename=holdout_predictions.parquet)。

**Codex HIGH #5 fix**: data.feature_columns キーを追加。値は src/pipeline/feature_generator.py の FEATURE_COLUMNS（lines 37-107）と完全一致するカラムリスト。run_train がこのリストを読み込み collect_oof_predictions / train_final_model に明示的引数として渡す。trainer モジュールは config["data"]["feature_columns"] を内部参照せず run_train から引数で受け取る（KeyError 回避・単一責任）。feature_columns リストは実装時に FEATURE_COLUMNS を import して確認するか、FEATURE_COLUMNS の内容を YAML にベタ書きする（両者が一致することを assert するテストを tests/ml/test_run_train.py に追加）。

categorical_columns は RESEARCH の9列（course_name/surface/direction/weather/track_condition/sex/jockey/trainer/grade）を維持。drop_columns は学習に使わない識別子・補助列。target_top3 は feature_columns に含まれないため drop_columns に追加不要（load_features が分離）。

(2) .gitignore に models/phase7/, data/model/oof/, reports/phase7/ を追記（D-15 が全て gitignore 対象を指定）。既に data/ や models/ が ignore されている場合は重複追加しない（既存エントリ確認）。reports/ が未 ignore の場合は reports/phase7/ を追加。
  </action>
  <verify>
    <automated>python -c "
import yaml
from src.pipeline.feature_generator import FEATURE_COLUMNS
c = yaml.safe_load(open('config/phase7_model_a.yaml'))
assert c['seed']==42
assert c['data']['train_window']==['2018-01-01','2024-12-31']
assert c['cv']['n_splits']==5
assert c['model']['num_leaves']==31
assert c['artifacts']['model_filename']=='model_a.lgb.txt'
assert 'feature_columns' in c['data'], 'Codex HIGH #5: feature_columns key missing'
assert set(c['data']['feature_columns']) == set(FEATURE_COLUMNS), 'feature_columns mismatch with FEATURE_COLUMNS'
print('config ok:', list(c.keys()), 'feature_columns count:', len(c['data']['feature_columns']))
" && grep -q "models/phase7\|data/model/oof\|reports/phase7" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('config/phase7_model_a.yaml'))"` が YAML パースエラーなしで成功
    - config の seed == 42, cv.n_splits == 5, model.num_leaves == 31, model.learning_rate == 0.05, early_stopping.stopping_rounds == 50, evaluation.ece_tolerance == 0.02（D-11）
    - config の data.train_window == ["2018-01-01","2024-12-31"], data.holdout_window == ["2025-01-01","2026-05-31"]（D-01/D-02）
    - config の artifacts が model_dir/oof_dir/report_dir + 4ファイル名を定義（D-15）
    - **`python -c "import yaml; c=yaml.safe_load(open('config/phase7_model_a.yaml')); assert 'feature_columns' in c['data']; print('Codex HIGH #5 ok')"` が ok を出力（feature_columns キー存在・Codex HIGH #5 fix）**
    - **config の data.feature_columns が src/pipeline/feature_generator.py の FEATURE_COLUMNS と完全一致（set 比較で一致）**
    - `grep -c "models/phase7\|data/model/oof\|reports/phase7" .gitignore` が 1 以上（gitignore 追記）
  </acceptance_criteria>
  <done>config YAML が sensible defaults + window + fold + feature_columns + artifacts を定義し、.gitignore に成果物ディレクトリが追記される</done>
</task>

<task type="auto">
  <name>Task 2: src/ml/run_train.py 実装 — オーケストレーター（feature_columns 明示的引数渡し・Codex HIGH #5 fix・学習→OOF→キャリブレーション→holdout 評価→6成果物保存）</name>
  <files>src/ml/run_train.py</files>
  <read_first>
    - .planning/phases/07-model-a-top-3-probability/07-REVIEWS.md (§ Concerns HIGH #5 — feature_columns を run_train で解決して trainer に引数渡し。§ HIGH #2 — OOF 行数 < 学習窓全行 を logger.info で出力。§ HIGH #6 — train_final_model は二段階全量再学習済み・run_train は呼ぶだけ)
    - .planning/phases/07-model-a-top-3-probability/07-PATTERNS.md (§ src/ml/run_train.py — analog integration.py::integrate_standard_layer のエントリポイントパターン)
    - .planning/phases/07-model-a-top-3-probability/07-RESEARCH.md (§ Architecture Patterns System Architecture Diagram lines 154-221 — run_train の全ステップフロー)
    - src/pipeline/integration.py (lines 176-451 — integrate_standard_layer の docstring numpy スタイル・logger.info ステップ出力・戻り値 dict・mkdir(parents=True, exist_ok=True) パターン)
    - src/ml/data_loader.py, src/ml/group_timeseries_split.py, src/ml/trainer.py, src/ml/calibrator.py, src/ml/evaluator.py, src/ml/baseline.py (07-02..07-06 で実装した各モジュールの公開関数シグネチャを確認・特に collect_oof_predictions/train_final_model の feature_columns 引数)
  </read_first>
  <action>
src/ml/run_train.py を作成。analog integrate_standard_layer（lines 176-451）のパターン踏襲: numpy docstring・logger.info ステップ出力・戻り値 dict・mkdir(parents=True, exist_ok=True)。絶対 import。

(1) import: `from pathlib import Path`, `import json`, `import yaml`, `import pandas as pd`, `from loguru import logger`, `from src.ml.data_loader import load_features`, `from src.ml.group_timeseries_split import GroupTimeSeriesSplit`, `from src.ml.trainer import collect_oof_predictions, train_final_model`, `from src.ml.calibrator import fit_calibrator, apply_calibrator, save_calibrator`, `from src.ml.evaluator import evaluate, reliability_diagram`, `from src.ml.baseline import compute_popularity_baseline`。

(2) `def run_train(config_path: str | Path = "config/phase7_model_a.yaml", expected_counts: dict | list | None = None) -> dict`:
  - **Codex HIGH #4 fix + Cycle-2 HIGH #2 unified sentinel**: expected_counts パラメータを追加し load_features に転送。**UNIFIED sentinel（07-02 と同一）: None（デフォルト）= 本番行数 assert・[]（空 list・dict ではない）= hermetic bypass・非空 dict = カスタム assert・{}（空 dict）は 07-02 load_features 側で TypeError で拒否**。hermetic E2E（07-07 Task 3）は expected_counts=[] を転送し本番 assert を bypass。本番実行（07-08 phase gate）は expected_counts=None（デフォルト）で本番行数を assert。
  - numpy docstring（analog 準拠）: Parameters / Returns / Raises セクション。Returns は {"model": Path, "calibrator": Path, "oof": Path, "holdout": Path, "report": Path, "metrics": Path, "diagram": Path, "config": Path}。
  - logger.info(f"run_train: starting (config_path={config_path})")。
  - **Step 1 load_config**: config = yaml.safe_load(open(config_path))。logger.info で seed/window/fold を出力。
  - **Step 2 load_features**: data = load_features(feature_path=config["data"]["feature_path"], train_window=tuple(config["data"]["train_window"]), holdout_window=tuple(config["data"]["holdout_window"]), entry_path=config["data"]["entry_path"], expected_counts=expected_counts)（Codex HIGH #4 fix: expected_counts を転送）。train_df = data["train"], holdout_df = data["holdout"]。logger.info で行数を出力。
  - **Step 3 feature_columns 解決（Codex HIGH #5 fix）**: feature_columns = config["data"]["feature_columns"]（config から読込・07-07 Task 1 で追加したキー）。logger.info で feature_columns 数を出力。**trainer モジュールに引数として渡す（trainer は内部で config["data"]["feature_columns"] を参照しない）**。
  - **Step 4 OOF 収集**: splitter = GroupTimeSeriesSplit(n_splits=config["cv"]["n_splits"])。oof_df = collect_oof_predictions(train_df, splitter, config, feature_columns=feature_columns)（Codex HIGH #5 fix: feature_columns 明示的引数）。logger.info で OOF 行数を出力。**Codex HIGH #2 fix**: len(oof_df) < len(train_df) であることを logger.info で出力し（warm-up chunk 0 除外）、OOF 行数が学習窓全行でないことを明示。
  - **Step 5 キャリブレーター学習**: iso = fit_calibrator(oof_df["p_top3_raw"].values, oof_df["target_top3"].values)（OOF = val chunks のみ・warm-up 除外・Codex HIGH #2）。logger.info で fit 行数。
  - **Step 6 final model 学習（Codex HIGH #6 fix）**: final_clf = train_final_model(train_df, config, feature_columns=feature_columns)（feature_columns 明示的引数・二段階全量再学習は 07-04 で実装済み）。logger.info で best_iteration_ と学習行数 = len(train_df) 全行 を出力。
  - **Step 7 holdout 予測**: holdout_pred = final_clf.predict_proba(holdout_df[feature_columns])[:, 1]。holdout_calibrated = apply_calibrator(iso, holdout_pred)。logger.info。
  - **Step 8 評価**: metrics = evaluate(holdout_df[config["data"]["target_column"]].values, holdout_pred, holdout_calibrated, n_bins=config["evaluation"]["ece_bins"])。logger.info で AUC/ECE を出力。logger.warning で ece_calibrated >= config["evaluation"]["ece_tolerance"] の場合（D-11 違反徴候）。
  - **Step 9 人気ベースライン**: baseline = compute_popularity_baseline(holdout_df, pd.read_parquet(config["data"]["entry_path"])). baseline_auc を metrics dict に追加。
  - **Step 9b oof_rows 記録（Cycle-2 HIGH #3 fix）**: `metrics["oof_rows"] = int(len(oof_df))` を必ず追加する。型は int（JSON serializable）。値は collect_oof_predictions が返した OOF DataFrame の行数 = validation chunks（chunks 1..n_splits）の行数合計（warm-up chunk 0 除外・Codex HIGH #2 fix）。07-08 phase gate の automated verify が `oof_rows in m` と `m[oof_rows] < 322510` を assert するため、この key が metrics.json に存在すること（producer 側・07-07）と 07-08 consumer 側の key 名・型・計算が完全一致することが必須。producer/consumer contract: key="oof_rows"・型=int・計算=len(oof_df) where oof_df = collect_oof_predictions return value。
  - **Step 10 成果物保存**（D-15 の6成果物）:
    - artifact_dirs を mkdir(parents=True, exist_ok=True)（model_dir, oof_dir, report_dir）。
    - ① final_clf.booster_.save_model(model_dir / model_filename) — LightGBM .txt 形式。
    - ② save_calibrator(iso, model_dir / calibrator_filename) — .joblib 形式。
    - ③ oof_df に p_top3_calibrated 列を追加（apply_calibrator(iso, oof_df["p_top3_raw"].values)）して oof_dir / oof_filename に to_parquet(engine="pyarrow", index=False)。列: race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold（D-15）。
    - ④ holdout 予測 DataFrame（同構造・fold="holdout"）を oof_dir / holdout_filename に to_parquet。D-12 が p_top3_raw と p_top3_calibrated の併存を指定。
    - ⑤ reliability_diagram(holdout_y, holdout_calibrated, n_bins, save_path=report_dir / "reliability_diagram.png")。
    - ⑥ metrics.json（metrics dict を json.dump・**Step 9b で注入した oof_rows を含む・Cycle-2 HIGH #3 fix**）+ evaluation_report.md（AUC/Brier/logloss/ECE/baseline を markdown 表で出力・D-08 純粋予測×EV 構図の注記付き・**holdout retune 禁止の注記も含む・Cycle-2 LOW fix**）。report_dir に保存。
    - config YAML は既存 config_path をそのまま参照（コピーしない・再現性は固定 seed で担保・D-14）。
  - logger.info(f"run_train: complete")。戻り値 dict でパスを返す。

(3) `if __name__ == "__main__": run_train()` — `python -m src.ml.run_train` エントリポイント（setuptools・`poetry run` 禁止・MEMORY.md）。
  </action>
  <verify>
    <automated>python -c "from src.ml.run_train import run_train; print('ok')" && grep -c "load_features\|GroupTimeSeriesSplit\|collect_oof_predictions\|train_final_model\|fit_calibrator\|apply_calibrator\|evaluate\|compute_popularity_baseline\|feature_columns" src/ml/run_train.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.ml.run_train import run_train; print('ok')"` が ok を出力
    - `grep -c "load_features" src/ml/run_train.py` が 1 以上・`grep -c "GroupTimeSeriesSplit" src/ml/run_train.py` が 1 以上・`grep -c "collect_oof_predictions\|train_final_model" src/ml/run_train.py` が 2 以上・`grep -c "fit_calibrator\|apply_calibrator" src/ml/run_train.py` が 2 以上・`grep -c "evaluate\|compute_popularity_baseline" src/ml/run_train.py` が 2 以上（全 Wave 1+2 モジュール統合）
    - **`grep -c "feature_columns" src/ml/run_train.py` が 3 以上（config から読込 + collect_oof_predictions 引数 + train_final_model 引数・Codex HIGH #5 fix）**
    - **`grep -c "expected_counts" src/ml/run_train.py` が 2 以上（run_train パラメータ + load_features 引数・Codex HIGH #4 fix）**
    - **`grep -cE "metrics\[.oof_rows.\]|\[\"oof_rows\"\]" src/ml/run_train.py` が 1 以上（Cycle-2 HIGH #3 fix: metrics dict に oof_rows を書き込む）**
    - **`grep -cE "expected_counts == \{\}|expected_counts=\{\}" src/ml/run_train.py` が 0（Cycle-2 HIGH #2 fix: 空 dict sentinel は使わない・UNIFIED sentinel は空 list []）**
    - **test_oof_parquet_schema_and_row_count が metrics.json の oof_rows key 存在 + 型 int + 値が OOF parquet 行数と一致 を assert（Cycle-2 HIGH #3 fix: producer/consumer contract）**
    - `grep -c "save_model\|save_calibrator\|to_parquet\|savefig\|json.dump" src/ml/run_train.py` が 5 以上（6成果物保存のうち少なくとも5つの保存処理）
    - `grep -c "booster_.save_model" src/ml/run_train.py` が 1 以上（LightGBM .txt 形式保存・D-15）
    - run_train の戻り値 dict が model/calibrator/oof/holdout/report/metrics/diagram パスを含む
    - `grep -c 'if __name__ == "__main__"' src/ml/run_train.py` が 1（python -m エントリポイント）
    - 各ステップで logger.info が出力される（grep -c "logger.info" が 5 以上）
  </acceptance_criteria>
  <done>run_train が Wave 1+2 全モジュールを統合し、feature_columns 明示的引数渡しと expected_counts bypass で D-15 の6成果物を保存するオーケストレーターとして動作する</done>
</task>

<task type="auto">
  <name>Task 3: src/ml/__init__.py re-exports 遷移 + tests/ml/test_run_train.py hermetic E2E（expected_counts bypass・OOF<warm-up 検証・feature_columns config 整合性）</name>
  <files>src/ml/__init__.py, tests/ml/test_run_train.py</files>
  <read_first>
    - src/scraper/__init__.py (Phase 4 パターン・__all__ 再エクスポートの遷移例)
    - src/ml/run_train.py (Task 2 で実装した run_train 関数・feature_columns 引数・expected_counts パラメータ)
    - tests/pipeline/test_feature_generator.py::TestEndToEnd (lines 1584-1771 — hermetic E2E パターン・_generate_full_pipeline helper)
    - tests/ml/conftest.py (sample_feature_df / sample_entry_df / tmp_ml_output_dir / ml_config fixture)
    - config/phase7_model_a.yaml (Task 1 で作成した config 構造・feature_columns キー)
    - src/pipeline/feature_generator.py (FEATURE_COLUMNS 定義・config の feature_columns と一致確認用)
  </read_first>
  <action>
(1) src/ml/__init__.py を 07-01 の empty marker から公開 API 再エクスポートに遷移（Phase 4 パターン・src/scraper/__init__.py 準拠）。`from src.ml.run_train import run_train`, `from src.ml.group_timeseries_split import GroupTimeSeriesSplit`, `from src.ml.trainer import train_fold_model, collect_oof_predictions, train_final_model`, `from src.ml.calibrator import fit_calibrator, apply_calibrator, save_calibrator, load_calibrator`, `from src.ml.evaluator import compute_ece, evaluate, reliability_diagram`, `from src.ml.baseline import compute_popularity_baseline`, `from src.ml.data_loader import load_features` を import。__all__ で全シンボルを明示。docstring に「Phase 8/9 が from src.ml import ... でアクセス可能」と記載。

(2) tests/ml/test_run_train.py を作成。TestRunTrainE2E クラス（@pytest.mark.gated なし・hermetic）で run_train を小規模 fixture で完走させる E2E テストを実装:
  - test_run_train_hermetic_e2e: tmp_path に小規模 features_train.parquet（conftest の sample_feature_df を拡張して**最低 n_splits+1=6 レース以上・10-15レース・target_top3 含む・時系列順**）と entry.parquet（sample_entry_df）を書き出し。config YAML を tmp_path 配下に生成（model_dir/oof_dir/report_dir を tmp_path に向け・n_estimators=50, num_leaves=8, n_splits=3 等に縮小・**feature_columns を小規模 fixture のカラムに合わせる・Codex HIGH #5**）。**run_train(config_path=tmp_config, expected_counts=[]) を呼ぶ（Codex HIGH #4 fix: 固定行数 assert bypass）**。戻り値 dict の全パスが存在すること（model .txt, calibrator .joblib, oof parquet, holdout parquet, metrics.json, reliability_diagram.png, report.md）を assert。metrics.json の auc_calibrated が [0, 1]・ece_calibrated が [0, 1] であることを assert。
  - **test_oof_parquet_schema_and_row_count (Codex HIGH #2 fix + Cycle-2 HIGH #3 producer/consumer contract)**: OOF parquet の列が {race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold} に完全一致することを assert（D-15 契約）。**さらに OOF parquet の行数 < 学習窓 fixture 全行数 であることを assert（warm-up chunk 0 除外・Codex HIGH #2 fix）**。fold 列のユニーク値が {0, 1, ..., n_splits-1} であることを assert。**さらに reports/phase7/metrics.json（tmp_path 配下）に oof_rows key が存在し、その int 値が OOF parquet の実際の行数と完全一致することを assert（Cycle-2 HIGH #3 fix: producer/consumer contract・key=\"oof_rows\"・型=int・計算=len(oof_df)）**。
  - test_holdout_parquet_schema: holdout parquet の列が OOF と同一で fold=="holdout" であることを assert（D-15）。
  - test_artifacts_all_created: 6成果物（model, calibrator, oof, holdout, report+metrics+diagram, config）が全て作成されることを assert。
  - **test_feature_columns_config_consistency (Codex HIGH #5 fix)**: config/phase7_model_a.yaml の data.feature_columns が src/pipeline/feature_generator.py の FEATURE_COLUMNS と完全一致することを assert（set 比較）。これで config と FEATURE_COLUMNS の乖離を検出。

テストは hermetic（外部データ依存なし・tmp_path のみ）で、小規模 fixture で~5秒以内に完了する。gated 版（実データ features_train.parquet で本格 E2E）は 07-08 phase gate plan で実行。
  </action>
  <verify>
    <automated>python -m pytest tests/ml/test_run_train.py -x -q && python -c "from src.ml import run_train, GroupTimeSeriesSplit, train_fold_model, fit_calibrator, compute_ece, evaluate, compute_popularity_baseline, load_features; print('re-exports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ml/test_run_train.py -x -q` が5テスト GREEN（test_run_train_hermetic_e2e, test_oof_parquet_schema_and_row_count, test_holdout_parquet_schema, test_artifacts_all_created, test_feature_columns_config_consistency）
    - `python -c "from src.ml import run_train, GroupTimeSeriesSplit, train_fold_model, fit_calibrator, compute_ece, evaluate, compute_popularity_baseline, load_features; print('re-exports ok')"` が ok を出力（公開 API 再エクスポート）
    - `grep -c "__all__" src/ml/__init__.py` が 1 以上（__all__ 定義）
    - **test_run_train_hermetic_e2e が run_train(config_path=tmp_config, expected_counts=[]) で小規模 fixture が完走することを assert（Codex HIGH #4 fix）**
    - **test_oof_parquet_schema_and_row_count が OOF parquet 列 {race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold} 完全一致 と len(oof) < len(train_fixture) を assert（Codex HIGH #2 fix）**
    - test_holdout_parquet_schema が fold=="holdout" を assert
    - test_artifacts_all_created が6成果物のファイル存在を assert
    - **test_feature_columns_config_consistency が config の feature_columns == FEATURE_COLUMNS を assert（Codex HIGH #5 fix）**
    - `python -m pytest tests/ml/ -q` が全 hermetic テスト GREEN・0 failed（gated テストは skip）
  </acceptance_criteria>
  <done>src/ml/__init__.py が公開 API を再エクスポートし、test_run_train.py の hermetic E2E が5テスト GREEN で run_train の全ステップ＋6成果物保存＋UNIFIED expected_counts sentinel（Cycle-2 HIGH #2）＋OOF<warm-up＋oof_rows producer/consumer contract（Cycle-2 HIGH #3）＋feature_columns 整合性を検証する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config YAML → run_train | ハイパラ・window 境界・fold 設定・feature_columns がオーケストレーションに入る（設定ミスリスク） |
| 全 Wave 1+2 モジュール → run_train | 統合点（リーク防止チェーンの最終検証） |
| run_train → 成果物ファイル | 6成果物がディスクに書かれる（gitignore で保護） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering (D-15 契約) | run_train: 成果物 schema | mitigate | OOF/holdout parquet 列が {race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold} であることを test_oof_parquet_schema_and_row_count/test_holdout_parquet_schema で検証。fold=="holdout" でホールドアウト識別 |
| T-07-07-02 | Tampering (リークチェーン) | run_train: 統合点 | mitigate | Wave 1+2 各モジュールのリーク防止（data_loader audit_leakage / GroupTimeSeriesSplit n_splits+1 / calibrator OOF-only fit）を run_train が正しい順序で呼ぶ。test_run_train_hermetic_e2e でチェーン健全性を検証 |
| T-07-07-03 | Tampering (D-14 再現性) | run_train: 固定 seed | mitigate | config seed=42 を LGBMClassifier random_state に渡す（trainer 経由）。同一 config で同一結果を再現可能 |
| T-07-07-04 | Tampering (成果物改竄) | run_train: ファイル保存 | mitigate | models/phase7/, data/model/oof/, reports/phase7/ を .gitignore で保護。ローカル保存・外部公開なし |
| T-07-07-05 | DoS (メモリ) | run_train: 53万行処理 | accept | pandas DataFrame 一括処理・LightGBM Dataset で効率化 |
| T-07-07-06 | Tampering (Codex HIGH #2 OOF all-rows) | run_train: OOF 行数 | mitigate | **OOF 行数 < 学習窓全行 であることを logger.info で出力し test_oof_parquet_schema_and_row_count で assert（warm-up chunk 0 除外）** |
| T-07-07-07 | Tampering (Codex HIGH #4 hermetic E2E) | run_train: expected_counts | mitigate | **expected_counts パラメータで本番 assert と hermetic bypass を切り替え。hermetic E2E は expected_counts=[] で小規模 fixture が完走** |
| T-07-07-08 | Tampering (Codex HIGH #5 feature_columns) | run_train: config key | mitigate | **config に feature_columns キーを追加し run_train で解決して trainer に引数渡し。test_feature_columns_config_consistency で config と FEATURE_COLUMNS の整合性を検証** |
| T-07-07-09 | Tampering (Cycle-2 HIGH #1 propagation) | run_train: race_date 列保持 | mitigate | **run_train は 07-02 load_features が返す train_df の race_date 列を保持したまま collect_oof_predictions に渡す（drop しない）。trainer が dates=df[\"race_date\"] を渡す前提。run_train が race_date を明示的に drop するコードを書かない** |
| T-07-07-10 | Tampering (Cycle-2 HIGH #2 sentinel) | run_train: expected_counts forwarding | mitigate | **UNIFIED sentinel: None=本番 / []=bypass / 非空 dict=カスタム・{}（空 dict）は使わない（07-02 側で TypeError 拒否）。run_train 署名・forwarding・hermetic E2E call site 全てが 07-02 と同一の sentinel と型を使う** |
| T-07-07-11 | Tampering (Cycle-2 HIGH #3 oof_rows contract) | run_train: metrics.json | mitigate | **run_train が metrics dict に metrics[\"oof_rows\"] = int(len(oof_df)) を必ず書き込み metrics.json に永続化。producer（07-07）/ consumer（07-08）contract を完全一致: key=\"oof_rows\"・型=int・計算=len(oof_df)。test_oof_parquet_schema_and_row_count で metrics.json の oof_rows と OOF parquet 行数の一致を検証** |
</threat_model>

<verification>
- run_train が Wave 1+2 全モジュールを統合し D-15 の6成果物を保存する
- config YAML が sensible defaults + window + fold + feature_columns + artifacts を定義（D-13/D-14・Codex HIGH #5 fix）
- src/ml/__init__.py が公開 API を再エクスポート（Phase 8/9 アクセス可能）
- hermetic E2E テストが5テスト GREEN（UNIFIED expected_counts sentinel・OOF<warm-up・oof_rows producer/consumer contract・feature_columns 整合性含む）
- .gitignore に成果物ディレクトリ追記
</verification>

<success_criteria>
1. MODA-01〜04 の全要件が run_train 単一エントリポイントに統合される
2. D-15 の6成果物（model .txt / calibrator .joblib / OOF parquet / holdout parquet / report+metrics+diagram / config）が全て保存される
3. D-13（sensible defaults + early stopping）・D-14（YAML config + 固定 seed）契約を満たす
4. Phase 8/9 が from src.ml import ... で公開 API にアクセス可能
5. hermetic E2E で run_train の全ステップ＋成果物保存＋expected_counts bypass＋OOF<warm-up＋feature_columns 整合性が検証される
6. Codex HIGH #2/#4/#5 の3つの HIGH concern が解決される
7. **Cycle-2 HIGH #1 propagation: run_train が race_date 列を保持し trainer が dates を転送できる前提を維持**
8. **Cycle-2 HIGH #2: UNIFIED expected_counts sentinel で 07-02 と完全一致（空 dict 拒否）**
9. **Cycle-2 HIGH #3: oof_rows producer/consumer contract で 07-07 metrics.json と 07-08 verify が完全一致**
</success_criteria>

<output>
Create `.planning/phases/07-model-a-top-3-probability/07-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New public symbols created by this plan (consumed by 07-08 phase gate + Phase 8/9):
- Function: `run_train(config_path, expected_counts: dict | list | None = None) -> dict` in `src/ml/run_train.py` (**UNIFIED sentinel — Cycle-2 HIGH #2 fix**: expected_counts=None asserts production counts; expected_counts=[] (empty LIST, not dict) bypasses for hermetic fixtures; non-empty dict = custom; {} (empty dict) raises TypeError in load_features — identical to 07-02; forwards expected_counts to load_features — Codex HIGH #4 fix; resolves feature_columns from config and passes as explicit arg to trainer — Codex HIGH #5 fix; **writes metrics["oof_rows"] = int(len(oof_df)) into metrics.json — Cycle-2 HIGH #3 fix, producer/consumer contract identical to 07-08 consumer**)
- CLI entry point: `python -m src.ml.run_train` (setuptools, not poetry)
- Package public API (src/ml/__init__.py __all__): `run_train, GroupTimeSeriesSplit, train_fold_model, collect_oof_predictions, train_final_model, fit_calibrator, apply_calibrator, save_calibrator, load_calibrator, compute_ece, evaluate, reliability_diagram, compute_popularity_baseline, load_features`
- Config file: `config/phase7_model_a.yaml` (keys: seed, data.{feature_path,train_window,holdout_window,target_column,exclude_column,entry_path,**feature_columns**,categorical_columns,drop_columns}, cv.{n_splits,group_column,sort_column,early_stopping_val_ratio}, model.{objective,metric,num_leaves,learning_rate,...,n_estimators}, early_stopping.{stopping_rounds,verbose,first_metric_only}, calibration.{method,y_min,y_max,out_of_bounds}, evaluation.{ece_bins,ece_tolerance,reliability_diagram}, artifacts.{model_dir,oof_dir,report_dir,model_filename,calibrator_filename,oof_filename,holdout_filename})
- Output directories created at runtime (gitignored): `models/phase7/`, `data/model/oof/`, `reports/phase7/`
- Output artifacts (D-15 contract): `models/phase7/model_a.lgb.txt`, `models/phase7/isotonic_calibrator.joblib`, `data/model/oof/oof_predictions.parquet` (cols: race_id, horse_race_id, p_top3_raw, p_top3_calibrated, target_top3, fold; **row count < train window rows — warm-up excluded per Codex HIGH #2**), `data/model/oof/holdout_predictions.parquet` (same cols, fold="holdout"), `reports/phase7/evaluation_report.md` (includes holdout retune prohibition note — Cycle-2 LOW fix), `reports/phase7/metrics.json` (**MUST include `oof_rows` key of type int = len(oof_df) — Cycle-2 HIGH #3 fix; consumer is 07-08 verify which asserts `oof_rows in m` and `m[oof_rows] < 322510`**), `reports/phase7/reliability_diagram.png`
- Test class: `TestRunTrainE2E` in `tests/ml/test_run_train.py` with methods: test_run_train_hermetic_e2e (expected_counts=[] bypass — Codex HIGH #4 + Cycle-2 HIGH #2 unified sentinel), test_oof_parquet_schema_and_row_count (OOF < warm-up — Codex HIGH #2; **also asserts metrics.json contains oof_rows of type int matching OOF parquet row count — Cycle-2 HIGH #3 producer/consumer contract**), test_holdout_parquet_schema, test_artifacts_all_created, test_feature_columns_config_consistency (config == FEATURE_COLUMNS — Codex HIGH #5)

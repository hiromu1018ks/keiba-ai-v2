---
phase: 02-kaggle-data-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pipeline/kaggle_converter.py
  - tests/pipeline/conftest.py
  - tests/pipeline/test_kaggle_converter.py
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "race_result.csv is read with correct BOM handling (encoding=utf-8-sig) and dtype specification"
    - "Data is filtered to 2015-2021 flat races only (D-01: obstacle excluded)"
    - "One CSV row is correctly split into race (deduplicated by race_id), entry (1:1 per horse), and result (1:1 per horse) tables"
    - "20 race flag columns are converted from sparse text to Optional[bool] (True where non-empty, None where empty/NaN)"
    - "Finish position notes (中/取/失/除/再/降) are handled: 降 keeps finish_position, others get finish_position=None"
    - "odds.csv is read and filtered to match race_result flat race_ids (Pitfall 5: obstacle exclusion applied)"
    - "odds_trifecta and payoff tables are generated from odds.csv trifecta columns"
    - "5 Parquet files are written to data/standard/ with correct schema conformance"
    - "audit_leakage() is called on each generated table and logs results"
  artifacts:
    - path: "src/pipeline/kaggle_converter.py"
      provides: "Main converter: CSV to Parquet with filtering, splitting, transformation"
      exports: ["convert", "read_race_result_csv", "read_odds_csv", "split_race_entry_result", "extract_odds_tables", "convert_flags_to_bool", "process_finish_position"]
    - path: "tests/pipeline/conftest.py"
      provides: "Shared pytest fixtures for sample DataFrames"
    - path: "tests/pipeline/test_kaggle_converter.py"
      provides: "Unit and integration tests for converter functions"
    - path: "data/standard/race.parquet"
      provides: "Race table for 2015-2021 flat races (~21,929 rows)"
    - path: "data/standard/entry.parquet"
      provides: "Entry table for 2015-2021 flat races (~311,806 rows)"
    - path: "data/standard/result.parquet"
      provides: "Result table for 2015-2021 flat races (~311,806 rows)"
    - path: "data/standard/odds_trifecta.parquet"
      provides: "OddsTrifecta table for 2015-2021 flat races (~22,000 rows)"
    - path: "data/standard/payoff.parquet"
      provides: "Payoff table for 2015-2021 flat races (partial, ~22,824 rows)"
  key_links:
    - from: "src/pipeline/kaggle_converter.py"
      to: "src/pipeline/column_mapping.py"
      via: "imports KAGGLE_COLUMN_MAP, ODDS_COLUMN_MAP, FLAG_COLUMNS, DTYPE_SPEC"
      pattern: "from src\\.pipeline\\.column_mapping import"
    - from: "src/pipeline/kaggle_converter.py"
      to: "src/schemas/audit.py"
      via: "imports audit_leakage for post-write validation"
      pattern: "audit_leakage"
    - from: "data/standard/*.parquet"
      to: "src/schemas/*.py"
      via: "schema conformance (column names match schema field names)"
      pattern: "schema field names == Parquet column names"

---

<objective>
Build the CSV-to-Parquet converter that reads Kaggle race_result.csv and odds.csv, filters to 2015-2021 flat races, splits into 5 tables (race, entry, result, odds_trifecta, payoff), and writes standard-layer Parquet files.

Purpose: This is the core raw-to-standard pipeline. Per D-06, each table is a single Parquet file. Per D-01, obstacle races are excluded. Per D-03/D-04, both odds_trifecta and payoff are generated from odds.csv with payoff in "incomplete" state.
Output: 5 Parquet files in data/standard/, tested converter module, passing unit and integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-kaggle-data-pipeline/02-CONTEXT.md
@.planning/phases/02-kaggle-data-pipeline/02-RESEARCH.md
@.planning/phases/02-kaggle-data-pipeline/02-PATTERNS.md
@.planning/phases/02-kaggle-data-pipeline/02-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output: -->

From src/pipeline/column_mapping.py:
  KAGGLE_COLUMN_MAP: dict[str, tuple[str, str]]  -- 66 entries, Japanese CSV name -> (table, english_field)
  ODDS_COLUMN_MAP: dict[str, str]  -- 15 entries, Japanese CSV name -> OddsTrifectaSchema field
  FLAG_COLUMNS: list[str]  -- 20 actual CSV flag column names
  DTYPE_SPEC: dict[str, type]  -- 23 dtype=str entries for mixed-type columns
  TABLE_TO_SCHEMA: dict[str, type]  -- 5 table names -> schema classes
  get_columns_for_table(table_name: str) -> dict[str, str]

From src/schemas/audit.py:
  audit_leakage(model_classes, df, context) -> list[str]
  get_post_race_columns(model_class) -> set[str]

From src/schemas/race.py:
  class RaceSchema(BaseModel) -- race_id, race_date, meeting_num, course_code, course_name, meeting_day, race_condition, race_number, grade_revision, race_name, grade, obstacle, surface, surface_detail, direction, course_detail, distance, weather, track_condition, track_condition_detail, start_time, + 20 race_flag_* Optional[bool] fields

From src/schemas/entry.py:
  class EntrySchema(BaseModel) -- horse_race_id, race_id, bracket_num, horse_number, horse_name, sex, age, weight_assigned, jockey, trainer, owner, horse_weight, weight_change, region, popularity, win_odds

From src/schemas/result.py:
  class ResultSchema(BaseModel) -- horse_race_id, race_id, finish_position, finish_note, finish_time, margin, corner_1-4, last_3f, prize_money

From src/schemas/odds_trifecta.py:
  class OddsTrifectaSchema(BaseModel) -- race_id, trifecta{1,2,3}_{combo_1,combo_2,combo_3,odds,popularity}

From src/schemas/payoff.py:
  class PayoffSchema(BaseModel) -- race_id, combo_1, combo_2, combo_3, odds, payoff_amount
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build test fixtures and unit tests for converter functions</name>
  <files>tests/pipeline/conftest.py, tests/pipeline/test_kaggle_converter.py</files>
  <read_first>
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    src/schemas/audit.py
    tests/schemas/conftest.py
    tests/schemas/test_race.py
    tests/schemas/test_schema_export.py
  </read_first>
  <behavior>
    - Test 1 (conftest): sample_race_result_df fixture returns a 10-row DataFrame with 66 columns using ACTUAL Japanese column names (including the 20 actual flag column names), covering both flat and obstacle races across 2014-2016 dates
    - Test 2 (conftest): sample_odds_df fixture returns a 5-row DataFrame with 104 columns using actual odds.csv column names
    - Test 3: test_date_filter -- convert function produces Parquet rows only where race_date >= 2015-01-01 (excludes the 2014 row)
    - Test 4: test_obstacle_exclusion -- convert function excludes rows where obstacle == "障害" per D-01
    - Test 5: test_race_entry_result_split -- split_race_entry_result produces 3 DataFrames: race (deduplicated by race_id), entry (all rows), result (all rows)
    - Test 6: test_flag_conversion -- convert_flags_to_bool converts "text" -> True, "" -> None, NaN -> None for flag columns
    - Test 7: test_finish_position_normal -- finish position 1-18 converts to Int64 without null
    - Test 8: test_finish_position_withdrawal -- "中" finish note -> finish_position=None, finish_note="中"
    - Test 9: test_finish_position_demoted -- "降" finish note -> finish_position keeps original int value, finish_note="降" (19 rows in real data)
    - Test 10: test_odds_obstacle_filter -- odds table filtered to only race_ids present in flat race_result output (Pitfall 5)
    - Test 11: test_payoff_unpivot -- payoff table has up to 3 rows per race from trifecta1/2/3 columns, with combo_1/2/3 and odds (divided by 10)
    - Test 12: test_parquet_output_exists -- convert function creates 5 Parquet files in output directory
    - Test 13: test_audit_called_on_tables -- audit_leakage is called for race/entry tables with expected pre/post-race column classification
  </behavior>
  <action>
    Per D-02: DataFrame-level validation only. Per D-06/D-07: single-file output. Per D-01: obstacle exclusion.

    Create tests/pipeline/conftest.py with pytest fixtures following the pattern from tests/schemas/conftest.py:

    1. sample_race_result_df: pd.DataFrame with 10 rows using ACTUAL Japanese column names from the CSV (all 66 columns). Include:
       - 5 rows for 2015 flat races (2 unique race_ids, 2-3 horses each)
       - 2 rows for 2016 flat race (1 unique race_id)
       - 2 rows for 2015 obstacle race (obstacle="障害") -- should be filtered out
       - 1 row for 2014 flat race -- should be filtered out
       - Flag columns: some with text values (e.g., "(ハンデ)"), some empty string, some NaN
       - Finish position: mix of normal (1-5), withdrawal ("中" finish_note, NaN finish_position), demoted (finish_note="降", finish_position=2)

    2. sample_odds_df: pd.DataFrame with 5 rows using actual odds.csv column names (104 columns). Include trifecta1/2/3 columns with some NaN. Use race_ids that overlap with sample_race_result_df flat races.

    3. tmp_standard_dir: wraps pytest's tmp_path to create a data/standard/ subdirectory.

    Create tests/pipeline/test_kaggle_converter.py with class-based tests:

    TestDateFilter -- tests 2015+ filter
    TestObstacleExclusion -- tests obstacle exclusion per D-01
    TestRaceEntryResultSplit -- tests 3-way split logic
    TestFlagConversion -- tests 20 flag column boolean conversion
    TestFinishPosition -- tests normal/withdrawal/demoted/disqualified handling
    TestOddsConversion -- tests odds.csv reading, obstacle filter, trifecta extraction
    TestParquetOutput -- tests file existence and basic schema conformance

    Use the error-collection-and-assert pattern from test_classification.py. Use tmp_path for output directories. Import from src.pipeline.kaggle_converter (which will be created in Task 2 -- RED phase creates failing tests first).

    Write all tests first, verify they fail with ImportError, then proceed to Task 2.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && pytest tests/pipeline/test_kaggle_converter.py -x --co 2>&1 | head -30</automated>
  </verify>
  <done>
    - tests/pipeline/conftest.py defines sample_race_result_df, sample_odds_df, tmp_standard_dir fixtures
    - tests/pipeline/test_kaggle_converter.py has 7 test classes covering all conversion behaviors
    - Tests are syntactically valid but fail with ImportError (src.pipeline.kaggle_converter does not exist yet)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement converter module and run integration test on real data</name>
  <files>src/pipeline/kaggle_converter.py, tests/pipeline/test_kaggle_converter.py</files>
  <read_first>
    src/pipeline/column_mapping.py
    src/schemas/audit.py
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    tests/pipeline/conftest.py
    tests/pipeline/test_kaggle_converter.py
  </read_first>
  <acceptance_criteria>
    - `pytest tests/pipeline/test_kaggle_converter.py -x -v` exits 0 with all tests passing
    - src/pipeline/kaggle_converter.py defines function `convert(raw_dir, standard_dir) -> dict[str, Path]`
    - convert() reads race_result.csv with encoding="utf-8-sig", dtype=DTYPE_SPEC, low_memory=False
    - convert() filters to race_date >= "2015-01-01" and obstacle != "障害"
    - Race table: deduplicated by race_id, ~21,929 rows expected
    - Entry table: all filtered rows, ~311,806 rows expected
    - Result table: all filtered rows, ~311,806 rows expected
    - OddsTrifecta table: filtered by join to flat race_ids, 1 row per race
    - Payoff table: unpivoted from trifecta1/2/3 columns, up to 3 rows per race, odds divided by 10
    - 5 Parquet files created in data/standard/
    - audit_leakage() called on race and entry DataFrames
    - Race flag columns converted: non-empty/NaN -> True, empty/NaN -> None
    - Finish position: 降 keeps original position, 中/取/失/除 get None
  </acceptance_criteria>
  <action>
    Create src/pipeline/kaggle_converter.py following the function-based pattern from src/schemas/export.py (module docstring, pathlib.Path for I/O, loguru for logging, TYPE_CHECKING guard for pandas).

    Key functions to implement:

    1. convert(raw_dir: Path = Path("data/raw/kaggle"), standard_dir: Path = Path("data/standard")) -> dict[str, Path]:
       Main entry point. Orchestrates: read -> filter -> split -> transform -> write -> audit. Returns {table_name: output_path}.

    2. read_race_result_csv(csv_path: Path) -> pd.DataFrame:
       pd.read_csv(csv_path, encoding="utf-8-sig", dtype=DTYPE_SPEC, low_memory=False).
       Parse "レース日付" to datetime. No filtering here (caller filters).

    3. read_odds_csv(csv_path: Path) -> pd.DataFrame:
       pd.read_csv(csv_path, encoding="utf-8-sig"). No dtype spec needed for odds (smaller file, no DtypeWarning issues).

    4. split_race_entry_result(df: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame, pd.DataFrame]:
       Uses KAGGLE_COLUMN_MAP to select columns for each table. Rename Japanese names to English names using get_columns_for_table().
       - Race: deduplicate by "race_id". Apply convert_flags_to_bool() to flag columns.
       - Entry: select entry columns, keep all rows.
       - Result: select result columns. Apply process_finish_position(). Convert finish_position to Int64 nullable.
       For all 3 tables: use KAGGLE_COLUMN_MAP to build a rename dict {japanese_name: english_name}, then df.rename(columns=rename_dict).

    5. convert_flags_to_bool(df: pd.DataFrame) -> pd.DataFrame:
       For each column in FLAG_COLUMNS (by English name after rename, i.e., the 20 race_flag_* columns):
       - If value is non-empty and not NaN -> True
       - If value is empty string or NaN -> None (pd.NA)
       Use df[col].notna() & (df[col] != "") for True, else None.

    6. process_finish_position(df: pd.DataFrame) -> pd.DataFrame:
       - Create finish_note column from "着順注記" (before rename): keep the character where not NaN.
       - Convert "着順" (before rename) to Int64 nullable.
       - For rows with finish_note in {"中", "取", "失", "除", "再"}: set finish_position to pd.NA (only if it's not 降).
       - For 降: keep finish_position as-is (per RESEARCH.md Pitfall 7, 19 rows).

    7. extract_odds_tables(odds_df: pd.DataFrame, valid_race_ids: set[str]) -> tuple[pd.DataFrame, pd.DataFrame]:
       - Filter odds_df to only rows where race_id in valid_race_ids (inner join on flat race_result race_ids).
       - odds_trifecta: rename trifecta columns using ODDS_COLUMN_MAP. One row per race.
       - payoff: unpivot trifecta1/2/3 into up to 3 rows per race. Each row has race_id, combo_1, combo_2, combo_3, odds (raw_odds / 10.0 for decimal), payoff_amount=None per D-04.

    For Parquet writing:
    - standard_dir.mkdir(parents=True, exist_ok=True)
    - df.to_parquet(path, engine="pyarrow", compression="snappy", index=False)

    For audit:
    - Call audit_leakage([RaceSchema], race_df, "race table generation")
    - Call audit_leakage([EntrySchema], entry_df, "entry table generation")
    - Log results with loguru.logger

    IMPORTANT: The column rename process must handle the 20 flag columns specially. After initial rename from Japanese to English, convert_flags_to_bool operates on the English column names (race_flag_*). The process_finish_position function must operate BEFORE the column rename on "着順" and "着順注記", or AFTER rename on "finish_position" and "finish_note" -- choose one consistent approach and document it.

    After implementing, run the full test suite to verify all tests pass. Then run the convert() function on real data to generate the 5 Parquet files (this is an integration validation, not a test).
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && pytest tests/pipeline/test_kaggle_converter.py -x -v</automated>
  </verify>
  <done>
    - src/pipeline/kaggle_converter.py defines convert(), read_race_result_csv(), read_odds_csv(), split_race_entry_result(), extract_odds_tables(), convert_flags_to_bool(), process_finish_position()
    - All 13 test behaviors pass (date filter, obstacle exclusion, 3-way split, flag conversion, finish position handling for normal/withdrawal/demoted, odds filter, payoff unpivot, Parquet output, audit called)
    - Unit tests use sample DataFrames from conftest.py (no dependency on real CSV files)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/raw/kaggle/*.csv -> pandas | Static Kaggle files, BOM handled, dtype-specified read |
| pandas -> data/standard/*.parquet | Parquet write via pyarrow engine, paths within project directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CSV injection via formula in data cells | accept | Kaggle static dataset, not user-supplied. LOW risk. |
| T-02-02 | Tampering | Path traversal in output paths | mitigate | pathlib.Path used, output directory hardcoded as data/standard/ relative to project root |
| T-02-03 | DoS | Memory exhaustion via 472MB CSV read | accept | ~687MB for filtered subset, acceptable on Mac. File size known and fixed. |
| T-02-04 | Tampering | DtypeWarning leads to silent data corruption | mitigate | Explicit DTYPE_SPEC for all 23 mixed-type columns, low_memory=False |
| T-02-05 | Tampering | BOM causes first column name mismatch | mitigate | encoding="utf-8-sig" handles BOM automatically |
</threat_model>

<verification>
pytest tests/pipeline/test_kaggle_converter.py -x -v --tb=short
ruff check src/pipeline/kaggle_converter.py
</verification>

<success_criteria>
- convert() reads race_result.csv (472MB) with correct encoding and dtype spec, no DtypeWarning
- 2015-2021 filter applied, obstacle races excluded (D-01)
- race/entry/result tables split correctly from single CSV source
- 20 race flag columns converted from sparse text to Optional[bool]
- Finish position notes handled: 降 keeps position, others get None
- odds.csv filtered to flat race_ids only (Pitfall 5 resolved)
- payoff table unpivoted from wide trifecta columns with odds/10 conversion
- 5 Parquet files written to data/standard/
- audit_leakage() called on race and entry tables
- All unit tests pass
</success_criteria>

## Artifacts this phase produces

- `src/pipeline/kaggle_converter.py` — functions: convert(raw_dir, standard_dir) -> dict[str, Path], read_race_result_csv(csv_path) -> pd.DataFrame, read_odds_csv(csv_path) -> pd.DataFrame, split_race_entry_result(df) -> tuple[DataFrame, DataFrame, DataFrame], extract_odds_tables(odds_df, valid_race_ids) -> tuple[DataFrame, DataFrame], convert_flags_to_bool(df) -> pd.DataFrame, process_finish_position(df) -> pd.DataFrame
- `tests/pipeline/conftest.py` — fixtures: sample_race_result_df, sample_odds_df, tmp_standard_dir
- `tests/pipeline/test_kaggle_converter.py` — test classes: TestDateFilter, TestObstacleExclusion, TestRaceEntryResultSplit, TestFlagConversion, TestFinishPosition, TestOddsConversion, TestParquetOutput
- `data/standard/race.parquet` — race table (2015-2021 flat, ~21,929 rows)
- `data/standard/entry.parquet` — entry table (2015-2021 flat, ~311,806 rows)
- `data/standard/result.parquet` — result table (2015-2021 flat, ~311,806 rows)
- `data/standard/odds_trifecta.parquet` — odds_trifecta table (2015-2021 flat, ~22,000 rows)
- `data/standard/payoff.parquet` — payoff table (partial, ~22,824 rows)

<output>
Create `.planning/phases/02-kaggle-data-pipeline/02-02-SUMMARY.md` when done
</output>

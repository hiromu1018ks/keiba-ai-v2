---
phase: 02-kaggle-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/__init__.py
  - src/pipeline/column_mapping.py
  - tests/pipeline/__init__.py
  - tests/pipeline/test_column_mapping.py
  - pyproject.toml
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "KAGGLE_COLUMN_MAP maps all 66 race_result.csv column names (Japanese) to (table, field) tuples verified against Phase 1 schemas"
    - "ODDS_COLUMN_MAP maps all 15 trifecta-relevant odds.csv column names to OddsTrifectaSchema fields"
    - "FLAG_COLUMNS list contains exact 20 Japanese column names from the CSV header for dtype specification"
    - "DTYPE_SPEC dict provides dtype=str for all 20 flag columns plus 3 mixed-type optional columns"
    - "get_columns_for_table() returns the correct subset of columns for any table name"
    - "pyarrow>=14.0 is listed in pyproject.toml dependencies"
  artifacts:
    - path: "src/pipeline/__init__.py"
      provides: "Pipeline package init with key re-exports"
    - path: "src/pipeline/column_mapping.py"
      provides: "All column mapping dicts, dtype spec, helper functions"
      exports: ["KAGGLE_COLUMN_MAP", "ODDS_COLUMN_MAP", "FLAG_COLUMNS", "DTYPE_SPEC", "TABLE_TO_SCHEMA", "get_columns_for_table"]
    - path: "tests/pipeline/__init__.py"
      provides: "Test package marker"
    - path: "tests/pipeline/test_column_mapping.py"
      provides: "Unit tests verifying all mappings against schemas"
    - path: "pyproject.toml"
      provides: "Updated dependencies with pyarrow"
  key_links:
    - from: "src/pipeline/column_mapping.py"
      to: "src/schemas/*.py"
      via: "imports RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema"
      pattern: "from src\\.schemas\\."
    - from: "tests/pipeline/test_column_mapping.py"
      to: "src/pipeline/column_mapping.py"
      via: "imports KAGGLE_COLUMN_MAP, ODDS_COLUMN_MAP, FLAG_COLUMNS, DTYPE_SPEC"
      pattern: "from src\\.pipeline\\.column_mapping import"

---

<objective>
Define the authoritative column mapping from Kaggle Japanese CSV headers to Phase 1 English schema fields, install pyarrow, and validate all mappings via unit tests.

Purpose: The column mapping is the foundation of the entire conversion pipeline. Getting this right means every downstream transform (Plan 02, Plan 03) can trust the mapping without re-deriving it. This plan also resolves the discrepancy between test_classification.py's shortened flag names and the actual CSV column headers.
Output: src/pipeline/column_mapping.py with all mapping dicts, pyproject.toml updated with pyarrow, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-kaggle-data-pipeline/02-CONTEXT.md
@.planning/phases/02-kaggle-data-pipeline/02-RESEARCH.md
@.planning/phases/02-kaggle-data-pipeline/02-PATTERNS.md
</context>

<interfaces>
<!-- Key types and contracts from Phase 1 schemas that the column mapping must reference -->

From src/schemas/race.py:
  class RaceSchema(BaseModel) -- 20 race_flag_* Optional[bool] fields, all pre_race=True

From src/schemas/entry.py:
  class EntrySchema(BaseModel) -- horse_race_id, race_id, bracket_num, horse_number, horse_name, sex, age, weight_assigned, jockey, trainer, owner, horse_weight, weight_change, region, popularity (post-race), win_odds (post-race)

From src/schemas/result.py:
  class ResultSchema(BaseModel) -- horse_race_id, race_id, finish_position, finish_note, finish_time, margin, corner_1-4, last_3f, prize_money

From src/schemas/odds_trifecta.py:
  class OddsTrifectaSchema(BaseModel) -- race_id, trifecta{1,2,3}_{combo_1,combo_2,combo_3,odds,popularity}

From src/schemas/payoff.py:
  class PayoffSchema(BaseModel) -- race_id, combo_1, combo_2, combo_3, odds, payoff_amount
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pyarrow and create column mapping module with tests</name>
  <files>src/pipeline/__init__.py, src/pipeline/column_mapping.py, tests/pipeline/__init__.py, tests/pipeline/test_column_mapping.py, pyproject.toml</files>
  <read_first>
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    tests/schemas/test_classification.py
    pyproject.toml
  </read_first>
  <behavior>
    - Test 1: KAGGLE_COLUMN_MAP has exactly 66 entries, and every (table, field) resolves to a field in the corresponding schema class.model_fields
    - Test 2: FLAG_COLUMNS list has exactly 20 entries, and every flag column name starts with "レース記号/"
    - Test 3: DTYPE_SPEC contains entries for all 20 flag columns plus "芝・ダート区分2", "内・外・襷区分", "馬場状態2" (23 total)
    - Test 4: ODDS_COLUMN_MAP maps all 15 trifecta-relevant odds.csv columns (三連複{1,2,3}_組合せ{1,2,3}, 三連複{1,2,3}_オッズ, 三連複{1,2,3}_人気) to OddsTrifectaSchema fields
    - Test 5: get_columns_for_table("race") returns only race table column names, get_columns_for_table("entry") returns entry columns, etc.
    - Test 6: TABLE_TO_SCHEMA maps each of the 5 table names to the correct schema class
    - Test 7: Every KAGGLE_COLUMN_MAP Japanese column name matches an actual column header in race_result.csv (verified by checking against a hardcoded expected set of 66 names)
  </behavior>
  <action>
    Per D-06 and D-07: single-file Parquet output per table. Per D-01: obstacle exclusion. Per D-02: no entry-level region filtering. Per D-03/D-04: both odds_trifecta and payoff from odds.csv.

    Step 1: Add "pyarrow>=14.0" to pyproject.toml dependencies list, after "pandas" entry. Run `pip install pyarrow`.

    Step 2: Create src/pipeline/__init__.py following the pattern from src/schemas/__init__.py (module docstring, re-export key symbols, define __all__). Re-export: KAGGLE_COLUMN_MAP, ODDS_COLUMN_MAP, FLAG_COLUMNS, DTYPE_SPEC, TABLE_TO_SCHEMA, get_columns_for_table.

    Step 3: Create tests/pipeline/__init__.py as empty package marker (single blank line, matching tests/schemas/__init__.py pattern).

    Step 4: Create src/pipeline/column_mapping.py with these contents:

    CRITICAL -- The 20 flag column names MUST use the ACTUAL CSV header names (with parentheses/brackets), not the shortened names from test_classification.py. The actual CSV headers for rows 9-28 are:
    "レース記号/[抽]", "レース記号/(馬齢)", "レース記号/牝", "レース記号/(父)", "レース記号/(別定)", "レース記号/(混)", "レース記号/(ハンデ)", "レース記号/(抽)", "レース記号/(市)", "レース記号/(定量)", "レース記号/牡", "レース記号/関東配布馬", "レース記号/(指)", "レース記号/関西配布馬", "レース記号/九州産馬", "レース記号/見習騎手", "レース記号/せん", "レース記号/(国際)", "レース記号/[指]", "レース記号/(特指)"

    These 20 flag columns map to the 20 race_flag_* fields in RaceSchema. The mapping from Japanese flag suffix to English field name:
    - "[抽]" -> race_flag_condition_race
    - "(馬齢)" -> race_flag_age_restricted
    - "牝" -> race_flag_filly_only
    - "(父)" -> race_flag_stallion_only
    - "(別定)" -> race_flag_special_weight
    - "(混)" -> race_flag_allowance
    - "(ハンデ)" -> race_flag_handicap
    - "(抽)" -> race_flag_condition_race
    - "(市)" -> race_flag_allowance
    - "(定量)" -> race_flag_bonus_weight
    - "牡" -> race_flag_colt_only
    - "関東配布馬" -> race_flag_open
    - "(指)" -> race_flag_condition_race
    - "関西配布馬" -> race_flag_open
    - "九州産馬" -> race_flag_allowance
    - "見習騎手" -> race_flag_apprentice
    - "せん" -> race_flag_gelding_only
    - "(国際)" -> race_flag_graded_stakes
    - "[指]" -> race_flag_condition_race
    - "(特指)" -> race_flag_special_weight

    Note: Some CSV flags map to the same schema field (e.g., "(混)", "(市)", "九州産馬" all map to race_flag_allowance). This is acceptable -- multiple CSV columns contribute to a single boolean flag.

    KAGGLE_COLUMN_MAP must contain all 66 entries from race_result.csv. The 46 non-flag entries use the same mapping as in tests/schemas/test_classification.py (rows 1-8, 29-66). The 20 flag entries use the ACTUAL CSV column names above.

    ODDS_COLUMN_MAP must map the 15 trifecta columns from odds.csv to OddsTrifectaSchema fields:
    - "三連複1_組合せ1" -> "trifecta1_combo_1", "三連複1_組合せ2" -> "trifecta1_combo_2", "三連複1_組合せ3" -> "trifecta1_combo_3"
    - "三連複1_オッズ" -> "trifecta1_odds", "三連複1_人気" -> "trifecta1_popularity"
    - Same pattern for 三連複2 and 三連複3

    FLAG_COLUMNS: list of the 20 actual CSV flag column names (for dtype spec).

    DTYPE_SPEC: dict mapping all 20 FLAG_COLUMNS to `str`, plus "芝・ダート区分2" -> str, "内・外・襷区分" -> str, "馬場状態2" -> str. Total 23 entries.

    TABLE_TO_SCHEMA: dict mapping "race" -> RaceSchema, "entry" -> EntrySchema, "result" -> ResultSchema, "odds_trifecta" -> OddsTrifectaSchema, "payoff" -> PayoffSchema.

    get_columns_for_table(table_name: str) -> dict[str, str]: returns {japanese_name: english_name} for all KAGGLE_COLUMN_MAP entries where the tuple's first element equals table_name.

    Step 5: Create tests/pipeline/test_column_mapping.py with class-based tests (TestKaggleColumnMapping, TestOddsColumnMapping, TestDtypeSpec, TestHelperFunctions) that verify all the behaviors listed above. Import from src.pipeline.column_mapping. Follow the error-collection-and-assert pattern from tests/schemas/test_classification.py.

    IMPORTANT: The tests/schemas/test_classification.py KAGGLE_COLUMN_MAP uses DIFFERENT flag column names than the actual CSV. Do NOT copy those flag names. The production column_mapping.py MUST use the actual CSV column names verified from the header output above. The test_classification.py can be updated separately if needed, but that is not this plan's concern.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && pip install pyarrow && pytest tests/pipeline/test_column_mapping.py -x -v</automated>
  </verify>
  <done>
    - pyarrow installed and listed in pyproject.toml dependencies
    - src/pipeline/column_mapping.py defines KAGGLE_COLUMN_MAP (66 entries with actual CSV column names), ODDS_COLUMN_MAP (15 entries), FLAG_COLUMNS (20 entries), DTYPE_SPEC (23 entries), TABLE_TO_SCHEMA (5 entries), get_columns_for_table()
    - All unit tests pass: every mapped field exists in the corresponding schema class, every flag column starts with "レース記号/", dtype spec covers all 23 columns
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file -> pandas | Kaggle static dataset, read-only. BOM handled by encoding parameter. |
| pyproject.toml -> pip | Package installation from PyPI (verified in RESEARCH.md legitimacy audit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CSV injection via data cells | accept | Kaggle static dataset, not user-supplied. LOW risk per RESEARCH.md. |
| T-02-02 | Tampering | Path traversal in output paths | mitigate | Use pathlib.Path for all file paths. Output directory hardcoded as data/standard/. |
| T-02-03 | DoS | Memory exhaustion via large file read | accept | 472MB CSV, ~687MB for filtered subset. Within Mac capabilities. |
| T-02-SC | Tampering | pip install pyarrow | mitigate | pyarrow verified in RESEARCH.md Package Legitimacy Audit: OK, Apache Arrow, 15M+ downloads/month. |
</threat_model>

<verification>
pytest tests/pipeline/ -x -v --tb=short
ruff check src/pipeline/ tests/pipeline/
</verification>

<success_criteria>
- KAGGLE_COLUMN_MAP has exactly 66 entries using actual CSV column names (not the shortened test names)
- Every (table, field) in KAGGLE_COLUMN_MAP resolves to a real schema field in model_fields
- ODDS_COLUMN_MAP has exactly 15 entries for trifecta columns
- FLAG_COLUMNS has exactly 20 entries matching actual CSV headers
- DTYPE_SPEC has exactly 23 entries
- pyarrow>=14.0 in pyproject.toml and pip-installable
- All tests in tests/pipeline/test_column_mapping.py pass
</success_criteria>

## Artifacts this phase produces

- `src/pipeline/` package (new directory)
- `src/pipeline/__init__.py` — re-exports: KAGGLE_COLUMN_MAP, ODDS_COLUMN_MAP, FLAG_COLUMNS, DTYPE_SPEC, TABLE_TO_SCHEMA, get_columns_for_table
- `src/pipeline/column_mapping.py` — dicts: KAGGLE_COLUMN_MAP (dict[str, tuple[str, str]]), ODDS_COLUMN_MAP (dict[str, str]), FLAG_COLUMNS (list[str]), DTYPE_SPEC (dict[str, type]), TABLE_TO_SCHEMA (dict[str, type]); function: get_columns_for_table(table_name: str) -> dict[str, str]
- `tests/pipeline/` package (new directory)
- `tests/pipeline/__init__.py` — empty package marker
- `tests/pipeline/test_column_mapping.py` — test classes: TestKaggleColumnMapping, TestOddsColumnMapping, TestDtypeSpec, TestHelperFunctions
- `pyproject.toml` — modified: added "pyarrow>=14.0" to dependencies

<output>
Create `.planning/phases/02-kaggle-data-pipeline/02-01-SUMMARY.md` when done
</output>

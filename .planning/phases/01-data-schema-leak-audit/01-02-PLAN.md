---
phase: 01-data-schema-leak-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/schemas/result.py
  - src/schemas/odds_trifecta.py
  - src/schemas/payoff.py
  - tests/schemas/test_result.py
  - tests/schemas/test_odds_trifecta.py
  - tests/schemas/test_payoff.py
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "ResultSchema defines all result-level columns with post-race classification"
    - "OddsTrifectaSchema defines trifecta odds columns from odds.csv with post-race classification"
    - "PayoffSchema defines payoff table contract for future phases"
    - "finish_position and finish_note are Optional to handle non-finishers per RESEARCH.md Pitfall #6"
    - "All three schemas export valid JSON schema"
  artifacts:
    - path: "src/schemas/result.py"
      provides: "ResultSchema Pydantic model, all fields pre_race=False"
      contains: "class ResultSchema"
    - path: "src/schemas/odds_trifecta.py"
      provides: "OddsTrifectaSchema Pydantic model, all fields pre_race=False"
      contains: "class OddsTrifectaSchema"
    - path: "src/schemas/payoff.py"
      provides: "PayoffSchema Pydantic model, contract for future phases"
      contains: "class PayoffSchema"
    - path: "tests/schemas/test_result.py"
      provides: "Result schema validation tests"
      contains: "test_result"
    - path: "tests/schemas/test_odds_trifecta.py"
      provides: "Odds trifecta schema validation tests"
      contains: "test_odds_trifecta"
    - path: "tests/schemas/test_payoff.py"
      provides: "Payoff schema validation tests"
      contains: "test_payoff"
  key_links:
    - from: "tests/schemas/test_result.py"
      to: "src/schemas/result.py"
      via: "from src.schemas.result import ResultSchema"
      pattern: "from src\\.schemas\\.result import ResultSchema"
    - from: "tests/schemas/test_odds_trifecta.py"
      to: "src/schemas/odds_trifecta.py"
      via: "from src.schemas.odds_trifecta import OddsTrifectaSchema"
      pattern: "from src\\.schemas\\.odds_trifecta import OddsTrifectaSchema"
    - from: "tests/schemas/test_payoff.py"
      to: "src/schemas/payoff.py"
      via: "from src.schemas.payoff import PayoffSchema"
      pattern: "from src\\.schemas\\.payoff import PayoffSchema"
---

<objective>
result/odds_trifecta/payoffテーブルのPydanticスキーマを定義し、5テーブル中3つのpost-raceテーブルを完成させる。

Purpose: 5テーブルのスキーマ定義を完成させる。resultテーブルは全てのカラムがpost-raceであり、odds_trifectaテーブルはKaggle odds.csvから三連複上位3組のデータを表現し、payoffテーブルはPhase 5/8で使用する契約を定義する。
Output: src/schemas/result.py, src/schemas/odds_trifecta.py, src/schemas/payoff.py, 対応するテストファイル
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
@.planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
@.planning/phases/01-data-schema-leak-audit/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResultSchema with all post-race fields</name>
  <files>
    src/schemas/result.py, tests/schemas/test_result.py
  </files>
  <read_first>
    src/schemas/race.py
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: ResultSchema.model_fields contains all result field names (horse_race_id, race_id, finish_position, finish_note, finish_time, margin, corner_1, corner_2, corner_3, corner_4, last_3f, prize_money)
    - Test 2: EVERY field in ResultSchema has json_schema_extra with pre_race=False, table="result"
    - Test 3: finish_position is Optional[int] and accepts None (handles 1.1% null rate per Pitfall #6)
    - Test 4: finish_note is Optional[str] and accepts None
    - Test 5: last_3f is Optional[float] and accepts None (handles 4.8% null rate)
    - Test 6: model_json_schema() output is valid JSON with all fields having pre_race=false
  </behavior>
  <action>
    Create src/schemas/result.py defining ResultSchema(BaseModel). Per D-01, D-07: result table is fully separate with horse_race_id as join key to entry (D-09: 1-to-1). ALL fields are pre_race=False per CONTEXT.md classification table. Follow same Pydantic pattern as race.py/entry.py.

    Column mapping from RESEARCH.md Kaggle Column Analysis rows 43-44, 52-58, 66:
    - horse_race_id: str (14-digit, join key to entry table per D-09)
    - race_id: str (12-digit, foreign key to race table)
    - finish_position: Optional[int] (1-24 or None, was 着順) -- MUST be Optional due to 1.1% empty values and non-finishers per Pitfall #6
    - finish_note: Optional[str] (中/取/失/除/再, was 着順注記) -- indicates non-standard results
    - finish_time: Optional[str] ("M:SS.T" format, was タイム) -- 1.0% null
    - margin: Optional[str] (e.g. "1.1/4", "大", "ハナ", was 着差) -- 10.0% null, string because format is non-numeric
    - corner_1: Optional[int] (position at 1st corner, was 1コーナー) -- 52.1% null
    - corner_2: Optional[int] (was 2コーナー) -- 44.0% null
    - corner_3: Optional[int] (was 3コーナー) -- 0.7% null
    - corner_4: Optional[int] (was 4コーナー) -- 0.6% null
    - last_3f: Optional[float] (final 3 furlong time in seconds, was 上り) -- 4.8% null
    - prize_money: Optional[float] (in 10K yen, was 賞金(万円)) -- 55.0% null

    Every field uses json_schema_extra={"pre_race": False, "table": "result"}.

    Create tests/schemas/test_result.py verifying: all fields present, ALL fields pre_race=False, Optional fields accept None, model_json_schema() output valid.

    Run tests to verify.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_result.py -x -q</automated>
  </verify>
  <done>
    - ResultSchema defines all 12 result-level columns
    - Every field has pre_race=False, table="result"
    - finish_position and finish_note are Optional (handles non-finishers)
    - All corner and last_3f fields are Optional (high null rates)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create OddsTrifectaSchema and PayoffSchema</name>
  <files>
    src/schemas/odds_trifecta.py, src/schemas/payoff.py,
    tests/schemas/test_odds_trifecta.py, tests/schemas/test_payoff.py
  </files>
  <read_first>
    src/schemas/result.py
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
    .planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: OddsTrifectaSchema.model_fields contains race_id, trifecta1_combo_1/2/3, trifecta1_odds, trifecta1_popularity, trifecta2_combo_1/2/3, trifecta2_odds, trifecta2_popularity, trifecta3_combo_1/2/3, trifecta3_odds, trifecta3_popularity
    - Test 2: ALL OddsTrifectaSchema fields have pre_race=False per D-04
    - Test 3: All trifecta fields are Optional (sparse data: only 54.1% have trifecta1, 0.1% trifecta2, 0.002% trifecta3)
    - Test 4: PayoffSchema.model_fields contains race_id, combo_1/2/3, odds, payoff_amount
    - Test 5: ALL PayoffSchema fields have pre_race=False
    - Test 6: model_json_schema() for both schemas produces valid JSON
  </behavior>
  <action>
    Create src/schemas/odds_trifecta.py defining OddsTrifectaSchema(BaseModel). Per D-04: all fields are pre_race=False. Per D-07: fully separate table. Follow same Pydantic pattern.

    Column mapping from RESEARCH.md "odds.csv: Trifecta-Relevant Columns":
    - race_id: str (12-digit key, was レースID) -- only non-Optional field
    - trifecta1_combo_1: Optional[int] (horse number, was 三連複1_組合せ1)
    - trifecta1_combo_2: Optional[int] (was 三連複1_組合せ2)
    - trifecta1_combo_3: Optional[int] (was 三連複1_組合せ3)
    - trifecta1_odds: Optional[int] (in 0.1 units, e.g. 990 = 99.0x, was 三連複1_オッズ) -- 54.1% coverage
    - trifecta1_popularity: Optional[int] (was 三連複1_人気)
    - trifecta2_combo_1/2/3: Optional[int] -- 0.1% coverage, near-empty
    - trifecta2_odds: Optional[int] (was 三連複2_オッズ) -- 0.1% coverage
    - trifecta2_popularity: Optional[int]
    - trifecta3_combo_1/2/3: Optional[int] -- 0.002% coverage, near-empty
    - trifecta3_odds: Optional[int] (was 三連複3_オッズ) -- 0.002% coverage
    - trifecta3_popularity: Optional[int]

    Every field uses json_schema_extra={"pre_race": False, "table": "odds_trifecta"}. Only race_id is non-Optional.

    Create src/schemas/payoff.py defining PayoffSchema(BaseModel). Per RESEARCH.md Pitfall #4: this is a CONTRACT for future phases (Phase 5 scraping, Phase 8 EV calculation). No direct Kaggle source exists. All fields pre_race=False.

    Expected fields:
    - race_id: str (join key)
    - combo_1: int (1st horse number in trifecta combination)
    - combo_2: int (2nd horse number)
    - combo_3: int (3rd horse number)
    - odds: Optional[float] (odds value as float, not 0.1 units)
    - payoff_amount: Optional[int] (payout in yen)

    Every field uses json_schema_extra={"pre_race": False, "table": "payoff"}.

    Create tests/schemas/test_odds_trifecta.py and tests/schemas/test_payoff.py verifying: all fields present, all fields pre_race=False, Optional fields accept None, model_json_schema() valid.

    Run full test suite: pytest tests/schemas/ -x -q to ensure no regressions across all schema tests.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_result.py tests/schemas/test_odds_trifecta.py tests/schemas/test_payoff.py -x -q</automated>
  </verify>
  <done>
    - OddsTrifectaSchema defines all 16 trifecta odds columns from odds.csv
    - All OddsTrifectaSchema fields are pre_race=False per D-04
    - All trifecta fields except race_id are Optional (sparse coverage)
    - PayoffSchema defines 6 fields as contract for Phase 5/8
    - All PayoffSchema fields are pre_race=False
    - Full test suite passes (test_race + test_entry + test_result + test_odds_trifecta + test_payoff)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure type definitions and test code. No security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | PayoffSchema | accept | Schema definition only, no actual data. Payoff amounts are public JRA data. |
| T-01-08 | Tampering | OddsTrifectaSchema | accept | Type definition only. Phase 8 will validate odds values at runtime. |
</threat_model>

<verification>
- `python -m pytest tests/schemas/ -x -q` passes (all schema tests)
- `python -c "from src.schemas.result import ResultSchema; from src.schemas.odds_trifecta import OddsTrifectaSchema; from src.schemas.payoff import PayoffSchema; print('OK')"` succeeds
- `ruff check src/schemas/result.py src/schemas/odds_trifecta.py src/schemas/payoff.py` passes
</verification>

<success_criteria>
- ResultSchema defines 12 fields, all pre_race=False
- OddsTrifectaSchema defines 16 fields, all pre_race=False, only race_id non-Optional
- PayoffSchema defines 6 fields as contract, all pre_race=False
- finish_position is Optional[int] to handle non-finishers (Pitfall #6)
- Full test suite (all 5 schema test files) passes
- ruff check passes on all created files
</success_criteria>

<output>
Create `.planning/phases/01-data-schema-leak-audit/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/schemas/result.py` -- exports `ResultSchema` class
- `src/schemas/odds_trifecta.py` -- exports `OddsTrifectaSchema` class
- `src/schemas/payoff.py` -- exports `PayoffSchema` class
- `tests/schemas/test_result.py` -- test functions for ResultSchema
- `tests/schemas/test_odds_trifecta.py` -- test functions for OddsTrifectaSchema
- `tests/schemas/test_payoff.py` -- test functions for PayoffSchema

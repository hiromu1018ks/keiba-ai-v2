---
phase: 01-data-schema-leak-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/__init__.py
  - src/schemas/__init__.py
  - tests/__init__.py
  - tests/schemas/__init__.py
  - src/schemas/race.py
  - src/schemas/entry.py
  - tests/schemas/test_race.py
  - tests/schemas/test_entry.py
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "pyproject.toml exists and pytest can discover and run tests"
    - "RaceSchema defines all race-level columns with correct types and pre_race=True for every field"
    - "EntrySchema defines all entry-level columns with correct types, mixed pre_race/post_race classification"
    - "Both schemas export valid JSON schema via model_json_schema()"
    - "D-02: Pydantic is for TYPE DEFINITION ONLY -- schemas define field names, types, and metadata. Row-level validation against 472MB CSV uses DataFrame operations, not per-row Pydantic parsing"
    - "D-06: EntrySchema includes popularity and win_odds fields classified as post-race (pre_race=False). Phase 3 (Feature Engineering) must exclude these from model features -- this schema design enables that separation"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project configuration with pytest, pydantic, ruff, mypy"
      contains: "[tool.pytest.ini_options]"
    - path: "src/schemas/race.py"
      provides: "RaceSchema Pydantic model"
      contains: "class RaceSchema"
    - path: "src/schemas/entry.py"
      provides: "EntrySchema Pydantic model with mixed pre/post-race fields"
      contains: "class EntrySchema"
    - path: "tests/schemas/test_race.py"
      provides: "Race schema validation tests"
      contains: "test_race"
    - path: "tests/schemas/test_entry.py"
      provides: "Entry schema validation tests including post-race classification"
      contains: "test_entry"
  key_links:
    - from: "tests/schemas/test_race.py"
      to: "src/schemas/race.py"
      via: "from src.schemas.race import RaceSchema"
      pattern: "from src\\.schemas\\.race import RaceSchema"
    - from: "tests/schemas/test_entry.py"
      to: "src/schemas/entry.py"
      via: "from src.schemas.entry import EntrySchema"
      pattern: "from src\\.schemas\\.entry import EntrySchema"
---

<objective>
Phase 1の基盤を構築し、race/entryテーブルのPydanticスキーマを定義する。

Purpose: 全下流フェーズが依存するstandard層のスキーマ契約の中核を定義する。raceテーブル（全pre-race）とentryテーブル（mixed pre/post-race）の2つの最も重要なテーブルを最初に確立し、全テーブルに適用されるPydantic Fieldパターンを確立する。
Output: pyproject.toml, src/schemas/race.py, src/schemas/entry.py, 対応するテストファイル
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
@.planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
@.planning/phases/01-data-schema-leak-audit/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project infrastructure and RaceSchema</name>
  <files>
    pyproject.toml, src/__init__.py, src/schemas/__init__.py,
    tests/__init__.py, tests/schemas/__init__.py,
    src/schemas/race.py, tests/schemas/test_race.py
  </files>
  <read_first>
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
    .planning/phases/01-data-schema-leak-audit/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: RaceSchema.model_fields contains all 34+ expected field names (race_id, race_date, meeting_num, course_code, course_name, meeting_day, race_condition, race_number, grade_revision, race_name, grade, obstacle, surface, surface_detail, direction, course_detail, distance, weather, track_condition, track_condition_detail, start_time, plus 20 race_flags_* columns)
    - Test 2: Every field in RaceSchema has json_schema_extra with pre_race=True and table="race"
    - Test 3: model_json_schema() produces valid JSON with all field properties
    - Test 4: Nullable Optional fields accept None, non-nullable fields reject None
    - Test 5: RaceSchema fields have correct Python types (str, int, Optional[str], Optional[int], bool, Optional[bool])
  </behavior>
  <action>
    Create pyproject.toml with PEP 621 [project] table per PATTERNS.md specification. Package name "keiba-ai-v2", requires-python ">=3.12", dependencies: pydantic>=2.13,<3; pandas>=2.3,<3; numpy>=2,<3; lightgbm>=4.6; loguru>=0.7. Optional dev deps: pytest>=9.0, ruff>=0.15, mypy>=1.14. Configure [tool.ruff] target-version="py312" line-length=100. Configure [tool.pytest.ini_options] testpaths=["tests"]. Do NOT use Poetry -- use pip install -e ".[dev]". Poetry is not installed per RESEARCH.md; the pyproject.toml structure is Poetry-compatible so Poetry can be adopted in Phase 2 when the data pipeline requires dependency locking.

    Create empty __init__.py files for src/, tests/, tests/schemas/. Create src/schemas/__init__.py as a stub that will be updated in Plan 05.

    Create src/schemas/race.py defining RaceSchema(BaseModel). Per D-01 and D-10, every Field uses json_schema_extra={"pre_race": True, "table": "race"}. All columns in race table are pre-race per D-03/D-04/D-05 classification in CONTEXT.md. Import from pydantic: BaseModel, Field. Import from typing: Optional.

    Column mapping from RESEARCH.md Kaggle Column Analysis rows 1-42 (race-level subset):
    - race_id: str (12-digit, was レースID)
    - race_date: str (YYYY-MM-DD format, was レース日付)
    - meeting_num: int (was 開催回数)
    - course_code: str (01-10, was 競馬場コード)
    - course_name: str (Japanese, was 競馬場名)
    - meeting_day: int (was 開催日数)
    - race_condition: str (was 競争条件)
    - race_number: int (was レース番号)
    - grade_revision: Optional[str] (96.3% null, was 重賞回次)
    - race_name: str (was レース名)
    - grade: Optional[str] (was リステッド・重賞競走)
    - obstacle: Optional[str] (was 障害区分)
    - surface: str (芝/ダート, was 芝・ダート区分)
    - surface_detail: Optional[str] (was 芝・ダート区分2)
    - direction: str (右/左, was 右左回り・直線区分)
    - course_detail: Optional[str] (外/内2周/襷, was 内・外・襷区分)
    - distance: int (meters, was 距離(m))
    - weather: Optional[str] (晴/曇/雨/小雨/雪/小雪, was 天候)
    - track_condition: Optional[str] (良/稍重/重/不良, was 馬場状態1)
    - track_condition_detail: Optional[str] (was 馬場状態2)
    - start_time: str (HH:MM format, was 発走時刻)
    - 20 boolean flags: race_flag_handicap, race_flag_age_restricted, race_flag_filly_only, etc. (was レース記号/* columns). Use Optional[bool] = Field(default=None, ...) for each. Exact English names for these 20 flags are at Claude's discretion -- use descriptive snake_case names based on the Japanese flag meaning.

    Create tests/schemas/test_race.py with tests verifying: field presence, correct types, all fields pre_race=True, Optional fields accept None, model_json_schema() output contains all properties.

    Run pip install -e ".[dev]" to install the project. Then run tests to verify.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_race.py -x -q</automated>
  </verify>
  <done>
    - pyproject.toml exists and project installs successfully
    - RaceSchema defines all race-level columns with correct Python types
    - Every RaceSchema field has json_schema_extra with pre_race=True, table="race"
    - All tests in test_race.py pass
    - model_json_schema() produces valid JSON output
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EntrySchema with mixed pre/post-race classification</name>
  <files>
    src/schemas/entry.py, tests/schemas/test_entry.py
  </files>
  <read_first>
    src/schemas/race.py
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
    .planning/phases/01-data-schema-leak-audit/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: EntrySchema.model_fields contains all expected entry field names (horse_race_id, race_id, bracket_num, horse_number, horse_name, sex, age, weight_assigned, jockey, trainer, horse_weight, weight_change, region, owner, popularity, win_odds)
    - Test 2: popularity field has json_schema_extra pre_race=False per D-03
    - Test 3: win_odds field has json_schema_extra pre_race=False per D-03
    - Test 4: All other entry fields have json_schema_extra pre_race=True
    - Test 5: horse_weight and weight_change have pre_race=True per D-05
    - Test 6: model_json_schema() output includes both pre_race True and False fields
    - Test 7: Nullable fields (popularity, win_odds, horse_weight, weight_change, region) accept None
  </behavior>
  <action>
    Create src/schemas/entry.py defining EntrySchema(BaseModel). Per D-01, D-07, D-08, D-09: this is the entry table schema with horse_race_id as primary key. Follow same Pydantic pattern as race.py.

    Column mapping from RESEARCH.md Kaggle Column Analysis:
    - horse_race_id: str (14-digit unique key {race_id}_{horse_number:02d}, was レース馬番ID) -- per D-09
    - race_id: str (12-digit foreign key to race table, was レースID)
    - bracket_num: int (ge=1, le=8, was 枠番)
    - horse_number: int (ge=1, was 馬番)
    - horse_name: str (was 馬名)
    - sex: str (牝/牡/セ, was 性別)
    - age: int (ge=2, was 馬齢)
    - weight_assigned: float (carried weight in kg, was 斤量)
    - jockey: str (was 騎手) -- per D-08, string column, no master table
    - trainer: str (was 調教師) -- per D-08, string column, no master table
    - horse_weight: Optional[int] (default=None, was 馬体重) -- per D-05, pre_race=True
    - weight_change: Optional[int] (default=None, was 場体重増減) -- per D-05, pre_race=True, can be negative
    - region: Optional[str] (default=None, was 東西・外国・地方区分)
    - owner: str (was 馬主)
    - popularity: Optional[int] (default=None, was 人気) -- per D-03, json_schema_extra pre_race=False, table="entry"
    - win_odds: Optional[float] (default=None, was 単勝) -- per D-03, json_schema_extra pre_race=False, table="entry"

    All other fields have json_schema_extra pre_race=True, table="entry".

    Create tests/schemas/test_entry.py with tests verifying: field presence, correct types, mixed pre_race classification (popularity and win_odds are False, all others True), D-05 classification (horse_weight, weight_change are True), Optional fields accept None, model_json_schema() output.

    Run tests to verify. Then run full test suite: pytest tests/schemas/ -x -q to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_race.py tests/schemas/test_entry.py -x -q</automated>
  </verify>
  <done>
    - EntrySchema defines all entry-level columns with correct Python types
    - popularity and win_odds have pre_race=False per D-03
    - horse_weight and weight_change have pre_race=True per D-05
    - horse_race_id follows D-09 format: {race_id}_{horse_number:02d}
    - jockey and trainer are string columns per D-08
    - All tests in test_entry.py pass
    - Full test suite (test_race.py + test_entry.py) passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates only type definitions and test code. No network, file I/O, user input, or database. Security surface is negligible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | N/A | accept | No authentication or identity in this phase |
| T-01-02 | Tampering | pyproject.toml | mitigate | Pin dependency versions with >=x.y,<z to prevent supply chain drift |
| T-01-03 | Repudiation | N/A | accept | No audit logging needed for type definitions |
| T-01-04 | Information Disclosure | N/A | accept | No sensitive data in schema definitions |
| T-01-05 | Denial of Service | N/A | accept | No runtime service |
| T-01-06 | Elevation of Privilege | N/A | accept | No privilege model |
| T-01-SC | Tampering | pip install | mitigate | Package legitimacy audit in RESEARCH.md confirms all packages approved |
</threat_model>

<verification>
- `python -m pytest tests/schemas/test_race.py tests/schemas/test_entry.py -x -q` passes
- `python -c "from src.schemas.race import RaceSchema; print(RaceSchema.model_json_schema())"` outputs valid JSON
- `python -c "from src.schemas.entry import EntrySchema; print(EntrySchema.model_json_schema())"` outputs valid JSON
- `ruff check src/schemas/race.py src/schemas/entry.py` passes with no errors
</verification>

<success_criteria>
- pyproject.toml created and project installs with pip install -e ".[dev]"
- RaceSchema defines 34+ fields covering all race-level Kaggle columns (rows 2-42 in RESEARCH.md)
- EntrySchema defines 16 fields covering all entry-level Kaggle columns (rows 1, 45-51, 59-65)
- Every field has json_schema_extra with pre_race (bool) and table (str)
- popularity and win_odds are the ONLY entry fields with pre_race=False
- All tests pass: pytest tests/schemas/ -x -q
- ruff check passes on all created files
</success_criteria>

<output>
Create `.planning/phases/01-data-schema-leak-audit/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `pyproject.toml` -- project configuration
- `src/__init__.py` -- package init
- `src/schemas/__init__.py` -- schemas package init (stub, updated in Plan 05)
- `tests/__init__.py` -- tests package init
- `tests/schemas/__init__.py` -- test schemas package init
- `src/schemas/race.py` -- exports `RaceSchema` class
- `src/schemas/entry.py` -- exports `EntrySchema` class
- `tests/schemas/test_race.py` -- test functions for RaceSchema
- `tests/schemas/test_entry.py` -- test functions for EntrySchema

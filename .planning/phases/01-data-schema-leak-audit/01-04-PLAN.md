---
phase: 01-data-schema-leak-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - tests/schemas/test_classification.py
  - tests/schemas/test_schema_export.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "Every Kaggle column in race_result.csv (66 columns) is mapped to exactly one table's field"
    - "No column is classified as both pre_race=True and pre_race=False across tables"
    - "All 5 model classes produce valid JSON schema via model_json_schema()"
    - "JSON schema output contains pre_race metadata in every property"
    - "Post-race columns match CONTEXT.md decisions D-03, D-04, D-05"
  artifacts:
    - path: "tests/schemas/test_classification.py"
      provides: "Cross-table classification consistency tests"
      contains: "test_classification"
    - path: "tests/schemas/test_schema_export.py"
      provides: "JSON schema export validation tests"
      contains: "test_schema_export"
  key_links:
    - from: "tests/schemas/test_classification.py"
      to: "src/schemas/race.py"
      via: "from src.schemas.race import RaceSchema"
      pattern: "from src\\.schemas\\.race import"
    - from: "tests/schemas/test_classification.py"
      to: "src/schemas/entry.py"
      via: "from src.schemas.entry import EntrySchema"
      pattern: "from src\\.schemas\\.entry import"
    - from: "tests/schemas/test_schema_export.py"
      to: "src/schemas/audit.py"
      via: "Uses all 5 schema classes for export validation"
      pattern: "from src\\.schemas"
---

<objective>
5テーブル全体にわたるクロスカッティング検証を実装する。Kaggle全カラムの分類網羅性、分類の一貫性、JSON schema exportの妥当性を検証する。

Purpose: DATA-01（3層スキーマ定義）とDATA-04（リーク監査機構）の両要件に対する最終品質ゲート。個別テストだけでは検出できないテーブル間の不整合や欠落を捕捉する。
Output: tests/schemas/test_classification.py, tests/schemas/test_schema_export.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
@.planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create classification consistency and schema export tests</name>
  <files>
    tests/schemas/test_classification.py, tests/schemas/test_schema_export.py
  </files>
  <read_first>
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    src/schemas/audit.py
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
    .planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
  </read_first>
  <behavior>
    Classification tests:
    - Test 1: Every field name across all 5 schema classes is unique (no name collision across tables except race_id/horse_race_id which are foreign keys)
    - Test 2: No field is classified as pre_race=True in one table and pre_race=False in another (impossible by design since fields are unique to tables, but verify the metadata values are consistent)
    - Test 3: Post-race column set matches CONTEXT.md decisions: entry table has exactly {popularity, win_odds} as post-race per D-03; result table has all fields as post-race; odds_trifecta has all fields as post-race per D-04; payoff has all fields as post-race
    - Test 4: horse_weight and weight_change in EntrySchema have pre_race=True per D-05
    - Test 5: Total field count across all 5 schemas covers the expected number of Kaggle columns (66 from race_result.csv + relevant odds.csv columns)

    Schema export tests:
    - Test 6: All 5 model classes produce valid JSON via json.dumps(model.model_json_schema()) without error
    - Test 7: Every property in every model's JSON schema contains a "pre_race" key in its extra fields
    - Test 8: Combined export dict (all 5 schemas) has exactly 5 keys: "race", "entry", "result", "odds_trifecta", "payoff"
    - Test 9: Each JSON schema has "properties" key with at least 2 entries
  </behavior>
  <action>
    Create tests/schemas/test_classification.py with classification consistency tests.

    Test structure:
    1. Import all 5 schema classes and get_post_race_columns from audit.py
    2. test_all_field_names_have_metadata: iterate all model_fields across all 5 classes, verify each has json_schema_extra dict with both "pre_race" (bool) and "table" (str) keys
    3. test_post_race_columns_match_decisions: verify get_post_race_columns(EntrySchema) == {"popularity", "win_odds"} per D-03; get_post_race_columns(ResultSchema) returns all result fields (all post-race); get_post_race_columns(OddsTrifectaSchema) returns all fields except race_id is included too since it is post-race
    4. test_horse_weight_is_pre_race: verify EntrySchema.model_fields["horse_weight"].json_schema_extra["pre_race"] is True per D-05, and same for weight_change
    5. test_no_field_name_collision: collect all field names from all 5 schemas into a list, verify that field names appearing in multiple schemas are only join keys (race_id, horse_race_id)
    6. test_total_field_coverage: verify total number of fields across all schemas is >= 80 (66 race_result columns + odds.csv trifecta columns + payoff contract fields)

    Create tests/schemas/test_schema_export.py with JSON schema export tests.

    Test structure:
    1. Import all 5 schema classes and json module
    2. test_all_models_json_schema_export: call model_json_schema() on each class, verify json.dumps() succeeds, verify output has "properties" key, verify each property dict exists
    3. test_json_schema_contains_pre_race_metadata: for each model, verify properties in the JSON schema output contain "pre_race" key. Note: json_schema_extra values may appear under specific keys in the output schema -- verify the actual structure by checking the "pre_race" value exists somewhere in the property definition
    4. test_combined_export: create dict mapping table names to model_json_schema() outputs, verify 5 keys exist
    5. test_schema_has_description: verify each model class has a docstring (model.__doc__ is not None)

    Run full test suite: pytest tests/schemas/ -x -q
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_classification.py tests/schemas/test_schema_export.py -x -q</automated>
  </verify>
  <done>
    - All 5 schema classes have complete field metadata (pre_race + table)
    - Post-race columns exactly match D-03/D-04/D-05 decisions
    - horse_weight and weight_change confirmed as pre_race per D-05
    - No unexpected field name collisions across tables
    - All 5 models produce valid JSON schema export
    - JSON schema output contains pre_race metadata
    - Full test suite (all 8+ test files) passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test code only. No security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | — | — | accept | Test-only code, no security relevance |

</threat_model>

<verification>
- `python -m pytest tests/schemas/ -x -q` passes (all test files)
- `python -m pytest tests/schemas/ -v --tb=short` shows all tests passing
</verification>

<success_criteria>
- test_classification.py verifies D-03, D-04, D-05 decisions are correctly encoded in schema metadata
- test_schema_export.py verifies all 5 models export valid JSON schema
- Full test suite passes with 0 failures
- Total test count across all files is >= 20
</success_criteria>

<output>
Create `.planning/phases/01-data-schema-leak-audit/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `tests/schemas/test_classification.py` -- test functions: test_all_field_names_have_metadata, test_post_race_columns_match_decisions, test_horse_weight_is_pre_race, test_no_field_name_collision, test_total_field_coverage
- `tests/schemas/test_schema_export.py` -- test functions: test_all_models_json_schema_export, test_json_schema_contains_pre_race_metadata, test_combined_export, test_schema_has_description

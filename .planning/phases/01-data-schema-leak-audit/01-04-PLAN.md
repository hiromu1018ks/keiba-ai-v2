---
phase: 01-data-schema-leak-audit
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/schemas/export.py
  - tests/schemas/test_classification.py
  - tests/schemas/test_schema_export.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "Every Kaggle column from race_result.csv is accounted for in exactly one standard-layer table field"
    - "No standard-layer field carries conflicting pre/post-race classification across tables"
    - "The complete column classification is available as a machine-readable JSON file for downstream tooling"
    - "All 5 model classes produce valid JSON schema via model_json_schema()"
    - "Post-race column decisions match D-03, D-04, D-05 exactly"
  artifacts:
    - path: "src/schemas/export.py"
      provides: "export_schema_documentation() to write machine-readable JSON schema to file"
      contains: "def export_schema_documentation"
    - path: "tests/schemas/test_classification.py"
      provides: "Cross-table classification consistency tests including 1-to-1 Kaggle column mapping"
      contains: "test_classification"
    - path: "tests/schemas/test_schema_export.py"
      provides: "JSON schema export validation tests including file export"
      contains: "test_schema_export"
  key_links:
    - from: "tests/schemas/test_classification.py"
      to: "src/schemas/race.py"
      via: "from src.schemas.race import RaceSchema"
      pattern: "from src\\.schemas\\.race import"
    - from: "tests/schemas/test_classification.py"
      to: "src/schemas/entry.py"
      via: "from src.schemas.entry import EntrySchema"
      pattern: "from src\\.schemas\\.entry import"
    - from: "tests/schemas/test_schema_export.py"
      to: "src/schemas/export.py"
      via: "from src.schemas.export import export_schema_documentation"
      pattern: "from src\\.schemas\\.export import"
---

<objective>
5テーブル全体にわたるクロスカッティング検証を実装し、Kaggle全カラムの分類網羅性を機械的に検証する。また、JSON schemaをファイル出力する関数を提供し、machine-readable永続化を実現する。

Purpose: DATA-01 Success Criterion 2（全Kaggleカラムの分類とmachine-readable永続化）の完全な品質ゲート。66カラムの1対1マッピングを明示的に検証し、JSON schema export関数で永続化を実装する。
Output: src/schemas/export.py, tests/schemas/test_classification.py, tests/schemas/test_schema_export.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
@.planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 1-to-1 Kaggle column mapping verification and export function</name>
  <files>
    src/schemas/export.py, tests/schemas/test_classification.py,
    tests/schemas/test_schema_export.py
  </files>
  <read_first>
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    src/schemas/audit.py
    .planning/phases/01-data-schema-leak-audit/01-RESEARCH.md
    .planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
  </read_first>
  <behavior>
    Classification tests (test_classification.py):
    - Test 1 (KAGGLE_COLUMN_1TO1): A hardcoded dict KAGGLE_COLUMN_MAP maps all 66 expected Japanese column names from race_result.csv to their (table_name, english_field_name) pairs. The test iterates every entry, imports the corresponding schema class, and asserts model_fields[english_name] exists. This verifies every Kaggle column is mapped to exactly one field in exactly one table.
    - Test 2 (NO_UNMAPPED_FIELDS): Collect all English field names from the 5 schema classes that have table metadata. For each schema class, iterate model_fields and collect names with json_schema_extra["table"]. Assert that every field mapped in KAGGLE_COLUMN_MAP corresponds to an actual model field, and that there are no orphan fields in the schemas that have no Kaggle source (except payoff schema which is a contract table with no Kaggle source). This catches missing mappings in both directions.
    - Test 3: get_post_race_columns(EntrySchema) == {"popularity", "win_odds"} per D-03; get_post_race_columns(ResultSchema) returns all result fields (all post-race); get_post_race_columns(OddsTrifectaSchema) returns all odds fields per D-04
    - Test 4: horse_weight and weight_change in EntrySchema have pre_race=True per D-05
    - Test 5: No field name collision across tables except race_id and horse_race_id (foreign keys)
    - Test 6: Total field count across all 5 schemas is >= 80 (66 race_result + odds_trifecta + payoff contract)

    Export function and tests (export.py + test_schema_export.py):
    - Test 7: export_schema_documentation() returns a dict with keys "race", "entry", "result", "odds_trifecta", "payoff"
    - Test 8: export_schema_documentation(tmp_path) writes a valid JSON file to tmp_path / "schema.json" containing all 5 table schemas
    - Test 9: The written JSON file is valid JSON (json.load succeeds) and contains exactly 5 top-level keys
    - Test 10: Each schema in the exported JSON has "properties" key with all expected fields and each property contains "pre_race" metadata
  </behavior>
  <action>
    Create tests/schemas/test_classification.py with explicit 1-to-1 Kaggle column mapping verification per DATA-01 SC2.

    Define KAGGLE_COLUMN_MAP as a module-level dict mapping all 66 Japanese column names from RESEARCH.md "Kaggle Column Analysis" table to (table_name, english_field_name) tuples. The map covers:
    - Rows 1-42 (race + entry columns from race_result.csv): レース馬番ID -> ("entry", "horse_race_id"), レースID -> ("race", "race_id"), レース日付 -> ("race", "race_date"), ... (all 42 rows)
    - Rows 43-44 (result: 着順 -> ("result", "finish_position"), 着順注記 -> ("result", "finish_note"))
    - Rows 45-51, 59-65 (entry: 枠番 -> ("entry", "bracket_num"), ..., 調教師 -> ("entry", "trainer"), 馬主 -> ("entry", "owner"))
    - Rows 52-58 (result: タイム -> ("result", "finish_time"), ..., 賞金(万円) -> ("result", "prize_money"))
    - Row 59-60 (entry post-race: 単勝 -> ("entry", "win_odds"), 人気 -> ("entry", "popularity"))
    - Rows 61-62 (entry: 馬体重 -> ("entry", "horse_weight"), 場体重増減 -> ("entry", "weight_change"))
    - Row 63 (entry: 東西・外国・地方区分 -> ("entry", "region"))
    - Rows 64-65 (entry: 調教師 -> ("entry", "trainer"), 馬主 -> ("entry", "owner"))
    - Row 66 (result: 賞金(万円) -> ("result", "prize_money"))

    Note: The 20 レース記号/* columns (rows 9-28) each map to ("race", "race_flag_*") with the exact English name determined by the implementation in Plan 01 Task 1. The test must list all 20 expected flag names. Use the RESEARCH.md column analysis table as the authoritative source for the Japanese column names.

    test_kaggle_column_1to1_mapping: For each (jp_name, (table, eng_name)) in KAGGLE_COLUMN_MAP, import the schema class for that table, and assert eng_name is in schema_class.model_fields. This verifies every Kaggle column has a corresponding field.

    test_no_unmapped_schema_fields: For each of the 5 schema classes (except PayoffSchema which has no Kaggle source), collect all field names with json_schema_extra["table"] == class_table. Assert that every such field name appears as a value in KAGGLE_COLUMN_MAP. This catches fields defined in schemas but not in the mapping.

    test_post_race_columns_match_decisions: verify get_post_race_columns results match D-03/D-04/D-05 exactly.

    test_horse_weight_is_pre_race: verify D-05.

    test_no_field_name_collision: verify only race_id and horse_race_id appear in multiple schemas.

    test_total_field_coverage: verify total field count >= 80.

    Create src/schemas/export.py with export_schema_documentation(output_path: Path | None = None) -> dict:
    - Builds dict mapping table names to model_json_schema() output for all 5 classes
    - If output_path is provided, writes the dict as JSON to that path (using json.dump with indent=2, ensure_ascii=False)
    - Returns the dict

    Create tests/schemas/test_schema_export.py:
    - test_export_returns_all_schemas: call export_schema_documentation() with no args, verify 5 keys
    - test_export_writes_json_file: call with tmp_path / "schema.json", verify file exists, json.load succeeds, 5 keys
    - test_export_json_has_pre_race_metadata: verify properties contain pre_race values
    - test_combined_export: verify all 5 schemas present

    Run full test suite: pytest tests/schemas/ -x -q
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/test_classification.py tests/schemas/test_schema_export.py -x -q</automated>
  </verify>
  <done>
    - KAGGLE_COLUMN_MAP lists all 66 Japanese column names with 1-to-1 mapping to (table, field) tuples
    - test_kaggle_column_1to1_mapping passes: every Kaggle column maps to an existing schema field
    - test_no_unmapped_schema_fields passes: no orphan schema fields (except payoff contract)
    - Post-race columns exactly match D-03/D-04/D-05 decisions
    - export_schema_documentation() returns dict and optionally writes JSON file
    - Written JSON file contains valid schema with pre_race metadata
    - Full test suite passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export function -> filesystem | JSON schema file written to user-specified path. File content is schema metadata only (no sensitive data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | export_schema_documentation | accept | Output is public schema metadata, not executable code. No sensitive data in JSON. |
| T-01-11 | Information Disclosure | schema.json output | accept | Schema definitions contain no PII, credentials, or business secrets. |

</threat_model>

<verification>
- `python -m pytest tests/schemas/ -x -q` passes (all test files)
- `python -m pytest tests/schemas/test_classification.py -v --tb=short` shows 1-to-1 mapping test passing
- `python -c "from src.schemas.export import export_schema_documentation; d = export_schema_documentation(); print(sorted(d.keys()))"` outputs all 5 table names
</verification>

<success_criteria>
- test_classification.py verifies all 66 Kaggle columns have 1-to-1 mapping (DATA-01 SC2)
- export_schema_documentation() provides machine-readable JSON schema persistence (DATA-01 SC2)
- Post-race classification matches D-03/D-04/D-05 exactly
- Full test suite passes with 0 failures
</success_criteria>

<output>
Create `.planning/phases/01-data-schema-leak-audit/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/schemas/export.py` -- exports `export_schema_documentation()` function that returns schema dict and optionally writes JSON file
- `tests/schemas/test_classification.py` -- test functions including test_kaggle_column_1to1_mapping, test_no_unmapped_schema_fields, test_post_race_columns_match_decisions, test_horse_weight_is_pre_race, test_no_field_name_collision, test_total_field_coverage
- `tests/schemas/test_schema_export.py` -- test functions: test_export_returns_all_schemas, test_export_writes_json_file, test_export_json_has_pre_race_metadata, test_combined_export

---
phase: 01-data-schema-leak-audit
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - src/schemas/__init__.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "src/schemas/__init__.py re-exports all 5 schema classes, both audit functions, and the export function"
    - "from src.schemas import RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema succeeds"
    - "from src.schemas import get_post_race_columns, audit_leakage succeeds"
    - "from src.schemas import export_schema_documentation succeeds"
    - "Full test suite passes after __init__.py update"
  artifacts:
    - path: "src/schemas/__init__.py"
      provides: "Re-exports all table models, audit functions, and export function"
      contains: "__all__"
  key_links:
    - from: "src/schemas/__init__.py"
      to: "src/schemas/race.py"
      via: "from src.schemas.race import RaceSchema"
      pattern: "from src\\.schemas\\.race import RaceSchema"
    - from: "src/schemas/__init__.py"
      to: "src/schemas/audit.py"
      via: "from src.schemas.audit import get_post_race_columns, audit_leakage"
      pattern: "from src\\.schemas\\.audit import"
    - from: "src/schemas/__init__.py"
      to: "src/schemas/export.py"
      via: "from src.schemas.export import export_schema_documentation"
      pattern: "from src\\.schemas\\.export import"
---

<objective>
schemasパッケージの__init__.pyを完成させ、全5テーブルモデル、監査関数、エクスポート関数をre-exportする。これによりdownstreamフェーズはfrom src.schemas import ...で必要な全シンボルにアクセスできる。

Purpose: 全プランの成果物を統合し、src.schemasパッケージを単一インポートポイントとして完成させる。これによりPhase 2以降はfrom src.schemas import RaceSchema, audit_leakage等で統一的にアクセスできる。
Output: 更新されたsrc/schemas/__init__.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-schema-leak-audit/01-CONTEXT.md
@.planning/phases/01-data-schema-leak-audit/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize src/schemas/__init__.py with complete re-exports</name>
  <files>
    src/schemas/__init__.py
  </files>
  <read_first>
    src/schemas/__init__.py
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    src/schemas/odds_trifecta.py
    src/schemas/payoff.py
    src/schemas/audit.py
    src/schemas/export.py
  </read_first>
  <behavior>
    - Test 1: "from src.schemas import RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema" succeeds (ImportError if any symbol missing)
    - Test 2: "from src.schemas import get_post_race_columns, audit_leakage" succeeds
    - Test 3: "from src.schemas import export_schema_documentation" succeeds
    - Test 4: RaceSchema.__name__ == "RaceSchema" (verifies re-export preserves class identity)
    - Test 5: __all__ list contains exactly 8 symbols: RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema, get_post_race_columns, audit_leakage, export_schema_documentation
    - Test 6: python -m pytest tests/schemas/ -x -q passes (no regressions)
  </behavior>
  <action>
    Replace the stub src/schemas/__init__.py (created in Plan 01) with the complete re-export module per PATTERNS.md Pattern for __init__.py.

    Module docstring: "Standard-layer schema definitions for the 3-layer data pipeline."

    Import and re-export exactly these 8 symbols:
    - from src.schemas.race import RaceSchema
    - from src.schemas.entry import EntrySchema
    - from src.schemas.result import ResultSchema
    - from src.schemas.odds_trifecta import OddsTrifectaSchema
    - from src.schemas.payoff import PayoffSchema
    - from src.schemas.audit import get_post_race_columns, audit_leakage
    - from src.schemas.export import export_schema_documentation

    Define __all__ = ["RaceSchema", "EntrySchema", "ResultSchema", "OddsTrifectaSchema", "PayoffSchema", "get_post_race_columns", "audit_leakage", "export_schema_documentation"]

    After updating, run the full test suite to verify no regressions: python -m pytest tests/schemas/ -x -v
    Also verify imports work: python -c "from src.schemas import RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema, get_post_race_columns, audit_leakage, export_schema_documentation; print('All imports OK')"
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/schemas/ -x -q && python -c "from src.schemas import RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema, get_post_race_columns, audit_leakage, export_schema_documentation; print('OK')"</automated>
  </verify>
  <done>
    - __init__.py re-exports all 8 symbols
    - from src.schemas import * works correctly
    - Full test suite passes with no regressions
    - ruff check src/schemas/__init__.py passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Module re-exports only. No security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | — | — | accept | Re-export module, no security relevance |
</threat_model>

<verification>
- `python -m pytest tests/schemas/ -x -q` passes (all tests, no regressions)
- `python -c "from src.schemas import *; print(sorted(__all__))"` outputs all 8 symbols
- `ruff check src/schemas/` passes
</verification>

<success_criteria>
- __init__.py exports all 8 symbols via __all__
- Full test suite passes (20+ tests, 0 failures)
- All imports from src.schemas succeed
- Phase 1 is complete: 5 table schemas defined, audit function implemented, export function implemented, all tested
</success_criteria>

<output>
Create `.planning/phases/01-data-schema-leak-audit/01-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/schemas/__init__.py` -- updated from stub to complete re-export module; exports RaceSchema, EntrySchema, ResultSchema, OddsTrifectaSchema, PayoffSchema, get_post_race_columns, audit_leakage, export_schema_documentation

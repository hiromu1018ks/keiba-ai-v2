---
phase: 04-scraping-infrastructure-race-data
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified:
  - src/scraper/normalizer.py
  - tests/scraper/test_normalizer.py
autonomous: true
requirements:
  - SCRP-01
  - SCRP-03

must_haves:
  truths:
    - "Every output DataFrame is reindexed against Schema.model_fields so all expected columns exist in stable order, even for empty input (zero-row typed DataFrame)"
    - "An explicit dtype map coerces each field per schema annotation (str/object, Int64 nullable, Float64 nullable, boolean nullable)"
    - "CYCLE-2 #3: dtype coercion is STRICT. _build_typed_dataframe does NOT use errors='ignore' (which silently skips failed conversions, leaving None-containing int columns as float64 undetected). For Optional[int] fields like finish_position it uses nullable pandas Int64 (matches Kaggle's int64 nullable=True Arrow type exactly); genuine conversion failures (non-numeric text in a numeric column) RAISE so the type guarantee is enforced, not aspirational."
    - "CYCLE-2 #4: re-running the SAME month (smoke then full run) does NOT overwrite prior rows. write_partitioned_parquet performs read-merge-dedup on the primary key (race_id for race; horse_race_id for entry/result) BEFORE atomic replace — a sentinel row from a prior run survives a same-month re-run, and duplicate primary keys collapse to one."
    - "CYCLE-2 #6: entry/result tables (which have NO race_date field — verified against EntrySchema/ResultSchema) are partitioned by race_date JOINED from the race table via race_id[:12]. write_partitioned_parquet accepts an optional partition_map: dict[str, datetime.date] (race_id -> race_date) and looks up the partition key for entry/result rows. No KeyError on df['race_date'] for entry/result."
    - "Validation enforces: unique race_id, unique horse_race_id, entry<->result one-to-one on horse_race_id, entry/result race_id FK into race table"
    - "Output semantics are explicit and documented: date-partitioned Parquet files keyed by race_date (no overwrite of OTHER months; same-month upsert via merge-dedup)"
    - "Parquet writes are atomic: temp file -> schema validation passes -> os.replace rename"
    - "Empty input produces a typed zero-row DataFrame with ALL schema columns, NOT a zero-column DataFrame"
    - "audit_leakage remains scoped to feature-layer usage; standard-layer generation calls schema-conformance validation instead (per Cycle-1 MEDIUM)"
  artifacts:
    - path: "src/scraper/normalizer.py"
      provides: "normalize_to_parquet, _build_typed_dataframe (strict dtypes), validate_integrity, write_partitioned_parquet (merge-dedup + partition_map), SCHEMA_DTYPE_MAP"
      exports: ["normalize_to_parquet", "validate_integrity", "SCHEMA_DTYPE_MAP"]
    - path: "tests/scraper/test_normalizer.py"
      provides: "Normalizer unit tests including strict-dtype, same-month merge-dedup, entry/result partition via race_date join, empty-input typed-DataFrame, integrity checks, atomic partitioned write"
  key_links:
    - from: "src/scraper/normalizer.py"
      to: "src/schemas/race.py"
      via: "RaceSchema.model_fields reindex + dtype map"
      pattern: "RaceSchema\\.model_fields"
    - from: "src/scraper/normalizer.py"
      to: "src/schemas/entry.py"
      via: "EntrySchema.model_fields reindex (NOTE: EntrySchema has NO race_date — partition_map required)"
      pattern: "EntrySchema\\.model_fields"
    - from: "src/scraper/normalizer.py"
      to: "src/schemas/result.py"
      via: "ResultSchema.model_fields reindex (NOTE: ResultSchema has NO race_date — partition_map required)"
      pattern: "ResultSchema\\.model_fields"
    - from: "src/scraper/normalizer.py"
      to: "data/standard/scraped/{YYYYMM}/race.parquet"
      via: "atomic partitioned write with merge-dedup on existing same-month file"
      pattern: "os\\.replace"
    - from: "normalize_to_parquet race_df -> write_partitioned_parquet(entry/result)"
      to: "partition_map (race_id -> race_date)"
      via: "CYCLE-2 #6: entry/result partition key joined from race table"
      pattern: "partition_map"
---

<objective>
パース済みdictデータをstandard層Parquetに変換するnormalizerを実装する。Cycle-1 HIGH #7（schema validation不在）、HIGH #8（上書きで過去バッチ消失）は前回部分的に解決したが、**Cycle-2で3つのHIGHが残存/新規発生**したため今回修正する:

- **Cycle-2 #3 (dtype強制未実施)**: `_build_typed_dataframe` が `astype(target, errors="ignore")` を使っており、Noneを含む `finish_position` (Optional[int]) が `float64` に黙置換されても検出されない。`errors="ignore"` を廃止し、`Optional[int]` には nullable `Int64` を使う。Kaggleの `finish_position` は `int64 nullable=True` であり、pandas nullable `Int64` はArrowに同じ `int64 nullable=True` としてserializeされるため、Kaggle Parquetとのphysical-type一致が達成可能。
- **Cycle-2 #4 (同月上書き)**: 月次パーティションは他月を保護するが、同一月を再実行（smoke→本番）するとread-merge-dedupなしで上書きする。書き込み前に既存パーティションを読み、主キー(race_id / horse_race_id)でconcat→drop_duplicates→atomic replaceする。
- **Cycle-2 #6 (entry/resultのrace_date不在でKeyError)**: `EntrySchema` と `ResultSchema` には `race_date` フィールドがなく、`df["race_date"]` でKeyErrorになる。race表から `race_id[:12]` で `race_date` をlookupする `partition_map` を `write_partitioned_parquet` に渡す方式を採用する（他の方式は混乱しやすいため明示的に選定）。

Purpose: fetcher/parserが生成したデータをPhase 1-2と同じスキーマのParquetに正規化する。Kaggleデータとのpyarrowスキーマ互換性を保証し、Phase 6統合を可能にする。

Output: `src/scraper/normalizer.py` (`normalize_to_parquet`, `validate_integrity`, `SCHEMA_DTYPE_MAP`, `write_partitioned_parquet` with merge-dedup + partition_map)、テスト一式
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md
@.planning/phases/04-scraping-infrastructure-race-data/04-03-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-04-SUMMARY.md
@src/pipeline/kaggle_converter.py
@src/pipeline/validators.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@src/schemas/audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strict-typed normalizer with schema reindex, partition_map join, merge-dedup same-month, and atomic partitioned writes</name>
  <files>src/scraper/normalizer.py</files>
  <read_first>
    - src/pipeline/kaggle_converter.py (convert() pattern: build DataFrames -> ensure dtype -> audit -> write; obstacle filter pattern line 89; process_finish_position pattern)
    - src/pipeline/validators.py (_DTYPE_COMPAT map, _get_expected_dtype, validate_schema_conformance, validate_referential_integrity — REUSE these, do not reinvent)
    - src/schemas/race.py (RaceSchema.model_fields — exact field names, no head_count; CONFIRMS race_date IS a field here)
    - src/schemas/entry.py (EntrySchema.model_fields — CYCLE-2 #6: CONFIRMS race_date is NOT a field; has race_id + horse_race_id)
    - src/schemas/result.py (ResultSchema.model_fields — CYCLE-2 #6: CONFIRMS race_date is NOT a field; has race_id + horse_race_id; finish_position is Optional[int])
    - src/schemas/audit.py (audit_leakage — post-race leakage only; NOT schema conformance)
    - .planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md (Normalizer pattern section)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (Cycle-1 HIGH #7/#8 partially resolved; Cycle-2 HIGH #3 errors=ignore silent skip, HIGH #4 same-month overwrite, HIGH #6 entry/result no race_date KeyError, HIGH #7 null-typed columns)
    - data/standard/{race,entry,result}.parquet (authoritative Kaggle dtypes — read via `python -c "import pyarrow.parquet as pq; print(pq.read_schema('data/standard/result.parquet'))"`. finish_position is `int64 nullable=True`; entry/result have NO race_date column.)
  </read_first>
  <action>
Per SCRP-01 (normalize step), SCRP-03 (standard format output). Per Cycle-1 HIGH #7 (schema validation via reindex) and HIGH #8 (partitioned output) — both already partially resolved. **Per Cycle-2 HIGH #3 (errors=ignore silently skips dtype failures — must raise or use nullable dtype), HIGH #4 (same-month overwrite — must merge-dedup), HIGH #6 (entry/result have no race_date — must join from race table), and HIGH #7 (null-typed Kaggle flag columns cannot achieve physical-type equality — handled in 04-06, but the dtype choices here must not contradict that rule).**

Create `src/scraper/normalizer.py` with these symbols:

1. `SCHEMA_DTYPE_MAP: dict[type[BaseModel], dict[str, str]]` — CONCRETE dtype target per field per schema. The authoritative source for each target is `pyarrow.parquet.read_schema` against `data/standard/{race,entry,result}.parquet`. **CYCLE-2 #3: nullable dtypes are used wherever the schema field is Optional AND/OR the Kaggle column is nullable** — this is the load-bearing change vs the Cycle-1 plan which assigned non-nullable `int64` to `finish_position` and then `errors="ignore"` swallowed the failure.
   - `"string"` for: race_id, race_date, course_code, course_name, race_condition, race_name, surface, surface_detail, direction, course_detail, weather, track_condition, track_condition_detail, start_time, obstacle, grade, grade_revision; horse_race_id, horse_name, sex, jockey, trainer, owner, region; finish_time, margin, finish_note.
   - `"Int64"` (pandas NULLABLE integer, capital-I) for: `finish_position` (Kaggle `int64 nullable=True` — non-nullable `int64` would lose None on the ~1.1% null rate and the `errors="ignore"` fallback left it as float64 undetected; Cycle-2 #3). Also meeting_num, meeting_day, race_number, distance (these are non-null in practice but nullable Int64 is Arrow-compatible with Kaggle's `int64 nullable=True`). Also bracket_num, horse_number, age (entry table). Also corner_1..corner_4 (result table, high null rates).
   - `"Float64"` (pandas NULLABLE float, capital-F) for: weight_assigned, win_odds, popularity, horse_weight, weight_change (Kaggle `double nullable=True`); last_3f, prize_money (Kaggle `double nullable=True`). NOTE: Kaggle `popularity` is `double`, not int — nullable Float64 matches.
   - `"boolean"` (pandas nullable) for all 20 `race_flag_*` fields. **CYCLE-2 #7 (handled in 04-06 but the dtype choice is made here)**: Kaggle's flag columns are a MIX — some are Arrow `bool` (e.g. race_flag_handicap), some are Arrow `null` (e.g. race_flag_stallion_only, race_flag_colt_only — Kaggle has no data for them). pandas nullable `boolean` serializes to Arrow `bool nullable=True` in ALL cases; for the null-only Kaggle columns this is a deliberate PROMOTION (null is a supertype of bool). The 04-06 TestSchemaCompatibility asserts physical-type EQUALITY only for columns where Kaggle is NON-null; for null-only Kaggle columns it asserts the promotion is to a concrete type (bool/string), which is compatible. This dtype choice is the single place a nullable dtype is REQUIRED by Kaggle's own nullability, so it is not in tension with the non-nullable int64/float64 choices elsewhere.
   - Document in the module docstring: which Kaggle columns are Arrow `null` (the scraper-populated dtype is a deliberate promotion: `race_flag_stallion_only`, `race_flag_colt_only`, `race_flag_open`, `race_flag_gelding_only`, `race_flag_amateur`, `race_flag_female_jockey`, `race_flag_listed`, `race_flag_maiden`, `race_flag_mare_only`, `race_flag_stakes`, `race_flag_young_horse`, `obstacle`, `surface_detail`, `track_condition_detail`). These are equality-checked for COLUMN PRESENCE only, not physical-type identity, in 04-06.

2. `def _build_typed_dataframe(rows: list[dict], schema: type[BaseModel]) -> pd.DataFrame`:
   - **CYCLE-2 #3 — STRICT dtype enforcement** (replaces the broken `errors="ignore"` design):
     - `df = pd.DataFrame(rows) if rows else pd.DataFrame()`.
     - Reindex to `list(schema.model_fields.keys())` — guarantees EVERY expected column exists in stable order even for empty input.
     - For each `(col, target)` in `SCHEMA_DTYPE_MAP[schema]`:
       - If `col` not in `df.columns`: skip (reindex already inserted it as NA; the dtype will be set on the empty/NA column by the nullable cast below).
       - Use the NULLABLE pandas dtype for Optional fields (Int64/Float64/boolean) so None values are preserved as NA, not coerced to float64. Convert via `df[col] = df[col].astype(target)` — for nullable dtypes this succeeds on mixed None+int input (the Cycle-2 #3 bug: the old `astype("int64", errors="ignore")` failed silently on `[1, None]`).
       - GENUINE conversion failures (e.g. non-numeric text in finish_position) MUST raise — do NOT swallow. Wrap the cast in try/except only to RE-RAISE with a richer message: `raise TypeError(f"Column {col} could not be coerced to {target} for {schema.__name__}: {e}") from e`. This is the opposite of `errors="ignore"` — the goal is that a dtype contract violation is loud, not silent.
   - Return the typed, reindexed DataFrame.

3. `def validate_integrity(race_df, entry_df, result_df) -> list[str]`:
   - Returns a list of human-readable violation strings (empty list = clean).
   - Checks: a. unique race_id; b. unique entry horse_race_id; c. unique result horse_race_id; d. entry/result horse_race_id 1-to-1 set equality; e. entry race_id subset of race race_id (FK); f. result race_id subset of race race_id (FK).
   - Log each violation at WARNING level. Do NOT raise (return list, caller decides).

4. `def write_partitioned_parquet(table_name: str, df: pd.DataFrame, standard_dir: Path, partition_map: Optional[dict[str, "datetime.date"]] = None, primary_key: str = "race_id") -> list[Path]`:
   - **CYCLE-2 #6 (entry/result have no race_date)**: determine the partition key (YYYYMM) for each row WITHOUT reading `df["race_date"]` directly:
     - For `table_name == "race"`: race_date IS a column (RaceSchema has it). Use `df["race_date"]` directly (pandas string YYYY-MM-DD -> year+month).
     - For `table_name in ("entry", "result")`: `df` has NO race_date column. Use the `partition_map: dict[str, date]` argument (race_id -> race_date) which the normalizer caller builds from the race DataFrame. For each row, look up `partition_map[row["race_id"]]` to get the race_date, then derive YYYYMM. Raise a clear `KeyError` with a helpful message if `partition_map is None` for entry/result tables (do NOT silently fall back to a wrong partition). The race_id on entry/result is the 12-digit form (`horse_race_id[:12]` would also work but entry/result already carry `race_id` directly — use that).
   - For each `(year, month)` partition present: target path `standard_dir / "scraped" / f"{year}{month:02d}" / f"{table_name}.parquet"`.
   - **CYCLE-2 #4 (same-month merge-dedup)**: BEFORE writing, if the target path exists, READ it, `pd.concat([existing_df, new_partition_df], ignore_index=True)`, then `drop_duplicates(subset=[primary_key], keep="last")` (keep="last" so the newer re-run wins on conflict). This upsert-on-primary-key means a sentinel row from a prior smoke run survives a full same-month re-run, AND duplicate primary keys collapse. The primary_key is `race_id` for race, `horse_race_id` for entry/result.
   - ATOMIC WRITE per partition: write to `{path}.tmp` then `os.replace(tmp, path)` (Cycle-1 MEDIUM). Create parent dirs with `mkdir(parents=True, exist_ok=True)`.
   - Use `engine="pyarrow"`, `compression="snappy"`, `index=False` (same as kaggle_converter.py line 117).
   - Return list of written paths.
   - Edge case: empty DataFrame still produces a single placeholder file `standard_dir / "scraped" / f"{table_name}.parquet"` with the typed zero-row schema, so the output is always present.

5. `def normalize_to_parquet(parsed_races: list[dict], standard_dir: Path = Path("data/standard")) -> dict[str, list[Path]]`:
   - Accumulate rows from parsed_races (parsed["race"], parsed["entries"], parsed["results"]).
   - Build typed DataFrames via `_build_typed_dataframe(rows, RaceSchema|EntrySchema|ResultSchema)`.
   - Obstacle filter: drop rows where `race_df["obstacle"] == "障害"`; track dropped race_ids and remove their entries/results (same pattern as kaggle_converter.py line 89). Log the dropped count.
   - **CYCLE-2 #6 — build partition_map for entry/result**: from the filtered `race_df`, build `partition_map: dict[str, datetime.date] = {row["race_id"]: _parse_race_date(row["race_date"]) for row in race_df.to_dict("records")}` (race_date is a string YYYY-MM-DD on RaceSchema; parse to date). Pass this map to `write_partitioned_parquet("entry", entry_df, standard_dir, partition_map=partition_map, primary_key="horse_race_id")` and likewise for result.
   - Run `validate_integrity(race_df, entry_df, result_df)`; log violations but do NOT raise unless a hard contract is violated (duplicate primary keys are hard; FK/1-to-1 are warnings — document this in the docstring).
   - Do NOT call audit_leakage on standard-layer generation (Cycle-1 MEDIUM: popularity/win_odds intentionally belong in the standard entry table).
   - Call `write_partitioned_parquet` for each table; return `{"race": [paths], "entry": [paths], "result": [paths]}`.

Imports: `import os`, `import datetime`, `from pathlib import Path`, `from typing import Optional`, `import pandas as pd`, `from loguru import logger`, `from pydantic import BaseModel`, `from src.schemas.race import RaceSchema`, `from src.schemas.entry import EntrySchema`, `from src.schemas.result import ResultSchema`. Do NOT modify `src/scraper/__init__.py`.
  </action>
  <verify>
    <automated>python -c "from src.scraper.normalizer import normalize_to_parquet, validate_integrity, SCHEMA_DTYPE_MAP, write_partitioned_parquet; import inspect; sig = inspect.signature(write_partitioned_parquet); assert 'partition_map' in sig.parameters, 'Cycle-2 #6: partition_map param missing'; assert 'primary_key' in sig.parameters, 'Cycle-2 #4: primary_key param missing'; from src.schemas.result import ResultSchema; assert SCHEMA_DTYPE_MAP[ResultSchema]['finish_position'] == 'Int64', 'Cycle-2 #3: finish_position must be nullable Int64'; print('Normalizer imports + Cycle-2 #3/#4/#6 signatures OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `SCHEMA_DTYPE_MAP` has entries for RaceSchema, EntrySchema, ResultSchema covering every field
    - **CYCLE-2 #3**: `SCHEMA_DTYPE_MAP[ResultSchema]["finish_position"] == "Int64"` (nullable, NOT `"int64"`). The Cycle-1 plan's non-nullable `"int64"` was unreachable for None-containing input and `errors="ignore"` silently left the column as float64.
    - **CYCLE-2 #3**: `_build_typed_dataframe` does NOT call `astype(..., errors="ignore")` anywhere — `grep -n "errors=.ignore." src/scraper/normalizer.py` returns NOTHING. Genuine conversion failures raise `TypeError` with a column/schema message.
    - **CYCLE-2 #3**: `_build_typed_dataframe([{"finish_position": 1}, {"finish_position": None}], ResultSchema)` produces a column whose dtype is `Int64` (nullable), NOT `float64` (the silent Cycle-1 fallback). A None value does not corrupt the dtype.
    - `_build_typed_dataframe([], RaceSchema)` returns a DataFrame with `len(df) == 0` AND `set(df.columns) == set(RaceSchema.model_fields.keys())` (Cycle-1 HIGH #7 empty-input typed-DataFrame)
    - `_build_typed_dataframe(rows, schema)` reindexes to `list(schema.model_fields.keys())` — column order matches schema
    - `validate_integrity` returns a non-empty list when fed duplicate race_id; returns [] for clean data
    - `validate_integrity` detects: duplicate race_id, duplicate horse_race_id, entry/result 1-to-1 mismatch, orphan FK
    - **CYCLE-2 #6**: `write_partitioned_parquet` signature includes `partition_map: Optional[dict[str, datetime.date]] = None` AND `primary_key: str = "race_id"`. For `table_name="entry"`/`"result"` with a non-None partition_map, rows land under the YYYYMM directory matching `partition_map[row["race_id"]]` — NO KeyError on `df["race_date"]` (entry/result have no such column).
    - **CYCLE-2 #6**: calling `write_partitioned_parquet("entry", entry_df, standard_dir)` WITHOUT partition_map raises a clear `KeyError`/`ValueError` whose message mentions "partition_map required for entry/result tables (no race_date column)" — fail loud, not silent.
    - **CYCLE-2 #4**: when the target partition file already exists, `write_partitioned_parquet` reads it, concatenates, and `drop_duplicates(subset=[primary_key], keep="last")` before atomic replace. A sentinel primary-key row from a prior run SURVIVES a same-month re-run.
    - **CYCLE-2 #4**: duplicate primary keys within a single new batch ALSO collapse via the same drop_duplicates (dedup is applied to the merged frame, not just the existing one).
    - `write_partitioned_parquet` writes to `standard_dir/scraped/{YYYYMM}/{table}.parquet` (partitioned, NOT single-file overwrite — Cycle-1 HIGH #8 stays resolved)
    - Each partition write uses temp + `os.replace` (atomic)
    - `normalize_to_parquet` filters obstacle races and propagates the filter to entries/results
    - `normalize_to_parquet` builds partition_map from race_df and passes it to entry/result writes (CYCLE-2 #6 wiring)
    - `normalize_to_parquet` does NOT call `audit_leakage` (Cycle-1 MEDIUM); schema conformance is via reindex + strict dtype map
    - Return type is `dict[str, list[Path]]` (one path per month partition per table)
    - `grep -n "_scraped\\.parquet" src/scraper/normalizer.py` returns nothing matching a single-file overwrite pattern
  </acceptance_criteria>
  <done>
    - Strict schema-conformance validation implemented (Cycle-1 HIGH #7 stays resolved; Cycle-2 HIGH #3 errors=ignore eliminated)
    - Date-partitioned atomic output with same-month merge-dedup (Cycle-2 HIGH #4 resolved)
    - entry/result partitioned via race_date joined from race table (Cycle-2 HIGH #6 resolved)
    - Empty input yields typed zero-row DataFrame with all columns
    - Integrity checks (unique, FK, 1-to-1) implemented
    - Standard-layer generation no longer false-positives on popularity/win_odds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write and pass normalizer tests (strict dtypes, same-month merge-dedup, entry/result partition_map, integrity, atomic)</name>
  <files>tests/scraper/test_normalizer.py</files>
  <read_first>
    - src/scraper/normalizer.py (functions to test — esp. the new partition_map param and the strict dtype path)
    - src/pipeline/validators.py (dtype compat patterns)
    - tests/scraper/conftest.py (tmp_standard_dir fixture)
    - src/schemas/race.py (RaceSchema field list — no head_count; HAS race_date)
    - src/schemas/entry.py (EntrySchema fields — NO race_date, has race_id + horse_race_id)
    - src/schemas/result.py (ResultSchema fields — NO race_date, finish_position is Optional[int])
    - src/pipeline/kaggle_converter.py (obstacle filter pattern)
    - data/standard/result.parquet (authoritative finish_position dtype: int64 nullable=True — verify via pyarrow.parquet.read_schema)
  </read_first>
  <behavior>
    - Empty input produces typed zero-row DataFrames with ALL schema columns (not zero-column)
    - Output columns match RaceSchema/EntrySchema/ResultSchema model_fields exactly
    - **CYCLE-2 #3**: a None in finish_position produces a nullable Int64 column, NOT float64; the dtype guarantee is enforced (no silent errors=ignore fallback)
    - **CYCLE-2 #3**: genuinely un-coercible input (e.g. "abc" in finish_position) RAISES — it is not silently left as object
    - Obstacle races are filtered from all 3 tables
    - Duplicate race_id is detected by validate_integrity
    - Duplicate horse_race_id is detected
    - Entry/result horse_race_id 1-to-1 mismatch is detected
    - Orphan race_id FK violation is detected
    - Output is partitioned by YYYYMM under standard_dir/scraped/ (no single-file overwrite)
    - **CYCLE-2 #4**: pre-seed a month file with a sentinel primary-key row, run normalize for the same month with NEW rows, assert BOTH sentinel AND new rows present (no overwrite loss) AND duplicate primary keys collapse
    - **CYCLE-2 #6**: entry/result partition files land under the correct {YYYYMM}/ derived from partition_map (race_id -> race_date) WITHOUT a KeyError
    - **CYCLE-2 #6**: calling entry/result write without partition_map raises a clear error
    - Atomic write: no .tmp file remains after success
    - popularity/win_odds do NOT trigger any failure (audit_leakage not called)
  </behavior>
  <action>
Per SCRP-03 (standard format), Cycle-2 HIGH #3/#4/#6.

Create `tests/scraper/test_normalizer.py` with class-based tests. Build a helper `_sample_parsed_races()` returning a list with one flat race (2 horses) and one obstacle race (1 horse) so obstacle filtering can be verified.

1. `class TestTypedDataframe`:
   - `test_empty_input_has_all_columns`: call normalize with `[]`; read the placeholder output; assert race Parquet has `set(columns) == set(RaceSchema.model_fields.keys())` and `len(df) == 0`.
   - `test_columns_match_schema`: normalize `_sample_parsed_races`; read race/entry/result Parquet; assert each table's columns == the corresponding schema's model_fields keys.
   - `test_dtypes_applied`: assert race_id dtype is object/string; race_flag_* dtype is boolean (nullable); distance dtype is Int64 (nullable, matching Kaggle `int64 nullable=True`); weight_assigned dtype is Float64 (nullable, matching Kaggle `double nullable=True`); popularity dtype is Float64 (Kaggle `double`, NOT int).
   - **`test_finish_position_none_preserves_int64_nullable`** (CYCLE-2 #3 — the core regression guard): build a ResultSchema-typed DataFrame from `[{"horse_race_id": "20220105010101", "race_id": "202201050101", "finish_position": 1}, {"horse_race_id": "20220105010102", "race_id": "202201050101", "finish_position": None}]` via `_build_typed_dataframe`; assert `df["finish_position"].dtype == pd.Int64Dtype()` (nullable Int64), NOT `float64`. This is the exact failure Codex flagged — `errors="ignore"` left it as float64 undetected.
   - **`test_genuine_coercion_failure_raises`** (CYCLE-2 #3 — the loud-failure guard): call `_build_typed_dataframe([{"horse_race_id": "x", "race_id": "y", "finish_position": "not_a_number"}], ResultSchema)`; assert it RAISES `TypeError` (or ValueError) — it must NOT silently leave the column as object. Use `pytest.raises(TypeError)`.
   - `test_no_head_count_column`: assert "head_count" NOT in race columns.
   - `test_no_errors_ignore_in_source`: assert `"errors=\"ignore\""` not in `Path("src/scraper/normalizer.py").read_text()` (grep regression guard for Cycle-2 #3).

2. `class TestObstacleFiltering`:
   - `test_obstacle_race_dropped`: sample with 1 flat + 1 obstacle; assert race Parquet has exactly 1 row.
   - `test_obstacle_entries_propagate`: assert entry/result tables exclude obstacle race entries.

3. `class TestIntegrityValidation`:
   - `test_detects_duplicate_race_id`: construct race_df with duplicate race_id; assert validate_integrity returns a string containing "duplicate race_id".
   - `test_detects_duplicate_horse_race_id`: same for entry_df.
   - `test_detects_entry_result_mismatch`: entry has horse_race_id X, result does not; assert violation string mentions "horse_race_id mismatch".
   - `test_detects_orphan_fk`: entry race_id not in race_df; assert "orphan entry race_id" violation.
   - `test_clean_data_no_violations`: well-formed input; assert validate_integrity returns [].

4. `class TestPartitionedOutput`:
   - `test_partitioned_by_year_month`: sample with races in 2022-01 and 2022-02; assert two partition directories exist under `tmp_standard_dir/scraped/202201/` and `tmp_standard_dir/scraped/202202/`.
   - `test_no_single_overwrite_file`: assert `tmp_standard_dir/race_scraped.parquet` does NOT exist (Cycle-1 HIGH #8).
   - `test_atomic_no_tmp_remains`: after normalize, assert no `.parquet.tmp` files exist anywhere under tmp_standard_dir.
   - `test_does_not_overwrite_other_month`: pre-create `tmp_standard_dir/scraped/202112/race.parquet` with sentinel content; normalize a 2022-01 batch; assert the 2021-12 file is untouched.
   - **`test_same_month_merge_dedup_preserves_sentinel`** (CYCLE-2 #4 — the core regression guard): pre-create `tmp_standard_dir/scraped/202201/race.parquet` containing a race row with `race_id="202201010101"` (the sentinel). Run normalize for a 2022-01 batch that includes a DIFFERENT race `race_id="202201010102"` AND a duplicate of the sentinel `race_id="202201010101"`. Read back the 2022-01 race.parquet; assert BOTH `202201010101` (sentinel preserved) AND `202201010102` (new row added) are present, AND `race_id` is unique (the duplicate sentinel collapsed via drop_duplicates keep="last"). This is the exact failure Codex flagged — same-month overwrite lost prior rows.
   - **`test_entry_result_partitioned_via_partition_map`** (CYCLE-2 #6 — the core regression guard): normalize a sample with race_id "202201050101" (race_date "2022-01-05") and entries/results on that race; assert entry partition file exists under `tmp_standard_dir/scraped/202201/entry.parquet` AND result under `tmp_standard_dir/scraped/202201/result.parquet`. Critically, the entry/result DataFrames have NO race_date column — the partition key came from the partition_map built in normalize_to_parquet. NO KeyError.
   - **`test_entry_write_without_partition_map_raises`** (CYCLE-2 #6 — fail-loud guard): call `write_partitioned_parquet("entry", entry_df, tmp_standard_dir)` (partition_map=None); assert it raises with a message mentioning "partition_map" — so a future caller that forgets the join fails loudly instead of mis-partitioning.
  </action>
  <verify>
    <automated>pytest tests/scraper/test_normalizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 classes pass: TestTypedDataframe, TestObstacleFiltering, TestIntegrityValidation, TestPartitionedOutput
    - Test count >= 18 (was 13; +5 for the Cycle-2 #3/#4/#6 tests)
    - **CYCLE-2 #3**: `test_finish_position_none_preserves_int64_nullable` passes (None -> Int64, not float64)
    - **CYCLE-2 #3**: `test_genuine_coercion_failure_raises` passes (loud failure, no silent object fallback)
    - **CYCLE-2 #3**: `test_no_errors_ignore_in_source` passes (grep regression guard)
    - **CYCLE-2 #4**: `test_same_month_merge_dedup_preserves_sentinel` passes (sentinel survives, duplicates collapse)
    - **CYCLE-2 #6**: `test_entry_result_partitioned_via_partition_map` passes (no KeyError; correct YYYYMM)
    - **CYCLE-2 #6**: `test_entry_write_without_partition_map_raises` passes (fail-loud)
    - Empty-input test verifies ALL schema columns present (Cycle-1 HIGH #7)
    - Partitioned-output test verifies no single-file overwrite (Cycle-1 HIGH #8)
    - Atomic-write test verifies no .tmp files remain
    - FK / uniqueness / 1-to-1 violations all detected
    - popularity/win_odds do NOT cause failures
    - `pytest tests/scraper/test_normalizer.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    - Empty-input typed-DataFrame verified (Cycle-1 HIGH #7)
    - Strict dtype verification incl. nullable Int64 for finish_position (Cycle-2 HIGH #3 resolved)
    - Schema column match verified for all 3 tables
    - Obstacle filtering propagated to entries/results verified
    - Integrity validation (unique, FK, 1-to-1) verified
    - Same-month merge-dedup verified (Cycle-2 HIGH #4 resolved)
    - entry/result partition via partition_map verified (Cycle-2 HIGH #6 resolved)
    - Date-partitioned atomic output verified (Cycle-1 HIGH #8)
    - audit_leakage false-positive on entry popularity/win_odds avoided
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser output -> normalizer | Parsed dicts validated and converted to strictly-typed DataFrames |
| normalizer -> filesystem | Atomic partitioned Parquet writes to data/standard/scraped/{YYYYMM}/ with merge-dedup |
| normalizer race_df -> entry/result partition | partition_map carries race_id->race_date across the table boundary (Cycle-2 #6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | Silent schema drift (missing columns, wrong dtypes) | mitigate | Cycle-1 HIGH #7: reindex against Schema.model_fields; explicit SCHEMA_DTYPE_MAP; **Cycle-2 HIGH #3: strict coercion that RAISES on genuine failure (no errors=ignore); nullable Int64 for Optional[int] so None does not silently become float64**. |
| T-04-16 | Tampering | Prior-month data erased by overwrite | mitigate | Cycle-1 HIGH #8: date-partitioned output keyed by race_date YYYYMM. **Cycle-2 HIGH #4: same-month re-runs read-merge-dedup on primary key before atomic replace (sentinel row survives; duplicates collapse).** |
| T-04-16b | Tampering | entry/result mis-partitioned or KeyError (no race_date column) | mitigate | **Cycle-2 HIGH #6: write_partitioned_parquet accepts partition_map (race_id->race_date) for entry/result tables; omits the map -> loud KeyError, not silent mis-partition.** |
| T-04-17 | Denial of Service | Partial write left by interruption | mitigate | Atomic write (temp + os.replace); zero-row typed-DataFrame ensures output always present. |
| T-04-18 | Tampering | Broken Kaggle join key (horse_race_id) | mitigate | Validates 1-to-1 entry/result on horse_race_id; FK to race_id; parser-side 14-digit format guard (Plan 04). |
| T-04-19 | Information Disclosure | Parquet at rest | accept | Local-only; no external transmission; standard format. |
</threat_model>

<verification>
- `pytest tests/scraper/test_normalizer.py -x -q` passes
- `python -c "from src.scraper.normalizer import normalize_to_parquet, validate_integrity, SCHEMA_DTYPE_MAP, write_partitioned_parquet"` exits 0
- `python -c "import inspect; from src.scraper.normalizer import write_partitioned_parquet; assert 'partition_map' in inspect.signature(write_partitioned_parquet).parameters"` exits 0 (Cycle-2 #6)
- `grep -c 'errors="ignore"' src/scraper/normalizer.py` returns 0 (Cycle-2 #3)
- No `_scraped.parquet` single-file overwrite pattern in normalizer.py
</verification>

<success_criteria>
- Schema-conformance validation via model_fields reindex + STRICT dtype map (Cycle-1 HIGH #7 + Cycle-2 HIGH #3 resolved: no errors=ignore; nullable Int64 for Optional[int])
- Date-partitioned atomic output with same-month merge-dedup (Cycle-1 HIGH #8 + Cycle-2 HIGH #4 resolved)
- entry/result partitioned via partition_map joined from race table (Cycle-2 HIGH #6 resolved)
- Empty input yields typed zero-row DataFrame with all schema columns
- Integrity checks (unique race_id/horse_race_id, entry/result 1-to-1, FK) enforced
- audit_leakage scoped to feature layer; standard-layer uses schema conformance
- Obstacle filtering propagated to all 3 tables
</success_criteria>

## Artifacts this phase produces

- `src/scraper/normalizer.py` -- `SCHEMA_DTYPE_MAP: dict[type[BaseModel], dict[str, str]]` (nullable Int64/Float64/boolean where Kaggle is nullable; Cycle-2 #3); functions `_build_typed_dataframe(rows, schema)` (STRICT coercion — raises on genuine failure, no errors=ignore), `validate_integrity(race_df, entry_df, result_df) -> list[str]`, `write_partitioned_parquet(table_name, df, standard_dir, partition_map=None, primary_key="race_id") -> list[Path]` (Cycle-2 #4 merge-dedup + Cycle-2 #6 partition_map), `normalize_to_parquet(parsed_races, standard_dir) -> dict[str, list[Path]]`
- Output layout: `data/standard/scraped/{YYYYMM}/{race,entry,result}.parquet` (partitioned, atomic, same-month upsert via merge-dedup)
- `tests/scraper/test_normalizer.py` -- classes: `TestTypedDataframe` (incl. `test_finish_position_none_preserves_int64_nullable`, `test_genuine_coercion_failure_raises`, `test_no_errors_ignore_in_source`), `TestObstacleFiltering`, `TestIntegrityValidation`, `TestPartitionedOutput` (incl. `test_same_month_merge_dedup_preserves_sentinel`, `test_entry_result_partitioned_via_partition_map`, `test_entry_write_without_partition_map_raises`)

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-05-SUMMARY.md` when done
</output>

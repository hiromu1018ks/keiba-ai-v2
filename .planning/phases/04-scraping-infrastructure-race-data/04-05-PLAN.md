---
phase: 04-scraping-infrastructure-race-data
plan: 05
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - src/scraper/__init__.py
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-05

must_haves:
  truths:
    - "from src.scraper import fetch_race_html, enumerate_race_ids, parse_race_html, normalize_to_parquet all work"
    - "Full test suite (tests/scraper/) passes"
    - "All 4 requirement IDs (SCRP-01, SCRP-02, SCRP-03, SCRP-05) have passing test coverage"
  artifacts:
    - path: "src/scraper/__init__.py"
      provides: "Updated package init with working imports"
      min_lines: 20
  key_links:
    - from: "src/scraper/__init__.py"
      to: "src/scraper/fetcher.py"
      via: "import fetch_race_html, enumerate_race_ids"
      pattern: "from src.scraper.fetcher import"
    - from: "src/scraper/__init__.py"
      to: "src/scraper/parser.py"
      via: "import parse_race_html"
      pattern: "from src.scraper.parser import"
    - from: "src/scraper/__init__.py"
      to: "src/scraper/normalizer.py"
      via: "import normalize_to_parquet"
      pattern: "from src.scraper.normalizer import"
---

<objective>
スクレイピングパッケージの統合とインポート修正、全テストスイートの通過確認。

Purpose: Plan 01-04で作成した全モジュールが正しく連携し、パッケージとしてimport可能であることを確認する。最終的な品質ゲートとして全テストを実行する。
Output: 更新されたsrc/scraper/__init__.py（実働import）、全テスト通過確認
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-02-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-03-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-04-SUMMARY.md
@src/scraper/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix package imports and run full test suite</name>
  <files>src/scraper/__init__.py</files>
  <read_first>
    - src/scraper/__init__.py (current state from Plan 01 -- may have placeholder imports)
    - src/scraper/fetcher.py (verify actual exported symbols)
    - src/scraper/parser.py (verify actual exported symbols)
    - src/scraper/normalizer.py (verify actual exported symbols)
  </read_first>
  <action>
Per SCRP-01 (integrated scraping infrastructure):

1. Update `src/scraper/__init__.py` to remove any placeholder comments and ensure all imports resolve:
   - `from src.scraper.fetcher import fetch_race_html, enumerate_race_ids`
   - `from src.scraper.parser import parse_race_html`
   - `from src.scraper.normalizer import normalize_to_parquet`
   - `__all__` = ["fetch_race_html", "enumerate_race_ids", "parse_race_html", "normalize_to_parquet"]

2. Verify all imports work: `python -c "from src.scraper import fetch_race_html, enumerate_race_ids, parse_race_html, normalize_to_parquet; print('All imports OK')"`

3. Run full scraper test suite: `pytest tests/scraper/ -x -q` -- all tests must pass

4. Run full project test suite: `pytest tests/ -x` -- verify no regressions from scraper additions

5. If any test fails, investigate and fix the root cause (likely import path issues or test data fixture mismatches between conftest.py and actual parser output)
  </action>
  <verify>
    <automated>pytest tests/scraper/ -x -q &amp;&amp; python -c "from src.scraper import fetch_race_html, enumerate_race_ids, parse_race_html, normalize_to_parquet; print('Package OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from src.scraper import fetch_race_html, enumerate_race_ids, parse_race_html, normalize_to_parquet` exits 0
    - `pytest tests/scraper/ -x -q` all tests pass
    - `pytest tests/ -x` all tests pass (no regressions)
    - src/scraper/__init__.py has no placeholder comments
  </acceptance_criteria>
  <done>
    - Package imports fully functional
    - All scraper tests pass (fetcher, parser, normalizer)
    - Full project test suite green (no regressions)
    - SCRP-01, SCRP-02, SCRP-03, SCRP-05 all covered by passing tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package integration | Import resolution and module wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Package import chain | accept | No external input at integration layer; import errors caught by tests |
</threat_model>

<verification>
- `pytest tests/scraper/ -x -q` passes
- `pytest tests/ -x` passes
- `python -c "from src.scraper import fetch_race_html, enumerate_race_ids, parse_race_html, normalize_to_parquet"` exits 0
</verification>

<success_criteria>
- All 4 scraper modules importable from src.scraper package
- Full test suite green (scraper tests + existing pipeline/feature tests)
- All phase 4 requirement IDs covered: SCRP-01, SCRP-02, SCRP-03, SCRP-05
</success_criteria>

## Artifacts this phase produces

- `src/scraper/__init__.py` -- updated with working imports from fetcher, parser, normalizer modules

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-05-SUMMARY.md` when done
</output>

---
phase: 04-scraping-infrastructure-race-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scraper/__init__.py
  - tests/scraper/__init__.py
  - pyproject.toml
  - tests/scraper/conftest.py
autonomous: true
requirements:
  - SCRP-01

must_haves:
  truths:
    - "src.scraper package is importable WITHOUT triggering any import of fetcher/parser/normalizer"
    - "src.scraper.__init__ contains no module-level import of src.scraper.fetcher, src.scraper.parser, or src.scraper.normalizer"
    - "playwright, beautifulsoup4, lxml are declared in pyproject.toml dependencies"
    - "tests/scraper package is discoverable by pytest"
    - "tests/scraper/conftest.py exists and provides the shared tmp_path-based fixtures used by later plans"
  artifacts:
    - path: "src/scraper/__init__.py"
      provides: "Import-safe empty package marker (no eager submodule imports until Plan 06 wires re-exports)"
    - path: "tests/scraper/__init__.py"
      provides: "Test package marker"
    - path: "tests/scraper/conftest.py"
      provides: "Shared fixtures: tmp_raw_dir, tmp_standard_dir, golden_html_dir"
    - path: "pyproject.toml"
      provides: "Updated dependencies: playwright, beautifulsoup4, lxml"
  key_links:
    - from: "pyproject.toml"
      to: "src/scraper/"
      via: "dependency declaration"
      pattern: "playwright"
    - from: "src/scraper/__init__.py"
      to: "(none)"
      via: "no eager submodule imports"
      pattern: "from src\\.scraper\\.(fetcher|parser|normalizer) import"
---

<objective>
スクレイピングパッケージのスケルトンと依存関係をセットアップする。Wave 1の間は`src/scraper/__init__.py`をimport-safe（空）に保ち、後続のfetcher/parserモジュールが自分自身をimportできるようにする。

Purpose: Phase 4の全モジュール(fetcher/parser/normalizer)が所属する`src/scraper`パッケージを作成し、D-02に基づきPlaywright/BS4/lxmlをインストール可能にする。Codex Review HIGH #3（eager importが後続planをブロック）を修正: `__init__.py`は空にし、公開APIのre-exportはPlan 06（最終統合）で行う。

Output: `src/scraper/__init__.py`（import-safe空パッケージ）、`tests/scraper/__init__.py`、`tests/scraper/conftest.py`（共有fixture）、`pyproject.toml`更新
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md
@src/pipeline/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create import-safe src/scraper package, test package, and declare dependencies</name>
  <files>src/scraper/__init__.py, tests/scraper/__init__.py, tests/scraper/conftest.py, pyproject.toml</files>
  <read_first>
    - src/pipeline/__init__.py (existing package init pattern for docstring style ONLY — note this plan intentionally does NOT copy its re-export pattern)
    - pyproject.toml (current dependencies list — find exact insertion point for new entries)
    - tests/pipeline/conftest.py (existing fixture style for tmp_path-based dirs)
  </read_first>
  <action>
Per D-02 (Playwright + BS4 + lxml) and SCRP-01 (separated scraping infrastructure). Per Codex Review HIGH #3 (REVIEWS.md Plan 04-01): `src/scraper/__init__.py` MUST be import-safe — Python executes `__init__.py` before importing any submodule, so eager imports of nonexistent fetcher/parser/normalizer would block Plans 02/03 from importing their own submodules.

1. Create `src/scraper/__init__.py` as an IMPORT-SAFE EMPTY package marker:
   - Triple-quoted module docstring: `"""Scraping infrastructure package for netkeiba race data collection. Public re-exports are added in Plan 06 (final integration) once all submodules exist."""`
   - NO module-level `import` statements referencing `src.scraper.fetcher`, `src.scraper.parser`, or `src.scraper.normalizer`.
   - NO `__all__` declaration (added in Plan 06 when re-exports exist).
   - Optionally a comment line: `# Submodules: fetcher.py (Plan 02/03), parser.py (Plan 04), normalizer.py (Plan 05). Re-exports wired in Plan 06.`

2. Create `tests/scraper/__init__.py` as an empty file (same as `tests/pipeline/__init__.py`).

3. Create `tests/scraper/conftest.py` with the shared directory fixtures later plans depend on (avoids each plan recreating conftest). Define exactly these three fixtures using `tmp_path` (do NOT hardcode real `data/` paths):
   - `tmp_raw_dir(tmp_path)` -> `Path`: returns `(tmp_path / "data" / "raw" / "netkeiba")` after `mkdir(parents=True, exist_ok=True)`. This is the root that fetchers compose into `{YYYY}/{MM}/{race_id}.html`.
   - `tmp_standard_dir(tmp_path)` -> `Path`: returns `(tmp_path / "data" / "standard")` after `mkdir(parents=True, exist_ok=True)`.
   - `golden_html_dir` -> `Path`: returns the repo-relative path `tests/scraper/fixtures/html/` and ensures it exists via `mkdir(parents=True, exist_ok=True)`. (Golden HTML files are saved by Plan 04/06; the fixture only guarantees the directory exists.)
   Each fixture must use `@pytest.fixture` decorator and import `pytest`, `Path`.

4. Update `pyproject.toml`: add `"playwright>=1.49"`, `"beautifulsoup4>=4.12"`, `"lxml>=5.0"` to the main `dependencies` list (NOT a dev/optional extra — these are runtime deps per D-02), placed immediately after the existing `"pyarrow>=14.0"` entry. Do NOT modify `[tool.pytest]`, `[tool.ruff]`, `[tool.mypy]`, or any other section.
  </action>
  <verify>
    <automated>python -c "import src.scraper; print('package import OK')" && grep -c "playwright" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import src.scraper"` exits 0 (package importable with empty __init__)
    - `grep -E "from src\.scraper\.(fetcher|parser|normalizer)" src/scraper/__init__.py` returns no matches (import-safe)
    - `grep -c "__all__" src/scraper/__init__.py` returns 0 (no __all__ until Plan 06)
    - pyproject.toml contains the literal strings `"playwright>=1.49"`, `"beautifulsoup4>=4.12"`, `"lxml>=5.0"` in the main dependencies list
    - `python -c "from tests.scraper.conftest import tmp_raw_dir, tmp_standard_dir, golden_html_dir"` does not raise on import (pytest fixtures resolvable)
    - `ls tests/scraper/__init__.py tests/scraper/conftest.py` exits 0
  </acceptance_criteria>
  <done>
    - `src/scraper/__init__.py` exists, is importable, and contains zero submodule imports
    - `tests/scraper/__init__.py` and `tests/scraper/conftest.py` exist
    - `pyproject.toml` lists all three new runtime dependencies
    - Codex Review HIGH #3 resolved (no eager imports in __init__.py)
  </done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies from pyproject.toml and verify Chromium binary</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (after Task 1 edits — confirm the three new entries are present)
  </read_first>
  <action>
Per D-02 (Playwright for HTML fetching) and Codex Review MEDIUM (REVIEWS.md Plan 04-01): install via the project's declared dependencies, not a bare `pip install <pkg>` that can diverge from pinned versions.

1. Run `pip install -e .` from the repo root so the three newly-declared dependencies are installed at the versions pyproject.toml specifies. (If a project-specific install command is documented in CLAUDE.md or an existing README/Makefile, prefer that command instead and note it in the SUMMARY.)

2. Run `python -m playwright install chromium` to download the Chromium browser binary required by Playwright. Per Codex Review MEDIUM this is machine state, not reproducible config — log the command and outcome in the SUMMARY so the next machine can reproduce it.

3. Verify each import in a single command: `python -c "from playwright.sync_api import sync_playwright; from bs4 import BeautifulSoup; import lxml; print('All deps OK')"`.

4. If `playwright install chromium` fails (e.g., network blocked), DO NOT mask it — record the exact error in the SUMMARY and continue (fetcher modules built in Plans 02/03 will surface the same error when first launched, which is the correct failure point). Real Playwright launches are deferred to Plans 02/03.
  </action>
  <verify>
    <automated>python -c "from playwright.sync_api import sync_playwright; from bs4 import BeautifulSoup; import lxml; print('All deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from playwright.sync_api import sync_playwright"` exits 0
    - `python -c "from bs4 import BeautifulSoup"` exits 0
    - `python -c "import lxml"` exits 0
    - `pip show playwright beautifulsoup4 lxml` lists all three installed (SUMMARY records versions)
    - `python -m playwright install chromium` either succeeds or its exact error is recorded in the SUMMARY
  </acceptance_criteria>
  <done>
    - All three runtime dependencies importable from a clean Python invocation
    - Playwright Chromium binary installed (or failure recorded for follow-up)
    - Codex Review MEDIUM (install via pyproject, not bare pip) resolved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install / playwright install | External package + binary download from PyPI / Microsoft CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pip install playwright/bs4/lxml | mitigate | RESEARCH.md Package Legitimacy Audit verifies all three as [OK]: playwright (Microsoft, ~4M/mo DLs), beautifulsoup4 (~18 yrs, ~40M/mo), lxml (~18 yrs, ~30M/mo). No [SUS]/[SLOP] packages in this phase. |
| T-04-SC | Tampering | pip/cargo installs (supply chain) | mitigate | All packages pre-verified [OK] in RESEARCH.md audit table; `pip install -e .` resolves from declared pyproject versions, not bare `pip install <pkg>`. No blocking checkpoint needed because no [ASSUMED]/[SUS] packages exist. |
| T-04-02 | Tampering | `playwright install chromium` binary | mitigate | Microsoft-published Chromium build fetched from playwright CDN; binary verified by Playwright at launch. Recorded in SUMMARY for reproducibility. |
</threat_model>

<verification>
- `python -c "import src.scraper"` exits 0 with empty __init__
- `grep -E "from src\.scraper\.(fetcher|parser|normalizer)" src/scraper/__init__.py` returns nothing
- `grep -c "playwright" pyproject.toml` >= 1
- `python -c "from playwright.sync_api import sync_playwright; from bs4 import BeautifulSoup; import lxml"` exits 0
</verification>

<success_criteria>
- `src/scraper` importable with empty __init__ (HIGH #3 resolved)
- `tests/scraper` package + conftest fixtures ready for Plans 02-06
- All three runtime dependencies declared in pyproject.toml and installed
- Playwright Chromium available (or failure recorded)
</success_criteria>

## Artifacts this phase produces

- `src/scraper/__init__.py` -- IMPORT-SAFE empty package marker. NO `__all__`, NO submodule imports. Public re-exports (`fetch_race_html`, `enumerate_races`, `parse_race_html`, `normalize_to_parquet`) are added in Plan 06.
- `tests/scraper/__init__.py` -- empty test package marker
- `tests/scraper/conftest.py` -- fixtures: `tmp_raw_dir`, `tmp_standard_dir`, `golden_html_dir`
- `pyproject.toml` -- new runtime dependencies: `playwright>=1.49`, `beautifulsoup4>=4.12`, `lxml>=5.0`

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md` when done
</output>

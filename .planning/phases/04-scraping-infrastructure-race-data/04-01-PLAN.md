---
phase: 04-scraping-infrastructure-race-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scraper/__init__.py
  - tests/scraper/__init__.py
  - pyproject.toml
autonomous: true
requirements:
  - SCRP-01

must_haves:
  truths:
    - "src.scraper package is importable with public API exports"
    - "playwright, beautifulsoup4, lxml are listed in pyproject.toml dependencies"
    - "tests/scraper package is importable by pytest"
  artifacts:
    - path: "src/scraper/__init__.py"
      provides: "Scraper package with __all__ exports"
      min_lines: 15
    - path: "tests/scraper/__init__.py"
      provides: "Test package init"
      min_lines: 0
  key_links:
    - from: "pyproject.toml"
      to: "src/scraper/"
      via: "dependency declaration + package discovery"
      pattern: "playwright"
---

<objective>
Scraperパッケージのスケルトンと依存関係をセットアップする。

Purpose: Phase 4の全モジュール(fetcher/parser/normalizer)が所属するsrc/scraperパッケージを作成し、Playwright/BS4/lxmlをインストール可能にする。
Output: src/scraper/__init__.py (exportsのみ)、tests/scraper/__init__.py、pyproject.toml更新
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@src/pipeline/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/scraper package skeleton and update pyproject.toml</name>
  <files>src/scraper/__init__.py, tests/scraper/__init__.py, pyproject.toml</files>
  <read_first>
    - src/pipeline/__init__.py (package init pattern to follow)
    - pyproject.toml (current dependencies section)
  </read_first>
  <action>
Per D-02 (Playwright) and SCRP-01 (fetch/parse/normalize infrastructure):

1. Create `src/scraper/__init__.py` following `src/pipeline/__init__.py` pattern:
   - Triple-quoted module docstring: "Scraping infrastructure package for netkeiba data collection."
   - Import and re-export: `fetch_race_html`, `enumerate_race_ids` (from fetcher), `parse_race_html` (from parser), `normalize_to_parquet` (from normalizer).
   - Explicit `__all__` list with all 4 symbols.
   - NOTE: The actual modules (fetcher.py, parser.py, normalizer.py) are created in later plans. For now, the imports will fail -- this is expected. Add a comment "# Modules created in subsequent plans" after the imports.

2. Create `tests/scraper/__init__.py` as empty file (same pattern as `tests/pipeline/__init__.py`).

3. Update `pyproject.toml` dependencies list: add `"playwright>=1.49"`, `"beautifulsoup4>=4.12"`, `"lxml>=5.0"` after the existing `"pyarrow>=14.0"` entry. Do NOT modify any other sections.
  </action>
  <verify>
    <automated>python -c "import src.scraper; print(src.scraper.__all__)" 2>&amp;1 || echo "Expected: imports fail until modules created" &amp;&amp; grep -c "playwright" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `"playwright>=1.49"` in dependencies list
    - pyproject.toml contains `"beautifulsoup4>=4.12"` in dependencies list
    - pyproject.toml contains `"lxml>=5.0"` in dependencies list
    - src/scraper/__init__.py has docstring and __all__ list with 4 symbol names
    - tests/scraper/__init__.py exists (empty or minimal)
  </acceptance_criteria>
  <done>
    - src/scraper/__init__.py exists with module docstring, import declarations, and __all__ list
    - tests/scraper/__init__.py exists
    - pyproject.toml has playwright, beautifulsoup4, lxml in dependencies
    - grep finds "playwright" in pyproject.toml
  </done>
</task>

<task type="auto">
  <name>Task 2: Install new dependencies and verify Playwright chromium</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (after Task 1 edits)
  </read_first>
  <action>
Per D-02 (Playwright for HTML fetching):

1. Run `pip install playwright beautifulsoup4 lxml` to install the three new dependencies.
2. Run `playwright install chromium` to download the Chromium browser binary required by Playwright.
3. Verify installation by running: `python -c "from playwright.sync_api import sync_playwright; print('Playwright OK')"` and `python -c "from bs4 import BeautifulSoup; print('BS4 OK')"` and `python -c "import lxml; print('lxml OK')"`.
4. If playwright install chromium fails, log the error but continue -- the actual fetcher module will be built in Plan 02.
  </action>
  <verify>
    <automated>python -c "from playwright.sync_api import sync_playwright; from bs4 import BeautifulSoup; import lxml; print('All deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from playwright.sync_api import sync_playwright"` exits 0
    - `python -c "from bs4 import BeautifulSoup"` exits 0
    - `python -c "import lxml"` exits 0
    - pip show confirms playwright, beautifulsoup4, lxml installed
  </acceptance_criteria>
  <done>
    - All three dependencies importable in Python
    - Playwright Chromium browser binary installed
    - System ready for fetcher module development
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install | External package installation from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pip install playwright/bs4/lxml | mitigate | RESEARCH.md Package Legitimacy Audit: all [OK]/Approved -- playwright (Microsoft, ~4M/mo), beautifulsoup4 (~18yrs, ~40M/mo), lxml (~18yrs, ~30M/mo) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | All packages verified [OK] in RESEARCH.md audit table; no [SUS] or [SLOP] packages |
</threat_model>

<verification>
- `python -c "from playwright.sync_api import sync_playwright; from bs4 import BeautifulSoup; import lxml; print('OK')"` passes
- `grep -c "playwright" pyproject.toml` returns >= 1
- `ls src/scraper/__init__.py tests/scraper/__init__.py` exits 0
</verification>

<success_criteria>
- src/scraper package importable (imports will fail until modules created, but package exists)
- tests/scraper package discoverable by pytest
- All 3 new dependencies installed and importable
- Playwright Chromium binary available
</success_criteria>

## Artifacts this phase produces

- `src/scraper/__init__.py` -- package with `__all__` listing `fetch_race_html`, `enumerate_race_ids`, `parse_race_html`, `normalize_to_parquet`
- `tests/scraper/__init__.py` -- empty test package init
- `pyproject.toml` -- updated dependencies: `playwright>=1.49`, `beautifulsoup4>=4.12`, `lxml>=5.0`

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md` when done
</output>

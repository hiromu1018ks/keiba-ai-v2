---
phase: 04-scraping-infrastructure-race-data
plan: 06
type: execute
wave: 5
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - src/scraper/__init__.py
  - src/scraper/orchestrator.py
  - tests/scraper/test_end_to_end.py
  - tests/scraper/test_orchestrator.py
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-05

must_haves:
  truths:
    - "CYCLE-2 #5: a SINGLE full-chain end-to-end test connects enumerate -> fetch (injected transport) -> parse -> normalize in ONE flow. It is DISTINCT from the parse-only fixture test and from the per-component mocked tests. The network boundary is mocked via an injectable fetch_html callable / FetcherSession stub returning saved golden HTML; the REAL enumerate_races, parse_race_html, normalize_to_parquet, and the race_id/horse_race_id/course-code/flag contracts all run. Final Parquet is asserted for schema, 14-digit horse_race_id, correct course code, graded flag, and a row count matching the fixture."
    - "CYCLE-2 #7: TestSchemaCompatibility asserts physical-type EQUALITY only for columns where the Kaggle source dtype is NON-null (int64/double/bool/string); for Kaggle-null columns (e.g. race_flag_stallion_only, race_flag_colt_only, obstacle, surface_detail, track_condition_detail — Kaggle has no data) it asserts the scraper-populated dtype (bool/string) is a deliberate PROMOTION (null is a supertype). The Cycle-1 'physical-type equality on every overlapping column' rule was unachievable because pandas nullable boolean serializes to Arrow bool even for an all-None column, while those Kaggle columns are Arrow null. The revised assertion is precise about which columns are equality-checked vs promotion-allowed."
    - "CYCLE-3 #2 (offline race-fetch path): run_scrape(live=False, fetch_html=transport) routes the injected transport to RACE fetching via fetch_race_html(fetch_callable=transport), not only to enumeration. A race for which the transport returns None (and whose raw HTML is not pre-saved) is skipped/quarantined (logged, race dropped, others proceed) — NOT an AttributeError on None.fetch_with_retry(). The happy-path e2e still pre-saves golden HTML (dedup short-circuits before the transport is consulted), but test_full_chain_handles_failed_fetch now exercises a real transport-None → fetch_race_html-returns-None → race-skipped flow that was previously unreachable."
    - "The orchestrator wires FetcherSession + enumeration + fetch_race_html + parse_race_html + normalize_to_parquet for a date range"
    - "src/scraper/__init__.py finally re-exports the public API (now that all submodules exist) — this is the ONLY plan that touches __init__.py re-exports"
    - "ruff check and mypy run clean on src/scraper/ and tests/scraper/"
    - "Scraped Parquet schema is compared against Kaggle Parquet schema with the explicit equality-vs-promotion rule (Cycle-2 #7)"
    - "All Playwright unit tests remain mocked; an OPT-IN live smoke test exists but is skipped by default"
  artifacts:
    - path: "src/scraper/__init__.py"
      provides: "Public re-exports now that fetcher/parser/normalizer/enumeration all exist"
      exports: ["fetch_race_html", "enumerate_races", "parse_race_html", "normalize_to_parquet", "FetcherSession", "RaceRef"]
    - path: "src/scraper/orchestrator.py"
      provides: "run_scrape(start_date, end_date, raw_dir, standard_dir, live=False, fetch_html=None)"
      exports: ["run_scrape"]
    - path: "tests/scraper/test_end_to_end.py"
      provides: "Full-chain e2e test (Cycle-2 #5), parse-only fixture test, dtype-fidelity test (Cycle-2 #7), opt-in live smoke"
    - path: "tests/scraper/test_orchestrator.py"
      provides: "Orchestrator unit test with all sub-steps mocked"
  key_links:
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/fetcher.py"
      via: "FetcherSession + fetch_race_html"
      pattern: "FetcherSession"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/enumeration.py"
      via: "enumerate_races + make_fetch_html_callable"
      pattern: "enumerate_races"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/parser.py"
      via: "parse_race_html"
      pattern: "parse_race_html"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/normalizer.py"
      via: "normalize_to_parquet"
      pattern: "normalize_to_parquet"
    - from: "src/scraper/__init__.py"
      to: "all submodules"
      via: "re-exports added in this plan"
      pattern: "from src\\.scraper\\.(fetcher|parser|normalizer|enumeration|models) import"
    - from: "tests/scraper/test_end_to_end.py TestFullChainE2E"
      to: "enumerate_races + parse_race_html + normalize_to_parquet (REAL) + injected fetch_html (Cycle-2 #5)"
      via: "single end-to-end flow with network boundary mocked"
      pattern: "def test_full_chain_end_to_end"
---

<objective>
スクレイピングパイプライン全体を統合し、Cycle-2で残存していた2つのHIGHを修正する:

- **Cycle-2 #5 (full-chain e2e不在)**: Cycle-1のe2eテストはparse→normalizeのみで、enumerate/fetchは別々の完全モックテストに分かれていた。単一テストでenumerate→fetch→parse→normalizeを繋ぐものがなかった。ネットワーク境界のみをモック（注入可能な `fetch_html` / FetcherSession stub が保存済みgolden HTMLを返す）し、それ以外はREALな関数を走らせるfull-chainテストを追加する。
- **Cycle-2 #7 (dtype-fidelity達成不可能)**: Cycle-1のDTYPE FIDELITY基準は「Kaggle Parquetと全重複列でphysical-type equality」を要求していたが、pandas nullable booleanは常にArrow boolにserializeされるのに対し、Kaggleの一部フラグ列はArrow null（データ欠損）のため、equalityは成立しない。明示的な「null→concrete昇格ルール」を導入し、Kaggle非null列のみequality検証、Kaggle-null列は具体型(bool/string)への意図的昇格として許容する。

Cycle-1 MEDIUM（orchestrator不在、ruff/mypy不在、Kaggle overlap比較不在、live smoke test）は前回解決済み。Plan 01で意図的に空にした`__init__.py`に、ここで初めて公開APIのre-exportを追加する。

Purpose: 個別テストは通っても実際のHTML構造やスキーマ互換性が壊れているケースを検出できるよう、golden fixtureを使ったfull-chain e2eテストでパイプライン全体を検証する。Phase 6統合に向け、スクレイプParquetとKaggle Parquetのスキーマ互換性を達成可能な形で比較する。

Output: `src/scraper/orchestrator.py` (`run_scrape`)、更新済み`src/scraper/__init__.py`（公開re-export）、`tests/scraper/test_end_to_end.py`（full-chain e2e + dtype-fidelity）、`tests/scraper/test_orchestrator.py`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-VALIDATION.md
@.planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md
@.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-02-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-03-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-04-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-05-SUMMARY.md
@src/scraper/__init__.py
@src/scraper/enumeration.py
@src/scraper/fetcher.py
@src/scraper/parser.py
@src/scraper/normalizer.py
@src/scraper/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire orchestrator (with injectable fetch_html for e2e) and add public re-exports to __init__.py</name>
  <files>src/scraper/orchestrator.py, src/scraper/__init__.py</files>
  <read_first>
    - src/scraper/__init__.py (currently import-safe empty — this plan adds the re-exports)
    - src/scraper/enumeration.py (enumerate_races signature: start_date, end_date, fetch_html; URLs are absolute per Cycle-2 #1)
    - src/scraper/fetcher.py (FetcherSession, fetch_race_html, make_fetch_html_callable, module-level fetch_with_retry per Cycle-2 #8)
    - src/scraper/parser.py (parse_race_html signature)
    - src/scraper/normalizer.py (normalize_to_parquet signature; write_partitioned_parquet partition_map per Cycle-2 #6)
    - src/scraper/models.py (RaceRef)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (Cycle-1 MEDIUM orchestrator; Cycle-2 #5 — e2e needs an injectable fetch boundary)
  </read_first>
  <action>
Per SCRP-01 (integrated scraping infrastructure), Cycle-1 MEDIUM (orchestrator), Cycle-2 HIGH #5 (e2e needs an injectable fetch boundary so a test can substitute golden HTML without monkeypatching Playwright internals).

1. Create `src/scraper/orchestrator.py` with `def run_scrape(start_date: datetime.date, end_date: datetime.date, raw_dir: Path = Path("data/raw/netkeiba"), standard_dir: Path = Path("data/standard"), live: bool = False, max_races: Optional[int] = None, fetch_html: Optional[Callable[[str], Optional[str]]] = None) -> dict[str, list[Path]]`:
   - **Cycle-2 #5 — injectable fetch boundary**: if `fetch_html` is provided, use it directly as the enumeration fetch callable AND skip launching FetcherSession for enumeration (but still use a session for `fetch_race_html` if `live=True`). If `fetch_html is None` AND `live=True`, open a real `FetcherSession()` and build the callable via `make_fetch_html_callable(session)`. If `fetch_html is None` AND `live=False`, raise `ValueError("run_scrape requires either live=True or an injected fetch_html callable")` — the Cycle-1 `live=False` default that still permitted network was a Codex MEDIUM (dead parameter); this makes `live=False` WITHOUT an injected fetch_html a hard error so the safety guarantee is real. This injection point is what the Cycle-2 #5 full-chain e2e test uses: it passes a `fetch_html` backed by saved golden HTML and runs the REAL enumerate→fetch→parse→normalize.
   - Calls `enumerate_races(start_date, end_date, fetch_html)` to get `list[RaceRef]`. If `max_races` is set, truncate for smoke runs.
   - For each `RaceRef`: **CYCLE-3 #2 (offline race-fetch path)**: `path = fetch_race_html(race_ref, session=session_or_none, raw_dir=raw_dir, fetch_callable=fetch_html if (not live and fetch_html is not None) else None)`. In the offline injected-transport mode (`live=False, fetch_html=transport`), the transport is now passed as `fetch_callable` to `fetch_race_html` so that a race NOT already pre-saved to the raw path is fetched via the transport (and a transport returning None is handled gracefully → race skipped), rather than dereferencing a None session and crashing with `AttributeError: 'NoneType' object has no attribute 'fetch_with_retry'` (the exact crash Codex flagged in HIGH #2). The live path passes `fetch_callable=None` and uses the real session as before. The happy-path e2e test STILL pre-saves golden HTML to the expected raw path (so `fetch_race_html`'s SCRP-05 dedup short-circuits before the transport is consulted) — but now the failed-fetch test can exercise a real transport-None → `fetch_race_html`-returns-None → race-skipped flow without pre-saving. If path is None, log and skip. Then `parsed = parse_race_html(path)`; append to `parsed_races`.
   - After the loop: `return normalize_to_parquet(parsed_races, standard_dir)`.
   - The `with FetcherSession() as session:` block is used ONLY in the live path. Document the two modes in the docstring.

2. Update `src/scraper/__init__.py` — REPLACE the empty body with the public re-exports (NOW that all submodules exist; this is the ONLY plan that adds re-exports per Cycle-1 HIGH #3):
   - Module docstring: `"""Scraping infrastructure for netkeiba race data. Public API re-exports."""`
   - `from src.scraper.fetcher import FetcherSession, fetch_race_html, fetch_with_retry`
   - `from src.scraper.enumeration import enumerate_races, enumerate_race_day_urls, enumerate_races_for_day`
   - `from src.scraper.parser import parse_race_html`
   - `from src.scraper.normalizer import normalize_to_parquet`
   - `from src.scraper.models import RaceRef`
   - `from src.scraper.orchestrator import run_scrape`
   - `__all__ = ["FetcherSession", "fetch_race_html", "fetch_with_retry", "enumerate_races", "enumerate_race_day_urls", "enumerate_races_for_day", "parse_race_html", "normalize_to_parquet", "RaceRef", "run_scrape"]`

Imports in orchestrator.py: `import datetime`, `from pathlib import Path`, `from typing import Callable, Optional`, `from loguru import logger`, `from src.scraper.fetcher import FetcherSession, fetch_race_html, make_fetch_html_callable`, `from src.scraper.enumeration import enumerate_races`, `from src.scraper.parser import parse_race_html`, `from src.scraper.normalizer import normalize_to_parquet`.
  </action>
  <verify>
    <automated>python -c "from src.scraper import run_scrape, fetch_race_html, fetch_with_retry, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef; import inspect; assert 'fetch_html' in inspect.signature(run_scrape).parameters, 'Cycle-2 #5: run_scrape must accept injectable fetch_html'; print('Package public API + Cycle-2 #5 injectable boundary OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.scraper import run_scrape, fetch_race_html, fetch_with_retry, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef"` exits 0
    - `run_scrape` signature includes `fetch_html: Optional[Callable[[str], Optional[str]]] = None` (Cycle-2 #5 injectable boundary)
    - `run_scrape(live=False)` WITHOUT `fetch_html` raises `ValueError` (Codex MEDIUM: `live` is no longer a dead parameter — False now FORBIDS network)
    - `run_scrape(live=False, fetch_html=stub)` runs using ONLY the injected stub (no real network, no real browser) — this is the path the Cycle-2 #5 e2e test exercises
    - **CYCLE-3 #2 (offline race-fetch routing)**: `run_scrape(live=False, fetch_html=transport)` passes `fetch_callable=transport` to `fetch_race_html` for each RaceRef (so race fetching, not only enumeration, uses the injected transport). A race for which the transport returns None is skipped/quarantined (no AttributeError). The live path passes `fetch_callable=None` and uses the real session as before.
    - `run_scrape` in live mode opens exactly ONE FetcherSession
    - `src/scraper/__init__.py` `__all__` lists at least: FetcherSession, fetch_race_html, fetch_with_retry, enumerate_races, parse_race_html, normalize_to_parquet, RaceRef, run_scrape
    - This is the only plan that modified `__init__.py` re-exports (Cycle-1 HIGH #3 sequencing respected)
  </acceptance_criteria>
  <done>
    - Orchestrator wires all 4 pipeline stages with an injectable fetch boundary for e2e (Cycle-2 #5 enabler)
    - `live` parameter is no longer dead (Cycle-1 MEDIUM resolved: False forbids network)
    - Public API re-exports added to __init__.py (Cycle-1 HIGH #3 final step)
    - All symbols importable from `src.scraper`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write full-chain end-to-end test, dtype-fidelity test, parse-only fixture test, and orchestrator unit test</name>
  <files>tests/scraper/test_end_to_end.py, tests/scraper/test_orchestrator.py</files>
  <read_first>
    - src/scraper/orchestrator.py (run_scrape — esp. the new fetch_html injection param, Cycle-2 #5)
    - src/scraper/enumeration.py (enumerate_races — REAL, uses the injected fetch_html; BASE_URL + urljoin from Cycle-2 #1)
    - src/scraper/parser.py (parse_race_html used in e2e)
    - src/scraper/normalizer.py (normalize_to_parquet used in e2e; partition_map for entry/result per Cycle-2 #6; strict dtypes per Cycle-2 #3)
    - src/scraper/models.py (RaceRef)
    - tests/scraper/fixtures/html/ (golden HTML saved in Plan 04 Task 3 — used to build the injected fetch_html map)
    - src/schemas/race.py, src/schemas/entry.py, src/schemas/result.py (schema for Parquet comparison)
    - src/pipeline/column_mapping.py (Kaggle column contract for overlap comparison)
    - data/standard/{race,entry,result}.parquet (authoritative Kaggle dtypes — read via pyarrow.parquet.read_schema; Cycle-2 #7: identify which columns are Arrow null vs non-null)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (Cycle-2 HIGH #5 full-chain e2e; Cycle-2 HIGH #7 dtype-fidelity null→concrete promotion rule; Cycle-1 MEDIUM Kaggle overlap, count-based sanity)
  </read_first>
  <behavior>
    - **CYCLE-2 #5 FULL-CHAIN**: a single `test_full_chain_end_to_end` runs REAL enumerate_races → (injected fetch_html returning golden HTML) → REAL parse_race_html → REAL normalize_to_parquet, asserting final Parquet schema, 14-digit horse_race_id, correct course code, graded flag, and a row count matching the fixture. Distinct from parse-only and per-component mocked tests.
    - End-to-end (parse-only, fixture-based, no network): for each golden HTML fixture, parse_race_html -> normalize_to_parquet produces typed Parquet with all schema columns
    - End-to-end output horse_race_id matches re.fullmatch(r"\d{14}", ...)
    - End-to-end output race_flag_graded_stakes is True for the graded fixture
    - End-to-end handles a fixture with 取/中 (finish_note) without crashing
    - End-to-end parses by header names (no hardcoded td index dependency)
    - Orchestrator test: run_scrape with all sub-steps mocked processes 2 RaceRefs and returns partitioned paths
    - Orchestrator test: fetch_race_html returning None for one race is skipped, others proceed
    - Orchestrator test: live=False without fetch_html raises ValueError (Cycle-1 MEDIUM live is not dead)
    - **CYCLE-2 #7 DTYPE-FIDELITY**: schema-compatibility test asserts physical-type EQUALITY only for columns where Kaggle dtype is NON-null (int64/double/bool/string); for Kaggle-null columns (race_flag_stallion_only, race_flag_colt_only, obstacle, surface_detail, track_condition_detail, etc.) it asserts the scraper dtype is a deliberate promotion (bool/string) — achievable, not the unachievable all-columns equality
  </behavior>
  <action>
Per Cycle-2 HIGH #5 (full-chain e2e), Cycle-2 HIGH #7 (dtype-fidelity promotion rule), Cycle-1 HIGH #9 (parse-only fixture e2e), Cycle-1 MEDIUM (Kaggle overlap, live-not-dead).

Create `tests/scraper/test_end_to_end.py`:

1. `class TestParseOnlyFixture` (Cycle-1 HIGH #9 — parse→normalize only; carried forward):
   - `test_parse_then_normalize_each_golden_fixture(tmp_standard_dir)`: parametrize over every `.html` file in `tests/scraper/fixtures/html/`. For each: call `parse_race_html(fixture_path)`; call `normalize_to_parquet([parsed], standard_dir=tmp_standard_dir)`; read back each output Parquet; assert (a) race columns == RaceSchema.model_fields keys, (b) every entry/result horse_race_id matches `re.fullmatch(r"\d{14}", ...)`, (c) NO `head_count` column, (d) the DataFrame is non-empty. NO network.
   - `test_graded_fixture_sets_graded_stakes_flag`, `test_finish_note_fixture_handled`, `test_diversity_axes_covered` (as before).

2. **`class TestFullChainE2E`** (CYCLE-2 #5 — the NEW single end-to-end test connecting all four stages):
   - `test_full_chain_end_to_end(tmp_raw_dir, tmp_standard_dir)`: this is the test Codex demanded. Build a `_GoldenTransport` test double: a dict mapping absolute URL -> saved golden HTML string, loaded from `tests/scraper/fixtures/html/`. The transport callable signature matches `Callable[[str], Optional[str]]` (returns the HTML for known URLs, None for unknown — exactly what enumerate_races expects). Then:
     - Pre-save each golden fixture's HTML to its expected raw path `tmp_raw_dir/{YYYY}/{MM}/{race_id}.html` (derived from the fixture's race_date) so `fetch_race_html`'s SCRP-05 dedup returns the path without calling the transport — this isolates the test to enumerate+parse+normalize while still exercising the real fetch_race_html dedup branch. (Alternative: pass the transport through to fetch_race_html via a thin wrapper; the pre-save approach is simpler and still exercises the dedup code path.)
     - Construct a fake `enumerate_races` input: the transport returns a calendar HTML listing the fixture race days, then race-day HTML listing the fixture race_ids. Easiest deterministic approach: build the calendar/day HTML inline in the test from the fixture set, OR mock `enumerate_races` to return `[RaceRef(race_id=fx.race_id, race_date=fx.race_date) for fx in fixtures]` — BUT to satisfy "exercises the REAL enumerate_races", prefer the inline-HTML approach so `parse_calendar_month_html` + `parse_race_day_html` actually run. If the fixture count makes inline HTML brittle, fall back to constructing RaceRefs directly but DOCUMENT in the test docstring which stages are real (parse + normalize + the RaceRef construction path) vs substituted (enumerate's HTML parsing).
     - Call `run_scrape(start_date=earliest_fixture_date, end_date=latest_fixture_date, raw_dir=tmp_raw_dir, standard_dir=tmp_standard_dir, live=False, fetch_html=transport)` — the injected transport means NO real network and NO real browser.
     - Assert: (a) the returned dict has race/entry/result path lists; (b) reading back the race Parquet, columns == RaceSchema.model_fields keys; (c) every entry/result horse_race_id matches `re.fullmatch(r"\d{14}", ...)`; (d) at least one row has the CORRECT course code (e.g. `"06"` for a 中山 fixture — guards Cycle-1 HIGH #5 end-to-end); (e) for the graded fixture, `race_flag_graded_stakes` is True (guards Cycle-1 HIGH #6 + Cycle-2 #2 end-to-end); (f) the entry/result partition files landed under the correct `{YYYYMM}/` directory derived from race_date (guards Cycle-2 #6 end-to-end); (g) total race row count == number of distinct fixture race_ids (guards against silent dedup/overwrite loss, Cycle-2 #4 end-to-end).
     - This test is DISTINCT from `TestParseOnlyFixture` (which skips enumerate and fetch entirely) and from `TestRunScrape` in test_orchestrator.py (which mocks all sub-steps). It is the only test that connects the REAL pipeline with ONLY the network boundary mocked.
   - **`test_full_chain_handles_failed_fetch` (CYCLE-3 #2 — now semantically valid)**: do NOT pre-save the failing race's HTML to the raw path. Configure the injected transport to return `None` for ONE race URL (a race that was enumerated but whose raw HTML is not pre-saved, so `fetch_race_html`'s SCRP-05 dedup does NOT short-circuit and the transport IS consulted via `fetch_callable`). Assert the OTHER races still parse and normalize (one race dropped), `run_scrape` returns normally (no crash), and the race for which the transport returned None was skipped/quarantined (logged, not an `AttributeError`). This exercises the real transport-None → `fetch_race_html(fetch_callable=transport)`-returns-None → race-skipped flow that was previously unreachable because the transport was never consulted for race fetching. Pre-save the OTHER races' HTML as in the happy-path test so they succeed via dedup.

3. **`class TestSchemaCompatibility`** (CYCLE-2 #7 — revised, achievable dtype-fidelity):
   - `_KAGGLE_NULL_COLUMNS`: a module-level set computed by reading `data/standard/{race,entry,result}.parquet` via `pyarrow.parquet.read_schema` and collecting every field whose Arrow type is `null` (these are the columns where Kaggle has no data). Expected to include at minimum: `race_flag_stallion_only, race_flag_colt_only, race_flag_open, race_flag_gelding_only, race_flag_amateur, race_flag_female_jockey, race_flag_listed, race_flag_maiden, race_flag_mare_only, race_flag_stakes, race_flag_young_horse, obstacle, surface_detail, track_condition_detail` (verify at test setup; skip with a clear message if the Kaggle Parquet is absent).
   - `test_scraped_columns_superset_of_kaggle_overlap`: for the field names that appear in BOTH scraped and Kaggle, assert the COLUMN EXISTS on both sides (presence check).
   - **`test_physical_type_equality_for_non_null_kaggle_columns`** (CYCLE-2 #7 — the achievable equality half): for each overlapping column where the Kaggle dtype is NON-null (int64/double/bool/string), assert the scraped Parquet Arrow physical type EQUALS the Kaggle Arrow physical type (e.g. `finish_position`: both `int64`; `weight_assigned`: both `double`; `race_flag_handicap`: both `bool`). Use `pq.read_schema` on both files and compare `str(field.type)`. This is where the strict equality holds — and it holds because 04-05 uses nullable Int64/Float64/boolean which serialize to the same Arrow physical types as Kaggle's nullable int64/double/bool.
   - **`test_promotion_allowed_for_null_kaggle_columns`** (CYCLE-2 #7 — the achievable promotion half): for each overlapping column where the Kaggle dtype IS `null` (in `_KAGGLE_NULL_COLUMNS`), assert the scraped Parquet Arrow physical type is a CONCRETE type (`bool` or `string`), NOT `null`. This is the deliberate promotion: Kaggle had no data so its type is the null supertype; the scraper populates the column so it gets a concrete type. The test asserts the promotion happened (scraped type != null) and that the promoted type is one of the allowed concrete types. Document in the test docstring: "null is a supertype of bool/string in Arrow, so a scraped bool/string column is compatible with a Kaggle null column — Phase 6 concatenation will not raise."
   - The Cycle-1 "physical-type equality on EVERY overlapping column" assertion is GONE (it was unachievable for null-typed Kaggle columns); it is replaced by these two precise assertions.

4. `class TestOptInLiveSmoke` (marked `@pytest.mark.live`, skipped by default):
   - `test_smoke_one_historical_race`: if `LIVE_SMOKE=1` env var set, run `run_scrape(start_date=date(2022,1,5), end_date=date(2022,1,5), max_races=1, live=True)`; assert at least one race Parquet file exists. Skipped unless env var present — NO network in CI.

Create `tests/scraper/test_orchestrator.py`:

5. `class TestRunScrape`:
   - `test_processes_racerefs_end_to_end_mocked`: patch `FetcherSession`, `enumerate_races` (return 2 RaceRefs), `fetch_race_html` (return a path to a golden fixture), `parse_race_html` (return parsed dict), `normalize_to_parquet` (return a dict of paths). Call `run_scrape(start, end, live=True)`; assert normalize_to_parquet was called once with a list of 2 parsed dicts.
   - `test_skips_failed_fetch`: configure mocked `fetch_race_html` to return None for the first RaceRef and a path for the second; assert normalize_to_parquet receives a list of length 1.
   - `test_single_session_per_run`: assert FetcherSession was entered exactly once (one `with` block) in live mode.
   - **`test_live_false_without_fetch_html_raises`** (Cycle-1 MEDIUM live-not-dead): call `run_scrape(start, end, live=False)` WITHOUT fetch_html; assert it raises `ValueError` (network is forbidden in this mode).
   - **`test_live_false_with_injected_fetch_html_runs_offline`** (Cycle-2 #5 enabler): call `run_scrape(start, end, live=False, fetch_html=lambda url: "<html>...")`; patch `fetch_race_html`/`parse_race_html`/`normalize_to_parquet` as needed; assert FetcherSession was NOT entered (no real browser) and the injected fetch_html was used by enumerate_races.
  </action>
  <verify>
    <automated>pytest tests/scraper/test_end_to_end.py tests/scraper/test_orchestrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All classes pass: TestParseOnlyFixture, TestFullChainE2E, TestSchemaCompatibility, TestOptInLiveSmoke, TestRunScrape
    - **CYCLE-2 #5**: `TestFullChainE2E.test_full_chain_end_to_end` passes — a SINGLE test connects REAL enumerate → injected-fetch → REAL parse → REAL normalize and asserts schema, 14-digit horse_race_id, course code, graded flag, partition YYYYMM, and row count. Distinct from parse-only and fully-mocked tests.
    - **CYCLE-2 #7**: `test_physical_type_equality_for_non_null_kaggle_columns` passes for all non-null Kaggle columns AND `test_promotion_allowed_for_null_kaggle_columns` passes for all null Kaggle columns. The unachievable "equality on every overlapping column" assertion is removed.
    - The opt-in live smoke test is skipped by default (no network in CI)
    - TestParseOnlyFixture exercises every golden fixture in tests/scraper/fixtures/html/
    - horse_race_id 14-digit assertion passes for all parsed entries (Cycle-1 HIGH #2 end-to-end)
    - race_flag_graded_stakes True for the graded fixture (Cycle-1 HIGH #6 + Cycle-2 #2 end-to-end)
    - Orchestrator unit test confirms single FetcherSession per live run
    - Orchestrator unit test confirms failed fetch is skipped, others proceed
    - **CYCLE-1 MEDIUM resolved**: `test_live_false_without_fetch_html_raises` passes (live is not a dead parameter)
    - `pytest tests/scraper/test_end_to_end.py tests/scraper/test_orchestrator.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    - **Cycle-2 HIGH #5 resolved: single full-chain e2e test connects enumerate→fetch→parse→normalize with only the network boundary mocked**
    - **Cycle-2 HIGH #7 resolved: dtype-fidelity assertion is achievable (equality for non-null Kaggle columns; promotion for null Kaggle columns)**
    - Parse-only fixture e2e retained (Cycle-1 HIGH #9)
    - Orchestrator wiring verified with mocks
    - Opt-in live smoke exists but is skipped in CI
    - Kaggle schema overlap compared with the precise equality-vs-promotion rule
    - `live` parameter no longer dead (Cycle-1 MEDIUM resolved)
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite, ruff, mypy, and confirm SCRP coverage</name>
  <files>src/scraper/__init__.py</files>
  <read_first>
    - src/scraper/__init__.py (final state after Task 1)
    - pyproject.toml ([tool.ruff], [tool.mypy], [tool.pytest] config sections)
    - .planning/phases/04-scraping-infrastructure-race-data/04-VALIDATION.md (full-suite commands, sampling continuity)
  </read_first>
  <action>
Per Cycle-1 MEDIUM (no ruff/mypy despite being configured; pytest -x less useful as final report). Per VALIDATION.md phase gate (full suite green before verify-work).

1. Run `pytest tests/scraper/ -q` (WITHOUT `-x` — get the full report so partial failures are visible, per Cycle-1 MEDIUM). All tests must pass.

2. Run `pytest tests/ -q` (full project suite — verify no regressions in pipeline/feature tests from scraper additions).

3. Run `ruff check src/scraper tests/scraper` — fix any lint errors (unused imports, line length).

4. Run `mypy src/scraper` — fix any type errors. Particular attention to: `RaceRef` dataclass typing; `Optional[str]` returns from fetcher; `list[RaceRef]` from enumerate_races; `dict[str, list[Path]]` from normalize_to_parquet; the new `partition_map: Optional[dict[str, datetime.date]]` and `fetch_html: Optional[Callable[[str], Optional[str]]]` params (Cycle-2 #5/#6).

5. Confirm SCRP coverage: every requirement ID (SCRP-01, SCRP-02, SCRP-03, SCRP-05) has at least one passing test in `tests/scraper/`. Document the test -> requirement mapping in the SUMMARY:
   - SCRP-01 (fetch/parse/normalize separation): test_fetcher, test_parser, test_normalizer, test_end_to_end (incl. TestFullChainE2E per Cycle-2 #5)
   - SCRP-02 (fetch HTML raw save): test_fetcher::TestPathDerivation, test_orchestrator
   - SCRP-03 (parse to standard format): test_parser, test_normalizer, test_end_to_end (incl. TestSchemaCompatibility per Cycle-2 #7)
   - SCRP-05 (dedup): test_fetcher::TestDedup, test_normalizer::TestPartitionedOutput::test_same_month_merge_dedup_preserves_sentinel (Cycle-2 #4)

6. If any check fails, investigate root cause and fix (do NOT mark skip without recording in SUMMARY). The phase is not done until ruff, mypy, and pytest are all clean.
  </action>
  <verify>
    <automated>pytest tests/scraper/ -q &amp;&amp; ruff check src/scraper tests/scraper &amp;&amp; mypy src/scraper</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/scraper/ -q` exits 0 (no `-x` — full report)
    - `pytest tests/ -q` exits 0 (no regressions)
    - `ruff check src/scraper tests/scraper` exits 0 (Cycle-1 MEDIUM resolved)
    - `mypy src/scraper` exits 0 (Cycle-1 MEDIUM resolved)
    - SUMMARY documents the test -> SCRP requirement mapping (all 4 IDs covered, incl. Cycle-2 #5 TestFullChainE2E for SCRP-01/02/03 and Cycle-2 #4/#7 tests for SCRP-03/05)
    - `python -c "from src.scraper import run_scrape, fetch_race_html, fetch_with_retry, enumerate_races, parse_race_html, normalize_to_parquet"` exits 0
  </acceptance_criteria>
  <done>
    - Full scraper test suite green (incl. Cycle-2 #5 full-chain e2e and Cycle-2 #7 dtype-fidelity)
    - Full project suite green (no regressions)
    - ruff + mypy clean on src/scraper and tests/scraper
    - SCRP-01/02/03/05 all covered by passing tests
    - Public API fully importable from src.scraper
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> external netkeiba | Live mode issues real HTTPS requests (opt-in only); injected fetch_html mode crosses the test/deterministic boundary (Cycle-2 #5) |
| package integration | Import resolution and module wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Denial of Service | Orchestrator issuing unbounded requests in live mode | mitigate | Sequential FetcherSession with 2s rate limiting (Plan 03); `max_races` parameter caps smoke runs; live mode opt-in only; **Cycle-1 MEDIUM resolved: `live=False` WITHOUT injected fetch_html now raises (network forbidden)**. |
| T-04-21 | Tampering | Integration regression breaking import chain | mitigate | Full test suite + ruff + mypy gate; public API import test asserts all symbols resolve. |
| T-04-22 | Tampering | Silent end-to-end breakage not caught by unit tests | mitigate | Cycle-1 HIGH #9: parse-only fixture e2e. **Cycle-2 HIGH #5: a SINGLE full-chain e2e connects enumerate→fetch(injected)→parse→normalize with real functions and asserts schema/14-digit-horse_race_id/course-code/graded-flag/partition-YYYYMM/row-count.** |
| T-04-23 | Tampering | Schema incompatibility with Kaggle Parquet blocking Phase 6 | mitigate | **Cycle-2 HIGH #7: TestSchemaCompatibility asserts physical-type EQUALITY for non-null Kaggle columns AND deliberate promotion (null→bool/string) for Kaggle-null columns. The unachievable all-columns equality is replaced by two precise, passing assertions.** |
</threat_model>

<verification>
- `pytest tests/scraper/ -q` passes (no `-x`)
- `pytest tests/ -q` passes (no regressions)
- `ruff check src/scraper tests/scraper` clean
- `mypy src/scraper` clean
- `python -c "from src.scraper import run_scrape, fetch_race_html, fetch_with_retry, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef"` exits 0
- `python -c "import inspect; from src.scraper import run_scrape; assert 'fetch_html' in inspect.signature(run_scrape).parameters"` exits 0 (Cycle-2 #5)
</verification>

<success_criteria>
- Orchestrator wires all 4 stages with an injectable fetch boundary for full-chain e2e (Cycle-1 MEDIUM orchestrator + Cycle-2 #5 enabler)
- **Cycle-2 HIGH #5 resolved: single full-chain e2e test (TestFullChainE2E) connects enumerate→fetch→parse→normalize with only the network boundary mocked**
- **Cycle-2 HIGH #7 resolved: dtype-fidelity assertion achievable (equality for non-null Kaggle columns; promotion for null Kaggle columns)**
- Parse-only fixture e2e retained (Cycle-1 HIGH #9)
- Public API re-exports wired in __init__.py (Cycle-1 HIGH #3 final step)
- `live` parameter no longer dead (Cycle-1 MEDIUM resolved)
- ruff + mypy clean (Cycle-1 MEDIUM resolved)
- SCRP-01, SCRP-02, SCRP-03, SCRP-05 all covered by passing tests
- Opt-in live smoke test exists but skipped in CI
</success_criteria>

## Artifacts this phase produces

- `src/scraper/__init__.py` -- public re-exports: `FetcherSession`, `fetch_race_html`, `fetch_with_retry` (Cycle-2 #8), `enumerate_races`, `enumerate_race_day_urls`, `enumerate_races_for_day`, `parse_race_html`, `normalize_to_parquet`, `RaceRef`, `run_scrape`; `__all__` list
- `src/scraper/orchestrator.py` -- `run_scrape(start_date, end_date, raw_dir, standard_dir, live=False, max_races=None, fetch_html=None) -> dict[str, list[Path]]` (Cycle-2 #5: injectable fetch_html boundary; live=False without fetch_html raises)
- `tests/scraper/test_end_to_end.py` -- classes: `TestParseOnlyFixture` (Cycle-1 HIGH #9, parametrized over golden fixtures), **`TestFullChainE2E` (Cycle-2 HIGH #5 — single full-chain test)**, **`TestSchemaCompatibility` (Cycle-2 HIGH #7 — equality for non-null + promotion for null Kaggle columns)**, `TestOptInLiveSmoke` (`@pytest.mark.live`, skipped by default)
- `tests/scraper/test_orchestrator.py` -- class: `TestRunScrape` (incl. `test_live_false_without_fetch_html_raises`, `test_live_false_with_injected_fetch_html_runs_offline`)

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-06-SUMMARY.md` when done
</output>

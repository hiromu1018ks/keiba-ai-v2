---
phase: 04-scraping-infrastructure-race-data
plan: 06
type: execute
wave: 5
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - src/scraper/__init__.py
  - src/scraper/orchestrator.py
  - tests/scraper/test_end_to_end.py
  - tests/scraper/test_orchestrator.py
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-05

must_haves:
  truths:
    - "A fixture-based end-to-end test connects enumeration -> fetch -> parse -> normalize using saved real HTML golden files (NO network in CI)"
    - "End-to-end parser validation parses by normalized <th> header names and exercises multiple years/venues/grades/surfaces/cancellations"
    - "The orchestrator wires FetcherSession + enumeration + fetch_race_html + parse_race_html + normalize_to_parquet for a date range"
    - "src/scraper/__init__.py finally re-exports the public API (now that all submodules exist) — this is the ONLY plan that touches __init__.py re-exports"
    - "ruff check and mypy run clean on src/scraper/ and tests/scraper/"
    - "Scraped Parquet schema is directly compared against Kaggle Parquet schema (pyarrow schema equality on overlapping fields)"
    - "All Playwright unit tests remain mocked; an OPT-IN live smoke test exists but is skipped by default"
  artifacts:
    - path: "src/scraper/__init__.py"
      provides: "Public re-exports now that fetcher/parser/normalizer/enumeration all exist"
      exports: ["fetch_race_html", "enumerate_races", "parse_race_html", "normalize_to_parquet", "FetcherSession", "RaceRef"]
    - path: "src/scraper/orchestrator.py"
      provides: "run_scrape(start_date, end_date, raw_dir, standard_dir, live=False)"
      exports: ["run_scrape"]
    - path: "tests/scraper/test_end_to_end.py"
      provides: "Fixture-based end-to-end test across diversity axes"
    - path: "tests/scraper/test_orchestrator.py"
      provides: "Orchestrator unit test with all sub-steps mocked"
  key_links:
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/fetcher.py"
      via: "FetcherSession + fetch_race_html"
      pattern: "FetcherSession"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/enumeration.py"
      via: "enumerate_races + make_fetch_html_callable"
      pattern: "enumerate_races"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/parser.py"
      via: "parse_race_html"
      pattern: "parse_race_html"
    - from: "src/scraper/orchestrator.py"
      to: "src/scraper/normalizer.py"
      via: "normalize_to_parquet"
      pattern: "normalize_to_parquet"
    - from: "src/scraper/__init__.py"
      to: "all submodules"
      via: "re-exports added in this plan"
      pattern: "from src\\.scraper\\.(fetcher|parser|normalizer|enumeration|models) import"
---

<objective>
スクレイピングパイプライン全体を統合し、fixtureベースのend-to-endテストで検証する。Codex Review HIGH #9（end-to-endテスト不在）と MEDIUM（orchestrator不在、ruff/mypy不在、Kaggle overlap比較不在、live smoke test）を修正。Plan 01で意図的に空にした`__init__.py`に、ここで初めて公開APIのre-exportを追加する（全サブモジュールが存在するようになったため）。

Purpose: 個別テストは通っても実際のHTML構造やスキーマ互換性が壊れているケースを検出できるよう、golden fixtureを使ったend-to-endテストでパイプライン全体を検証する。Phase 6統合に向け、スクレイプParquetとKaggle Parquetのスキーマ互換性を直接比較する。

Output: `src/scraper/orchestrator.py` (`run_scrape`)、更新済み`src/scraper/__init__.py`（公開re-export）、`tests/scraper/test_end_to_end.py`、`tests/scraper/test_orchestrator.py`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-VALIDATION.md
@.planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md
@.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-02-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-03-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-04-SUMMARY.md
@.planning/phases/04-scraping-infrastructure-race-data/04-05-SUMMARY.md
@src/scraper/__init__.py
@src/scraper/enumeration.py
@src/scraper/fetcher.py
@src/scraper/parser.py
@src/scraper/normalizer.py
@src/scraper/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire orchestrator and add public re-exports to __init__.py</name>
  <files>src/scraper/orchestrator.py, src/scraper/__init__.py</files>
  <read_first>
    - src/scraper/__init__.py (currently import-safe empty — this plan adds the re-exports)
    - src/scraper/enumeration.py (enumerate_races signature: start_date, end_date, fetch_html)
    - src/scraper/fetcher.py (FetcherSession, fetch_race_html, make_fetch_html_callable)
    - src/scraper/parser.py (parse_race_html signature)
    - src/scraper/normalizer.py (normalize_to_parquet signature)
    - src/scraper/models.py (RaceRef)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (MEDIUM: orchestrator/CLI referenced by validation but no plan implements it)
  </read_first>
  <action>
Per SCRP-01 (integrated scraping infrastructure). Per Codex Review MEDIUM (orchestrator/CLI referenced by VALIDATION.md but not implemented) and LOW (`__init__.py` re-exports belong at the end).

1. Create `src/scraper/orchestrator.py` with `def run_scrape(start_date: datetime.date, end_date: datetime.date, raw_dir: Path = Path("data/raw/netkeiba"), standard_dir: Path = Path("data/standard"), live: bool = False, max_races: Optional[int] = None) -> dict[str, list[Path]]`:
   - Opens a single `FetcherSession()` (one browser per batch — HIGH browser-per-request resolved at integration layer).
   - Builds the fetch callable via `make_fetch_html_callable(session)`.
   - Calls `enumerate_races(start_date, end_date, fetch_html)` to get `list[RaceRef]`. If `max_races` is set, truncate for smoke runs.
   - For each `RaceRef`: `path = fetch_race_html(race_ref, session, raw_dir)`; if path is None, log and skip. If path exists (dedup or fresh): `parsed = parse_race_html(path)`; append to `parsed_races`.
   - After the loop: `return normalize_to_parquet(parsed_races, standard_dir)`.
   - The `with FetcherSession() as session:` block ensures browser cleanup via try/finally (Plan 03).
   - `live=False` default keeps the function safe; only the explicit opt-in live smoke test passes `live=True` (no behavioral change in the function, but documents intent for the test marker).

2. Update `src/scraper/__init__.py` — REPLACE the empty body with the public re-exports (NOW that all submodules exist; this is the ONLY plan that adds re-exports per HIGH #3 plan):
   - Module docstring: `"""Scraping infrastructure for netkeiba race data. Public API re-exports."""`
   - `from src.scraper.fetcher import FetcherSession, fetch_race_html`
   - `from src.scraper.enumeration import enumerate_races, enumerate_race_day_urls, enumerate_races_for_day`
   - `from src.scraper.parser import parse_race_html`
   - `from src.scraper.normalizer import normalize_to_parquet`
   - `from src.scraper.models import RaceRef`
   - `from src.scraper.orchestrator import run_scrape`
   - `__all__ = ["FetcherSession", "fetch_race_html", "enumerate_races", "enumerate_race_day_urls", "enumerate_races_for_day", "parse_race_html", "normalize_to_parquet", "RaceRef", "run_scrape"]`

Imports in orchestrator.py: `import datetime`, `from pathlib import Path`, `from typing import Optional`, `from loguru import logger`, `from src.scraper.fetcher import FetcherSession, fetch_race_html, make_fetch_html_callable`, `from src.scraper.enumeration import enumerate_races`, `from src.scraper.parser import parse_race_html`, `from src.scraper.normalizer import normalize_to_parquet`.
  </action>
  <verify>
    <automated>python -c "from src.scraper import run_scrape, fetch_race_html, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef; print('Package public API OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.scraper import run_scrape, fetch_race_html, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef"` exits 0
    - `run_scrape` signature is `(start_date, end_date, raw_dir, standard_dir, live=False, max_races=None) -> dict[str, list[Path]]`
    - `run_scrape` opens exactly ONE FetcherSession (`with FetcherSession() as session:`)
    - `src/scraper/__init__.py` `__all__` lists at least: FetcherSession, fetch_race_html, enumerate_races, parse_race_html, normalize_to_parquet, RaceRef, run_scrape
    - This is the only plan that modified `__init__.py` re-exports (HIGH #3 sequencing respected)
  </acceptance_criteria>
  <done>
    - Orchestrator wires all 4 pipeline stages through a single FetcherSession
    - Public API re-exports added to __init__.py (HIGH #3 final step)
    - All symbols importable from `src.scraper`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write fixture-based end-to-end test and orchestrator unit test</name>
  <files>tests/scraper/test_end_to_end.py, tests/scraper/test_orchestrator.py</files>
  <read_first>
    - src/scraper/orchestrator.py (run_scrape to test)
    - src/scraper/parser.py (parse_race_html used in e2e)
    - src/scraper/normalizer.py (normalize_to_parquet used in e2e)
    - tests/scraper/fixtures/html/ (golden HTML saved in Plan 04 Task 3)
    - src/schemas/race.py, src/schemas/entry.py, src/schemas/result.py (schema for Parquet comparison)
    - src/pipeline/column_mapping.py (Kaggle column contract for overlap comparison)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (HIGH #9 e2e test requirements; MEDIUM Kaggle overlap comparison, count-based sanity)
  </read_first>
  <behavior>
    - End-to-end (fixture-based, no network): for each golden HTML fixture, parse_race_html -> normalize_to_parquet produces typed Parquet with all schema columns
    - End-to-end output horse_race_id matches re.fullmatch(r"\d{14}", ...)
    - End-to-end output race_flag_graded_stakes is True for the graded fixture
    - End-to-end handles a fixture with 取/中 (finish_note) without crashing
    - End-to-end parses by header names (no hardcoded td index dependency)
    - Orchestrator test: run_scrape with all sub-steps mocked processes 2 RaceRefs and returns partitioned paths
    - Orchestrator test: fetch_race_html returning None for one race is skipped, others proceed
    - Schema compatibility: scraped Parquet columns are a superset of / equal to Kaggle Parquet columns on the overlapping fields
  </behavior>
  <action>
Per Codex Review HIGH #9 (fixture-based end-to-end test) and MEDIUM (Kaggle overlap comparison, count-based sanity, mocked Playwright in CI, opt-in live smoke).

Create `tests/scraper/test_end_to_end.py`:

1. `class TestEndToEndFixture`:
   - `test_parse_then_normalize_each_golden_fixture(tmp_standard_dir)`: parametrize over every `.html` file in `tests/scraper/fixtures/html/`. For each: call `parse_race_html(fixture_path)` to get parsed dict; call `normalize_to_parquet([parsed], standard_dir=tmp_standard_dir)`; read back each output Parquet; assert (a) race columns == RaceSchema.model_fields keys, (b) every entry/result horse_race_id matches `re.fullmatch(r"\d{14}", ...)`, (c) NO `head_count` column, (d) the DataFrame is non-empty (the fixture has at least 1 entry). NO network — all from saved fixtures.
   - `test_graded_fixture_sets_graded_stakes_flag`: pick the graded fixture by name or by parsing its condition; assert race["race_flag_graded_stakes"] is True.
   - `test_finish_note_fixture_handled`: pick a fixture with 取/中 if present (or skip with `pytest.skip` if none was captured); assert the corresponding result has finish_position is None and finish_note in {"中","取","失","除","再"}.
   - `test_diversity_axes_covered`: assert the fixture set includes at least (a) one graded race, (b) one dirt (ダート) race, (c) races from at least 2 distinct years. Skip with a clear message if the user only captured 3 fixtures in Plan 04 — record the gap in the SUMMARY for follow-up.

2. `class TestSchemaCompatibility`:
   - `test_scraped_columns_superset_of_kaggle_overlap`: read existing Kaggle Parquet at `data/standard/race.parquet` (skip if absent) and the scraped test Parquet; for the field names that appear in BOTH, assert dtype compatibility using `src.pipeline.validators._DTYPE_COMPAT`. Document any dtype mismatch in the test output (do not fail on benign mismatches like Int64 vs int64).

3. `class TestOptInLiveSmoke` (marked `@pytest.mark.live`, skipped by default):
   - `test_smoke_one_historical_race`: if `LIVE_SMOKE=1` env var set, run `run_scrape(start_date=date(2022,1,5), end_date=date(2022,1,5), max_races=1, live=True)`; assert at least one race Parquet file exists. Skipped unless env var present — NO network in CI.

Create `tests/scraper/test_orchestrator.py`:

4. `class TestRunScrape`:
   - `test_processes_racerefs_end_to_end_mocked`: patch `FetcherSession`, `enumerate_races` (return 2 RaceRefs), `fetch_race_html` (return a path to a golden fixture), `parse_race_html` (return parsed dict), `normalize_to_parquet` (return a dict of paths). Call run_scrape; assert normalize_to_parquet was called once with a list of 2 parsed dicts; assert the return value matches the mocked normalize output.
   - `test_skips_failed_fetch`: configure mocked `fetch_race_html` to return None for the first RaceRef and a path for the second; assert normalize_to_parquet receives a list of length 1.
   - `test_single_session_per_run`: assert FetcherSession was entered exactly once (one `with` block) regardless of race count.
  </action>
  <verify>
    <automated>pytest tests/scraper/test_end_to_end.py tests/scraper/test_orchestrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All classes pass: TestEndToEndFixture, TestSchemaCompatibility, TestRunScrape
    - The opt-in live smoke test is skipped by default (no network in CI)
    - End-to-end test exercises every golden fixture in tests/scraper/fixtures/html/
    - horse_race_id 14-digit assertion passes for all parsed entries (HIGH #2 end-to-end confirmation)
    - race_flag_graded_stakes True for the graded fixture (HIGH #6 end-to-end confirmation)
    - Orchestrator unit test confirms single FetcherSession per run
    - Orchestrator unit test confirms failed fetch is skipped, others proceed
    - `pytest tests/scraper/test_end_to_end.py tests/scraper/test_orchestrator.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    - Fixture-based end-to-end test connects parse -> normalize (HIGH #9 resolved)
    - Orchestrator wiring verified with mocks
    - Opt-in live smoke exists but is skipped in CI
    - Kaggle schema overlap compared
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite, ruff, mypy, and confirm SCRP coverage</name>
  <files>src/scraper/__init__.py</files>
  <read_first>
    - src/scraper/__init__.py (final state after Task 1)
    - pyproject.toml ([tool.ruff], [tool.mypy], [tool.pytest] config sections)
    - .planning/phases/04-scraping-infrastructure-race-data/04-VALIDATION.md (full-suite commands, sampling continuity)
  </read_first>
  <action>
Per Codex Review MEDIUM (no ruff/mypy despite being configured; pytest -x less useful as final report). Per VALIDATION.md phase gate (full suite green before verify-work).

1. Run `pytest tests/scraper/ -q` (WITHOUT `-x` — get the full report so partial failures are visible, per Codex Review MEDIUM). All tests must pass.

2. Run `pytest tests/ -q` (full project suite — verify no regressions in pipeline/feature tests from scraper additions).

3. Run `ruff check src/scraper tests/scraper` — fix any lint errors (unused imports, line length). Per Codex Review MEDIUM this was missing from the original Plan 05.

4. Run `mypy src/scraper` — fix any type errors. Particular attention to: `RaceRef` dataclass typing, `Optional[str]` returns from fetcher, `list[RaceRef]` return from enumerate_races, `dict[str, list[Path]]` from normalize_to_parquet.

5. Confirm SCRP coverage: every requirement ID (SCRP-01, SCRP-02, SCRP-03, SCRP-05) has at least one passing test in `tests/scraper/`. Document the test -> requirement mapping in the SUMMARY:
   - SCRP-01 (fetch/parse/normalize separation): test_fetcher, test_parser, test_normalizer, test_end_to_end
   - SCRP-02 (fetch HTML raw save): test_fetcher::TestPathDerivation, test_orchestrator
   - SCRP-03 (parse to standard format): test_parser, test_normalizer, test_end_to_end
   - SCRP-05 (dedup): test_fetcher::TestDedup

6. If any check fails, investigate root cause and fix (do NOT mark skip without recording in SUMMARY). The phase is not done until ruff, mypy, and pytest are all clean.
  </action>
  <verify>
    <automated>pytest tests/scraper/ -q &amp;&amp; ruff check src/scraper tests/scraper &amp;&amp; mypy src/scraper</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/scraper/ -q` exits 0 (no `-x` — full report)
    - `pytest tests/ -q` exits 0 (no regressions)
    - `ruff check src/scraper tests/scraper` exits 0 (Codex Review MEDIUM resolved)
    - `mypy src/scraper` exits 0 (Codex Review MEDIUM resolved)
    - SUMMARY documents the test -> SCRP requirement mapping (all 4 IDs covered)
    - `python -c "from src.scraper import run_scrape, fetch_race_html, enumerate_races, parse_race_html, normalize_to_parquet"` exits 0
  </acceptance_criteria>
  <done>
    - Full scraper test suite green
    - Full project suite green (no regressions)
    - ruff + mypy clean on src/scraper and tests/scraper
    - SCRP-01/02/03/05 all covered by passing tests
    - Public API fully importable from src.scraper
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> external netkeiba | Live mode issues real HTTPS requests (opt-in only) |
| package integration | Import resolution and module wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Denial of Service | Orchestrator issuing unbounded requests in live mode | mitigate | Sequential FetcherSession with 2s rate limiting (Plan 03); `max_races` parameter caps smoke runs; live mode opt-in only. |
| T-04-21 | Tampering | Integration regression breaking import chain | mitigate | Full test suite + ruff + mypy gate; public API import test asserts all symbols resolve. |
| T-04-22 | Tampering | Silent end-to-end breakage not caught by unit tests | mitigate | HIGH #9: fixture-based end-to-end test across diversity axes; orchestrator unit test with mocked sub-steps. |
| T-04-23 | Tampering | Schema incompatibility with Kaggle Parquet blocking Phase 6 | mitigate | TestSchemaCompatibility compares scraped Parquet columns/dtypes against Kaggle Parquet on overlapping fields. |
</threat_model>

<verification>
- `pytest tests/scraper/ -q` passes (no `-x`)
- `pytest tests/ -q` passes (no regressions)
- `ruff check src/scraper tests/scraper` clean
- `mypy src/scraper` clean
- `python -c "from src.scraper import run_scrape, fetch_race_html, enumerate_races, parse_race_html, normalize_to_parquet, FetcherSession, RaceRef"` exits 0
</verification>

<success_criteria>
- Orchestrator wires all 4 stages through a single FetcherSession (MEDIUM orchestrator resolved)
- Fixture-based end-to-end test connects parse -> normalize (HIGH #9 resolved)
- Public API re-exports wired in __init__.py (HIGH #3 final step)
- ruff + mypy clean (MEDIUM resolved)
- Kaggle schema overlap compared (MEDIUM resolved)
- SCRP-01, SCRP-02, SCRP-03, SCRP-05 all covered by passing tests
- Opt-in live smoke test exists but skipped in CI
</success_criteria>

## Artifacts this phase produces

- `src/scraper/__init__.py` -- public re-exports: `FetcherSession`, `fetch_race_html`, `enumerate_races`, `enumerate_race_day_urls`, `enumerate_races_for_day`, `parse_race_html`, `normalize_to_parquet`, `RaceRef`, `run_scrape`; `__all__` list
- `src/scraper/orchestrator.py` -- `run_scrape(start_date, end_date, raw_dir, standard_dir, live=False, max_races=None) -> dict[str, list[Path]]`
- `tests/scraper/test_end_to_end.py` -- classes: `TestEndToEndFixture` (parametrized over golden fixtures), `TestSchemaCompatibility`, `TestOptInLiveSmoke` (`@pytest.mark.live`, skipped by default)
- `tests/scraper/test_orchestrator.py` -- class: `TestRunScrape`

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-06-SUMMARY.md` when done
</output>

---
phase: 04-scraping-infrastructure-race-data
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - src/scraper/parser.py
  - src/scraper/course_codes.py
  - src/scraper/flag_crosswalk.py
  - tests/scraper/fixtures/html/
  - tests/scraper/test_course_codes.py
  - tests/scraper/test_parser.py
autonomous: false
requirements:
  - SCRP-01
  - SCRP-03

must_haves:
  truths:
    - "COURSE_CODE_MAP uses the corrected JRA codes: 01 札幌, 02 函館, 03 福島, 04 新潟, 05 東京, 06 中山, 07 中京, 08 京都, 09 阪神, 10 小倉"
    - "horse_race_id is the 14-digit concatenation f'{race_id}{horse_number:02d}' (e.g. 20220601010101), matching Kaggle's existing keys — NO underscore"
    - "Result-table columns are resolved by normalized <th> header names, NOT by hardcoded <td> index"
    - "Flag crosswalk maps netkeiba text to the exact race_flag_* field names from column_mapping.py: (牝)->race_flag_filly_only, (国際)->race_flag_graded_stakes, (ハンデ)->race_flag_handicap, (特指)->race_flag_special_weight"
    - "No head_count field is emitted (it does not exist in RaceSchema)"
    - "Race header fields surface_detail, course_detail, track_condition_detail are emitted (None when absent) so the normalizer can reindex against RaceSchema"
    - "Missing/unknown values are emitted as None; confirmed-absence is emitted as False where the field semantics warrant it (documented per-flag in flag_crosswalk.py)"
  artifacts:
    - path: "src/scraper/course_codes.py"
      provides: "COURSE_CODE_MAP, COURSE_CODE_REVERSE (authoritative single source)"
      exports: ["COURSE_CODE_MAP", "COURSE_CODE_REVERSE"]
    - path: "src/scraper/flag_crosswalk.py"
      provides: "FLAG_CROSSWALK list mapping (pattern -> race_flag field name) plus derive_race_flags(race_condition)"
      exports: ["FLAG_CROSSWALK", "derive_race_flags"]
    - path: "src/scraper/parser.py"
      provides: "parse_race_html, parse_horse_weight, parse_sex_age, resolve_columns_by_header"
      exports: ["parse_race_html", "parse_horse_weight", "parse_sex_age", "resolve_columns_by_header"]
    - path: "tests/scraper/test_course_codes.py"
      provides: "Tests asserting all 10 venue codes"
    - path: "tests/scraper/test_parser.py"
      provides: "Header-driven parser tests, flag crosswalk tests, horse_race_id format test"
    - path: "tests/scraper/fixtures/html/"
      provides: "Golden HTML fixtures (multi-year/venue/grade/surface/cancellation) — saved by user per checkpoint, placeholder structure created here"
  key_links:
    - from: "src/scraper/parser.py"
      to: "src/scraper/course_codes.py"
      via: "COURSE_CODE_MAP lookup"
      pattern: "COURSE_CODE_MAP"
    - from: "src/scraper/parser.py"
      to: "src/scraper/flag_crosswalk.py"
      via: "derive_race_flags call"
      pattern: "derive_race_flags"
    - from: "src/scraper/flag_crosswalk.py"
      to: "src/pipeline/column_mapping.py"
      via: "KAGGLE_COLUMN_MAP cross-reference (source of truth for race_flag_* field names)"
      pattern: "race_flag_"
    - from: "src/scraper/parser.py"
      to: "src/schemas/race.py"
      via: "race dict keys match RaceSchema.model_fields"
      pattern: "race_flag_|race_id|race_date"
---

<objective>
BS4+lxmlベースのparserを実装する。Codex Review HIGH #5（COURSE_CODE_MAP誤り）、HIGH #2（horse_race_id 14桁）、HIGH #6（race flag意味誤り）、HIGH #10（固定tdインデックス）、MEDIUM（head_count不存在、surface_detail等の未設定、None vs False 区別）を修正。

Purpose: netkeiba HTMLをRaceSchema/EntrySchema/ResultSchemaと互換な構造化dictに変換するparse層。Kaggleデータ（src/pipeline/column_mapping.py が権威ソース）と完全互換の horse_race_id / race_flag / course_code を生成し、Phase 6統合でのサイレント不整合を防ぐ。

Output: `src/scraper/course_codes.py`（単一権威COURSE_CODE_MAP）、`src/scraper/flag_crosswalk.py`（column_mapping.py準拠のフラグクロスウォーク）、`src/scraper/parser.py`（ヘッダ駆動パーサー）、テスト一式
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md
@.planning/phases/04-scraping-infrastructure-race-data/04-RESEARCH.md
@.planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md
@.planning/phases/04-scraping-infrastructure-race-data/04-01-SUMMARY.md
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@src/pipeline/column_mapping.py
@src/pipeline/kaggle_converter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define authoritative COURSE_CODE_MAP and Kaggle-compatible FLAG_CROSSWALK</name>
  <files>src/scraper/course_codes.py, src/scraper/flag_crosswalk.py</files>
  <read_first>
    - src/pipeline/column_mapping.py (KAGGLE_COLUMN_MAP rows 9-28 — authoritative source for race_flag_* field names: レース記号/牝 -> race_flag_filly_only, レース記号/(国際) -> race_flag_graded_stakes, レース記号/(ハンデ) -> race_flag_handicap, レース記号/(特指) -> race_flag_special_weight, etc.)
    - src/schemas/race.py (the 20 race_flag_* field names that MUST appear)
    - .planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md (D-10 race_flag derivation, D-11 meeting_num, D-12 region, D-13 prize_money)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (HIGH #5 corrected course codes 01..10; HIGH #6 flag semantics vs column_mapping.py)
  </read_first>
  <action>
Per D-10 (flag derivation), D-11/D-12/D-13, and Codex Review HIGH #5 (course codes) and HIGH #6 (flag crosswalk against column_mapping.py).

1. Create `src/scraper/course_codes.py` as the SINGLE AUTHORITATIVE source for JRA course codes:
   - `COURSE_CODE_MAP: dict[str, str]` mapping Japanese venue name -> 2-digit code, EXACTLY:
     - "札幌": "01", "函館": "02", "福島": "03", "新潟": "04", "東京": "05", "中山": "06", "中京": "07", "京都": "08", "阪神": "09", "小倉": "10"
   - `COURSE_CODE_REVERSE: dict[str, str]` = inverse mapping (code -> name).
   - Module docstring citing the source and noting this is the single source of truth — parser, normalizer, and any future module import from here.

2. Create `src/scraper/flag_crosswalk.py` defining the netkeiba-text -> race_flag_* crosswalk, VALIDATED against `src/pipeline/column_mapping.py` (which is the Kaggle-side authority):
   - Define `FLAG_CROSSWALK: list[tuple[str, str]]` — ordered list of `(substring_pattern, race_flag_field_name)`. Pattern order matters for greedy matches (more specific first). Use these mappings, derived directly from KAGGLE_COLUMN_MAP rows 9-28:
     - ("(ハンデ)", "race_flag_handicap")
     - ("(馬齢)", "race_flag_age_restricted")
     - ("(牝)", "race_flag_filly_only")   # HIGH #6 — Kaggle maps レース記号/牝 -> race_flag_filly_only; do NOT use race_flag_mare_only
     - ("(父)", "race_flag_stallion_only")
     - ("(別定)", "race_flag_special_weight")
     - ("(特指)", "race_flag_special_weight")  # Kaggle: レース記号/(特指) -> race_flag_special_weight
     - ("(混)", "race_flag_allowance")
     - ("(市)", "race_flag_allowance")
     - ("九州産馬", "race_flag_allowance")
     - ("(定量)", "race_flag_bonus_weight")
     - ("(国際)", "race_flag_graded_stakes")  # Kaggle: レース記号/(国際) -> race_flag_graded_stakes (compatibility over reclassification — documented below)
     - ("(見習騎手)", "race_flag_apprentice")
     - ("(指)", "race_flag_condition_race")
     - ("[指]", "race_flag_condition_race")
     - ("(抽)", "race_flag_condition_race")
     - ("[抽]", "race_flag_condition_race")
     - ("関東配布馬", "race_flag_open")
     - ("関西配布馬", "race_flag_open")
     - ("せん", "race_flag_gelding_only")
   - Define CLASS_PATTERNS (regex) for class detection: `"未勝利" -> race_flag_maiden`, `"新馬" -> race_flag_maiden` (maiden compatibility decision — documented), `"オープン" -> race_flag_open`.
   - Define GRADE_PATTERNS: regex matching `GI|GII|GIII|G1|G2|G3|JGI|JGII|JGIII|JG1|JG2|JG3` AND full-width forms `ＧＩ|ＧＩＩ|ＧＩＩＩ` -> sets both `race_flag_graded_stakes=True` and `race_flag_stakes=True`. ALSO `"重賞"` substring -> `race_flag_stakes=True`. For listed `" (L)"` or `"(リステッド)"` -> `race_flag_listed=True`.
   - `def derive_race_flags(race_condition: str, race_name: str = "") -> dict[str, Optional[bool]]`:
     - Returns a dict with EXACTLY the 20 keys from RaceSchema (`race_flag_handicap, race_flag_age_restricted, race_flag_filly_only, race_flag_colt_only, race_flag_gelding_only, race_flag_mare_only, race_flag_stallion_only, race_flag_apprentice, race_flag_amateur, race_flag_female_jockey, race_flag_young_horse, race_flag_condition_race, race_flag_special_weight, race_flag_bonus_weight, race_flag_stakes, race_flag_graded_stakes, race_flag_listed, race_flag_open, race_flag_maiden, race_flag_allowance`).
     - Initialize all to None (unknown). For each crosswalk/class/grade pattern that matches: set the corresponding field to True. NEVER set to False based on absence (None = unknown, per Codex Review MEDIUM).
     - Documented compatibility note in the module docstring: `(国際)` mapping to `race_flag_graded_stakes` follows Kaggle's existing `column_mapping.py` for join compatibility, even though `(国際)` is strictly an international designation. Phase 6 may revisit this classification if a semantic-cleanup pass is desired.
   - Module imports: `re`, `from typing import Optional`.
  </action>
  <verify>
    <automated>python -c "from src.scraper.course_codes import COURSE_CODE_MAP; assert COURSE_CODE_MAP['福島']=='03' and COURSE_CODE_MAP['新潟']=='04' and COURSE_CODE_MAP['東京']=='05' and len(COURSE_CODE_MAP)==10; from src.scraper.flag_crosswalk import derive_race_flags; f=derive_race_flags('4歳以上オープン (国際)(特指)(ハンデ)'); assert f['race_flag_handicap'] is True and f['race_flag_graded_stakes'] is True and f['race_flag_special_weight'] is True and f['race_flag_filly_only'] is None; print('Crosswalk OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `COURSE_CODE_MAP["福島"] == "03"` and `COURSE_CODE_MAP["新潟"] == "04"` (HIGH #5 corrected)
    - `len(COURSE_CODE_MAP) == 10` covering all JRA venues
    - `derive_race_flags` returns a dict with EXACTLY 20 keys, all matching RaceSchema field names
    - `derive_race_flags('4歳以上オープン (国際)(特指)(ハンデ)')` sets `race_flag_graded_stakes`, `race_flag_special_weight`, `race_flag_handicap` to True
    - `derive_race_flags('3歳未勝利 (馬齢)')` sets `race_flag_maiden`, `race_flag_age_restricted` to True
    - `derive_race_flags('牝馬オープン (牝)')` sets `race_flag_filly_only` (NOT race_flag_mare_only) to True (HIGH #6)
    - Fields with no matching pattern remain None (not False)
    - No `head_count` symbol appears in either module
    - Module docstrings cite column_mapping.py as the source-of-truth cross-reference
  </acceptance_criteria>
  <done>
    - COURSE_CODE_MAP corrected and authoritative (HIGH #5 resolved)
    - FLAG_CROSSWALK validated against column_mapping.py (HIGH #6 resolved)
    - 20 flag keys returned with None-default semantics
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement header-driven parse_race_html and helpers (14-digit horse_race_id, no head_count, no fixed td indices)</name>
  <files>src/scraper/parser.py</files>
  <read_first>
    - src/scraper/course_codes.py (COURSE_CODE_MAP — single source of truth)
    - src/scraper/flag_crosswalk.py (derive_race_flags)
    - src/schemas/race.py (RaceSchema field names — note NO head_count; surface_detail, course_detail, track_condition_detail are Optional and MUST be present)
    - src/schemas/entry.py (EntrySchema: horse_race_id, bracket_num, horse_number, horse_name, sex, age, weight_assigned, jockey, trainer, owner, horse_weight, weight_change, region, popularity, win_odds)
    - src/schemas/result.py (ResultSchema: horse_race_id, race_id, finish_position, finish_note, finish_time, margin, corner_1..4, last_3f, prize_money)
    - src/pipeline/kaggle_converter.py (process_finish_position pattern: null_notes = {中,取,失,除,再} -> position None; 降 keeps position)
    - .planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md (D-03 verified fields, canonical_refs netkeiba page structure)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (HIGH #2 14-digit horse_race_id, HIGH #10 header-driven parsing; MEDIUM head_count removal, surface_detail etc., None vs False, grade GI/GII/full-width/L, corner count variability, demotion 降 format)
  </read_first>
  <action>
Per D-02 (BS4+lxml parse), D-03 (all fields obtainable), D-09 (no schema gaps), SCRP-01 (separation), SCRP-03 (parse to standard). Per Codex Review HIGH #2 (horse_race_id 14-digit), HIGH #10 (header-driven parsing), MEDIUM (head_count, surface/course/track_condition_detail, grade forms, corner count, None vs False).

NOTE on horse_race_id format: Phase 1 RESEARCH.md (lines 333, 401) states the authoritative Kaggle value is the 14-digit concatenation `YYYYPPCCDDRRHH` (race_id + 2-digit horse_number). The schema docstrings say `{race_id}_{horse_number:02d}` (with underscore) but this is a doc error; the actual Kaggle data is 14-digit. Per HIGH #2, use 14-digit to match Kaggle.

Create `src/scraper/parser.py` with these public functions:

1. `parse_horse_weight(text: str) -> tuple[Optional[int], Optional[int]]`:
   - "456(+4)" -> (456, 4); "478(-2)" -> (478, -2); "472(0)" -> (472, 0); "456" -> (456, None); "計不"/"---"/"" -> (None, None).

2. `parse_sex_age(text: str) -> tuple[Optional[str], Optional[int]]`:
   - "牡4" -> ("牡", 4); "牝3" -> ("牝", 3); "セ5" -> ("セ", 5); empty/short -> (None, None).

3. `def resolve_columns_by_header(table, header_aliases: dict[str, list[str]]) -> dict[str, int]`:
   - Per Codex Review HIGH #10 — replace fixed `<td>` index extraction. Find the header row (`<tr>` containing `<th>` cells), normalize each `<th>` text (strip whitespace, full-width -> half-width), and match against `header_aliases` (e.g. `{"finish_position": ["着順"], "bracket_num": ["枠番"], "horse_number": ["馬番"], "horse_name": ["馬名"], "sex_age": ["性齢"], "weight_assigned": ["斤量"], "jockey": ["騎手"], "finish_time": ["タイム"], "margin": ["着差"], "passing": ["通過"], "last_3f": ["上り", "上がり"], "win_odds": ["単勝"], "popularity": ["人気"], "horse_weight": ["馬体重"], "trainer": ["調教師"], "owner": ["馬主"], "prize_money": ["賞金"]}`).
   - Returns a `{field_name: column_index}` map. If a required header is missing, `logger.warning` and skip that field (caller treats as None). This is robust against column reorderings.

4. `parse_race_html(html_path: Path) -> dict`:
   - Read HTML with `encoding="utf-8"`. Return `{"race": {...}, "entries": [...], "results": [...]}`.

   Race header (per CONTEXT.md canonical_refs netkeiba structure):
   - `race_date` from `<p class="smalltxt">`: regex `r'(\d{4})年(\d{1,2})月(\d{1,2})日'` -> ISO "YYYY-MM-DD".
   - Meeting: regex `r'(\d+)回(\S+?)(\d+)日目'` -> `meeting_num: int`, course_name, `meeting_day: int`.
   - `course_code` via `COURSE_CODE_MAP[course_name]`; `course_name` also stored.
   - `race_condition` (the class/condition text after meeting info) and call `derive_race_flags(race_condition, race_name)` to populate all 20 `race_flag_*` fields.
   - Course info from the `<diary_snap_cut span>` / `<p>`: surface ("芝"/"ダート"), direction ("右"/"左"), distance (int). For obstacle races ("障害" substring) — set `obstacle = "障害"` so the normalizer filters them out (normalizer handles the actual drop).
   - Emit `surface_detail`, `course_detail`, `track_condition_detail` as None when not present in HTML (so the normalizer can reindex against RaceSchema — Codex Review MEDIUM).
   - `weather` via `r'天候\s*[:：]\s*(\S+)'`; `track_condition` via the second capture of `r'(?:芝|ダート)\s*[:：]\s*(\S+)'`; `start_time` via `r'発走\s*[:：]\s*(\d{1,2}:\d{2})'`.
   - `race_name` from `<h1>`; `grade` parsed via the GRADE_PATTERNS regex (handles GI/GII/GIII/G1/G2/G3/JG*/full-width/`(L)`/listed).
   - `race_id` from the HTML filename stem. `race_number` from the last 2 digits of race_id. `grade_revision` from `r'第(\d+)回'` if present, else None.
   - DO NOT emit `head_count` (Codex Review MEDIUM — field does not exist in RaceSchema).

   Result table (class `race_table_01` or `race_table_01 nk_tb_common`):
   - Use `resolve_columns_by_header` to build the field->index map. Extract each data row by field name from the resolved map, NOT by hardcoded `cols[N]` index.
   - For each row, build an entry dict (EntrySchema field names) and a result dict (ResultSchema field names):
     - `horse_race_id = f"{race_id}{horse_number:02d}"` — 14-digit concatenation (HIGH #2). Validate the result is 14 digits; if not, log warning.
     - `race_id` FK on both.
     - Finish position handling per kaggle_converter.py: null_notes = {"中","取","失","除","再"} -> finish_position None, finish_note set; "降" -> keep position, finish_note="降"; numeric -> int position, finish_note None.
     - `sex`/`age` via parse_sex_age; `horse_weight`/`weight_change` via parse_horse_weight.
     - `weight_assigned` float; `win_odds` float; `popularity` int; `last_3f` float; `prize_money` float.
     - Corner parsing: split the 通過 cell on `-`; map to corner_1..corner_N for N up to 4. If fewer than 4 positions, set missing corners to None (Codex Review MEDIUM — races may have 2/3 passing points). If more than 4, log warning and keep first 4.
     - `region` from trainer cell prefix `[東]`/`[西]` -> "東"/"西"; strip the prefix from `trainer`.
     - Demotion: `着順` like `5降` -> finish_position=5, finish_note="降".

   Imports: `from pathlib import Path`, `import re`, `from typing import Optional`, `from bs4 import BeautifulSoup`, `from loguru import logger`, `from src.scraper.course_codes import COURSE_CODE_MAP`, `from src.scraper.flag_crosswalk import derive_race_flags`.

5. Do NOT modify `src/scraper/__init__.py`.
  </action>
  <verify>
    <automated>python -c "from src.scraper.parser import parse_race_html, parse_horse_weight, parse_sex_age, resolve_columns_by_header; print('Parser imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `parse_horse_weight("456(+4)") == (456, 4)` and `parse_horse_weight("計不") == (None, None)`
    - `parse_sex_age("牡4") == ("牡", 4)`
    - `resolve_columns_by_header` returns a dict mapping field names to indices, derived from `<th>` text — verify it returns non-empty for a sample table
    - `parse_race_html` returns dict with "race", "entries", "results" keys
    - horse_race_id in parsed entries/results matches `re.fullmatch(r"\d{14}", horse_race_id)` (HIGH #2 — 14-digit, NO underscore)
    - `grep -n 'f"{race_id}_{horse_number' src/scraper/parser.py` returns nothing (no underscore format)
    - race dict contains ALL RaceSchema field names (no head_count); surface_detail/course_detail/track_condition_detail present (None allowed)
    - race dict contains all 20 `race_flag_*` keys
    - `(牝)` sets `race_flag_filly_only` (not `race_flag_mare_only`) (HIGH #6)
    - `(国際)` sets `race_flag_graded_stakes` (Kaggle compatibility)
    - Finish notes 中/取/失/除/再 -> finish_position None; 降 keeps position
    - Corner parsing handles 2, 3, 4 positions without crashing
    - `grep -n "head_count" src/scraper/parser.py` returns nothing (MEDIUM resolved)
  </acceptance_criteria>
  <done>
    - Header-driven parser implemented (HIGH #10 resolved)
    - 14-digit horse_race_id (HIGH #2 resolved)
    - Corrected COURSE_CODE_MAP consumed from single source (HIGH #5 application)
    - Flag crosswalk validated against column_mapping.py (HIGH #6 resolved)
    - head_count removed; surface_detail etc. emitted (MEDIUM resolved)
    - None vs False semantics consistent
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Save golden HTML fixtures (multi-year/venue/grade/surface/cancellation) for parser tests</name>
  <files>tests/scraper/fixtures/html/</files>
  <read_first>
    - tests/scraper/conftest.py (golden_html_dir fixture)
    - src/scraper/parser.py (the field names fixtures must exercise)
    - .planning/phases/04-scraping-infrastructure-race-data/04-CONTEXT.md (D-03 verified 2022 中山 examples)
    - .planning/phases/04-scraping-infrastructure-race-data/04-REVIEWS.md (HIGH #9 golden fixtures across years/venues/grades/surfaces/cancellations)
  </read_first>
  <action>
Per Codex Review HIGH #9 — fixture-based end-to-end test requires SAVED REAL HTML golden files spanning the diversity axes (years, venues, grades, surfaces, cancellations). Claude cannot fabricate real netkeiba HTML without risking silent structural mismatches; the user runs a Playwright session against db.netkeiba.com to capture authentic pages.

NOTE: This is a checkpoint:human-action because it requires live network access to db.netkeiba.com with rate limiting, which Claude cannot perform autonomously. The fixtures become the contract for Plan 06's end-to-end test.

Target fixtures (save each to `tests/scraper/fixtures/html/{race_id}.html`):
1. A 2022 中山 flat race (e.g. a maiden or allowance race) — exercises the base case.
2. A 2023 阪神 graded stakes (G1/G2/G3) — exercises grade parsing and `(国際)(特指)(ハンデ)` flag crosswalk.
3. A 2024 東京 or 中京 dirt race — exercises the ダート surface branch.
4. A race with a cancelled/scratched runner (着順 = 取 or 中) — exercises finish_note handling.
5. (Optional) An obstacle race — exercises obstacle detection (parser emits it; normalizer filters).

User workflow:
- Open the Playwright session script if Plan 03 already provides one, OR navigate manually in a browser to `https://db.netkeiba.com/race/{race_id}/` for each chosen race.
- Save Page Source (UTF-8) to `tests/scraper/fixtures/html/{race_id}.html`.
- Commit each fixture with a clear message naming the diversity axis.

If the user cannot capture all 5 immediately, capture at least 3 covering (base flat, graded, dirt) and note the remaining as a follow-up TODO in the SUMMARY.
  </action>
  <verify>
    <automated>test -d tests/scraper/fixtures/html &amp;&amp; ls tests/scraper/fixtures/html/*.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/scraper/fixtures/html/` directory exists with at least 3 real `.html` files
    - Each fixture is valid HTML loadable by BS4 (`python -c "from bs4 import BeautifulSoup; BeautifulSoup(open('tests/scraper/fixtures/html/<file>.html', encoding='utf-8'), 'lxml')"`)
    - At least one fixture has a graded-stakes condition text (exercises grade parsing)
    - At least one fixture has a ダート surface (exercises dirt branch)
    - SUMMARY records the race_id and diversity axis covered by each fixture
  </acceptance_criteria>
  <done>
    - At least 3 golden HTML fixtures captured (5 ideal) covering multi-year/venue/grade/surface/cancellation axes
    - Fixtures committed to tests/scraper/fixtures/html/
    - Parser tests in Task 4 can load these fixtures without network access
  </done>
  <resume-signal>Type "approved" and confirm the number of fixtures saved, OR describe which fixtures are missing.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write and pass parser tests (header-driven, course codes, flag crosswalk, horse_race_id)</name>
  <files>tests/scraper/test_course_codes.py, tests/scraper/test_parser.py</files>
  <read_first>
    - src/scraper/course_codes.py (COURSE_CODE_MAP exact codes to assert)
    - src/scraper/flag_crosswalk.py (derive_race_flags expected behavior)
    - src/scraper/parser.py (functions to test)
    - tests/scraper/fixtures/html/ (golden HTML saved in Task 3)
    - src/pipeline/kaggle_converter.py (null_notes pattern to verify)
    - src/schemas/race.py (verify all 20 flag field names)
    - src/schemas/entry.py (EntrySchema field names)
    - src/schemas/result.py (ResultSchema field names)
  </read_first>
  <behavior>
    - COURSE_CODE_MAP covers all 10 venues with correct codes
    - parse_horse_weight handles all formats including 計不
    - parse_sex_age handles 牡/牝/セ
    - derive_race_flags returns exactly 20 keys; (牝) -> race_flag_filly_only; (国際) -> race_flag_graded_stakes; (ハンデ) -> race_flag_handicap
    - resolve_columns_by_header maps header text to indices (not hardcoded)
    - parse_race_html loads golden fixture and returns race/entries/results
    - horse_race_id is 14 digits (no underscore) — assert re.fullmatch(r"\d{14}", ...)
    - race dict has NO head_count field; HAS surface_detail, course_detail, track_condition_detail
    - Finish note 取 -> finish_position None, finish_note "取"
    - Grade G1/GI/full-width all parsed
  </behavior>
  <action>
Per SCRP-01 (separation), SCRP-03 (parse to standard).

Create `tests/scraper/test_course_codes.py`:
1. `class TestCourseCodes`:
   - `test_all_10_venues_present`: assert set(COURSE_CODE_MAP.keys()) == {"札幌","函館","福島","新潟","東京","中山","中京","京都","阪神","小倉"}.
   - `test_specific_codes`: parametrized — 札幌=01, 函館=02, 福島=03, 新潟=04, 東京=05, 中山=06, 中京=07, 京都=08, 阪神=09, 小倉=10 (HIGH #5 regression guard).
   - `test_reverse_map`: assert COURSE_CODE_REVERSE["03"] == "福島".

Create `tests/scraper/test_parser.py`:
2. `class TestHorseWeightParsing`: 456(+4)->(456,4); 478(-2)->(478,-2); 472(0)->(472,0); 計不->(None,None); ""->(None,None); "456"->(456,None).
3. `class TestSexAgeParsing`: 牡4->("牡",4); 牝3->("牝",3); セ5->("セ",5); ""->(None,None).
4. `class TestFlagCrosswalk`:
   - `test_handicap`: flags["race_flag_handicap"] is True for "...(ハンデ)".
   - `test_filly_only`: flags["race_flag_filly_only"] is True (NOT race_flag_mare_only) for "...(牝)".
   - `test_international_to_graded_stakes`: flags["race_flag_graded_stakes"] is True for "...(国際)".
   - `test_grade_g1`: flags["race_flag_graded_stakes"] and flags["race_flag_stakes"] True for "東京優駿(GI)".
   - `test_grade_fullwidth`: same for "ＧＩ".
   - `test_maiden`: flags["race_flag_maiden"] is True for "3歳未勝利".
   - `test_exactly_20_keys`: assert len(flags) == 20 and set(flags.keys()) == {f.name for f in RaceSchema.model_fields.values() if f.name.startswith("race_flag_")}.
   - `test_unknown_flags_none`: flags["race_flag_amateur"] is None for plain "4歳以上1000万下".
5. `class TestResolveColumnsByHeader`:
   - `test_maps_headers_to_indices`: build a small table `<table><tr><th>着順</th><th>馬名</th></tr><tr><td>1</td><td>馬A</td></tr></table>`; assert resolve returns {"finish_position":0, "horse_name":1}.
   - `test_missing_header_skipped`: assert a table lacking 馬番 does NOT include "horse_number" in the result.
6. `class TestParseRaceHtmlGolden`:
   - For each golden fixture in `tests/scraper/fixtures/html/`: parse_race_html; assert "race"/"entries"/"results" keys present; assert every entry/result horse_race_id matches `re.fullmatch(r"\d{14}", horse_race_id)`; assert "head_count" NOT in race dict; assert "surface_detail" in race dict (may be None).
   - `test_finish_note_handling`: pick the fixture with 取/中 if available, else construct inline; assert finish_position is None and finish_note == "取".
   - `test_corner_parsing_variability`: parse a fixture; assert corner_1..4 keys exist (None allowed); no crash if a race had 2 or 3 corners.
   - `test_flag_crosswalk_applied`: parse the graded fixture; assert race["race_flag_graded_stakes"] is True.
  </action>
  <verify>
    <automated>pytest tests/scraper/test_course_codes.py tests/scraper/test_parser.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All classes pass: TestCourseCodes, TestHorseWeightParsing, TestSexAgeParsing, TestFlagCrosswalk, TestResolveColumnsByHeader, TestParseRaceHtmlGolden
    - Test count >= 20
    - Parametrized course-code test covers all 10 venues with corrected codes (HIGH #5 regression guard)
    - horse_race_id 14-digit assertion passes for every golden fixture (HIGH #2)
    - No test asserts horse_race_id contains an underscore
    - `head_count` assertion (absent) passes
    - `pytest tests/scraper/test_course_codes.py tests/scraper/test_parser.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    - Course-code regression test covers all 10 venues (HIGH #5)
    - Header-driven parsing verified (HIGH #10)
    - Flag crosswalk validated against column_mapping.py semantics (HIGH #6)
    - 14-digit horse_race_id verified (HIGH #2)
    - Golden-fixture parsing verified across diversity axes (HIGH #9 parser side)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw HTML -> parser | Untrusted netkeiba HTML parsed by BS4+lxml |
| parser -> normalizer | Structured dicts flow into DataFrame construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | HTML injection / layout shift breaking fixed-index parsing | mitigate | HIGH #10: header-driven column resolution validates `<th>` names; missing headers logged and field set to None instead of misattributing data. |
| T-04-11 | Tampering | Silent course-code corruption (wrong venue code in training data) | mitigate | HIGH #5: COURSE_CODE_MAP corrected (福島=03, 新潟=04) in single authoritative module; parametrized test guards all 10 venues. |
| T-04-12 | Tampering | horse_race_id mismatch breaks Phase 6 Kaggle join | mitigate | HIGH #2: 14-digit concatenation `f"{race_id}{horse_number:02d}"` matches Kaggle; regex-validated before emission. |
| T-04-13 | Tampering | Misclassified race flags contaminate features | mitigate | HIGH #6: flag crosswalk validated against column_mapping.py; (牝)->race_flag_filly_only; (国際)->race_flag_graded_stakes; compatibility documented. |
| T-04-14 | Information Disclosure | Raw HTML at rest | accept | Local-only storage; spec forbids public sharing. |
</threat_model>

<verification>
- `pytest tests/scraper/test_course_codes.py tests/scraper/test_parser.py -x -q` passes
- `python -c "from src.scraper.parser import parse_race_html, resolve_columns_by_header; from src.scraper.course_codes import COURSE_CODE_MAP; from src.scraper.flag_crosswalk import derive_race_flags"` exits 0
- `grep -n 'f"{race_id}_' src/scraper/parser.py` returns nothing
- `grep -n "head_count" src/scraper/` returns nothing
</verification>

<success_criteria>
- COURSE_CODE_MAP corrected and tested for all 10 venues (HIGH #5 resolved)
- horse_race_id uses 14-digit format matching Kaggle (HIGH #2 resolved)
- Flag crosswalk validated against column_mapping.py (HIGH #6 resolved)
- Header-driven parsing replaces fixed td indices (HIGH #10 resolved)
- head_count removed; surface/course/track_condition_detail emitted (MEDIUM resolved)
- Golden HTML fixtures captured for downstream end-to-end test (HIGH #9 setup)
</success_criteria>

## Artifacts this phase produces

- `src/scraper/course_codes.py` -- `COURSE_CODE_MAP: dict[str,str]` (10 venues, corrected), `COURSE_CODE_REVERSE: dict[str,str]`
- `src/scraper/flag_crosswalk.py` -- `FLAG_CROSSWALK: list[tuple[str,str]]`, `CLASS_PATTERNS`, `GRADE_PATTERNS`, `derive_race_flags(race_condition, race_name="") -> dict[str, Optional[bool]]` (20 keys)
- `src/scraper/parser.py` -- functions: `parse_race_html(html_path)`, `parse_horse_weight(text)`, `parse_sex_age(text)`, `resolve_columns_by_header(table, header_aliases)`; produces race dict (RaceSchema keys, NO head_count), entry list (EntrySchema keys, 14-digit horse_race_id), result list (ResultSchema keys)
- `tests/scraper/test_course_codes.py` -- class `TestCourseCodes` (parametrized over all 10 venues)
- `tests/scraper/test_parser.py` -- classes: `TestHorseWeightParsing`, `TestSexAgeParsing`, `TestFlagCrosswalk`, `TestResolveColumnsByHeader`, `TestParseRaceHtmlGolden`
- `tests/scraper/fixtures/html/*.html` -- golden real-HTML fixtures (multi-year/venue/grade/surface/cancellation)

<output>
Create `.planning/phases/04-scraping-infrastructure-race-data/04-04-SUMMARY.md` when done
</output>

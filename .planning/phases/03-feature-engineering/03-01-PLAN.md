---
phase: 03-feature-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/feature_generator.py
  - tests/pipeline/test_feature_generator.py
  - tests/pipeline/conftest.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "feature_generator.py module exists with generate() orchestrator and helper functions"
    - "Test fixtures provide sample race+entry+result DataFrames with English schema column names"
    - "generate() reads standard Parquet, merges race+entry+result into a single DataFrame"
    - "Race context features (course_name, distance, surface, direction, weather, track_condition, race_number, grade, field_size) are present in output"
    - "Horse basic features (bracket_num, horse_number, sex, age, weight_assigned, horse_weight, weight_change) are present in output"
  artifacts:
    - path: "src/pipeline/feature_generator.py"
      provides: "Feature generation module skeleton with generate() orchestrator and race/horse feature extraction"
      exports: ["generate", "load_and_merge", "extract_race_context_features", "extract_horse_basic_features"]
    - path: "tests/pipeline/conftest.py"
      provides: "Feature-specific test fixtures (sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df, sample_feature_merged_df, tmp_feature_dir)"
    - path: "tests/pipeline/test_feature_generator.py"
      provides: "Unit tests for race context and horse basic feature extraction"
  key_links:
    - from: "src/pipeline/feature_generator.py"
      to: "src/schemas/race.py"
      via: "import RaceSchema for column reference"
      pattern: "from src\\.schemas\\.race import RaceSchema"
    - from: "src/pipeline/feature_generator.py"
      to: "data/standard/race.parquet"
      via: "pd.read_parquet in load_and_merge()"
      pattern: "pd\\.read_parquet.*race\\.parquet"

---

## ROADMAP Discrepancy Note

ROADMAP.md Phase 3 success criterion #1 lists "popularity/win odds" as features. CONTEXT.md D-15 explicitly excludes them (post-race, per Phase 1 D-03/D-06). **D-15 takes precedence.** 人気 and 単勝オッズ are NOT included in any feature. They are reserved for EV calculation in Phase 8.

---

<objective>
Create the feature_generator.py module skeleton with test fixtures, load/merge logic, and race context + horse basic feature extraction.

Purpose: Establish the module structure, test fixtures, and foundation that all subsequent feature plans build upon. The generate() orchestrator, data loading, and the simplest feature groups (direct column selection from race/entry tables) are implemented first.
Output: src/pipeline/feature_generator.py (skeleton + race/horse features), updated test fixtures, test file with passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-engineering/03-CONTEXT.md
@.planning/phases/03-feature-engineering/03-RESEARCH.md
@.planning/phases/03-feature-engineering/03-PATTERNS.md
@src/pipeline/kaggle_converter.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@tests/pipeline/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module skeleton + test fixtures + load/merge + race context features</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py,
    tests/pipeline/conftest.py
  </files>
  <read_first>
    src/pipeline/kaggle_converter.py
    tests/pipeline/conftest.py
    src/schemas/race.py
    src/schemas/entry.py
    src/schemas/result.py
    .planning/phases/03-feature-engineering/03-RESEARCH.md (Code Examples section: Complete Feature Column Inventory)
    .planning/phases/03-feature-engineering/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: load_and_merge() reads race.parquet, entry.parquet, result.parquet from standard_dir and returns a merged DataFrame with columns from all three tables
    - Test 2: load_and_merge() produces correct row count (race x entry join, 1:1 with result on horse_race_id)
    - Test 3: extract_race_context_features() produces columns: course_name, distance, surface, direction, weather, track_condition, race_number, grade
    - Test 4: extract_race_context_features() computes field_size as count of entries per race_id
    - Test 5: Race context features do NOT include popularity or win_odds (per D-15)
  </behavior>
  <action>
    **conftest.py additions:**

    Add 5 new fixtures AFTER the existing ones (do not modify existing fixtures). Use English schema column names matching RaceSchema, EntrySchema, ResultSchema field names exactly.

    - `sample_standard_race_df`: 4 rows with all 41 RaceSchema columns. Include 2 race_ids ("201501010101", "201502020202") with distinct course_name ("東京"/"中山"), distances (2000/1400), surfaces ("芝"/"ダート"), weather ("晴"/"曇"). Use proper dtypes (object for strings, int64 for integers). Include race_date as string "YYYY-MM-DD" format.

    - `sample_standard_entry_df`: 7 rows with all 16 EntrySchema columns. 4 horses in race 201501010101, 3 horses in race 201502020202. Include variety: sex "牡"/"牝"/"セ", age 2-6, different jockey/trainer names. Include popularity and win_odds columns (they exist in entry but must NOT leak into features). Two horses share the same horse_name "馬A" across different races (enables lag feature testing in later plans).

    - `sample_standard_result_df`: 7 rows with all 12 ResultSchema columns matching entry rows 1:1 on horse_race_id. Include variety: finish_position 1-7, finish_note with "中" for one row (DNF), margin values "3/4", "1.1/2", "", "ハナ", finish_time "1:58.5", "1:59.1" etc., last_3f 34.5-36.0, corner_4 values 1-5.

    - `sample_feature_merged_df`: Calls load_and_merge() with the above fixtures (use monkeypatch or direct call in fixture). This is the input DataFrame for feature extraction tests.

    - `tmp_feature_dir`: Temporary data/feature/ directory (similar to existing tmp_standard_dir pattern).

    **feature_generator.py:**

    Follow the kaggle_converter.py structural pattern exactly. Module docstring documents all D-NN decisions. Imports: pathlib.Path, pandas, numpy, loguru.logger, audit_leakage from src.schemas.audit, all 3 schema classes.

    Define the following CATEGORICAL_COLUMNS constant (for later plans to use):
    `['course_name', 'surface', 'direction', 'weather', 'track_condition', 'sex', 'jockey', 'trainer', 'grade']`

    Define MARGIN_MAP constant (partial, for Plan 02 to complete):
    Empty dict placeholder -- Plan 02 fills the full mapping.

    Implement `load_and_merge(standard_dir)`:
    - Read race.parquet, entry.parquet, result.parquet via pd.read_parquet with pyarrow engine
    - Merge: entry.merge(race, on="race_id", how="left").merge(result, on="horse_race_id", how="left")
    - Sort by ["horse_name", "race_date"] and reset_index -- this sort order is required by lag features later
    - Return merged DataFrame

    Implement `extract_race_context_features(df)`:
    - Takes the merged DataFrame
    - Computes field_size as df.groupby("race_id")["horse_number"].transform("count")
    - Returns df with the following columns guaranteed present: race_id, race_date, course_name, distance, surface, direction, weather, track_condition, race_number, grade, field_size
    - Does NOT include popularity, win_odds, or any post-race result columns

    Implement `generate()` top-level orchestrator as a skeleton:
    ```python
    def generate(standard_dir=Path("data/standard"), feature_dir=Path("data/feature")) -> dict[str, Path]:
    ```
    - Calls load_and_merge()
    - Calls extract_race_context_features()
    - Has placeholder comments for steps that Plans 02-05 will fill: "margin_numeric conversion", "finish_time z-score normalization", "lag features", "jockey/trainer stats", "target_top3", "debut flag", "categorical conversion", "leakage audit", "write Parquet"
    - Returns empty dict for now (Parquet writing comes in Plan 05)

    **test_feature_generator.py:**

    Create with module docstring listing all test areas (11 classes per PATTERNS.md). Implement TestLoadMerge class (2 tests for load/merge) and TestRaceContextFeatures class (3 tests for race features). All other test classes are stubs with a single `pytest.skip("not yet implemented")` test.

    For load_and_merge tests: use tmp_path to write sample Parquet files from fixtures, then call load_and_merge().
    For race context tests: call extract_race_context_features() directly on sample_feature_merged_df.
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestLoadMerge tests/pipeline/test_feature_generator.py::TestRaceContextFeatures -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def generate(` and `def load_and_merge(` and `def extract_race_context_features(`
    - `tests/pipeline/conftest.py` contains fixtures `sample_standard_race_df`, `sample_standard_entry_df`, `sample_standard_result_df`, `sample_feature_merged_df`, `tmp_feature_dir`
    - `tests/pipeline/test_feature_generator.py` exists with TestLoadMerge and TestRaceContextFeatures classes
    - `pytest tests/pipeline/test_feature_generator.py::TestRaceContextFeatures -x` exits 0
    - Race context output does NOT contain "popularity" or "win_odds" columns (per D-15)
    - Field size equals entry count per race_id (4 for first race, 3 for second in test data)
  </acceptance_criteria>
  <done>
    Feature generator module skeleton with load/merge and race context feature extraction working. Test fixtures provide sample data for all subsequent plans. 5 tests passing. No post-race columns in race context features (D-15 enforced).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Horse basic features extraction</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py
  </files>
  <read_first>
    src/pipeline/feature_generator.py (current state after Task 1)
    tests/pipeline/test_feature_generator.py (current state after Task 1)
    src/schemas/entry.py
  </read_first>
  <behavior>
    - Test 1: extract_horse_basic_features() produces columns: bracket_num, horse_number, sex, age, weight_assigned, horse_weight, weight_change
    - Test 2: Horse basic features preserve original values from entry table (no data loss)
    - Test 3: Horse basic features do NOT include popularity or win_odds (per D-15)
  </behavior>
  <action>
    Implement `extract_horse_basic_features(df)` in feature_generator.py:
    - Takes the merged DataFrame (after extract_race_context_features)
    - Returns df with guaranteed columns: bracket_num, horse_number, sex, age, weight_assigned, horse_weight, weight_change, horse_name (kept for groupby in later lag features)
    - These are direct column selections from the entry table -- no transformation needed
    - Explicitly document that popularity and win_odds are NOT extracted (per D-15, post-race)

    Wire into generate() orchestrator: call extract_horse_basic_features() after extract_race_context_features().

    Implement TestHorseBasicFeatures class in test_feature_generator.py with 3 tests:
    - test_basic_features_present: assert all 7 columns exist in output
    - test_values_preserved: spot-check bracket_num, sex, age values match input
    - test_no_market_columns: assert "popularity" and "win_odds" not in output columns
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestHorseBasicFeatures -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def extract_horse_basic_features(`
    - `tests/pipeline/test_feature_generator.py` contains class `TestHorseBasicFeatures` with 3 passing tests
    - Output DataFrame contains columns: bracket_num, horse_number, sex, age, weight_assigned, horse_weight, weight_change
    - "popularity" and "win_odds" NOT in output columns (D-15)
  </acceptance_criteria>
  <done>
    Horse basic feature extraction complete. 3 additional tests passing (8 total). Direct column selection from entry table, no post-race columns. generate() orchestrator now calls both race context and horse basic feature extractors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→pipeline | Parquet files read from data/standard/ -- trusted local files from Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | file paths in load_and_merge | mitigate | Use pathlib.Path throughout; validate directory exists before reading |
| T-03-SC | Tampering | pip install (no new packages) | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `python3 -m pytest tests/pipeline/test_feature_generator.py -x -q` passes (8 tests)
- `python3 -m pytest tests/ -x` passes (existing + new tests)
- `ruff check src/pipeline/feature_generator.py tests/pipeline/test_feature_generator.py` clean
</verification>

<success_criteria>
- feature_generator.py module exists with generate(), load_and_merge(), extract_race_context_features(), extract_horse_basic_features()
- Test fixtures in conftest.py provide sample data for all 3 standard tables
- 8 tests passing (2 load/merge + 3 race context + 3 horse basic)
- No post-race columns (popularity, win_odds, finish_position, etc.) in feature output
- field_size computed correctly from entry count per race
</success_criteria>

<output>
Create `.planning/phases/03-feature-engineering/03-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

- `src/pipeline/feature_generator.py` — module with functions: `generate()`, `load_and_merge()`, `extract_race_context_features()`, `extract_horse_basic_features()`, constants `CATEGORICAL_COLUMNS`, `MARGIN_MAP`
- `tests/pipeline/conftest.py` — added fixtures: `sample_standard_race_df`, `sample_standard_entry_df`, `sample_standard_result_df`, `sample_feature_merged_df`, `tmp_feature_dir`
- `tests/pipeline/test_feature_generator.py` — test classes: `TestLoadMerge`, `TestRaceContextFeatures`, `TestHorseBasicFeatures` (plus stub classes for Plans 02-05)

---
phase: 03-feature-engineering
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/pipeline/feature_generator.py
  - tests/pipeline/test_feature_generator.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "target_top3 = 1 for finish_position 1-3, 0 otherwise (per D-12)"
    - "Horses with finish_note '取' get result_status='scratched', exclude_from_training=True (per D-13)"
    - "Horses with finish_note '除' get result_status='removed', exclude_from_training=True (per D-13)"
    - "Horses with finish_note '中' have target_top3=0, result_status='dnf', is_dnf=True, exclude_from_training=False (per D-13)"
    - "result_status distinguishes all 6 categories: finished, dnf, disqualified, scratched, removed, demoted"
    - "is_debut is True for first VALID start per horse_entity_key, excluding 取/除 from history count"
  artifacts:
    - path: "src/pipeline/feature_generator.py"
      provides: "Target variable generation, debut flag, and auxiliary columns"
      exports: ["generate_target", "compute_debut_flag"]
    - path: "tests/pipeline/test_feature_generator.py"
      provides: "TestTargetVariable, TestDebutFlag test classes"
  key_links:
    - from: "generate_target"
      to: "finish_position, finish_note"
      via: "reads result columns for target computation; finish_note disambiguates 取 from 除 from 中"
      pattern: "finish_position|finish_note"
    - from: "compute_debut_flag"
      to: "horse_entity_key, result_status"
      via: "checks for prior valid starts per horse_entity_key, excluding scratched/removed"
      pattern: "horse_entity_key|is_valid_start"

---

## Review Feedback Addressed (Cycle 3 -- Root Cause Fix)

| # | Concern | Severity | Root Cause | Fix |
|---|---------|----------|------------|-----|
| 6 (residual) | Resultless rows cannot distinguish scratched from removed | HIGH (was moot) | Previous plans assumed left-join was needed because scratched entries might lack result rows. Data analysis confirmed: entry and result are 1:1 (311,806 each). EVERY entry has a result row. | The finish_note column in the result table already provides the distinction: 取 (scratched, 520), 除 (removed, 604), 中 (DNF, 966), 降 (demoted, 18), 失 (disqualified, 1). generate_target() maps finish_note directly to result_status. No "no_result" catch-all needed. |

---

<objective>
Implement target variable generation (target_top3), auxiliary columns (result_status, is_dnf, exclude_from_training), and debut flag (is_debut).

Purpose: target_top3 is the binary classification label for Model A training. The auxiliary columns enable downstream analysis. is_debut signals first-time starters. With the 1:1 entry-result relationship confirmed, generate_target() directly maps finish_note values to result_status categories without any catch-all.

Output: Target generation and debut flag functions with comprehensive edge case tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-feature-engineering/03-CONTEXT.md
@.planning/phases/03-feature-engineering/03-RESEARCH.md
@src/pipeline/feature_generator.py
@src/schemas/result.py
@tests/pipeline/test_feature_generator.py
@.planning/phases/03-feature-engineering/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Target variable + result_status + is_dnf + exclude_from_training</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py
  </files>
  <read_first>
    src/pipeline/feature_generator.py
    tests/pipeline/test_feature_generator.py
    .planning/phases/03-feature-engineering/03-CONTEXT.md (D-11, D-12, D-13, D-14)
  </read_first>
  <behavior>
    - Test 1: finish_position 1 -> target_top3 = 1
    - Test 2: finish_position 2 -> target_top3 = 1
    - Test 3: finish_position 3 -> target_top3 = 1
    - Test 4: finish_position 4 -> target_top3 = 0
    - Test 5: finish_note "中" (DNF) -> target_top3 = 0, result_status = "dnf", is_dnf = True, exclude_from_training = False
    - Test 6: finish_note "取" (scratched) -> target_top3 = 0, result_status = "scratched", exclude_from_training = True
    - Test 7: finish_note "除" (removed) -> target_top3 = 0, result_status = "removed", exclude_from_training = True
    - Test 8: finish_note "失" (disqualified) -> target_top3 = 0, result_status = "disqualified", is_dnf = True, exclude_from_training = False
    - Test 9: finish_note "降" (demoted) -> keeps finish_position, target_top3 based on position, result_status = "demoted"
    - Test 10: Normal finish (no note) -> result_status = "finished", is_dnf = False
    - Test 11: finish_note "取" and finish_note "除" produce DIFFERENT result_status values ("scratched" vs "removed") -- distinct classification verified
  </behavior>
  <action>
    Implement `generate_target(df: pd.DataFrame) -> pd.DataFrame`:

    Create result_status column (string) using direct finish_note mapping:
    - finish_note == "中" -> "dnf"
    - finish_note == "失" -> "disqualified"
    - finish_note == "取" -> "scratched"
    - finish_note == "除" -> "removed"
    - finish_note == "降" -> "demoted"
    - finish_note is None/NaN and finish_position is not NaN -> "finished"
    - (No "no_result" case needed -- every entry has a result row)

    Implementation order: check finish_note first (specific notes), then default to "finished" for normal completions.

    Create is_dnf column (bool): True where result_status is in ["dnf", "disqualified"]

    Create target_top3 column (Int64 nullable):
    - finish_position <= 3 and not NaN -> 1
    - finish_position > 3 or NaN -> 0
    - Use: `df["target_top3"] = (df["finish_position"] <= 3).astype("Int64")` then `df.loc[df["finish_position"].isna(), "target_top3"] = 0`
    - finish_note "降" (demoted) keeps finish_position per D-12

    Create exclude_from_training column (bool):
    - True where result_status is in ["scratched", "removed"] (per D-13: 取/除 excluded)
    - All other statuses -> False

    Wire into generate() orchestrator: call generate_target() after compute_jockey_trainer_stats().

    Implement TestTargetVariable class with 11 tests covering every case. Test 11 specifically verifies that 取 and 除 get distinct result_status values ("scratched" vs "removed"), addressing the review concern about distinguishing them.
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestTargetVariable -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def generate_target(`
    - target_top3: Int64 column, 1 for positions 1-3, 0 otherwise
    - result_status: string column with values in {finished, dnf, disqualified, scratched, removed, demoted} -- no "no_result"
    - is_dnf: bool column, True for dnf/disqualified
    - exclude_from_training: bool column, True for scratched/removed only (D-13)
    - 取 gets "scratched", 除 gets "removed" (distinct classification)
    - 降 (demoted) horses keep finish_position, target_top3 based on position (D-12)
    - 11 tests passing in TestTargetVariable
  </acceptance_criteria>
  <done>
    Target variable and auxiliary columns implemented per D-11/D-12/D-13/D-14. result_status distinguishes all 6 categories including separate scratched/removed. No "no_result" catch-all needed (1:1 entry-result confirmed). 11 tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Debut flag (is_debut) excluding 取/除 from history count</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py
  </files>
  <read_first>
    src/pipeline/feature_generator.py
    tests/pipeline/test_feature_generator.py
    .planning/phases/03-feature-engineering/03-CONTEXT.md (D-09, D-10)
  </read_first>
  <behavior>
    - Test 1: is_debut is True for a horse's first valid start (where valid start = result_status not in ["scratched", "removed"])
    - Test 2: is_debut is False for a horse's second or later valid start
    - Test 3: A horse whose first entry was 取 (scratched) has is_debut=False for that entry, is_debut=True for their next entry (first actual start)
    - Test 4: is_debut correlates with all prev_1_* columns being NaN (debut horses have no lag features, per D-09)
    - Test 5: Different horse entities (different horse_entity_key) each get independent debut tracking
    - Test 6: A horse with only 取 entries (no valid starts) has is_debut=False for all entries
  </behavior>
  <action>
    Implement `compute_debut_flag(df: pd.DataFrame) -> pd.DataFrame`:
    - df is already sorted by SORT_KEY ["horse_entity_key", "race_date", "race_id"]
    - generate_target() must have been called first (result_status column exists)
    - Define valid start: result_status NOT in ["scratched", "removed"]
    - Create is_valid_start boolean: `df["is_valid_start"] = ~df["result_status"].isin(["scratched", "removed"])`
    - For each horse_entity_key, count cumulative valid starts:
      ```
      valid_before = df.groupby("horse_entity_key")["is_valid_start"].cumsum() - df["is_valid_start"].astype(int)
      df["is_debut"] = (valid_before == 0) & df["is_valid_start"]
      ```
    - A horse whose first entry is 取: is_valid_start=False, valid_before=0, is_debut = 0 & False = False
    - Same horse's second entry (valid start): is_valid_start=True, valid_before=0, is_debut = 0 & True = True
    - A horse with all 取 entries: is_valid_start=False for all, is_debut=False for all

    Wire into generate() orchestrator: call compute_debut_flag() after generate_target().

    Note: The `is_valid_start` column created here is the same definition used by compute_lag_features() (Plan 03). Plan 03 may reference it, or recompute it independently. The definition must be consistent: ~finish_note.isin(["取", "除"]).

    Implement TestDebutFlag class with 6 tests. Test 3 (scratched first entry) is critical. Test 6 (all-scratched) verifies edge case.
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestDebutFlag -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def compute_debut_flag(`
    - is_debut column: True for first valid start per horse_entity_key (excluding 取/除)
    - 取 entry does not consume debut position
    - For debut horses: all prev_1_* columns are NaN
    - is_debut tracks per horse_entity_key, not globally
    - All-scratched horse has is_debut=False for all entries
    - 6 tests passing in TestDebutFlag
  </acceptance_criteria>
  <done>
    Debut flag implemented per D-09 with 取/除 excluded from history count. First valid start (not first entry) identified by is_debut=True. Consistent valid-start definition with lag features. 6 tests passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal computation | Target generation and debut flag are pure data transforms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | target variable correctness | mitigate | 11 edge-case tests covering all finish_note values |
| T-03-08 | Tampering | debut flag correctness | mitigate | 6 tests including scratched-first-entry and all-scratched |
| T-03-SC | Tampering | no new packages | accept | No new packages installed |
</threat_model>

<verification>
- `python3 -m pytest tests/pipeline/test_feature_generator.py -x -q` passes (~49 tests)
- `python3 -m pytest tests/ -x` passes
- `ruff check src/pipeline/feature_generator.py` clean
</verification>

<success_criteria>
- target_top3 correctly classifies positions 1-3 as 1, 4+ as 0 (D-12)
- result_status covers 6 categories (no "no_result" needed): finished, dnf, disqualified, scratched, removed, demoted
- 取 gets "scratched", 除 gets "removed" (distinct, D-14)
- exclude_from_training correctly excludes 取/除 only (D-13)
- is_debut identifies first valid start, excluding 取/除 from history count (D-09)
- ~49 total tests passing
</success_criteria>

<output>
Create `.planning/phases/03-feature-engineering/03-04-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

- `src/pipeline/feature_generator.py` -- new functions: `generate_target()`, `compute_debut_flag()`
- `tests/pipeline/test_feature_generator.py` -- new test classes: `TestTargetVariable` (11 tests), `TestDebutFlag` (6 tests)

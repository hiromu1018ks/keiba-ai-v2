---
phase: 03-feature-engineering
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/pipeline/feature_generator.py
  - tests/pipeline/test_feature_generator.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "Lag features (prev_1 through prev_5) for 5 metrics exist: finish_position, last_3f, corner_4, finish_time_zscore, margin_numeric"
    - "prev_1_finish_position for race N equals finish_position for race N-1 (temporal safety)"
    - "3-race and 5-race statistics (mean, std) are computed for each metric per D-05"
    - "Lag features use .shift(1) so no current-race result information leaks into features"
    - "Jockey rolling stats (top3_rate, win_rate, rides) are computed using expanding windows with shift(1)"
    - "Trainer rolling stats follow the same pattern as jockey stats"
    - "Rolling stats respect D-08: min(last 100 rides, last 1 year) constraint"
  artifacts:
    - path: "src/pipeline/feature_generator.py"
      provides: "Lag feature generation and jockey/trainer rolling statistics"
      exports: ["compute_lag_features", "compute_jockey_trainer_stats"]
    - path: "tests/pipeline/test_feature_generator.py"
      provides: "TestLagFeatures and TestJockeyTrainerStats test classes"
  key_links:
    - from: "compute_lag_features"
      to: "finish_time_zscore, margin_numeric"
      via: "reads columns produced by Plan 02"
      pattern: "finish_time_zscore|margin_numeric"
    - from: "compute_lag_features"
      to: "horse_name"
      via: "groupby key for per-horse temporal shift"
      pattern: "groupby.*horse_name"
    - from: "compute_jockey_trainer_stats"
      to: "jockey, trainer, race_date"
      via: "groupby for rolling stats with temporal constraint"
      pattern: "groupby.*jockey|groupby.*trainer"

---

<objective>
Implement temporal-safe lag features (45 columns) and jockey/trainer rolling statistics (6 columns).

Purpose: Lag features encode each horse's recent performance history -- the most information-rich feature group for the model. Jockey/trainer stats capture human-factor signals. Both are critical for Model A's 3着内確率 prediction. Temporal safety (shift(1)) prevents data leakage (D-01, D-02, D-05, D-06, D-07, D-08).
Output: Two transformation functions producing 51 feature columns total, with comprehensive tests including temporal safety verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-feature-engineering/03-CONTEXT.md
@.planning/phases/03-feature-engineering/03-RESEARCH.md
@.planning/phases/03-feature-engineering/03-PATTERNS.md
@src/pipeline/feature_generator.py
@tests/pipeline/test_feature_generator.py
@.planning/phases/03-feature-engineering/03-01-SUMMARY.md
@.planning/phases/03-feature-engineering/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lag features with temporal safety</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py
  </files>
  <read_first>
    src/pipeline/feature_generator.py
    tests/pipeline/test_feature_generator.py
    tests/pipeline/conftest.py
    .planning/phases/03-feature-engineering/03-RESEARCH.md (Pattern 1: Temporal-Safe Lag Features, Pitfall 1)
    .planning/phases/03-feature-engineering/03-CONTEXT.md (D-01, D-02, D-05, D-09, D-10)
  </read_first>
  <behavior>
    - Test 1: Lag feature columns exist: prev_1_finish_position through prev_5_finish_position, prev_1_last_3f through prev_5_last_3f, prev_1_corner_4 through prev_5_corner_4, prev_1_finish_time_zscore through prev_5_finish_time_zscore, prev_1_margin_numeric through prev_5_margin_numeric (25 raw lag columns total)
    - Test 2: prev_1_finish_position for horse "馬A" in their second race equals finish_position from their first race (temporal safety -- the critical test per RESEARCH.md Pitfall 1)
    - Test 3: prev_1_finish_position for a horse's first race is NaN (no previous data)
    - Test 4: 3-race statistics exist: prev3_finish_position_mean, prev3_finish_position_std, prev3_last_3f_mean, prev3_last_3f_std, etc. (10 columns for 3-race window)
    - Test 5: 5-race statistics exist: prev5_finish_position_mean, prev5_finish_position_std, etc. (10 columns for 5-race window)
    - Test 6: For a horse with only 2 past races, prev3 stats use 2 races (not NaN), but prev5 stats use 2 races as well (per D-10: compute with available data)
    - Test 7: Statistics do NOT include current race data -- they are computed from lag values only (shift(1) already applied to lag columns, so stats from lag columns are inherently safe)
  </behavior>
  <action>
    **Test fixture update required:** Update sample_standard_entry_df and sample_standard_result_df in conftest.py to ensure horse "馬A" appears in at least 3 different races (different race_ids with different dates). The current fixture has 7 rows across 2 races; expand to include a 3rd race with "馬A" to enable temporal safety testing. Keep total rows manageable (~10-12).

    Implement `compute_lag_features(df: pd.DataFrame) -> pd.DataFrame`:
    - df is already sorted by ["horse_name", "race_date"] from load_and_merge()
    - The 5 lag metrics per D-02: finish_position, last_3f, corner_4, finish_time_zscore, margin_numeric
    - For each metric, create prev_{1..5} columns using:
      `df.groupby("horse_name")[metric_col].shift(n)` for n in 1..5
    - This produces 25 raw lag columns

    - For 3-race statistics (D-05), compute for each metric:
      - prev3_{metric}_mean: rolling(3).mean() on the lag-1 column within horse group, shifted by 0 (since lag-1 already excludes current race)
        Implementation: `df.groupby("horse_name")[f"prev_1_{metric}"].rolling(3, min_periods=1).mean().reset_index(drop=True)`
        Note: min_periods=1 per D-10 (compute with available data)
      - prev3_{metric}_std: same with .std() (min_periods=2 to avoid NaN for single value)

    - For 5-race statistics, same pattern with rolling(5):
      - prev5_{metric}_mean: rolling(5, min_periods=1).mean()
      - prev5_{metric}_std: rolling(5, min_periods=2).std()

    - Total stat columns: 5 metrics x 2 stats x 2 windows = 20 columns
    - Total lag columns: 25 raw + 20 stats = 45 columns per D-05

    **Critical temporal safety note:** The lag columns (prev_1_*) already use shift(1), so they reference ONLY past races. The rolling statistics on these lag columns inherit this safety -- they operate on prev_1_* values which are all from past races. No additional shift needed for stats.

    Wire into generate() orchestrator after compute_finish_time_zscore().

    Implement TestLagFeatures class with 7 tests per behavior block. Test 2 (temporal safety) is the most critical: manually construct expected values for "馬A" across 3 races and verify prev_1_* matches the previous race's raw value.
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestLagFeatures -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def compute_lag_features(`
    - 25 raw lag columns exist: prev_{1..5}_{finish_position,last_3f,corner_4,finish_time_zscore,margin_numeric}
    - 20 stat columns exist: prev{3,5}_{finish_position,last_3f,corner_4,finish_time_zscore,margin_numeric}_{mean,std}
    - prev_1_finish_position for a horse's second race == finish_position from first race (temporal safety)
    - prev_1_* for first race is NaN
    - 7 tests passing in TestLagFeatures
  </acceptance_criteria>
  <done>
    45 lag feature columns generated with temporal safety guaranteed via groupby("horse_name").shift(1). All 7 tests pass including the critical temporal safety verification (D-01, D-02, D-05, D-09, D-10).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jockey/trainer rolling statistics</name>
  <files>
    src/pipeline/feature_generator.py,
    tests/pipeline/test_feature_generator.py
  </files>
  <read_first>
    src/pipeline/feature_generator.py
    tests/pipeline/test_feature_generator.py
    tests/pipeline/conftest.py
    .planning/phases/03-feature-engineering/03-RESEARCH.md (Pattern 2: Expanding Window Statistics, Pitfall 2)
    .planning/phases/03-feature-engineering/03-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <behavior>
    - Test 1: Jockey stat columns exist: jockey_rolling_top3_rate, jockey_rolling_win_rate, jockey_rolling_rides
    - Test 2: Trainer stat columns exist: trainer_rolling_top3_rate, trainer_rolling_win_rate, trainer_rolling_rides
    - Test 3: jockey_rolling_rides for a jockey's first race is 0 (no prior rides)
    - Test 4: jockey_rolling_win_rate for a jockey's second race equals their win result (0 or 1) from first race divided by 1
    - Test 5: Stats use only past data -- the current race's result does not influence the stats (shift(1) verification per Pitfall 2)
    - Test 6: jockey_rolling_top3_rate and trainer_rolling_top3_rate values are between 0.0 and 1.0
    - Test 7: jockey and trainer columns are preserved as string (not converted to category yet -- Plan 05 handles that)
  </behavior>
  <action>
    **Test fixture update:** Ensure sample_standard_entry_df has the same jockey appearing in at least 2 races (different dates) to enable temporal safety testing of rolling stats. Add a row where jockey "騎手A" appears in both race 201501010101 and race 201502020202.

    Implement `compute_jockey_trainer_stats(df: pd.DataFrame) -> pd.DataFrame`:

    Helper function `_compute_person_stats(df, person_col, prefix)` that works for both jockey and trainer:
    - Sort by [person_col, "race_date"] and reset_index
    - Create binary columns: is_top3 = (finish_position <= 3), is_win = (finish_position == 1)
      Handle NaN finish_position: is_top3 and is_win are False for NaN positions
    - Group by person_col
    - Compute expanding stats with shift(1) to exclude current race:
      - `{prefix}_rolling_rides` = group.cumcount() (count of prior rows, no shift needed -- cumcount counts before current)
      - `{prefix}_rolling_wins` = group["is_win"].expanding().sum().shift(1).values
      - `{prefix}_rolling_top3` = group["is_top3"].expanding().sum().shift(1).values
    - Compute rates:
      - `{prefix}_rolling_win_rate` = wins / rides (handle 0 rides -> 0.0)
      - `{prefix}_rolling_top3_rate` = top3 / rides (handle 0 rides -> 0.0)

    **D-08 constraint (100 rides or 1 year):**
    For the initial implementation, use expanding window stats as the baseline. The "min(100 rides, 1 year)" constraint can be approximated:
    - Sort within each person group by race_date
    - For each row, the expanding window naturally includes all prior rides
    - Add a post-processing step: for rows where rolling_rides > 100, cap at last 100 rides by re-computing with rolling(100)
    - For the 1-year constraint: compute days since the earliest ride in the window; if > 365, adjust
    - Implementation: compute both expanding and rolling(100) versions, then select the one with fewer rides per row (min constraint)

    However, given that this is a per-row operation, a practical approach:
    - Compute expanding stats (all prior data)
    - Compute rolling(100) stats (last 100)
    - For each row, take the stats from whichever has fewer data points (expanding if < 100, rolling if >= 100)
    - The 1-year constraint is secondary; document that expanding stats naturally recency-weight due to the 100-ride cap

    Call _compute_person_stats for jockey and trainer columns.

    Wire into generate() orchestrator after compute_lag_features().

    Implement TestJockeyTrainerStats class with 7 tests. Test 5 (temporal safety) is critical: verify that jockey_rolling_win_rate for a jockey's Nth race uses only results from races 1 through N-1.
  </action>
  <verify>
    <automated>python3 -m pytest tests/pipeline/test_feature_generator.py::TestJockeyTrainerStats -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/feature_generator.py` contains `def compute_jockey_trainer_stats(` and `def _compute_person_stats(`
    - 6 rolling stat columns exist: jockey_rolling_{top3_rate,win_rate,rides}, trainer_rolling_{top3_rate,win_rate,rides}
    - jockey_rolling_rides for first appearance is 0
    - Stats do not include current race result (shift(1) verified)
    - Rate values in [0.0, 1.0] range
    - 7 tests passing in TestJockeyTrainerStats
  </acceptance_criteria>
  <done>
    Jockey and trainer rolling statistics implemented with temporal safety (D-06, D-07, D-08). 6 rolling stat columns plus original jockey/trainer string columns. 7 tests passing (25 total for Plans 01-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal computation | Lag features and rolling stats are pure pandas operations on trusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | temporal safety of lag features | mitigate | Comprehensive test verifying shift correctness; audit_leakage() in Plan 05 provides final gate |
| T-03-04 | Tampering | rolling stats temporal safety | mitigate | Test verifying shift(1) excludes current race; prevents near-perfect feature-target correlation (Pitfall 2) |
| T-03-SC | Tampering | no new packages | accept | No new packages installed |
</threat_model>

<verification>
- `python3 -m pytest tests/pipeline/test_feature_generator.py -x -q` passes (~25 tests)
- `python3 -m pytest tests/ -x` passes
- `ruff check src/pipeline/feature_generator.py` clean
</verification>

<success_criteria>
- 45 lag feature columns generated from 5 metrics across 5 past races with 3/5-race statistics
- Temporal safety verified: prev_1_* references only past race data (shift(1))
- 6 jockey/trainer rolling stat columns computed with expanding window + shift(1)
- D-08 "min(100 rides, 1 year)" constraint implemented
- ~25 total tests passing
</success_criteria>

<output>
Create `.planning/phases/03-feature-engineering/03-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

- `src/pipeline/feature_generator.py` — new functions: `compute_lag_features()`, `compute_jockey_trainer_stats()`, `_compute_person_stats()`
- `tests/pipeline/conftest.py` — updated fixtures with temporal test data (horse "馬A" in 3+ races, jockey appearing in multiple races)
- `tests/pipeline/test_feature_generator.py` — new test classes: `TestLagFeatures`, `TestJockeyTrainerStats`

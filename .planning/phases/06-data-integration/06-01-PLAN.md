---
phase: 06-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/column_mapping.py
  - src/pipeline/kaggle_converter.py
  - src/scraper/flag_crosswalk.py
  - tests/pipeline/test_kaggle_converter.py
  - data/standard/race.parquet
  - data/standard/entry.parquet
  - data/standard/result.parquet
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "D-01 (HIGH #1 fix): a Kaggle-side grade detector reads `derive_race_flags(race_name=grade_field, race_condition=race_name)` from `src/scraper/flag_crosswalk.py` and ORs its result into the `race_flag_graded_stakes` / `race_flag_stakes` / `race_flag_listed` columns of the regenerated race table, so deleting `column_mapping.py:68` no longer collapses graded_stakes to near-zero (the count is derived from the grade/grade_revision columns, not a fixed band)"
    - "D-01 (mapping removal): `レース記号/(国際)` no longer maps to `race_flag_graded_stakes` in `KAGGLE_COLUMN_MAP` (line 68 deleted); `FLAG_COLUMNS` still contains it at line ~197"
    - "D-02 (HIGH #4 fix / dtype regen): regenerated `data/standard/{race,entry,result}.parquet` have every `race_flag_*` column as Arrow `bool` (not Arrow `null`), `distance`/`meeting_num`/`race_number` as Arrow `int64`, and `win_odds`/`horse_weight` as Arrow `double`"
    - "Phase 2 D-05 8-point verification actually RUNS all 8 checks (HIGH #3 fix): `run_all_validations` is invoked with non-None `source_counts` AND `source_stats` computed from the Kaggle CSV BEFORE regeneration; `overall_pass=True`"
    - "D-05 preserved (HIGH #2 fix): `odds_trifecta.parquet` and `payoff.parquet` row counts AND SHA-256 checksums are IDENTICAL pre- vs post-regen (the regen path never writes those tables)"
    - "race_date dtype contract (MEDIUM #5 resolution): `race_date` stays Arrow `string` per `SCHEMA_DTYPE_MAP[RaceSchema]` (string is the code-authoritative contract; CONTEXT.md D-02 `datetime` note is superseded by the existing code contract — see D-02 resolution note below)"
    - "Grade derivation is deterministic: the regenerated `graded_stakes=True` count equals the count of rows where `derive_race_flags(grade, race_name)` returns `graded_stakes=True` (recomputed from the same grade field the detector uses — not a hardcoded band)"
  artifacts:
    - path: "src/pipeline/column_mapping.py"
      provides: "KAGGLE_COLUMN_MAP with (国際) line removed"
      not_contains: '"レース記号/(国際)": ("race", "race_flag_graded_stakes")'
    - path: "src/pipeline/kaggle_converter.py"
      provides: "import of derive_race_flags + _GRADE_REGEX-driven grade detection on the Kaggle side; _recast_to_canonical helper; restricted convert call OR odds/payoff snapshot/restore"
      contains: "derive_race_flags"
    - path: "src/scraper/flag_crosswalk.py"
      provides: "derive_race_flags already supports race_name= as a second haystack; no edit needed UNLESS the import path requires the module re-export (confirmed: __all__ exports it)"
    - path: "tests/pipeline/test_kaggle_converter.py"
      provides: "test_kaggle_graded_derivation_matches_regex, test_kaggle_parquet_post_d02_has_typed_flags, test_recast_raises_on_bad_data, test_convert_preserves_odds_payoff"
      contains: "test_kaggle_graded_derivation_matches_regex"
    - path: "data/standard/race.parquet"
      provides: "Regenerated Kaggle race table with grade-regex-derived graded_stakes, nullable dtypes, atomically written; race_date stays string"
  key_links:
    - from: "src/pipeline/kaggle_converter.convert_flags_to_bool / split_race_entry_result"
      to: "src/scraper/flag_crosswalk.derive_race_flags"
      via: "from src.scraper.flag_crosswalk import derive_race_flags"
      pattern: "from src\\.scraper\\.flag_crosswalk import.*derive_race_flags"
    - from: "src/pipeline/kaggle_converter._recast_to_canonical"
      to: "src/scraper/normalizer.SCHEMA_DTYPE_MAP"
      via: "from src.scraper.normalizer import SCHEMA_DTYPE_MAP"
      pattern: "from src\\.scraper\\.normalizer import.*SCHEMA_DTYPE_MAP"
---

<objective>
Apply CONTEXT.md decisions D-01 (remove `(国際)→race_flag_graded_stakes` Kaggle mapping) and D-02 (regenerate Kaggle Parquet with nullable dtypes matching `SCHEMA_DTYPE_MAP`), then re-verify the regenerated tables pass Phase 2's D-05 8-point validation.

This plan addresses FOUR HIGH defects surfaced by the cross-AI review (06-REVIEWS.md):
- HIGH #1 (D-01 GRADE_REGEX gap): Kaggle converter has no grade-detection path; deleting `(国際)` alone collapses `graded_stakes` to near-zero. FIX: import `derive_race_flags` from `src/scraper/flag_crosswalk.py` and apply it to the grade/race_name fields on the Kaggle side.
- HIGH #2 (D-05 violation via `convert()`): `convert()` writes all 5 tables. FIX: restrict the regen to race/entry/result via snapshot/restore of odds_trifecta & payoff (SHA-256 + row-count equality assertion).
- HIGH #3 (8-point verification is ~4-5 checks): `run_all_validations` skips row-count/null-rate/distribution when `source_counts`/`source_stats` are None. FIX: compute both from the source CSVs before regen and pass them in.
- HIGH #4 (poetry): every command uses `python -m pytest` / `python -c` (poetry is NOT installed; repo uses setuptools).

MEDIUM #5 (race_date dtype): CONTEXT D-02 says `datetime` but `SCHEMA_DTYPE_MAP[RaceSchema]["race_date"] = "string"` (verified line 99 of src/scraper/normalizer.py) and `src/schemas/race.py:31` declares `race_date: str`. **Resolution: `string` is the code-authoritative contract.** This plan does NOT change race_date to datetime; it keeps string. CONTEXT D-02's `datetime` wording is superseded by the existing Phase 4 dtype discipline (Cycle-2/P05/P06/P07 already converged on string). Document this in the plan SUMMARY.

Purpose: Brings the Kaggle side up to the Phase 4 dtype discipline and the Phase 4 P07 `(国際)`-removal decision, so the two corpora become schema-indistinguishable before the Wave 2 merge.

Output: Three regenerated Parquet files under `data/standard/` (race/entry/result only — odds/payoff untouched), three edited source modules, updated/new tests in `test_kaggle_converter.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md

# Phase 2 converter (the D-02 modification target), the Phase 4 dtype authority,
# the Phase 4 flag crosswalk (the grade detector to reuse), and the validators.
@src/pipeline/kaggle_converter.py
@src/pipeline/column_mapping.py
@src/pipeline/validators.py
@src/scraper/normalizer.py
@src/scraper/flag_crosswalk.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@tests/pipeline/test_kaggle_converter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply D-01 — remove `(国際)→race_flag_graded_stakes` mapping (column_mapping.py:68); introduce Kaggle-side grade detection via derive_race_flags (HIGH #1 fix)</name>
  <files>src/pipeline/column_mapping.py, src/pipeline/kaggle_converter.py</files>
  <read_first>
    - src/pipeline/column_mapping.py (KAGGLE_COLUMN_MAP line 68 — the exact entry to delete; FLAG_COLUMNS list around line 197 — MUST remain untouched)
    - src/scraper/flag_crosswalk.py (lines 110-130 `_GRADE_REGEX` + lines 135-215 `derive_race_flags(race_condition, race_name)` — the EXISTING grade detector. It searches `haystack = f"{race_condition} {race_name}"` for GI/GII/GIII and sets `race_flag_graded_stakes=True` AND `race_flag_stakes=True`. Also sets `race_flag_listed=True` for `(L)`/`(リステッド)` and `race_flag_stakes=True` for `重賞`. This function is ALREADY the single source of graded-stakes truth; Phase 6 just needs to CALL it on the Kaggle side.)
    - src/scraper/flag_crosswalk.py (lines 218-223 `__all__` — confirms `derive_race_flags` is exported, importable from `src.scraper.flag_crosswalk`)
    - src/pipeline/kaggle_converter.py (lines 207-278 `split_race_entry_result` — where the race table is built; lines 244-248 where `_select_and_rename` + `drop_duplicates` + `convert_flags_to_bool` produce the race_df; the `grade` column exists on race_df because `リステッド・重賞競走` maps to `("race", "grade")` at column_mapping.py:78, and `race_name` exists because `レース名` maps at column_mapping.py:76)
    - src/pipeline/kaggle_converter.py (lines 281-300 `convert_flags_to_bool` — currently ONLY converts text presence to bool; does NOT do grade detection. This is the function to extend, OR add a separate `_apply_grade_detection` helper called after `convert_flags_to_bool`)
  </read_first>
  <behavior>
    - Test 1: `grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py` returns 0 (line 68 removed)
    - Test 2: `grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py` returns >= 1 (FLAG_COLUMNS entry at ~197 preserved)
    - Test 3: `grep -c 'derive_race_flags' src/pipeline/kaggle_converter.py` returns >= 2 (import + call)
    - Test 4 (NEW hermetic test in test_kaggle_converter.py — `test_kaggle_graded_derivation_matches_regex`): given a synthetic raw CSV with a row whose `重賞回次` (grade_revision) and/or `リステッド・重賞競走` (grade) field contains `GI`/`GII`/`GIII`/`ＧＩ`, the regenerated race table has `race_flag_graded_stakes=True` for that row. Given a row whose grade field contains only `(国際)` (no GI token), the regenerated race table has `race_flag_graded_stakes` in {None, False} (NOT True). This proves the grade detector works AND the `(国際)` removal is safe.
    - Test 5: existing `tests/pipeline/test_column_mapping.py` still passes (the mapping is removed but FLAG_COLUMNS still lists the column — no orphaned CSV header reference)
  </behavior>
  <action>
    Step A (per D-01, HIGH #1): in src/pipeline/column_mapping.py, delete ONLY the line containing `"レース記号/(国際)": ("race", "race_flag_graded_stakes"),` from `KAGGLE_COLUMN_MAP` (around line 68). Do NOT remove the corresponding entry from `FLAG_COLUMNS` (around line 197) — that list models the JRA CSV header set; removing it would orphan the column during conversion. Add a one-line comment citing Phase 4 P07 UAT-Test-3 + `src/scraper/flag_crosswalk.py:20-46`.

    Step B (HIGH #1 — Kaggle-side grade detector): in src/pipeline/kaggle_converter.py, add `from src.scraper.flag_crosswalk import derive_race_flags` to the imports (next to the existing `src.schemas.audit` / `src.schemas.entry` / `src.schemas.race` imports at lines 24-31). Then add a module-level helper `_apply_grade_detection(race_df: pd.DataFrame) -> pd.DataFrame` that, for every row, calls `derive_race_flags(race_condition=str(row.get("race_condition", "") or ""), race_name=str(row.get("race_name", "") or ""))` — wait, that double-counts. INSPECT FIRST: the Kaggle CSV exposes `リステッド・重賞競走` (mapped to `grade`, e.g. "GⅠ"/"GⅡ"/"GⅢ"/"重賞"/"(L)") and `重賞回次` (mapped to `grade_revision`, e.g. "第64回") and `レース名` (mapped to `race_name`, which sometimes has a "(GI)" suffix like "宝塚記念(GI)"). The cleanest Kaggle-side haystack is `f"{grade} {race_name}"` because `grade` is the AUTHORITATIVE graded-classification field in the CSV (it is `null` for non-graded races). So: `derive_race_flags(race_condition=str(row["grade"]) if pd.notna(row["grade"]) else "", race_name=str(row["race_name"]) if pd.notna(row["race_name"]) else "")`. Apply the returned dict's `race_flag_graded_stakes`, `race_flag_stakes`, `race_flag_listed` to the corresponding race_df columns (overwriting the text-derived boolean with the grade-regex-derived boolean ONLY for these three columns; the other 17 flags keep their text-derived values from `convert_flags_to_bool`). Call `_apply_grade_detection(race_df)` immediately AFTER `convert_flags_to_bool(race_df)` in `split_race_entry_result` (around line 248). NEVER use `errors='ignore'` anywhere.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_column_mapping.py tests/pipeline/test_kaggle_converter.py -q -k 'grade or column_mapping' && test "$(grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py)" = "0" && test "$(grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py)" -ge "1" && test "$(grep -c 'derive_race_flags' src/pipeline/kaggle_converter.py)" -ge "2"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py` returns no match
    - `grep -n 'レース記号/(国際)' src/pipeline/column_mapping.py` shows the FLAG_COLUMNS entry still present (~line 197)
    - `grep -c 'derive_race_flags' src/pipeline/kaggle_converter.py` returns >= 2 (import + at least one call)
    - The new test `test_kaggle_graded_derivation_matches_regex` passes: a synthetic CSV row with `grade="GⅠ"` produces `race_flag_graded_stakes=True` in the regenerated race table; a row with grade/race_name containing only `(国際)` produces `race_flag_graded_stakes` in {None, False}
    - `python -m pytest tests/pipeline/test_column_mapping.py tests/pipeline/test_kaggle_converter.py -q` is green
  </acceptance_criteria>
  <done>
    - `(国際)→race_flag_graded_stakes` mapping removed from KAGGLE_COLUMN_MAP
    - Kaggle-side grade detector wired via `derive_race_flags(grade, race_name)`
    - Hermetic test proves graded_stakes derives from GI/GII/GIII tokens, not `(国際)`
    - Existing column_mapping tests green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply D-02 — add `_recast_to_canonical` + `_atomic_write_parquet` to kaggle_converter; restrict regen to race/entry/result (odds/payoff snapshot/restore — HIGH #2 fix); regenerate Parquet</name>
  <files>src/pipeline/kaggle_converter.py, tests/pipeline/test_kaggle_converter.py, data/standard/race.parquet, data/standard/entry.parquet, data/standard/result.parquet</files>
  <read_first>
    - src/pipeline/kaggle_converter.py (full file — imports lines 19-31; `convert()` at lines 47-131 builds a 5-table `tables` dict at lines 107-113 and writes ALL FIVE at lines 115-119 with a non-atomic `to_parquet`; this is the D-05 violation point)
    - src/scraper/normalizer.py (lines 95-192 SCHEMA_DTYPE_MAP — the dtype authority; note `race_date: "string"` at line 99, NOT datetime; lines 643-653 `_atomic_write_parquet(df, path)` — REUSE this function for the hardened write)
    - src/schemas/race.py (line 31 — `race_date: str` confirms string is the schema contract)
    - tests/pipeline/test_kaggle_converter.py (existing test style — uses `convert(raw_dir=..., standard_dir=tmp_standard_dir)` invocation; mirror this)
    - tests/pipeline/conftest.py (existing `tmp_standard_dir` fixture around line 291; sample_*_df fixtures)
  </read_first>
  <behavior>
    - Test 1 (test_kaggle_parquet_post_d02_has_typed_flags): regenerated `data/standard/race.parquet` read via `pyarrow.parquet.read_schema` has zero columns whose `str(type) == 'null'`; every `race_flag_*` column has `str(type) == 'bool'`
    - Test 2 (test_kaggle_race_distance_is_int64): `distance` column has `str(type) == 'int64'` (Int64 → Arrow int64)
    - Test 3 (test_kaggle_race_date_is_string): `race_date` column has `str(type) == 'string'` (HIGH #5 dtype contract resolution — string, NOT datetime)
    - Test 4 (test_recast_raises_on_bad_data): a synthetic DataFrame with `distance='abc'` causes `_recast_to_canonical` to raise `TypeError` (no `errors='ignore'` path)
    - Test 5 (test_convert_preserves_odds_payoff — HIGH #2 fix): a sentinel `odds_trifecta.parquet` and `payoff.parquet` placed in `standard_dir` BEFORE the regen run have IDENTICAL row counts AND SHA-256 checksums AFTER the regen run. This proves the regen path does not write odds/payoff.
    - Test 6: existing `tests/pipeline/test_kaggle_converter.py` suite remains green (dtype-string assertions updated from object/float64 to Int64/Float64/boolean/string as needed)
  </behavior>
  <action>
    Add to src/pipeline/kaggle_converter.py imports: `from src.schemas.result import ResultSchema`, `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet`.

    Add module-level `_recast_to_canonical(df: pd.DataFrame, schema: type) -> pd.DataFrame` mirroring `src/scraper/normalizer.py:_build_typed_dataframe` (lines ~242-255): iterate `SCHEMA_DTYPE_MAP[schema].items()`, skip missing columns, `df[col] = df[col].astype(target)` inside try/except that re-raises as `TypeError` with a column-name message. NEVER use `errors='ignore'`. Call `_recast_to_canonical` for race/entry/result at the END of `split_race_entry_result` (immediately before the `return` at line 278): `_recast_to_canonical(race_df, RaceSchema)`, `_recast_to_canonical(entry_df, EntrySchema)`, `_recast_to_canonical(result_df, ResultSchema)`.

    HIGH #2 FIX (odds/payoff protection — D-05): Restrict the regen path so it does NOT touch `odds_trifecta.parquet` / `payoff.parquet`. The cleanest approach: add a module-level helper `_snapshot_and_restore_odds_payoff(standard_dir: Path)` context manager (or a snapshot/restore pair of functions) that, BEFORE the regen write loop, captures the SHA-256 of each existing odds/payoff file (if present) and the row count; AFTER the write loop, restores them (the file content is byte-identical — if the regen wrote a new version, restore the snapshot; if the regen left them alone, the assertion passes trivially). Then modify `convert()` to use it: wrap the write loop (lines 115-119) so odds/payoff are snapshotted before and restored after. Alternative: change the `tables` dict at lines 107-113 to include ONLY `race`/`entry`/`result` (skip `odds_trifecta`/`payoff`) when a new flag `regenerate_core_tables_only=True` is passed; the existing `extract_odds_tables` step at lines 97-101 still runs so the audit can see the data, but the write loop skips those tables. PICK ONE: snapshot/restore (more defensive — works regardless of caller) OR the restricted-write flag (more explicit). The snapshot/restore approach is preferred because it survives future edits to the `tables` dict.

    ALSO harden the write site (preserve Plan-01 P03 fix): replace the non-atomic `table_df.to_parquet(output_path, ...)` call at lines 115-119 with `_atomic_write_parquet(table_df, output_path)` (same engine/compression/index params — see normalizer.py:643-653).

    Then regenerate the Parquet files by invoking the public `convert()` entry point via `python -c "from pathlib import Path; from src.pipeline.kaggle_converter import convert; convert()"` (defaults: `raw_dir=Path('data/raw/kaggle')`, `standard_dir=Path('data/standard')`). Do NOT invoke via the poetry toolchain — poetry is not installed (this repo uses setuptools; use `python -c`/`python -m` only).

    Add the new tests (items 1-5 above) to tests/pipeline/test_kaggle_converter.py; follow the existing `convert(raw_dir=..., standard_dir=tmp_standard_dir)` invocation pattern.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_kaggle_converter.py tests/pipeline/test_column_mapping.py -q && test "$(grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py)" -ge "1" && ! grep -q 'errors=.ignore.' src/pipeline/kaggle_converter.py && python -c "import pyarrow.parquet as pq; s=pq.read_schema('data/standard/race.parquet'); nulls=[f.name for f in s if str(f.type)=='null']; assert not nulls, f'Arrow null cols remain: {nulls}'; rd=[f for f in s if f.name=='race_date'][0]; assert str(rd.type)=='string', f'race_date must be string, got {rd.type}'; print('OK: zero Arrow-null columns; race_date=string')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_recast_to_canonical' src/pipeline/kaggle_converter.py` returns >= 4 (1 definition + 3 call sites at end of split_race_entry_result)
    - `grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py` returns >= 1
    - `grep 'errors=.ignore.' src/pipeline/kaggle_converter.py` returns NO matches
    - `grep 'from src.scraper.normalizer import.*SCHEMA_DTYPE_MAP' src/pipeline/kaggle_converter.py` returns a match
    - `grep 'from src.scraper.normalizer import.*_atomic_write_parquet' src/pipeline/kaggle_converter.py` returns a match
    - `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero columns whose `str(type) == 'null'`; every `race_flag_*` is Arrow bool
    - `race_date` column has `str(type) == 'string'` (HIGH #5 resolution: string, NOT datetime)
    - `distance` has `str(type) == 'int64'`; `win_odds` has `str(type) == 'double'`
    - `test_convert_preserves_odds_payoff` passes: odds/payoff SHA-256 + row count identical pre/post regen
    - `python -m pytest tests/pipeline/test_kaggle_converter.py -q` is green
  </acceptance_criteria>
  <done>
    - `_recast_to_canonical` defined and called 3× at end of `split_race_entry_result`
    - Write loop hardened to `_atomic_write_parquet`; odds/payoff snapshot-protected (HIGH #2)
    - Regenerated race.parquet has 0 Arrow-null columns; `race_flag_*` all bool; `race_date` string
    - `test_convert_preserves_odds_payoff` proves odds/payoff untouched
    - All test_kaggle_converter.py tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-verify regenerated Kaggle Parquet — run ALL 8 D-05 checks (HIGH #3 fix) + assert graded derivation consistency</name>
  <files></files>
  <read_first>
    - src/pipeline/validators.py (lines 764-892 `run_all_validations(raw_dir, parquet_dir, source_counts=None, source_stats=None)` — the entry point; the 8 D-05 checks; the GATING at lines 791/801/807/825/847/855 where `source_counts is None` / `source_stats is None` SKIPS validate_row_counts / validate_null_rates / validate_distributions. The function returns a dict with `overall_pass: bool`.)
    - src/pipeline/validators.py (lines 72-115 `validate_row_counts(source_counts, parquet_dir)` — takes a `{table: expected_count}` dict; lines 227-282 `validate_null_rates(source_stats, parquet_dir)` — takes a `{table: {"null_rates": {col: rate}}}` dict; lines 283-350 `validate_distributions(source_stats, parquet_dir)` — takes a `{table: {"distributions": {col: {min,max,mean}}}}` dict)
    - src/pipeline/kaggle_converter.py (the `read_race_result_csv` and `read_odds_csv` functions — these are how the source CSVs are read; you'll mirror them to compute `source_counts`/`source_stats` from the SAME filtered CSV that `convert()` uses, so the comparison is apples-to-apples)
    - tests/pipeline/test_validators.py (existing validator tests — mirror style for the new graded-derivation assertion)
    - .planning/phases/02-kaggle-data-pipeline/02-CONTEXT.md (D-05 8-point definition — confirms which 8 checks run when source_counts/source_stats are supplied)
  </read_first>
  <action>
    HIGH #3 FIX: compute `source_counts` and `source_stats` from the source Kaggle CSV BEFORE regeneration and PASS them to `run_all_validations` so all 8 checks actually run.

    `source_counts` shape (per validators.py:72-115): `{table_name: expected_row_count}`. For the 5 tables (race/entry/result/odds_trifecta/payoff), compute the count from the FILTERED source CSV (post-2015-01-01 + obstacle-excluded — the same filter `convert()` applies at lines 82-90). Mirror `convert()`'s filter logic: read `data/raw/kaggle/19860105-20210731_race_result.csv` via `read_race_result_csv`, apply `df["レース日付"] >= "2015-01-01"` and `df["障害区分"] != "障害"`, then count: `race` = `df["レースID"].nunique()` (after dedup), `entry` = `result` = `len(df)` (1 row per horse per race; the existing converter produces this 1:1 cardinality per Phase 2 D-05). For odds tables, read `19860105-20210731_odds.csv` and count rows filtered to the valid_race_ids set.

    `source_stats` shape (per validators.py:227-350): `{table: {"null_rates": {col: rate}, "distributions": {col: {min, max, mean}}}}`. For the numeric columns (e.g. race.distance, entry.popularity/win_odds/horse_weight, result.finish_position/last_3f), compute null_rate and {min,max,mean} from the source CSV. The simplest path: after computing the filtered `df` above, build the same race_df/entry_df/result_df via `split_race_entry_result(df)` (the OLD pre-D-02 version is fine for source stats — it's the SOURCE contract, not the output) and compute null_rates and distributions from those DataFrames.

    Invoke programmatically: `python -c "from pathlib import Path; from src.pipeline.kaggle_converter import read_race_result_csv, read_odds_csv, split_race_entry_result, extract_odds_tables; from src.pipeline.validators import run_all_validations; import pandas as pd; raw=Path('data/raw/kaggle'); df=read_race_result_csv(raw/'19860105-20210731_race_result.csv'); df['レース日付']=pd.to_datetime(df['レース日付'], errors='coerce'); df=df[df['レース日付'].notna()].copy(); df=df[df['レース日付']>='2015-01-01']; df=df[df['障害区分']!='障害']; race_df,entry_df,result_df=split_race_entry_result(df); source_counts={'race':int(race_df['race_id'].nunique()),'entry':len(entry_df),'result':len(result_df)}; source_stats={'race':{'null_rates':{c:float(race_df[c].isna().mean()) for c in ['distance','meeting_num']},'distributions':{'distance':{'min':float(race_df['distance'].min()),'max':float(race_df['distance'].max()),'mean':float(race_df['distance'].mean())}}}}; r=run_all_validations(raw, Path('data/standard'), source_counts=source_counts, source_stats=source_stats); print('overall_pass=',r['overall_pass']); assert r['overall_pass'] is True, {k:v for k,v in r.items() if v is False and k!='overall_pass'}"`. Adjust the source_stats dict to include enough columns to exercise validate_null_rates and validate_distributions meaningfully (at minimum: race.distance, entry.popularity, entry.win_odds, result.finish_position). If the dict structure is hard to build inline, write a small throwaway helper `_compute_source_stats(raw_dir)` in a scratch file and call it — do NOT modify validators.py.

    Then HIGH #1 follow-up (graded derivation consistency): assert the regenerated `race_flag_graded_stakes=True` count EQUALS the count of rows where `derive_race_flags(grade, race_name)` returns `graded_stakes=True` — i.e., recompute the expected count from the SAME grade derivation the converter now uses, and assert equality. This replaces the brittle fixed `780 <= count <= 880` band with a deterministic check: `python -c "import pandas as pd; from src.scraper.flag_crosswalk import derive_race_flags; df=pd.read_parquet('data/standard/race.parquet'); expected=sum(1 for _,r in df.iterrows() if derive_race_flags(str(r.get('grade') or ''), str(r.get('race_name') or '')).get('race_flag_graded_stakes') is True); actual=int(df['race_flag_graded_stakes'].fillna(False).sum()); print(f'expected={expected} actual={actual}'); assert expected==actual, f'grade derivation mismatch: expected {expected}, got {actual}'"`. Document the actual count in the plan summary; if expected != actual, STOP and investigate (likely the `_apply_grade_detection` helper is using the wrong haystack or the convert_flags_to_bool overwrote a True with False).
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
from pathlib import Path
from src.pipeline.kaggle_converter import read_race_result_csv, split_race_entry_result
from src.pipeline.validators import run_all_validations
import pandas as pd
raw=Path('data/raw/kaggle')
df=read_race_result_csv(raw/'19860105-20210731_race_result.csv')
df['レース日付']=pd.to_datetime(df['レース日付'], errors='coerce')
df=df[df['レース日付'].notna()].copy()
df=df[df['レース日付']>='2015-01-01']
df=df[df['障害区分']!='障害']
race_df,entry_df,result_df=split_race_entry_result(df)
source_counts={'race':int(race_df['race_id'].nunique()),'entry':len(entry_df),'result':len(result_df)}
source_stats={'race':{'null_rates':{c:float(race_df[c].isna().mean()) for c in ['distance','meeting_num']},'distributions':{'distance':{'min':float(race_df['distance'].min()),'max':float(race_df['distance'].max()),'mean':float(race_df['distance'].mean())}}},'entry':{'null_rates':{c:float(entry_df[c].isna().mean()) for c in ['popularity','win_odds']},'distributions':{c:{'min':float(entry_df[c].min()),'max':float(entry_df[c].max()),'mean':float(entry_df[c].mean())} for c in ['popularity','win_odds']}}}
r=run_all_validations(raw, Path('data/standard'), source_counts=source_counts, source_stats=source_stats)
print('overall_pass=',r['overall_pass'])
assert r['overall_pass'] is True, {k:v for k,v in r.items() if v is False and k!='overall_pass'}
print('OK: all 8 D-05 checks ran and passed')
" && python -c "
import pandas as pd
from src.scraper.flag_crosswalk import derive_race_flags
df=pd.read_parquet('data/standard/race.parquet')
expected=sum(1 for _,r in df.iterrows() if derive_race_flags(str(r.get('grade') or ''), str(r.get('race_name') or '')).get('race_flag_graded_stakes') is True)
actual=int(df['race_flag_graded_stakes'].fillna(False).sum())
print(f'graded: expected={expected} actual={actual}')
assert expected==actual, f'grade derivation mismatch: expected {expected}, got {actual}'
"</automated>
  </verify>
  <acceptance_criteria>
    - `run_all_validations(raw_dir, parquet_dir, source_counts=<computed>, source_stats=<computed>)` returns `overall_pass=True` (HIGH #3 — all 8 checks RAN, none skipped)
    - The graded_stakes count is DETERMINISTICALLY equal to the count of rows where `derive_race_flags(grade, race_name)` returns `graded_stakes=True` (HIGH #1 follow-up — no brittle fixed band)
    - All existing `tests/pipeline/test_validators.py` tests still pass
  </acceptance_criteria>
  <done>
    - All 8 Phase 2 D-05 checks actually execute (source_counts + source_stats supplied)
    - `overall_pass=True`
    - Graded count matches the grade-regex derivation deterministically
    - Actual graded count recorded in plan SUMMARY for traceability
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle CSV → standard Parquet | Raw external data crosses into the typed contract. Validated by recast helper + grade detector + audit. |
| Regen write → existing odds_trifecta/payoff.parquet | D-05 requires these Phase 5 seeds to be untouched. Snapshot/restore + SHA-256 equality is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `column_mapping.py` KAGGLE_COLUMN_MAP + Kaggle grade detector | mitigate | HIGH #1 fix: `(国際)` mapping removed; `derive_race_flags(grade, race_name)` becomes the SOLE graded source on both Kaggle and scraped sides. Verified by `test_kaggle_graded_derivation_matches_regex` and the deterministic count assertion in Task 3. |
| T-06-02 | Tampering | `kaggle_converter._recast_to_canonical` | mitigate | Recast raises TypeError on uncoercible values; NEVER uses `errors='ignore'`. Verified by `test_recast_raises_on_bad_data`. |
| T-06-03 | Tampering | Destructive overwrite of `data/standard/{race,entry,result}.parquet` | mitigate | Write site now calls `_atomic_write_parquet` (temp + os.replace, POSIX-atomic). |
| T-06-04 | Tampering / Information Disclosure | D-05 violation: regen overwrites odds_trifecta/payoff (HIGH #2) | mitigate | Snapshot/restore odds/payoff around the write loop; SHA-256 + row-count equality asserted by `test_convert_preserves_odds_payoff`. If the snapshot is ever clobbered, the test fails. |
| T-06-05 | Tampering | 8-point verification silently skips 3-4 checks (HIGH #3) | mitigate | Task 3 computes `source_counts` + `source_stats` from the source CSV BEFORE regen and passes them to `run_all_validations`. All 8 checks run. |
| T-06-SC | Tampering | No package installs (pandas/pyarrow/numpy/pydantic all pre-installed; Phase 4 audit verified) | accept | No new dependencies; no slopcheck needed. |
</threat_model>

<verification>
- `python -m pytest tests/pipeline/test_kaggle_converter.py tests/pipeline/test_column_mapping.py tests/pipeline/test_validators.py -q` green
- `python -m pytest tests/ -q` full suite green (Phase 1/3/4 tests unaffected by Kaggle-side dtype changes)
- `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns; `race_date` is `string`; `distance` is `int64`
- `run_all_validations(raw, parquet, source_counts=..., source_stats=...)` returns `overall_pass=True` with all 8 checks running
- Graded count is deterministically equal to the grade-regex derivation
- odds_trifecta.parquet + payoff.parquet SHA-256 unchanged across the regen
</verification>

<success_criteria>
- HIGH #1 resolved: Kaggle-side grade detector wired; graded_stakes derives from `derive_race_flags(grade, race_name)`, not `(国際)`
- HIGH #2 resolved: regen path restricted to race/entry/result; odds/payoff SHA-256-protected
- HIGH #3 resolved: all 8 D-05 checks actually run (source_counts + source_stats supplied)
- HIGH #4 resolved: every command uses `python -m` / `python -c` (poetry is NOT installed)
- MEDIUM #5 resolved: race_date stays `string` per `SCHEMA_DTYPE_MAP`; CONTEXT D-02 `datetime` wording superseded by code contract (documented in SUMMARY)
- D-01 + D-02 fully applied; Kaggle corpus schema-aligned with scraped corpus
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-01-SUMMARY.md` when done. Record: actual graded_stakes count, odds/payoff SHA-256 values pre/post regen, race_date dtype resolution note, source_counts/source_stats structure used for the 8-point verification.
</output>

## Artifacts this phase produces

- `src/pipeline/kaggle_converter.py`
  - NEW import: `from src.scraper.flag_crosswalk import derive_race_flags`
  - NEW import: `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet`
  - NEW import: `from src.schemas.result import ResultSchema`
  - NEW helper: `_apply_grade_detection(race_df) -> pd.DataFrame` — calls `derive_race_flags(grade, race_name)` and writes `race_flag_graded_stakes`/`race_flag_stakes`/`race_flag_listed` (HIGH #1)
  - NEW helper: `_recast_to_canonical(df, schema) -> pd.DataFrame` — SCHEMA_DTYPE_MAP recast; raises TypeError
  - NEW helper: `_snapshot_and_restore_odds_payoff(standard_dir)` (context manager or pair) — HIGH #2 D-05 protection
  - 3 recast call sites at end of `split_race_entry_result`
  - Write loop replaced: `_atomic_write_parquet(table_df, output_path)`
- `src/pipeline/column_mapping.py`
  - REMOVED: line 68 `"レース記号/(国際)": ("race", "race_flag_graded_stakes")` from `KAGGLE_COLUMN_MAP`
  - PRESERVED: FLAG_COLUMNS entry at ~line 197
- `tests/pipeline/test_kaggle_converter.py`
  - NEW: `test_kaggle_graded_derivation_matches_regex` (HIGH #1)
  - NEW: `test_kaggle_parquet_post_d02_has_typed_flags` (HIGH #4 dtype)
  - NEW: `test_kaggle_race_date_is_string` (MEDIUM #5)
  - NEW: `test_kaggle_race_distance_is_int64`
  - NEW: `test_recast_raises_on_bad_data`
  - NEW: `test_convert_preserves_odds_payoff` (HIGH #2)
- `data/standard/race.parquet` / `entry.parquet` / `result.parquet` — regenerated, post-D-01 + post-D-02, atomically written
- `data/standard/odds_trifecta.parquet` / `payoff.parquet` — UNCHANGED (SHA-256-equal to pre-regen)

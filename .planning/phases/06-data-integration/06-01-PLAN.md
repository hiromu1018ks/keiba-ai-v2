---
phase: 06-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/column_mapping.py
  - src/pipeline/kaggle_converter.py
  - src/scraper/flag_crosswalk.py
  - tests/pipeline/test_kaggle_converter.py
  - data/standard/kaggle/race.parquet
  - data/standard/kaggle/entry.parquet
  - data/standard/kaggle/result.parquet
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #1 (D-01 grade detection — FIXED in cycle 3): _apply_grade_detection is called AFTER split_race_entry_result() returns and AFTER the _UNMAPPED_RACE_FLAGS loop that creates all 20 race_flag_* columns (including race_flag_stakes/listed at kaggle_converter.py:36-44, set as pd.NA at :251-252). The helper therefore never KeyErrors on missing columns and its OR-merge result is never clobbered by a later pd.NA write. The helper calls derive_race_flags(race_condition=grade_or_space, race_name) where grade_or_space is the `grade` column value when non-null and ' ' (single space — NOT '') when grade is null, because derive_race_flags early-returns when race_condition is falsy (flag_crosswalk.py:184-185); passing ' ' ensures race_name-only GI tokens still match _GRADE_REGEX."
    - "HIGH #1 (grade test value — FIXED in cycle 3): the hermetic test uses grade='G1' (half-width G + half-width 1) which matches _GRADE_REGEX's `G3|G2|G1` alternation (flag_crosswalk.py:127). The cycle-2 value `GⅠ` (half-width G + FULL-WIDTH Ⅰ U+FF21) matched NEITHER the half-width `GI` nor the full-width `ＧＩ` alternatives and was silently undetected."
    - "HIGH #1 (WARNING-2 OR-merge — KEPT from cycle 2): _apply_grade_detection ORs new grade-derived True values onto existing flags via `race_df[col] = race_df[col].fillna(False) | new_series.fillna(False)`. The .fillna(False) on BOTH sides is load-bearing: pandas treats `True | pd.NA` as `<NA>` not `True`, so without the fillna an existing True would be downgraded when the new value is None. The helper is applied to race_flag_graded_stakes, race_flag_stakes, race_flag_listed ONLY; the other 17 flags keep their convert_flags_to_bool-derived values."
    - "D-01 (mapping removal — KEPT): `レース記号/(国際)` no longer maps to `race_flag_graded_stakes` in KAGGLE_COLUMN_MAP (line 68 deleted); the FLAG_COLUMNS entry at ~197 is preserved."
    - "D-02 (HIGH #4 dtype regen — KEPT): regenerated `data/standard/kaggle/{race,entry,result}.parquet` have every `race_flag_*` column as Arrow `bool` (not Arrow `null`), `distance`/`meeting_num`/`race_number` as Arrow `int64`, and `win_odds`/`horse_weight` as Arrow `double`."
    - "HIGH #2 (D-05 — FIXED in cycle 3): convert(core_tables_subdir='kaggle') SKIPS writing odds_trifecta and payoff ENTIRELY when core_tables_subdir is set. The write loop in convert() (kaggle_converter.py:107-119) is split so that odds/payoff are conditionally excluded from the write when core_tables_subdir is truthy — they are never written to ANY location in that invocation. This protects the Phase 5 seed at data/standard/{odds_trifecta,payoff}.parquet from any overwrite. The test test_convert_preserves_odds_payoff now proves NON-OVERWRITE (odds/payoff bytes UNCHANGED after convert(core_tables_subdir='kaggle')), not just value-equality."
    - "HIGH #3 (8-point verification — FIXED in cycle 3 via PHASE SPLIT): the 8-point run_all_validations is REMOVED from 06-01-T3 entirely. 06-01-T3 now validates the regenerated 3-table kaggle/ subdir SPECIFICALLY (schema column-set, Arrow dtype check, graded count via derive_race_flags, 3-table FK orphan check on race↔entry↔result). It does NOT call run_all_validations against kaggle/ because that function iterates 5 tables and appends 'Missing odds_trifecta/payoff.parquet' when they are absent (validators.py:377-383), forcing overall_pass=False. The FULL 8-point run_all_validations(raw_dir, data/standard/, source_counts, source_stats) now runs in 06-03-T2 against the unified ROOT (where all 5 tables coexist after integration) — this is the natural validation point and the 5-table contract is honored."
    - "race_date dtype contract (MEDIUM #5 — KEPT): race_date stays Arrow `string` per SCHEMA_DTYPE_MAP[RaceSchema] (string is the code-authoritative contract; CONTEXT.md D-02 `datetime` note is superseded by the existing code contract)."
    - "Grade derivation is deterministic: the regenerated `graded_stakes=True` count equals the count of rows where derive_race_flags(grade_or_space, race_name) returns `graded_stakes=True` (recomputed from the same inputs the detector uses)."
requirements_resolved:
  - "HIGH #1 (cycle 2 NOT RESOLVED → cycle 3 RESOLVED): _apply_grade_detection call ordering moved AFTER _UNMAPPED_RACE_FLAGS loop; test uses grade='G1' matching _GRADE_REGEX; race_condition=' ' when grade null (early-return guard bypass)"
  - "HIGH #2 (cycle 2 NOT RESOLVED → cycle 3 RESOLVED): convert(core_tables_subdir='kaggle') SKIPS odds/payoff writes entirely; non-overwrite test replaces value-equality test"
  - "HIGH #3 (cycle 2 PARTIALLY RESOLVED → cycle 3 RESOLVED): 8-point run_all_validations moved to 06-03-T2 against unified root; 06-01-T3 does 3-table-specific validation (no run_all_validations against kaggle/)"
  - "HIGH #4 (cycle 2 RESOLVED — KEPT): all commands python -m / python -c"
artifacts:
  - path: "src/pipeline/column_mapping.py"
    provides: "KAGGLE_COLUMN_MAP with (国際) line removed"
    not_contains: '"レース記号/(国際)": ("race", "race_flag_graded_stakes")'
  - path: "src/pipeline/kaggle_converter.py"
    provides: "import of derive_race_flags; _apply_grade_detection called AFTER _UNMAPPED_RACE_FLAGS loop; _recast_to_canonical helper; convert() with core_tables_subdir param that writes race/entry/result to subdir AND SKIPS odds/payoff entirely when subdir is set"
    contains: "derive_race_flags"
  - path: "src/scraper/flag_crosswalk.py"
    provides: "derive_race_flags already supports race_name= as a second haystack; no edit needed"
  - path: "tests/pipeline/test_kaggle_converter.py"
    provides: "test_kaggle_graded_derivation_matches_regex (grade='G1'), test_grade_detection_preserves_existing_true, test_kaggle_parquet_post_d02_has_typed_flags, test_recast_raises_on_bad_data, test_convert_preserves_odds_payoff (NON-OVERWRITE), test_convert_writes_core_tables_to_subdir, test_convert_skips_odds_payoff_when_subdir_set"
    contains: "test_kaggle_graded_derivation_matches_regex"
  - path: "data/standard/kaggle/race.parquet"
    provides: "Regenerated Kaggle race table with grade-regex-derived graded_stakes, nullable dtypes, atomically written; race_date stays string"
  key_links:
    - from: "src/pipeline/kaggle_converter.split_race_entry_result (AFTER _UNMAPPED_RACE_FLAGS loop)"
      to: "src/scraper/flag_crosswalk.derive_race_flags"
      via: "from src.scraper.flag_crosswalk import derive_race_flags"
      pattern: "from src\\.scraper\\.flag_crosswalk import.*derive_race_flags"
    - from: "src/pipeline/kaggle_converter._recast_to_canonical"
      to: "src/scraper/normalizer.SCHEMA_DTYPE_MAP"
      via: "from src.scraper.normalizer import SCHEMA_DTYPE_MAP"
      pattern: "from src\\.scraper\\.normalizer import.*SCHEMA_DTYPE_MAP"
    - from: "src/pipeline/kaggle_converter.convert(core_tables_subdir='kaggle')"
      to: "data/standard/kaggle/{race,entry,result}.parquet"
      via: "core_tables_subdir param redirects race/entry/result write site; odds/payoff are SKIPPED (not written anywhere) when subdir is set"
      pattern: "core_tables_subdir"
---

<objective>
Apply CONTEXT.md decisions D-01 (remove `(国際)→race_flag_graded_stakes` Kaggle mapping) and D-02 (regenerate Kaggle Parquet with nullable dtypes matching `SCHEMA_DTYPE_MAP`), then verify the regenerated 3-table kaggle/ subdir passes a 3-table-specific validation. The regenerated race/entry/result Parquet are written to `data/standard/kaggle/` (a NEW subdir) so Plan 06-02's `integrate_standard_layer` can read them as a STABLE separate input path (HIGH #5 idempotency fix), and odds/payoff are NOT WRITTEN AT ALL by the subdir invocation (Phase 5 seed protected).

This plan resolves THREE cycle-2 HIGH defects (the remaining unresolved ones for 06-01) using the cycle-3 design:

- **HIGH #1 (D-01 grade detection — NOT RESOLVED in cycle 2):** cycle 2 placed `_apply_grade_detection` at line ~248 (immediately after `convert_flags_to_bool`), but `race_flag_stakes`/`race_flag_listed` are in `_UNMAPPED_RACE_FLAGS` (`kaggle_converter.py:39,42`) and added as `pd.NA` at lines 251-252, AFTER line 248. The helper therefore either (a) KeyErrors because `race_df['race_flag_stakes']` does not exist at line 248, or (b) has its OR-merge result clobbered to pd.NA by the subsequent unmapped-flags loop. Additionally the cycle-2 test used `grade="GⅠ"` (half-width G + FULL-WIDTH Ⅰ U+FF21) which matches NEITHER the half-width `GI` nor the full-width `ＧＩ` alternatives in `_GRADE_REGEX` (`flag_crosswalk.py:124-130`). **FIX (cycle 3):** move `_apply_grade_detection` to AFTER `split_race_entry_result()` returns (after line 278), or at minimum after the `_UNMAPPED_RACE_FLAGS` loop at lines 251-252; change the test value to `grade="G1"` (matches `G3|G2|G1`); pass `race_condition=' '` (single space) when `grade` is null so `derive_race_flags`'s early-return guard (`flag_crosswalk.py:184-185`) does not suppress race_name-only GI matches.

- **HIGH #2 (convert() overwrites odds/payoff — NOT RESOLVED in cycle 2):** cycle 2's `core_tables_subdir` only redirected race/entry/result; odds/payoff were still written to `standard_dir/` root on every invocation, overwriting the Phase 5 seed. The SHA-256 equality test proved only that regenerated odds/payoff matched the pre-existing ones in VALUE (re-derived from the same CSV), not that they were not rewritten. **FIX (cycle 3):** when `core_tables_subdir` is set, SKIP writing odds_trifecta/payoff ENTIRELY — they belong to Phase 5 and persist at `data/standard/{odds_trifecta,payoff}.parquet` from Phase 2/5. Split the write loop in convert() (kaggle_converter.py:107-119) so odds/payoff are excluded from the `tables` dict iteration when `core_tables_subdir` is truthy. The test now proves NON-OVERWRITE (odds/payoff bytes UNCHANGED after the subdir invocation).

- **HIGH #3 (8-point verification cannot pass against 3-table subdir — PARTIALLY RESOLVED in cycle 2):** `validate_referential_integrity` (`validators.py:377-383`) iterates `child_tables = ["entry", "result", "odds_trifecta", "payoff"]` and appends "Missing odds_trifecta.parquet"/"Missing payoff.parquet" if absent. It CANNOT pass `overall_pass=True` against the 3-table `kaggle/` subdir. Cycle 2 deferred the resolution to "read the source first." **FIX (cycle 3):** REMOVE `run_all_validations` from 06-01-T3 entirely. 06-01-T3 now validates the regenerated 3-table kaggle/ subdir SPECIFICALLY: schema column-set matches RaceSchema/EntrySchema/ResultSchema; zero Arrow-null dtype columns (post-D-02); graded_stakes count derives correctly via derive_race_flags; race↔entry↔result referential integrity via a 3-table orphan check (entry/result race_ids ⊆ race race_ids). The FULL 8-point `run_all_validations` is MOVED to 06-03-T2, where it runs against `data/standard/` ROOT AFTER integration — the natural place where all 5 tables (unified race/entry/result + Phase 5 odds/payoff) coexist.

MEDIUM #5 (race_date dtype — KEPT from cycle 2): `SCHEMA_DTYPE_MAP[RaceSchema]["race_date"] = "string"` (verified `src/scraper/normalizer.py:99`) and `src/schemas/race.py:31` declares `race_date: str`. This plan does NOT change race_date to datetime; it keeps string. CONTEXT D-02's `datetime` wording is superseded by the existing Phase 4 dtype discipline.

Purpose: Brings the Kaggle side up to the Phase 4 dtype discipline and the Phase 4 P07 `(国際)`-removal decision, so the two corpora become schema-indistinguishable before the Wave 2 merge. Writing the regenerated Kaggle corpus to `data/standard/kaggle/` establishes the STABLE separate input path that Plan 06-02's idempotent integration requires, and SKIPPING odds/payoff writes protects the Phase 5 seed.

Output: Three regenerated Parquet files under `data/standard/kaggle/` (race/entry/result only), `data/standard/{odds_trifecta,payoff}.parquet` UNCHANGED (never written by the subdir invocation), three edited source modules, updated/new tests in `test_kaggle_converter.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md

# Phase 2 converter (the D-02 modification target), the Phase 4 dtype authority,
# the Phase 4 flag crosswalk (the grade detector to reuse), and the validators.
@src/pipeline/kaggle_converter.py
@src/pipeline/column_mapping.py
@src/pipeline/validators.py
@src/scraper/normalizer.py
@src/scraper/flag_crosswalk.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@tests/pipeline/test_kaggle_converter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply D-01 — remove `(国際)→race_flag_graded_stakes` mapping (column_mapping.py:68); introduce Kaggle-side grade detection via derive_race_flags called AFTER _UNMAPPED_RACE_FLAGS loop (HIGH #1 cycle-3 fix); OR-merge preserves existing True (WARNING-2); race_condition=' ' when grade null (early-return guard bypass)</name>
  <files>src/pipeline/column_mapping.py, src/pipeline/kaggle_converter.py, tests/pipeline/test_kaggle_converter.py</files>
  <read_first>
    - src/pipeline/column_mapping.py (line 68 — the exact entry to delete; FLAG_COLUMNS list around line 197 — MUST remain untouched)
    - src/scraper/flag_crosswalk.py (lines 124-130 `_GRADE_REGEX` — matches `GIII|GII|GI|JGIII|JGII|JGI|G3|G2|G1|JG3|JG2|JG1|ＧＩＩＩ|ＧＩＩ|ＧＩ`; HALF-WIDTH `G1` matches, FULL-WIDTH-only `GⅠ` does NOT)
    - src/scraper/flag_crosswalk.py (lines 184-185 — `if not race_condition: return flags` early-return guard; passing `''` suppresses race_name-only GI matches; pass `' '` to bypass)
    - src/scraper/flag_crosswalk.py (lines 200-215 — grade detection: `haystack = f"{race_condition} {race_name}"`; searches BOTH condition and name for `_GRADE_REGEX`; sets graded_stakes AND stakes on match)
    - src/scraper/flag_crosswalk.py (lines 218-223 `__all__` — confirms `derive_race_flags` is exported)
    - src/pipeline/kaggle_converter.py (lines 36-44 `_UNMAPPED_RACE_FLAGS` — INCLUDES race_flag_stakes [line 42] AND race_flag_listed [line 39]; these are added as pd.NA at lines 251-252 AFTER convert_flags_to_bool at line 248)
    - src/pipeline/kaggle_converter.py (lines 244-278 — race_df build: _select_and_rename → drop_duplicates → convert_flags_to_bool (line 248) → _UNMAPPED_RACE_FLAGS loop (lines 251-252) → string casts → return. The grade detector MUST run AFTER the unmapped-flags loop so all 20 columns exist.)
    - src/pipeline/kaggle_converter.py (lines 92-94 — convert() calls split_race_entry_result(df) and gets race_df/entry_df/result_df back; the grade detection is best applied to race_df AFTER this call, OR inside split_race_entry_result at the very end before return)
  </read_first>
  <behavior>
    - Test 1: `grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py` returns 0 (line 68 removed)
    - Test 2: `grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py` returns >= 1 (FLAG_COLUMNS entry at ~197 preserved)
    - Test 3: `grep -c 'derive_race_flags' src/pipeline/kaggle_converter.py` returns >= 2 (import + call)
    - Test 4 (NEW hermetic test — `test_kaggle_graded_derivation_matches_regex` — HIGH #1 cycle-3 fix): given a synthetic raw CSV row whose `リステッド・重賞競走` (grade) column contains `"G1"` (half-width, matches `G3|G2|G1` at flag_crosswalk.py:127), the regenerated race table has `race_flag_graded_stakes=True` for that row. Given a row whose grade/race_name contains only `(国際)` (no GI/G1 token), the regenerated race table has `race_flag_graded_stakes` in {None, False} (NOT True). Given a row whose `grade` is null BUT `レース名` (race_name) contains `"(G1)"`, the regenerated race table STILL has `race_flag_graded_stakes=True` (proves the race_condition=' ' bypass works — derive_race_flags does not early-return).
    - Test 5 (WARNING-2 — `test_grade_detection_preserves_existing_true`): construct a synthetic race_df where `convert_flags_to_bool` already set `race_flag_stakes=True` for some row, AND `derive_race_flags` returns `None` for that column on the same row (no grade token). After `_apply_grade_detection`, the column STILL has `True` for that row — the helper did NOT downgrade True to None. Construct the inverse: `convert_flags_to_bool` left `race_flag_graded_stakes` as None, and `derive_race_flags` returns True; after the helper, the column has True.
    - Test 6 (`test_apply_grade_detection_runs_after_unmapped_flags` — cycle-3 ordering regression guard): a synthetic test asserting that AFTER `_apply_grade_detection` runs, `race_df['race_flag_stakes']` and `race_df['race_flag_listed']` both EXIST as columns (no KeyError) AND contain the grade-derived True values (not clobbered to pd.NA). This guards against a future refactor reordering the call back to line 248.
    - Test 7: existing `tests/pipeline/test_column_mapping.py` still passes (the mapping is removed but FLAG_COLUMNS still lists the column)
  </behavior>
  <action>
    Step A (per D-01, HIGH #1): in src/pipeline/column_mapping.py, delete ONLY the line containing `"レース記号/(国際)": ("race", "race_flag_graded_stakes"),` from `KAGGLE_COLUMN_MAP` (line 68). Do NOT remove the corresponding entry from `FLAG_COLUMNS` (~line 197). Add a one-line comment citing Phase 4 P07 UAT-Test-3 + `src/scraper/flag_crosswalk.py:20-46`.

    Step B (HIGH #1 cycle-3 fix — Kaggle-side grade detector with CORRECT ordering + OR-merge + early-return bypass): in src/pipeline/kaggle_converter.py, add `from src.scraper.flag_crosswalk import derive_race_flags` to the imports (next to the existing `src.schemas.*` imports at lines 24-31). Then add a module-level helper `_apply_grade_detection(race_df: pd.DataFrame) -> pd.DataFrame` with this contract:

    For each row, build the derive_race_flags inputs:
    - `grade_val = row["grade"]` if `"grade"` in race_df.columns else None
    - `race_name_val = row["race_name"]` if `"race_name"` in race_df.columns else None
    - `race_condition_arg = str(grade_val) if pd.notna(grade_val) and str(grade_val).strip() != "" else " "` — the SINGLE SPACE is load-bearing: `derive_race_flags` early-returns when `race_condition` is falsy (`flag_crosswalk.py:184-185`); passing `" "` ensures the haystack `f"{race_condition} {race_name}"` is built and `_GRADE_REGEX` is applied even when grade is null but race_name contains a GI token.
    - `race_name_arg = str(race_name_val) if pd.notna(race_name_val) else ""`

    Call `derive_race_flags(race_condition=race_condition_arg, race_name=race_name_arg)` per row and collect into a list of dicts. Build a per-column Series for `race_flag_graded_stakes`, `race_flag_stakes`, `race_flag_listed` (the 3 flags derive_race_flags can set to True for graded/listed races).

    Apply OR-merge (WARNING-2 fix) for each of those 3 columns:
    - `existing = race_df[col]` (nullable boolean Series — all 20 race_flag_* columns now EXIST because this helper runs AFTER the _UNMAPPED_RACE_FLAGS loop)
    - `new = pd.Series([flags.get(col) for flags in per_row_results], dtype='boolean')` (True / None per row)
    - `race_df[col] = existing.fillna(False) | new.fillna(False)` — the `.fillna(False)` on BOTH sides is load-bearing: pandas treats `True | pd.NA` as `<NA>`, NOT `True`; without fillna an existing True would be downgraded when the new value is None. After fillna, `True | False = True`, `False | True = True`, `False | False = False`. Cast the result back to nullable boolean: `.astype('boolean')`.

    The other 17 flags KEEP their text-derived values from `convert_flags_to_bool` (the helper does NOT touch them).

    CRITICAL ORDERING (HIGH #1 cycle-3 fix): call `_apply_grade_detection(race_df)` AFTER the `_UNMAPPED_RACE_FLAGS` loop (kaggle_converter.py:251-252) so all 20 race_flag_* columns already exist as pd.NA-filled nullable booleans. The recommended placement is at the END of `split_race_entry_result`, immediately before the `return race_df, entry_df, result_df` at line 278 — OR equivalently inside `convert()` at lines 92-94 after `race_df, entry_df, result_df = split_race_entry_result(df)`. Pick ONE placement and add a comment: "HIGH #1 cycle-3: MUST run after _UNMAPPED_RACE_FLAGS loop so race_flag_stakes/listed/graded_stakes exist as columns; calling earlier KeyErrors or has its OR-merge result clobbered to pd.NA."

    NEVER use `errors='ignore'` anywhere.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_column_mapping.py tests/pipeline/test_kaggle_converter.py -q -k 'grade or column_mapping or preserves_existing_true or apply_grade_detection' && test "$(grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py)" = "0" && test "$(grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py)" -ge "1" && test "$(grep -c 'derive_race_flags' src/pipeline/kaggle_converter.py)" -ge "2" && test "$(grep -c 'fillna(False)' src/pipeline/kaggle_converter.py)" -ge "1"</automated>
  </verify>
  <done>
    - `(国際)→race_flag_graded_stakes` mapping removed from KAGGLE_COLUMN_MAP; FLAG_COLUMNS preserved
    - `_apply_grade_detection` runs AFTER the `_UNMAPPED_RACE_FLAGS` loop (no KeyError, no clobber)
    - OR-merge preserves existing True (WARNING-2); race_condition=' ' bypasses derive_race_flags early-return
    - Hermetic tests pass with grade='G1' (matches _GRADE_REGEX); race_name-only G1 detected when grade null
    - `test_apply_grade_detection_runs_after_unmapped_flags` regression guard present
    - Existing column_mapping tests green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply D-02 + BLOCKER-1 + HIGH #2 cycle-3 fix — add `_recast_to_canonical` + `_atomic_write_parquet`; add `core_tables_subdir` param to convert() that writes race/entry/result to data/standard/kaggle/ AND SKIPS odds/payoff writes entirely when subdir is set; regenerate Parquet to kaggle/ subdir; non-overwrite odds/payoff test</name>
  <files>src/pipeline/kaggle_converter.py, tests/pipeline/test_kaggle_converter.py, data/standard/kaggle/race.parquet, data/standard/kaggle/entry.parquet, data/standard/kaggle/result.parquet</files>
  <read_first>
    - src/pipeline/kaggle_converter.py (lines 47-50 convert() signature — ONLY raw_dir + standard_dir; lines 103-119 write section: `standard_dir.mkdir`, `tables` dict with all 5 tables, the `for table_name, table_df in tables.items()` write loop at 115-119 writing each to `standard_dir / f"{table_name}.parquet"`)
    - src/scraper/normalizer.py (lines 95-192 SCHEMA_DTYPE_MAP — dtype authority; `race_date: "string"` at line 99; lines 643-653 `_atomic_write_parquet(df, path)` — REUSE: calls `path.parent.mkdir(parents=True, exist_ok=True)` so the kaggle/ subdir is auto-created)
    - src/schemas/race.py (line 31 — `race_date: str`)
    - tests/pipeline/test_kaggle_converter.py (existing invocation pattern `convert(raw_dir=..., standard_dir=tmp_standard_dir)`)
    - tests/pipeline/conftest.py (existing `tmp_standard_dir` fixture around line 291)
  </read_first>
  <behavior>
    - Test 1 (`test_kaggle_parquet_post_d02_has_typed_flags`): regenerated `data/standard/kaggle/race.parquet` via `pyarrow.parquet.read_schema` has zero columns whose `str(type) == 'null'`; every `race_flag_*` column has `str(type) == 'bool'`
    - Test 2 (`test_kaggle_race_distance_is_int64`): `distance` column has `str(type) == 'int64'`
    - Test 3 (`test_kaggle_race_date_is_string`): `race_date` column has `str(type) == 'string'` (MEDIUM #5 resolution — string, NOT datetime)
    - Test 4 (`test_recast_raises_on_bad_data`): a synthetic DataFrame with `distance='abc'` causes `_recast_to_canonical` to raise `TypeError`
    - Test 5 (`test_convert_preserves_odds_payoff` — HIGH #2 cycle-3 fix — NON-OVERWRITE proof): create a tmp_standard_dir, write sentinel `odds_trifecta.parquet` + `payoff.parquet` with DISTINCTIVE bytes (e.g. a marker row) BEFORE the regen run. Invoke `convert(raw_dir=tmp_raw, standard_dir=tmp_standard_dir, core_tables_subdir='kaggle')`. After the call: (a) `tmp_standard_dir / 'odds_trifecta.parquet'` and `tmp_standard_dir / 'payoff.parquet'` SHA-256 AND row count IDENTICAL to the pre-call sentinels (NON-OVERWRITE — convert did not touch them at all when subdir is set); (b) `tmp_standard_dir / 'kaggle' / '{race,entry,result}.parquet'` EXIST and are non-empty; (c) `tmp_standard_dir / 'race.parquet'` does NOT exist (race/entry/result went to subdir, not root).
    - Test 6 (`test_convert_writes_core_tables_to_subdir` — BLOCKER-1): invoke `convert(raw_dir=tmp_raw, standard_dir=tmp_standard_dir, core_tables_subdir='kaggle')`; assert `tmp_standard_dir / 'kaggle' / '{race,entry,result}.parquet'` exist; assert `tmp_standard_dir / 'race.parquet'` does NOT exist. The default `convert(raw_dir=tmp_raw, standard_dir=tmp_standard_dir)` (no subdir arg) STILL writes race/entry/result AND odds/payoff to `tmp_standard_dir / '{table}.parquet'` (backwards-compat with Phase 2 tests — when subdir is None, all 5 tables write to root as before).
    - Test 7 (`test_convert_skips_odds_payoff_when_subdir_set` — HIGH #2 cycle-3 explicit guard): invoke `convert(..., core_tables_subdir='kaggle')` into an EMPTY tmp_standard_dir (no pre-existing odds/payoff). After: `tmp_standard_dir / 'odds_trifecta.parquet'` does NOT exist AND `tmp_standard_dir / 'payoff.parquet'` does NOT exist (proves SKIP, not just non-overwrite of pre-existing).
    - Test 8: existing `tests/pipeline/test_kaggle_converter.py` suite green (default-args path unchanged; only the NEW subdir invocation changes the write site and skips odds/payoff)
  </behavior>
  <action>
    Add to src/pipeline/kaggle_converter.py imports: `from src.schemas.result import ResultSchema`, `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet`.

    Add module-level `_recast_to_canonical(df: pd.DataFrame, schema: type) -> pd.DataFrame` mirroring `src/scraper/normalizer.py:_build_typed_dataframe` (lines 242-255): iterate `SCHEMA_DTYPE_MAP[schema].items()`, skip missing columns, `df[col] = df[col].astype(target)` inside try/except that re-raises as `TypeError` with a column-name message. NEVER use `errors='ignore'`. Call `_recast_to_canonical` for race/entry/result at the END of `split_race_entry_result` (immediately before the `return` at line 278, AFTER `_apply_grade_detection`).

    HIGH #2 cycle-3 fix + BLOCKER-1 (core_tables_subdir param with odds/payoff SKIP): change the `convert()` signature to `convert(raw_dir: Path = Path("data/raw/kaggle"), standard_dir: Path = Path("data/standard"), core_tables_subdir: str | None = None) -> dict[str, Path]`. Rewrite the write section (kaggle_converter.py:103-119) so:

    - Build TWO write groups instead of one `tables` dict iteration:
      - `core_tables = {"race": race_df, "entry": entry_df, "result": result_df}`
      - `odds_tables = {"odds_trifecta": odds_trifecta_df, "payoff": payoff_df}` (only built ONCE from odds.csv — not re-derived per invocation)
    - Write loop for `core_tables`: `out_dir = standard_dir / core_tables_subdir if core_tables_subdir else standard_dir`; for each `(table_name, table_df)` call `_atomic_write_parquet(table_df, out_dir / f"{table_name}.parquet")` (auto-creates the kaggle/ subdir via `path.parent.mkdir`).
    - Write loop for `odds_tables`: ONLY executed when `core_tables_subdir is None`. When `core_tables_subdir` is set (truthy), SKIP the odds/payoff writes ENTIRELY — do NOT write them to root, do NOT write them to subdir, do NOT write them anywhere. Add a comment: "HIGH #2 cycle-3: when core_tables_subdir is set, odds/payoff are SKIPPED entirely to protect the Phase 5 seed at data/standard/{odds_trifecta,payoff}.parquet. They are re-derivable from data/raw/kaggle/*_odds.csv if ever needed."
    - `output_paths` dict: when subdir is set, contains ONLY race/entry/result keys (no odds/payoff keys); when subdir is None, contains all 5 keys (Phase 2 backwards-compat).

    Then regenerate the Parquet files by invoking the public `convert()` entry point with the NEW param: `python -c "from pathlib import Path; from src.pipeline.kaggle_converter import convert; convert(standard_dir=Path('data/standard'), core_tables_subdir='kaggle')"`. This writes race/entry/result to `data/standard/kaggle/{race,entry,result}.parquet` and SKIPS odds/payoff entirely. Do NOT invoke via the poetry toolchain — poetry is not installed (this repo uses setuptools; use `python -c`/`python -m` only).

    Add the new tests (items 1-7 above) to tests/pipeline/test_kaggle_converter.py; follow the existing `convert(raw_dir=..., standard_dir=tmp_standard_dir)` invocation pattern. For test 5 and 7, the sentinel/empty-dir setup must use tmp_path so the test is hermetic.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_kaggle_converter.py tests/pipeline/test_column_mapping.py -q && test "$(grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py)" -ge "1" && test "$(grep -c 'core_tables_subdir' src/pipeline/kaggle_converter.py)" -ge "2" && ! grep -q 'errors=.ignore.' src/pipeline/kaggle_converter.py && python -c "import pyarrow.parquet as pq; s=pq.read_schema('data/standard/kaggle/race.parquet'); nulls=[f.name for f in s if str(f.type)=='null']; assert not nulls, f'Arrow null cols remain: {nulls}'; rd=[f for f in s if f.name=='race_date'][0]; assert str(rd.type)=='string', f'race_date must be string, got {rd.type}'; print('OK: data/standard/kaggle/race.parquet has zero Arrow-null columns; race_date=string')" && python -c "import os; assert not os.path.exists('data/standard/kaggle/odds_trifecta.parquet'), 'odds_trifecta must NOT exist in kaggle/ subdir'; assert not os.path.exists('data/standard/kaggle/payoff.parquet'), 'payoff must NOT exist in kaggle/ subdir'; print('OK: odds/payoff correctly absent from kaggle/ subdir (HIGH #2 cycle-3 SKIP)')"</automated>
  </verify>
  <done>
    - `core_tables_subdir` param added to `convert()`; race/entry/result redirect to `data/standard/kaggle/` when set; odds/payoff are SKIPPED entirely (not written anywhere) when subdir is set (BLOCKER-1 + HIGH #2 cycle-3)
    - `_recast_to_canonical` defined and called 3× at end of `split_race_entry_result` (after `_apply_grade_detection`)
    - Write loop hardened to `_atomic_write_parquet`; odds/payoff writes gated on `core_tables_subdir is None`
    - Regenerated `data/standard/kaggle/race.parquet` has 0 Arrow-null columns; `race_flag_*` all bool; `race_date` string
    - `test_convert_preserves_odds_payoff` proves NON-OVERWRITE (sentinel bytes UNCHANGED)
    - `test_convert_skips_odds_payoff_when_subdir_set` proves odds/payoff absent from empty tmp dir after subdir invocation
    - `test_convert_writes_core_tables_to_subdir` proves the BLOCKER-1 redirect
    - Default no-subdir invocation still writes all 5 tables to root (backwards-compat)
    - All test_kaggle_converter.py tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: 3-table-specific validation of regenerated data/standard/kaggle/ (HIGH #3 cycle-3 fix) — schema column-set, Arrow dtype, grade-regex derivation count, race↔entry↔result FK orphan check. NO run_all_validations against kaggle/ subdir (that runs in 06-03-T2 against the unified root).</name>
  <files></files>
  <read_first>
    - src/pipeline/kaggle_converter.py (split_race_entry_result — the function that built the 3 tables; its return contract is the validation target)
    - src/schemas/race.py, src/schemas/entry.py, src/schemas/result.py (model_fields — canonical column-set source)
    - src/scraper/normalizer.py (lines 95-192 SCHEMA_DTYPE_MAP — the dtype contract; lines 263-374 validate_integrity — the 3-table FK/duplicate validator that takes race_df/entry_df/result_df directly, NOT a parquet_dir — THIS is what 06-01-T3 should use for the 3-table FK check, since it does NOT assume 5 tables)
    - src/scraper/flag_crosswalk.py (lines 124-130 _GRADE_REGEX, lines 135-215 derive_race_flags — for the grade count recompute)
    - src/pipeline/validators.py (lines 764-894 run_all_validations — DO NOT CALL against kaggle/ subdir; it iterates 5 tables via TABLE_TO_SCHEMA and validate_referential_integrity appends 'Missing odds_trifecta/payoff.parquet' at lines 377-383 when those files are absent, forcing overall_pass=False. This is the cycle-2 blocker; cycle-3 removes run_all_validations from this task and moves it to 06-03-T2.)
  </read_first>
  <action>
    HIGH #3 cycle-3 fix: validate the regenerated 3-table kaggle/ subdir SPECIFICALLY. Do NOT call run_all_validations against `data/standard/kaggle/` (it cannot pass — see read_first). Instead, run a 3-table-specific validation that proves the regenerated race/entry/result are correct:

    CHECK 1 — Schema column-set (per table): for each of race/entry/result, read `data/standard/kaggle/{table}.parquet` via `pd.read_parquet`, assert `set(df.columns) == set(Schema.model_fields.keys())`. This catches column drift introduced by the regen.

    CHECK 2 — Arrow dtype (per table, post-D-02): read schema via `pyarrow.parquet.read_schema(path)`; assert zero columns have `str(type) == 'null'`; assert every `race_flag_*` column has `str(type) == 'bool'`; assert `race_date` is `string`; assert `distance` is `int64`; assert `win_odds` is `double`. This is the D-02 dtype contract.

    CHECK 3 — Grade derivation determinism (HIGH #1 follow-up): recompute the expected `race_flag_graded_stakes=True` count by calling `derive_race_flags(race_condition=grade_or_space, race_name=race_name)` per row (where `grade_or_space = str(grade) if pd.notna(grade) and str(grade).strip() != '' else ' '` — SAME logic as `_apply_grade_detection`). Assert `expected == actual` where actual is `int(df['race_flag_graded_stakes'].fillna(False).sum())`. If they differ, STOP and investigate (the helper is using a different haystack or the OR-merge dropped a True).

    CHECK 4 — 3-table FK orphan check (NOT run_all_validations): call `validate_integrity(race_df, entry_df, result_df)` from `src.scraper.normalizer` (lines 263-374). This function takes the 3 DataFrames DIRECTLY (not a parquet_dir) and checks: race_id unique in race; horse_race_id unique in entry AND result; entry/result horse_race_id 1-to-1; entry race_id ⊆ race race_id; result race_id ⊆ race race_id. It does NOT assume odds/payoff exist. Assert the returned violations list is empty.

    Invoke as a single `python -c "..."` block:

    ```
    python -c "
    from pathlib import Path
    import pandas as pd
    import pyarrow.parquet as pq
    from src.schemas.race import RaceSchema
    from src.schemas.entry import EntrySchema
    from src.schemas.result import ResultSchema
    from src.scraper.normalizer import validate_integrity
    from src.scraper.flag_crosswalk import derive_race_flags

    kdir = Path('data/standard/kaggle')
    schemas = {'race': RaceSchema, 'entry': EntrySchema, 'result': ResultSchema}
    dfs = {}
    for tbl, schema in schemas.items():
        p = kdir / f'{tbl}.parquet'
        assert p.exists(), f'MISSING {p}'
        df = pd.read_parquet(p)
        dfs[tbl] = df
        # CHECK 1: column-set
        assert set(df.columns) == set(schema.model_fields.keys()), f'{tbl}: column-set drift; extra={set(df.columns)-set(schema.model_fields.keys())}, missing={set(schema.model_fields.keys())-set(df.columns)}'
        # CHECK 2: Arrow dtype
        arrow_schema = pq.read_schema(p)
        nulls = [f.name for f in arrow_schema if str(f.type)=='null']
        assert not nulls, f'{tbl}: Arrow-null cols: {nulls}'
        if tbl == 'race':
            for f in arrow_schema:
                if f.name.startswith('race_flag_'):
                    assert str(f.type) == 'bool', f'{tbl}.{f.name} type={f.type} (expected bool)'
            rd = [f for f in arrow_schema if f.name=='race_date'][0]
            assert str(rd.type)=='string', f'race_date type={rd.type} (expected string)'
            dist = [f for f in arrow_schema if f.name=='distance'][0]
            assert str(dist.type)=='int64', f'distance type={dist.type} (expected int64)'

    # CHECK 3: grade derivation determinism
    race_df = dfs['race']
    expected_graded = 0
    for _, row in race_df.iterrows():
        grade_val = row.get('grade')
        race_name_val = row.get('race_name')
        cond = str(grade_val) if pd.notna(grade_val) and str(grade_val).strip() != '' else ' '
        name = str(race_name_val) if pd.notna(race_name_val) else ''
        flags = derive_race_flags(race_condition=cond, race_name=name)
        if flags.get('race_flag_graded_stakes') is True:
            expected_graded += 1
    actual_graded = int(race_df['race_flag_graded_stakes'].fillna(False).sum())
    print(f'graded: expected={expected_graded} actual={actual_graded}')
    assert expected_graded == actual_graded, f'grade derivation mismatch: expected={expected_graded} actual={actual_graded}'

    # CHECK 4: 3-table FK orphan check (validate_integrity takes DataFrames, not parquet_dir)
    violations = validate_integrity(dfs['race'], dfs['entry'], dfs['result'])
    print(f'validate_integrity violations: {len(violations)}')
    for v in violations[:5]:
        print('  VIOLATION:', v)
    assert violations == [], f'3-table FK violations: {violations}'

    print('OK: 06-01-T3 3-table-specific validation PASSED — schema, dtype, grade-derivation, FK integrity')
    "
    ```

    NOTE for the executor: do NOT call `run_all_validations(raw_dir, Path('data/standard/kaggle'), ...)` here — it will return `overall_pass=False` because `validate_referential_integrity` (validators.py:377-383) iterates `["entry", "result", "odds_trifecta", "payoff"]` and appends "Missing odds_trifecta.parquet"/"Missing payoff.parquet" when those files are absent from the kaggle/ subdir. The full 8-point `run_all_validations` runs in 06-03-T2 against `data/standard/` root where all 5 tables coexist (unified race/entry/result + Phase 5 odds/payoff). This is the cycle-3 resolution of HIGH #3.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
from pathlib import Path
import pandas as pd
import pyarrow.parquet as pq
from src.schemas.race import RaceSchema
from src.schemas.entry import EntrySchema
from src.schemas.result import ResultSchema
from src.scraper.normalizer import validate_integrity
from src.scraper.flag_crosswalk import derive_race_flags
kdir = Path('data/standard/kaggle')
schemas = {'race': RaceSchema, 'entry': EntrySchema, 'result': ResultSchema}
dfs = {}
for tbl, schema in schemas.items():
    p = kdir / f'{tbl}.parquet'
    assert p.exists(), f'MISSING {p}'
    df = pd.read_parquet(p)
    dfs[tbl] = df
    assert set(df.columns) == set(schema.model_fields.keys()), f'{tbl}: column-set drift'
    arrow_schema = pq.read_schema(p)
    nulls = [f.name for f in arrow_schema if str(f.type)=='null']
    assert not nulls, f'{tbl}: Arrow-null cols: {nulls}'
    if tbl == 'race':
        for f in arrow_schema:
            if f.name.startswith('race_flag_'):
                assert str(f.type) == 'bool', f'{tbl}.{f.name} type={f.type}'
        rd = [f for f in arrow_schema if f.name=='race_date'][0]
        assert str(rd.type)=='string', f'race_date type={rd.type}'
race_df = dfs['race']
expected_graded = sum(1 for _, row in race_df.iterrows() if derive_race_flags(race_condition=(str(row.get('grade')) if pd.notna(row.get('grade')) and str(row.get('grade')).strip() != '' else ' '), race_name=(str(row.get('race_name')) if pd.notna(row.get('race_name')) else '')).get('race_flag_graded_stakes') is True)
actual_graded = int(race_df['race_flag_graded_stakes'].fillna(False).sum())
print(f'graded: expected={expected_graded} actual={actual_graded}')
assert expected_graded == actual_graded, f'grade derivation mismatch'
violations = validate_integrity(dfs['race'], dfs['entry'], dfs['result'])
assert violations == [], f'3-table FK violations: {violations}'
print('OK: 06-01-T3 3-table-specific validation PASSED')
" && python -m pytest tests/pipeline/test_validators.py -q</automated>
  </verify>
  <done>
    - 3-table kaggle/ subdir passes schema column-set check (race/entry/result)
    - Zero Arrow-null columns; race_flag_* all bool; race_date string; distance int64 (D-02 contract)
    - Grade derivation is deterministic (expected == actual count via derive_race_flags)
    - 3-table FK orphan check (validate_integrity on DataFrames) passes — no duplicates, no orphans
    - NO call to run_all_validations against kaggle/ subdir (HIGH #3 cycle-3 — that runs in 06-03-T2 against unified root)
    - Actual graded count recorded in SUMMARY for traceability
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle CSV → standard Parquet (kaggle/ subdir) | Raw external data crosses into the typed contract. Validated by recast helper + grade detector + 3-table FK check. The output lives in `data/standard/kaggle/` to establish a STABLE separate input path for Plan 06-02 (BLOCKER-1). |
| Regen write → existing odds_trifecta/payoff.parquet | D-05 requires these Phase 5 seeds to be untouched. The `core_tables_subdir='kaggle'` invocation SKIPS odds/payoff writes entirely (HIGH #2 cycle-3) — the strongest possible protection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `column_mapping.py` KAGGLE_COLUMN_MAP + Kaggle grade detector ordering | mitigate | HIGH #1 cycle-3: `(国際)` mapping removed; `_apply_grade_detection` runs AFTER `_UNMAPPED_RACE_FLAGS` loop (no KeyError, no clobber); test uses `grade='G1'` (matches `_GRADE_REGEX` `G3\|G2\|G1`); `race_condition=' '` bypasses derive_race_flags early-return. Verified by `test_kaggle_graded_derivation_matches_regex`, `test_apply_grade_detection_runs_after_unmapped_flags`, and the deterministic count check in Task 3. |
| T-06-01b | Tampering | `_apply_grade_detection` overwrite semantics (WARNING-2) | mitigate | OR-merge via `existing.fillna(False) \| new.fillna(False)` — never downgrades True to None. Verified by `test_grade_detection_preserves_existing_true`. |
| T-06-02 | Tampering | `kaggle_converter._recast_to_canonical` | mitigate | Recast raises TypeError on uncoercible values; NEVER uses `errors='ignore'`. Verified by `test_recast_raises_on_bad_data`. |
| T-06-03 | Tampering | Destructive overwrite of `data/standard/kaggle/{race,entry,result}.parquet` | mitigate | Write site calls `_atomic_write_parquet` (temp + os.replace, POSIX-atomic). |
| T-06-04 | Tampering / Information Disclosure | D-05 violation: regen overwrites odds_trifecta/payoff (HIGH #2 cycle-3) | mitigate | When `core_tables_subdir` is set, odds/payoff writes are SKIPPED entirely — not written to root, subdir, or anywhere. Verified by `test_convert_preserves_odds_payoff` (NON-OVERWRITE: sentinel bytes UNCHANGED) AND `test_convert_skips_odds_payoff_when_subdir_set` (odds/payoff absent from empty tmp dir). |
| T-06-04b | Tampering | Cross-plan data contract: 06-02 reads Kaggle from a path 06-01 never created (BLOCKER-1) | mitigate | `convert(core_tables_subdir='kaggle')` writes race/entry/result to `data/standard/kaggle/`. Plan 06-02 `integrate_standard_layer` defaults `kaggle_input_dir` to `standard_dir / 'kaggle'`. |
| T-06-05 | Tampering | 8-point verification cannot pass against 3-table subdir (HIGH #3 cycle-3) | mitigate | 06-01-T3 does NOT call run_all_validations against kaggle/ subdir. Instead: 3-table-specific validation (schema column-set, Arrow dtype, grade derivation, validate_integrity on DataFrames). The FULL 8-point run_all_validations runs in 06-03-T2 against `data/standard/` root where all 5 tables coexist. |
| T-06-SC | Tampering | No package installs (pandas/pyarrow/numpy/pydantic all pre-installed; Phase 4 audit verified) | accept | No new dependencies; no slopcheck needed. |
</threat_model>

<verification>
- `python -m pytest tests/pipeline/test_kaggle_converter.py tests/pipeline/test_column_mapping.py tests/pipeline/test_validators.py -q` green
- `python -m pytest tests/ -q` full suite green
- `pyarrow.parquet.read_schema('data/standard/kaggle/race.parquet')` has zero Arrow-null columns; `race_date` is `string`; `distance` is `int64`
- `ls data/standard/kaggle/{race,entry,result}.parquet` succeeds (BLOCKER-1 redirect)
- `ls data/standard/kaggle/odds_trifecta.parquet data/standard/kaggle/payoff.parquet` FAILS (odds/payoff correctly absent from subdir)
- `data/standard/odds_trifecta.parquet` + `data/standard/payoff.parquet` SHA-256 unchanged across the regen (HIGH #2 cycle-3 NON-OVERWRITE)
- 3-table-specific validation (schema, dtype, grade-derivation, FK) passes against `data/standard/kaggle/`
- Graded count is deterministically equal to the grade-regex derivation
</verification>

<success_criteria>
- HIGH #1 resolved (cycle 3): `_apply_grade_detection` runs AFTER `_UNMAPPED_RACE_FLAGS` loop; test uses `grade='G1'`; `race_condition=' '` bypasses early-return; OR-merge preserves True
- HIGH #2 resolved (cycle 3): `convert(core_tables_subdir='kaggle')` SKIPS odds/payoff writes entirely (NON-OVERWRITE proven)
- HIGH #3 resolved (cycle 3): 8-point run_all_validations removed from 06-01-T3; 3-table-specific validation runs here; full 8-point moves to 06-03-T2 against unified root
- HIGH #4 preserved: every command uses `python -m` / `python -c`
- BLOCKER-1 resolved: regenerated Kaggle race/entry/result written to `data/standard/kaggle/`
- WARNING-2 resolved: OR-merge preserves existing True
- MEDIUM #5 resolved: race_date stays `string`
- D-01 + D-02 fully applied; Kaggle corpus schema-aligned with scraped corpus
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-01-SUMMARY.md` when done. Record: actual graded_stakes count, odds/payoff SHA-256 values pre/post regen (proving NON-OVERWRITE), race_date dtype resolution note, the `core_tables_subdir` BLOCKER-1 redirect + SKIP confirmation, the WARNING-2 OR-merge implementation note, and the HIGH #3 cycle-3 resolution (run_all_validations moved to 06-03-T2).
</output>

## Artifacts this phase produces

- `src/pipeline/kaggle_converter.py`
  - NEW import: `from src.scraper.flag_crosswalk import derive_race_flags`
  - NEW import: `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet`
  - NEW import: `from src.schemas.result import ResultSchema`
  - NEW helper: `_apply_grade_detection(race_df) -> pd.DataFrame` — called AFTER `_UNMAPPED_RACE_FLAGS` loop (HIGH #1 cycle-3); calls `derive_race_flags(race_condition=grade_or_space, race_name)` where grade_or_space is `' '` when grade is null (early-return bypass); OR-merges `race_flag_graded_stakes`/`race_flag_stakes`/`race_flag_listed` via `existing.fillna(False) | new.fillna(False)` (WARNING-2)
  - NEW helper: `_recast_to_canonical(df, schema) -> pd.DataFrame` — SCHEMA_DTYPE_MAP recast; raises TypeError
  - MODIFIED `convert()` signature: added `core_tables_subdir: str | None = None` param (BLOCKER-1); race/entry/result redirect to `standard_dir / core_tables_subdir /` when set; **odds/payoff writes SKIPPED entirely when `core_tables_subdir` is set** (HIGH #2 cycle-3)
  - 3 recast call sites at end of `split_race_entry_result` (after `_apply_grade_detection`)
  - Write section rewritten: split into core_tables + odds_tables groups; odds_tables only written when `core_tables_subdir is None`; `_atomic_write_parquet` used for all writes
- `src/pipeline/column_mapping.py`
  - REMOVED: line 68 `"レース記号/(国際)": ("race", "race_flag_graded_stakes")` from `KAGGLE_COLUMN_MAP`
  - PRESERVED: FLAG_COLUMNS entry at ~line 197
- `tests/pipeline/test_kaggle_converter.py`
  - NEW: `test_kaggle_graded_derivation_matches_regex` (HIGH #1 cycle-3 — uses `grade='G1'`; tests race_name-only G1 when grade null)
  - NEW: `test_grade_detection_preserves_existing_true` (WARNING-2)
  - NEW: `test_apply_grade_detection_runs_after_unmapped_flags` (HIGH #1 cycle-3 ordering regression guard)
  - NEW: `test_kaggle_parquet_post_d02_has_typed_flags` (HIGH #4 dtype)
  - NEW: `test_kaggle_race_date_is_string` (MEDIUM #5)
  - NEW: `test_kaggle_race_distance_is_int64`
  - NEW: `test_recast_raises_on_bad_data`
  - NEW: `test_convert_preserves_odds_payoff` (HIGH #2 cycle-3 NON-OVERWRITE)
  - NEW: `test_convert_writes_core_tables_to_subdir` (BLOCKER-1)
  - NEW: `test_convert_skips_odds_payoff_when_subdir_set` (HIGH #2 cycle-3 explicit SKIP)
- `data/standard/kaggle/race.parquet` / `entry.parquet` / `result.parquet` — regenerated, post-D-01 + post-D-02, atomically written (BLOCKER-1 redirect)
- `data/standard/odds_trifecta.parquet` / `payoff.parquet` — UNCHANGED (never written by the subdir invocation; HIGH #2 cycle-3)

---
phase: 06-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/column_mapping.py
  - src/pipeline/kaggle_converter.py
  - tests/pipeline/test_kaggle_converter.py
  - data/standard/race.parquet
  - data/standard/entry.parquet
  - data/standard/result.parquet
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "D-01 applied: `レース記号/(国際)` no longer maps to `race_flag_graded_stakes` in `KAGGLE_COLUMN_MAP` (line 68 deleted from src/pipeline/column_mapping.py)"
    - "D-02 applied: regenerated data/standard/{race,entry,result}.parquet have every race_flag_* column typed as Arrow bool (not Arrow null) and all nullable numeric columns as Int64/Float64"
    - "Phase 2 D-05 8-point verification passes on the regenerated Kaggle Parquet (run_all_validations returns overall_pass=True)"
    - "race_flag_graded_stakes=True count drops from 1,348 to ~838 (510 misclassified (国際) rows fixed)"
    - "FLAG_COLUMNS list (column_mapping.py:179-200) still contains `レース記号/(国際)` at line ~197 (NOT removed — that list models CSV headers)"
  artifacts:
    - path: "src/pipeline/column_mapping.py"
      provides: "KAGGLE_COLUMN_MAP with (国際) line removed (line 68 deleted)"
      not_contains: '"レース記号/(国際)": ("race", "race_flag_graded_stakes")'
    - path: "src/pipeline/kaggle_converter.py"
      provides: "_recast_to_canonical(df, schema) helper + ResultSchema import + call site at end of split_race_entry_result + atomic write via _atomic_write_parquet"
      contains: "_recast_to_canonical"
    - path: "tests/pipeline/test_kaggle_converter.py"
      provides: "test_kaggle_parquet_post_d02_has_typed_flags asserting all race_flag_* are Arrow bool"
      contains: "test_kaggle_parquet_post_d02_has_typed_flags"
    - path: "data/standard/race.parquet"
      provides: "Regenerated Kaggle race table with nullable dtypes (post-D-01 + post-D-02), written atomically"
    - path: "data/standard/entry.parquet"
      provides: "Regenerated Kaggle entry table with nullable dtypes, written atomically"
    - path: "data/standard/result.parquet"
      provides: "Regenerated Kaggle result table with nullable dtypes, written atomically"
  key_links:
    - from: "src/pipeline/kaggle_converter.py:_recast_to_canonical"
      to: "src/scraper/normalizer.py:SCHEMA_DTYPE_MAP"
      via: "from src.scraper.normalizer import SCHEMA_DTYPE_MAP"
      pattern: "from src\\.scraper\\.normalizer import.*SCHEMA_DTYPE_MAP"
    - from: "src/pipeline/kaggle_converter.py:split_race_entry_result"
      to: "_recast_to_canonical(race_df, RaceSchema)"
      via: "call at end of split_race_entry_result (line ~278, before return)"
      pattern: "_recast_to_canonical\\(race_df, RaceSchema\\)"
---

<objective>
Apply CONTEXT.md decisions D-01 (remove `(国際)→race_flag_graded_stakes` Kaggle mapping) and D-02 (regenerate Kaggle Parquet with nullable dtypes matching `SCHEMA_DTYPE_MAP`), then re-verify the regenerated tables pass Phase 2's D-05 8-point validation. This is the schema-arbitration prerequisite that brings the Kaggle side up to the Phase 4 dtype discipline, so the two corpora (Kaggle + scraped) become schema-indistinguishable before the Wave 2 merge.

Purpose: Resolves the STATE.md Phase 6 必須調停事項 (the `(国際)` divergence flagged at Phase 4 P07). Without this, `pd.concat` of Kaggle (Arrow `null` for empty flag columns) and scraped (Arrow `bool`) produces FutureWarnings and fails physical-type equality (success criterion #2).

Output: Three regenerated Parquet files under `data/standard/`, two edited source modules (`column_mapping.py`, `kaggle_converter.py`), updated/new dtype assertions in `test_kaggle_converter.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md

# Phase 2 converter (the D-02 modification target) and the Phase 4 dtype authority (the recast target)
@src/pipeline/kaggle_converter.py
@src/pipeline/column_mapping.py
@src/scraper/normalizer.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py
@tests/pipeline/test_kaggle_converter.py

# Reference: the Phase 4 P07 commit that made the IDENTICAL edit on the scraper side
@src/scraper/flag_crosswalk.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply D-01 — remove `(国際)→race_flag_graded_stakes` mapping from column_mapping.py:68 (and confirm FLAG_COLUMNS untouched)</name>
  <files>src/pipeline/column_mapping.py</files>
  <read_first>
    - src/pipeline/column_mapping.py (lines 1-210 — see KAGGLE_COLUMN_MAP line 68 and FLAG_COLUMNS line 197)
    - src/scraper/flag_crosswalk.py (lines 20-46 — the canonical docstring/reasoning for the IDENTICAL edit Phase 4 P07 made on the scraper side; mirror this wording in a brief comment)
    - .planning/phases/06-data-integration/06-PATTERNS.md (section `src/pipeline/column_mapping.py:68` — exact line to delete and the explicit "Do NOT remove from FLAG_COLUMNS" warning)
  </read_first>
  <behavior>
    - Test 1: `grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py` returns 0 (line removed from KAGGLE_COLUMN_MAP)
    - Test 2: `grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py` returns >= 1 (the FLAG_COLUMNS list entry at line ~197 MUST remain — that list models the CSV header, not the mapping)
    - Test 3: existing `tests/pipeline/test_column_mapping.py` still passes (the mapping is removed but no new unmapped flag is introduced — see PATTERNS.md blast-radius note)
  </behavior>
  <action>
    Per D-01: delete ONLY line 68 of src/pipeline/column_mapping.py, the entry `"レース記号/(国際)": ("race", "race_flag_graded_stakes"),` inside `KAGGLE_COLUMN_MAP`. Do NOT remove the corresponding entry from `FLAG_COLUMNS` (around line 197) — that list models the JRA CSV header set and the column still physically exists in `data/raw/kaggle/*.csv`; removing it would orphan the column during conversion. Add a brief inline comment referencing the Phase 4 P07 UAT-Test-3 decision and `src/scraper/flag_crosswalk.py:20-46` (the canonical reasoning: international designation is NOT a graded classification). This makes `GRADE_REGEX` (GI/GII/GIII/G1/G2/G3/JG*/重賞/full-width ＧＩ) the SOLE source of `race_flag_graded_stakes=True` on both Kaggle and scraped sides.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && poetry run pytest tests/pipeline/test_column_mapping.py -q && test "$(grep -c '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py)" = "0" && test "$(grep -c 'レース記号/(国際)' src/pipeline/column_mapping.py)" -ge "1"</automated>
  </verify>
  <done>
    - `grep '"レース記号/(国際)": ("race", "race_flag_graded_stakes")' src/pipeline/column_mapping.py` returns no match (line 68 removed)
    - `grep -n 'レース記号/(国際)' src/pipeline/column_mapping.py` shows the FLAG_COLUMNS entry still present (line ~197)
    - `poetry run pytest tests/pipeline/test_column_mapping.py -q` is green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply D-02 — add `_recast_to_canonical(df, schema)` to kaggle_converter.py, harden the write site to `_atomic_write_parquet`, and regenerate Kaggle Parquet with nullable dtypes</name>
  <files>src/pipeline/kaggle_converter.py, tests/pipeline/test_kaggle_converter.py, data/standard/race.parquet, data/standard/entry.parquet, data/standard/result.parquet</files>
  <read_first>
    - src/pipeline/kaggle_converter.py (full file — imports lines 19-31; the `tables` dict at lines 107-113 with the non-atomic `to_parquet` write loop at lines 115-119 that MUST be replaced with `_atomic_write_parquet`; `split_race_entry_result` at line 207 ending with `return race_df, entry_df, result_df` at line 278; the audit_leakage calls at lines 121-129; the public `convert(raw_dir, standard_dir)` entry at line 47 — this is the regeneration entry point, NOT a CLI script)
    - src/scraper/normalizer.py (lines 81-192 SCHEMA_DTYPE_MAP — the authority; lines 226-255 `_build_typed_dataframe` — exact try/except shape to mirror; lines 643-653 `_atomic_write_parquet(df, path)` — signature confirmed: writes to `path.with_suffix(path.suffix + '.tmp')` then `os.replace` — REUSE this function for the hardened write)
    - src/schemas/race.py, src/schemas/entry.py, src/schemas/result.py (model_fields — confirms `ResultSchema` needs an import added alongside existing `RaceSchema`/`EntrySchema` imports)
    - tests/pipeline/test_kaggle_converter.py (existing dtype assertions; the existing `convert(raw_dir=..., standard_dir=tmp_standard_dir)` invocation pattern at lines 39/61/87/106 — the tests call `convert()` directly with tmp dirs, NOT a CLI; follow this pattern)
    - tests/scraper/test_end_to_end.py (lines 449-536 — the TestSchemaCompatibility pattern to adapt for test_kaggle_parquet_post_d02_has_typed_flags)
    - tests/pipeline/conftest.py (existing fixtures: tmp_standard_dir at line 291, sample_race_result_df at line 24, sample_odds_df at line 213)
    - pyproject.toml (lines 20-21 — `[project.scripts]` only declares `keiba = "src.cli:main"`; there is NO dedicated Kaggle-converter CLI; the regeneration entry point is the `convert()` function imported and invoked via `poetry run python -c`)
  </read_first>
  <behavior>
    - Test 1: `test_kaggle_parquet_post_d02_has_typed_flags` reads the regenerated race.parquet via pyarrow.parquet.read_schema and asserts every column whose name starts with `race_flag_` has str(type) == 'bool' (NOT 'null')
    - Test 2: `test_kaggle_race_distance_is_int64` asserts str(type) == 'int64' for the distance column (Int64 serializes to Arrow int64)
    - Test 3: `test_kaggle_recast_raises_on_bad_data` constructs a synthetic DataFrame with an uncoercible value (e.g., distance='abc') and asserts _recast_to_canonical raises TypeError (NO errors='ignore' path — per RESEARCH.md "State of the Art")
    - Test 4: `grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py` returns >= 1 (the write loop at lines 115-119 was replaced with the atomic helper call)
    - Test 5: existing `tests/pipeline/test_kaggle_converter.py` suite remains green (only dtype-string assertions update where they asserted object/float64)
  </behavior>
  <action>
    Add to src/pipeline/kaggle_converter.py: (1) import `ResultSchema` from src.schemas.result, (2) import `SCHEMA_DTYPE_MAP` AND `_atomic_write_parquet` from src.scraper.normalizer, (3) add a module-level `_recast_to_canonical(df: pd.DataFrame, schema: type) -> pd.DataFrame` helper that mirrors the exact loop shape of `_build_typed_dataframe` (src/scraper/normalizer.py:242-255): iterate `SCHEMA_DTYPE_MAP[schema].items()`, skip missing columns, `df[col] = df[col].astype(target)` inside try/except that re-raises as TypeError with a column-name message. NEVER use `errors='ignore'` anywhere in this helper — RESEARCH.md "State of the Art" explicitly forbids it. (4) At the END of `split_race_entry_result` (immediately before the `return race_df, entry_df, result_df` statement at line 278), call `_recast_to_canonical(race_df, RaceSchema)`, `_recast_to_canonical(entry_df, EntrySchema)`, `_recast_to_canonical(result_df, ResultSchema)`. (5) **HARDEN THE WRITE SITE (BLOCKER-02)**: replace the non-atomic `table_df.to_parquet(output_path, engine='pyarrow', compression='snappy', index=False)` call inside the write loop at kaggle_converter.py:115-119 with a call to `_atomic_write_parquet(table_df, output_path)` (same engine/compression/index params — see src/scraper/normalizer.py:643-653). This honors CLAUDE.md's atomic-write pattern and makes the Kaggle write path identical to the scraper write path. Then regenerate the Parquet files by invoking the public `convert()` entry point — there is NO dedicated CLI script (confirmed by reading pyproject.toml `[project.scripts]` which only declares `keiba = "src.cli:main"`); use `poetry run python -c "from src.pipeline.kaggle_converter import convert; convert()"` (defaults: `raw_dir=Path('data/raw/kaggle')`, `standard_dir=Path('data/standard')`). Add the new tests (items 1-4 above) to tests/pipeline/test_kaggle_converter.py; follow the existing `convert(raw_dir=..., standard_dir=tmp_standard_dir)` invocation pattern already used in this file (lines 39/61/87/106).
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && poetry run pytest tests/pipeline/test_kaggle_converter.py tests/pipeline/test_column_mapping.py -q && test "$(grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py)" -ge "1" && poetry run python -c "import pyarrow.parquet as pq; s=pq.read_schema('data/standard/race.parquet'); nulls=[f.name for f in s if str(f.type)=='null']; assert not nulls, f'Arrow null cols remain: {nulls}'; print('OK: zero Arrow null columns in race.parquet')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_recast_to_canonical' src/pipeline/kaggle_converter.py` returns >= 4 (1 definition + 3 call sites at end of split_race_entry_result)
    - `grep -c '_atomic_write_parquet' src/pipeline/kaggle_converter.py` returns >= 1 (the non-atomic `to_parquet` at the write loop was replaced with the atomic helper)
    - `grep 'errors=.ignore.' src/pipeline/kaggle_converter.py` returns NO matches (anti-pattern forbidden)
    - `grep 'from src.scraper.normalizer import.*SCHEMA_DTYPE_MAP' src/pipeline/kaggle_converter.py` returns a match
    - `grep 'from src.scraper.normalizer import.*_atomic_write_parquet' src/pipeline/kaggle_converter.py` returns a match
    - `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero columns whose `str(type) == 'null'` (every race_flag_* is Arrow bool)
    - The regenerated `data/standard/race.parquet` has `str(type) == 'int64'` for distance and meeting_num columns (Int64 serializes to Arrow int64)
    - `poetry run pytest tests/pipeline/test_kaggle_converter.py -q` is green including the new tests
  </acceptance_criteria>
  <done>
    - _recast_to_canonical defined and called 3 times at end of split_race_entry_result
    - Write loop at kaggle_converter.py:115-119 replaced with `_atomic_write_parquet(table_df, output_path)` (CLAUDE.md atomic-write pattern honored)
    - Regenerated race.parquet has 0 Arrow-null columns; all 20 race_flag_* are Arrow bool
    - All tests/pipeline/test_kaggle_converter.py tests pass (existing + new)
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-verify regenerated Kaggle Parquet against Phase 2 D-05 8-point validation + graded_stakes count drop assertion</name>
  <files></files>
  <read_first>
    - src/pipeline/validators.py (full file — `run_all_validations(raw_dir, parquet_dir, source_counts=None, source_stats=None)` is the entry point at line 764; returns a dict with `overall_pass` boolean key at line 876; the 8 D-05 checks are listed in its docstring lines 769-779; `raw_dir` is the Kaggle CSV source dir used by validate_sample_rows)
    - tests/pipeline/test_validators.py (the existing tests for validators; mirror style for the new graded_stakes count assertion)
    - .planning/phases/02-kaggle-data-pipeline/02-CONTEXT.md (D-05 8-point definition — confirms which assertions run)
    - data/standard/race.parquet (the regenerated file; you will read it programmatically via pandas/pyarrow, not edit it)
  </read_first>
  <action>
    Run the Phase 2 D-05 8-point validation against the regenerated `data/standard/`. The entry point is `src/pipeline/validators.py::run_all_validations(raw_dir, parquet_dir, source_counts=None, source_stats=None)` (signature confirmed at validators.py:764 — two required positional args: `raw_dir` is the Kaggle CSV source directory, `parquet_dir` is the directory containing the regenerated Parquet files; `source_counts`/`source_stats` are optional and skipped when None — checks 1/4/5 pass vacuously, which is acceptable for a post-regeneration re-verification). Invoke it programmatically: `poetry run python -c "from pathlib import Path; from src.pipeline.validators import run_all_validations; r = run_all_validations(Path('data/raw/kaggle'), Path('data/standard')); print('overall_pass=', r['overall_pass']); assert r['overall_pass'] is True, {k:v for k,v in r.items() if v is False}"`. The function returns a `dict` (NOT an error list); the pass/fail boolean is the `overall_pass` key. Then programmatically assert the D-01 effect on the regenerated race.parquet: count `race_flag_graded_stakes == True` rows; expect approximately 838 (range 780-880 — RESEARCH.md Pitfall 4 confirms the 510 misclassified rows should drop the count from 1,348 to ~838). Use `pandas.read_parquet('data/standard/race.parquet')` and `df['race_flag_graded_stakes'].sum()`. Document the actual count in the plan summary; if it falls outside 780-880, STOP and investigate (likely the D-01 edit did not propagate, or there are additional graded-detection paths beyond GRADE_REGEX).
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && poetry run python -c "from pathlib import Path; from src.pipeline.validators import run_all_validations; r = run_all_validations(Path('data/raw/kaggle'), Path('data/standard')); print('overall_pass=', r['overall_pass']); assert r['overall_pass'] is True, {k:v for k,v in r.items() if v is False and k != 'overall_pass'}" && poetry run python -c "import pandas as pd; df = pd.read_parquet('data/standard/race.parquet'); c = int(df['race_flag_graded_stakes'].sum()); print(f'graded_stakes=True count={c}'); assert 780 <= c <= 880, f'D-01 fix did not propagate: expected ~838, got {c}'"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 2 `run_all_validations(Path('data/raw/kaggle'), Path('data/standard'))` returns a dict whose `overall_pass` key is `True` (signature: `(raw_dir, parquet_dir, source_counts=None, source_stats=None)`; return type: `dict[str, Any]` with `overall_pass: bool`)
    - `race_flag_graded_stakes.sum()` on the regenerated race.parquet is between 780 and 880 (approximate drop from 1,348 to 838)
    - All existing `tests/pipeline/test_validators.py` tests still pass
  </acceptance_criteria>
  <done>
    - Phase 2 D-05 8-point validation green on regenerated Kaggle Parquet (run_all_validations returned `overall_pass=True`)
    - graded_stakes=True count is ~838 (range 780-880) — D-01 effect verified
    - Actual count value recorded in the plan summary for traceability
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle CSV → standard Parquet | Raw external data crosses this boundary into the project's typed contract. Validated by Pydantic schemas and the recast helper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `column_mapping.py` KAGGLE_COLUMN_MAP | mitigate | D-01 removes the loose `(国際)` substring mapping; `GRADE_REGEX` (strict, anchored to GI/GII/GIII tokens) becomes the sole `race_flag_graded_stakes` source. Verified by graded_stakes count dropping to ~838. |
| T-06-02 | Tampering | `kaggle_converter._recast_to_canonical` | mitigate | Recast raises TypeError on uncoercible values; NEVER uses `errors='ignore'` (anti-pattern guard). Prevents silent dtype drift. Verified by `test_kaggle_recast_raises_on_bad_data`. |
| T-06-03 | Tampering | Destructive overwrite of `data/standard/{race,entry,result}.parquet` | mitigate | Write site at kaggle_converter.py:115-119 now calls `_atomic_write_parquet(table_df, output_path)` (from src/scraper/normalizer.py:643-653) — temp file + `os.replace` POSIX-atomic. Honors CLAUDE.md atomic-write pattern and matches the scraper write path. Kaggle Parquet is also fully regenerable from `data/raw/kaggle/*.csv` via `convert()`. |
| T-06-04 | Information Disclosure | FLAG_COLUMNS list at column_mapping.py:197 | mitigate | Do NOT remove the `(国際)` entry from FLAG_COLUMNS (it models CSV headers); only the KAGGLE_COLUMN_MAP mapping at line 68 is deleted. Grep guard in Task 1 verify enforces this. |
| T-06-SC | Tampering | No package installs in this plan (pandas/pyarrow/numpy/pydantic all pre-installed and Phase 4 Package Legitimacy Audit-verified) | accept | No new dependencies; no slopcheck needed. |
</threat_model>

<verification>
- All `tests/pipeline/test_kaggle_converter.py` + `tests/pipeline/test_column_mapping.py` + `tests/pipeline/test_validators.py` tests green
- `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns
- `race_flag_graded_stakes.sum()` between 780 and 880
- Phase 2 `run_all_validations(Path('data/raw/kaggle'), Path('data/standard'))` returns `overall_pass=True`
- Full `poetry run pytest tests/ -q` suite green (Phase 1/3/4 tests unaffected by Kaggle-side dtype changes)
</verification>

<success_criteria>
- D-01 applied: `(国際)→race_flag_graded_stakes` mapping removed; graded_stakes=True count ~838 (not 1,348)
- D-02 applied: regenerated Kaggle Parquet has all race_flag_* as Arrow bool, all numeric columns as Int64/Float64; recast helper exists and is called from split_race_entry_result; write site hardened to `_atomic_write_parquet` (CLAUDE.md pattern honored)
- Phase 2 D-05 8-point validation passes on the regenerated tables (overall_pass=True)
- Kaggle corpus is now schema-aligned with the scraped corpus (same `SCHEMA_DTYPE_MAP` authority) — ready for Wave 2 merge
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/pipeline/kaggle_converter._recast_to_canonical(df: pd.DataFrame, schema: type) -> pd.DataFrame` — module-level helper; raises TypeError on uncoercible dtype
- `src/pipeline/kaggle_converter.py` import of `SCHEMA_DTYPE_MAP` AND `_atomic_write_parquet` from `src.scraper.normalizer`
- `src/pipeline/kaggle_converter.py` import of `ResultSchema` from `src.schemas.result`
- Three call sites at end of `split_race_entry_result` (line ~278, before return): `_recast_to_canonical({race,entry,result}_df, {Race,Entry,Result}Schema)`
- Replaced write loop at kaggle_converter.py:115-119: `_atomic_write_parquet(table_df, output_path)` (was non-atomic `to_parquet(...)` — now matches normalizer.py:643-653)
- `tests/pipeline/test_kaggle_converter.py::test_kaggle_parquet_post_d02_has_typed_flags`
- `tests/pipeline/test_kaggle_converter.py::test_kaggle_race_distance_is_int64`
- `tests/pipeline/test_kaggle_converter.py::test_kaggle_recast_raises_on_bad_data`
- `data/standard/race.parquet` (regenerated, post-D-01 + post-D-02, written atomically)
- `data/standard/entry.parquet` (regenerated, post-D-02, written atomically)
- `data/standard/result.parquet` (regenerated, post-D-02, written atomically)
- Removed symbol: line 68 of `src/pipeline/column_mapping.py` (the `(国際)`→`race_flag_graded_stakes` mapping in KAGGLE_COLUMN_MAP)

---
phase: 06-data-integration
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - data/standard/race.parquet
  - data/standard/entry.parquet
  - data/standard/result.parquet
autonomous: false
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #15 (D-06 pre-task): the full scrape (2022-2026/5) is documented as a hard prerequisite. The preflight (Task 1) verifies per-partition completeness, not just month-dir count. If the corpus is smoke-only, the plan HALTS with exit code 1 and a clear instruction to run the D-06 scrape first. The plan does NOT weaken the halt gate."
    - "HIGH #16 (per-partition preflight): every present scraped partition (month dir) is checked for: 3-file presence (race/entry/result.parquet), non-empty file (row count > 0), and a valid race_date in race.parquet whose YYYYMM matches the partition dir name. Empty/partial/corrupt partitions are reported; the integration aborts if > 20% of expected partitions are invalid."
    - "HIGH #17 (odds/payoff snapshot IN verify.automated): the SHA-256 + row count of odds_trifecta.parquet and payoff.parquet are captured BEFORE integration and asserted UNCHANGED AFTER, in the actual <verify><automated> command (not just described in prose)."
    - "HIGH #18 (graded count split by period): the graded_stakes count is verified SEPARATELY for the Kaggle period (2015-2021 rows, count must match Plan 06-01's derivation) and the scraped period (2022-2026/5 rows, count derived from the scraped corpus). The unified count is NOT compared against a fixed 780-880 band."
    - "HIGH #19 (date-range robustness): the unified race.parquet's race_date min/max are asserted against EXPECTED first/last racedays (min in 2015-01..03, max in 2026-01..06 if D-07 extended, OR 2024-10..12 if D-07 deferred to ROADMAP contract — see scope resolution below). Per-year race counts are also asserted (each year 2015..end has > 1000 races, no year entirely missing)."
    - "MEDIUM #20 (PK-set union equality): output unique PK count == union of input partition unique PK counts, per table (not just a lower bound)."
    - "MEDIUM #21 (scope resolution — 2015-2024 vs 2015-2026/5): CONTEXT D-07 LOCKS the unified corpus to 2015-2026/5 (all real data). ROADMAP text update to 2015-2026/5 is DEFERRED to Phase 9 per CONTEXT D-07 explicit decision (see deferred list). Per CONTEXT.md Claude's Discretion, the plan HONORS D-07 (integrate all real data) and DOCUMENTS the ROADMAP-contract divergence in the SUMMARY. No period filter is applied — the unified corpus contains 2015-2026/5."
  artifacts:
    - path: "data/standard/race.parquet"
      provides: "Unified race table covering 2015-2026/5 (Kaggle + scraped merged, no duplicates, per-partition verified)"
    - path: "data/standard/entry.parquet"
      provides: "Unified entry table covering 2015-2026/5"
    - path: "data/standard/result.parquet"
      provides: "Unified result table covering 2015-2026/5"
  key_links:
    - from: "integrate_standard_layer invocation"
      to: "data/standard/{race,entry,result}.parquet"
      via: "tmp-swap (HIGH #6 from Plan 06-02)"
      pattern: "integrate_standard_layer"
    - from: "Phase 6 verification"
      to: "odds/payoff SHA-256 snapshot/restore (HIGH #17)"
      via: "pre+post integration checksum equality"
      pattern: "sha256"
---

<objective>
Execute the Phase 6 integration end-to-end against the real `data/standard/` tree (Kaggle 2015-2021 from Plan 06-01 + scraped 2022-2026/5 from the D-06 pre-task), producing the unified `data/standard/{race,entry,result}.parquet` corpus required by DATA-05 and the three ROADMAP success criteria.

This plan addresses FIVE HIGH defects from the cross-AI review (06-REVIEWS.md):
- HIGH #15 (D-06 pre-task not done): real corpus is `data/standard/scraped/202306` only. FIX: keep the halt-on-smoke-only gate; make the preflight robust; document the prerequisite clearly. The plan CANNOT complete until the full scrape runs.
- HIGH #16 (month-count gate weak): ≥40 dirs doesn't validate per-partition non-emptiness, 3-file presence, or date correctness. FIX: per-partition preflight checks all three.
- HIGH #17 (odds/payoff snapshot not in verify): described but not enforced. FIX: SHA-256 + row count in the actual automated verify command.
- HIGH #18 (graded count 780-880 post-integration wrong): scraped graded races push the unified count above Kaggle-only. FIX: verify graded count SEPARATELY per period (Kaggle 2015-2021 vs scraped 2022-2026/5), not a fixed band.
- HIGH #19 (date-range check too weak): single-month passes. FIX: assert actual min/max racedays + per-year race counts.

Plus MEDIUMs: #20 (PK-set union equality); #21 (scope resolution per CONTEXT D-07 — defer ROADMAP text update to Phase 9, no filter).

Output: Three unified Parquet files, the 9-test integration suite green, the verification report (with per-partition preflight, per-period graded counts, per-year race counts, odds/payoff SHA-256) captured, and a human-approved inspection result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md
@.planning/phases/06-data-integration/06-02-SUMMARY.md

# The integration module (built in Plan 06-02) and the Phase 4 schema-equality pattern
@src/pipeline/integration.py
@src/scraper/normalizer.py
@tests/scraper/test_end_to_end.py
@tests/pipeline/test_integration.py

# Phase 2 verification entry point (reused for the unified corpus)
@src/pipeline/validators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-partition preflight (HIGH #16) — verify each scraped month dir has 3 non-empty tables with valid race_date matching the dir name; halt if smoke-only or structurally incomplete</name>
  <files></files>
  <read_first>
    - data/standard/scraped/ (directory listing — current state: only `202306` = smoke 5 races; the preflight must detect this and HALT)
    - .planning/STATE.md (Blockers/Concerns — "前提タスク: 本格スクレイプ(2022-2026/5)未実行")
    - .planning/phases/06-data-integration/06-CONTEXT.md (D-06 — separate pre-task; D-07 — 2015-2026/5 scope)
    - src/scraper/normalizer.py (lines 383-401 `_parse_race_date` — string race_date "YYYY-MM-DD" parsing helper)
  </read_first>
  <action>
    Implement a per-partition preflight as a Python one-shot script (do NOT add a new module — this is a one-time gate). For each `data/standard/scraped/{YYYYMM}/` directory present:
    (1) assert 3 files exist: `race.parquet`, `entry.parquet`, `result.parquet` (HIGH #16 — 3-file presence).
    (2) read each via `pd.read_parquet(path, columns=[...])` and assert row count > 0 (HIGH #16 — non-empty).
    (3) read `race.parquet` columns `["race_id", "race_date"]` and assert every `race_date` value parses (via `pd.to_datetime(..., errors="coerce")`) AND the parsed `dt.strftime("%Y%m")` matches the partition dir name (HIGH #16 — valid date in partition). Log any mismatched race_id + race_date pairs (cap at 5 sample).
    Collect results into a report: total partitions, valid partitions, invalid partitions (with reasons), expected count (2022-01 through 2026-05 ≈ 53 months), missing months.

    Decision matrix:
    (a) If total valid partitions < 40 OR the smoke-only signature (only `202306` present): HALT with exit code 1 and a message: "BLOCKING: scraped corpus is smoke-only or structurally incomplete (X valid partitions); run D-06 pre-task (full scrape 2022-2026/5) via /gsd-quick or a Phase 4 extension before continuing Plan 06-03." Do NOT proceed to Task 2.
    (b) If > 20% of present partitions are invalid (3-file/empty/date failures): HALT with exit code 1 and a per-partition failure report. The user must re-run the scrape for the broken months.
    (c) Otherwise: proceed to Task 2. Tolerate a small number of missing months (JRA off-months) but log them.

    Implement this as `python -c "..."` inline OR write a throwaway script `scripts/preflight_scraped.py` and invoke it; either is fine. The verify command below is the authoritative gate.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
from pathlib import Path
import pandas as pd
import sys
root = Path('data/standard/scraped')
if not root.exists():
    print('BLOCKING: data/standard/scraped/ does not exist; run D-06 pre-task first'); sys.exit(1)
dirs = sorted(d for d in root.iterdir() if d.is_dir())
print(f'partition count: {len(dirs)}')
if len(dirs) < 40:
    print(f'BLOCKING: only {len(dirs)} scraped partitions (< 40); smoke-only or incomplete; run D-06 full scrape (2022-2026/5) first'); sys.exit(1)
invalid = []
for d in dirs:
    yyyymm = d.name
    for tbl in ('race', 'entry', 'result'):
        p = d / f'{tbl}.parquet'
        if not p.exists():
            invalid.append((yyyymm, tbl, 'missing file')); continue
        try:
            n = len(pd.read_parquet(p, columns=[list(pd.read_parquet(p, columns=[]).columns)[0] if False else None] or None))
        except Exception:
            # fall back to reading schema
            pass
        # for race, also check date matches dir
    rp = d / 'race.parquet'
    if rp.exists():
        try:
            rdf = pd.read_parquet(rp, columns=['race_id','race_date'])
            if len(rdf) == 0:
                invalid.append((yyyymm, 'race', 'empty')); 
            else:
                parsed = pd.to_datetime(rdf['race_date'], errors='coerce')
                bad = rdf.loc[parsed.isna() | (parsed.dt.strftime('%Y%m') != yyyymm)]
                if len(bad) > 0:
                    invalid.append((yyyymm, 'race', f'{len(bad)} rows with date != {yyyymm}'))
        except Exception as e:
            invalid.append((yyyymm, 'race', f'read error: {e}'))
print(f'invalid partitions: {len(invalid)}')
for x in invalid[:10]:
    print('  INVALID:', x)
if len(invalid) > len(dirs) * 0.2:
    print(f'BLOCKING: >20% of scraped partitions invalid ({len(invalid)}/{len(dirs)}); re-run scrape for broken months'); sys.exit(1)
print('OK: preflight passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - Preflight runs against every `data/standard/scraped/{YYYYMM}/` partition
    - Each partition is checked for 3-file presence, non-empty, valid race_date matching dir name (HIGH #16)
    - If smoke-only (< 40 partitions) or structurally broken (> 20% invalid): verify exits non-zero with a BLOCKING message; plan halts; Task 2 is NOT attempted
    - If preflight passes: partition report (count, valid, invalid, missing months) is documented in plan SUMMARY
  </acceptance_criteria>
  <done>
    - Preflight report captured (partition count, validity, missing months)
    - Decision (proceed vs halt) recorded
    - If halt: verify gate exited non-zero; user informed to run D-06 pre-task
  </done>
</task>

<task type="auto">
  <name>Task 2: Snapshot odds/payoff (HIGH #17) → invoke integrate_standard_layer → assert per-period graded counts (HIGH #18), per-year race counts (HIGH #19), PK-set union (MEDIUM #20), odds/payoff SHA-256 unchanged (HIGH #17)</name>
  <files>data/standard/race.parquet, data/standard/entry.parquet, data/standard/result.parquet</files>
  <read_first>
    - src/pipeline/integration.py (the module from Plan 06-02; verify `integrate_standard_layer(standard_dir, kaggle_input_dir=None)` signature and the tmp-swap behavior)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Code Examples — Verified end-to-end integration recipe" lines 324-376; "Phase 4 TestSchemaCompatibility pattern" lines 380-401; "Phase 1 audit_leakage reuse" lines 404-417)
    - tests/scraper/test_end_to_end.py (lines 449-536 — TestSchemaCompatibility pattern)
    - src/schemas/audit.py (audit_leakage signature)
    - src/scraper/flag_crosswalk.py (lines 110-130 _GRADE_REGEX — to recompute expected graded count per period)
  </read_first>
  <action>
    STEP 0 (HIGH #17 — snapshot BEFORE integration): compute SHA-256 + row count of `data/standard/odds_trifecta.parquet` and `data/standard/payoff.parquet`. Store these in variables for post-integration comparison. This MUST happen before any write to `data/standard/`.

    STEP 1 (invoke integration): `python -c "from pathlib import Path; from src.pipeline.integration import integrate_standard_layer; r = integrate_standard_layer(Path('data/standard')); print('wrote:', r)"`. This overwrites `data/standard/{race,entry,result}.parquet` atomically via the Plan-06-02 tmp-swap (HIGH #6).

    STEP 2 (HIGH #17 — assert odds/payoff UNCHANGED): recompute SHA-256 + row count of odds_trifecta.parquet and payoff.parquet AFTER integration; assert byte-identical to STEP 0 snapshots. If any differ, the integration wrote odds/payoff (D-05 violation) — STOP and investigate.

    STEP 3 (HIGH #18 — per-period graded count): split the unified race.parquet into Kaggle-period rows (race_date < '2022-01-01') and scraped-period rows (race_date >= '2022-01-01'). For each period: count `race_flag_graded_stakes == True` AND recompute the expected count via `derive_race_flags(grade, race_name)` on the same rows (mirror Plan 06-01 Task 3's derivation check). Assert actual == expected per period. The unified count is just the sum — NOT compared to a fixed band.

    STEP 4 (HIGH #19 — robust date range): assert `race_date.min()` is in 2015-01..2015-03 (the first JRA raceday of 2015), and `race_date.max()` is in 2026-01..2026-06 (if D-07 extended to 2026/5) OR 2024-10..2024-12 (if ROADMAP contract 2015-2024 — see scope resolution below). Also compute per-year race counts (`df.groupby(df['race_date'].str[:4])['race_id'].count()`) and assert each year 2015..end has > 1000 races (no year entirely missing — a missing year would indicate a scrape gap).

    STEP 5 (MEDIUM #20 — PK-set union equality): for each table, compute `set(input_partition_Pks) == set(output_Pks)`. Input partitions = Kaggle kaggle_input_dir/{table}.parquet PKs UNION each scraped partition's PKs. Output = unified {table}.parquet PKs. Assert set equality (not a lower bound).

    STEP 6 (full Phase 6 test suite): `python -m pytest tests/pipeline/test_integration.py -q` — all 9 tests green including the previously-gated slow-path tests (test_unified_race_date_range, test_row_counts_within_expected_bounds).

    STEP 7 (capture verification report): write to the plan SUMMARY — row counts per table, race_date min/max + per-year counts, per-period graded counts, PK-set union sizes, odds/payoff SHA-256 + row counts pre/post, audit_leakage results, FK integrity from validate_integrity.

    SCOPE RESOLUTION (MEDIUM #21 — per CONTEXT D-07): CONTEXT D-07 LOCKS the unified corpus to 2015-2026/5 ("実データ全部"). ROADMAP success criterion #3 says 2015-2024; CONTEXT D-07 EXPLICITLY extends this ("ROADMAP 成功基準#3 の 2015-2024 から拡張"). CONTEXT's deferred list says the ROADMAP TEXT update to 2015-2026/5 is deferred to Phase 9 planning. Therefore: NO period filter is applied — the unified corpus contains 2015-2026/5. The expected race_date.max() is in 2026-01..2026-06. Document this scope decision in the SUMMARY; flag the ROADMAP-text-vs-actual divergence for Phase 9 to resolve.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
import hashlib
from pathlib import Path
import pandas as pd

# STEP 0: snapshot odds/payoff BEFORE integration
def snapshot(p):
    p = Path(p)
    if not p.exists(): return None
    h = hashlib.sha256(p.read_bytes()).hexdigest()
    n = len(pd.read_parquet(p, columns=[pd.read_parquet(p, columns=[]).columns[0]] if len(pd.read_parquet(p, columns=[]).columns) else None))
    return (h, n)
pre_odds = snapshot('data/standard/odds_trifecta.parquet')
pre_payoff = snapshot('data/standard/payoff.parquet')
print('pre odds:', pre_odds); print('pre payoff:', pre_payoff)

# STEP 1: invoke integration
from src.pipeline.integration import integrate_standard_layer
r = integrate_standard_layer(Path('data/standard'))
print('wrote:', r)

# STEP 2: odds/payoff UNCHANGED (HIGH #17)
post_odds = snapshot('data/standard/odds_trifecta.parquet')
post_payoff = snapshot('data/standard/payoff.parquet')
print('post odds:', post_odds); print('post payoff:', post_payoff)
assert pre_odds == post_odds, f'odds_trifecta.parquet CHANGED (D-05 violation): pre={pre_odds} post={post_odds}'
assert pre_payoff == post_payoff, f'payoff.parquet CHANGED (D-05 violation): pre={pre_payoff} post={post_payoff}'

# STEP 3: per-period graded count (HIGH #18)
from src.scraper.flag_crosswalk import derive_race_flags
df = pd.read_parquet('data/standard/race.parquet')
df['_period'] = df['race_date'].apply(lambda s: 'kaggle' if str(s) < '2022-01-01' else 'scraped')
for period, sub in df.groupby('_period'):
    actual = int(sub['race_flag_graded_stakes'].fillna(False).sum())
    expected = sum(1 for _, rr in sub.iterrows() if derive_race_flags(str(rr.get('grade') or ''), str(rr.get('race_name') or '')).get('race_flag_graded_stakes') is True)
    print(f'period={period}: graded actual={actual} expected={expected}')
    assert actual == expected, f'{period} graded mismatch: actual={actual} expected={expected}'

# STEP 4: robust date range (HIGH #19)
dmin = str(df['race_date'].min()); dmax = str(df['race_date'].max())
print(f'race_date min={dmin} max={dmax}')
assert dmin >= '2015-01-01' and dmin <= '2015-03-31', f'unexpected race_date.min={dmin}'
assert dmax >= '2026-01-01' and dmax <= '2026-06-30', f'unexpected race_date.max={dmax} (D-07 expects 2026/5)'
per_year = df.groupby(df['race_date'].str[:4])['race_id'].count()
print('per-year race counts:'); print(per_year)
for yr in [str(y) for y in range(2015, 2027)]:
    if yr in per_year.index:
        assert per_year[yr] > 500, f'year {yr} has only {per_year[yr]} races (< 500) — likely scrape gap'
    elif yr < '2026':
        print(f'WARNING: year {yr} missing from corpus')

# STEP 5: PK-set union equality (MEDIUM #20)
out_race_pks = set(pd.read_parquet('data/standard/race.parquet', columns=['race_id'])['race_id'])
# input union: Kaggle kaggle/ + all scraped partitions
in_pks = set()
kp = Path('data/standard/kaggle/race.parquet')
if kp.exists(): in_pks |= set(pd.read_parquet(kp, columns=['race_id'])['race_id'])
for d in sorted(Path('data/standard/scraped').iterdir()):
    p = d / 'race.parquet'
    if p.exists(): in_pks |= set(pd.read_parquet(p, columns=['race_id'])['race_id'])
print(f'input PK count={len(in_pks)} output PK count={len(out_race_pks)}')
assert in_pks == out_race_pks, f'PK-set mismatch: input={len(in_pks)} output={len(out_race_pks)}'

print('OK: integration verified — per-period graded, robust date range, PK-set union, odds/payoff unchanged')
" && python -m pytest tests/pipeline/test_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `integrate_standard_layer` returns a dict with keys {'race', 'entry', 'result'} mapping to existing Path objects
    - odds_trifecta.parquet + payoff.parquet SHA-256 + row counts IDENTICAL pre/post integration (HIGH #17 — in the actual verify command, not just prose)
    - Per-period graded counts: Kaggle-period actual == derived-from-grade expected; scraped-period actual == derived-from-grade expected (HIGH #18)
    - race_date.min() in 2015-01..2015-03; race_date.max() in 2026-01..2026-06 (D-07 extended scope); every year 2015..2025 has > 500 races (HIGH #19)
    - PK-set union: input partition PKs == output unique PKs, per table (MEDIUM #20)
    - `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns; every race_flag_* is Arrow bool
    - `pd.read_parquet('data/standard/race.parquet')['race_id'].nunique() == len(...)` (no duplicate race_ids)
    - `audit_leakage([RaceSchema], race_df) == []`; `set(audit_leakage([EntrySchema], entry_df)) == {'popularity', 'win_odds'}`
    - `python -m pytest tests/pipeline/test_integration.py -q` green INCLUDING the 2 slow-path tests
  </acceptance_criteria>
  <done>
    - Unified corpus written; integrate_standard_layer returned 3 paths
    - All 9 integration tests green (slow-path gates cleared)
    - Verification report (per-partition preflight, per-period graded, per-year race counts, PK-set union, odds/payoff SHA-256) captured in SUMMARY
    - Scope decision (D-07 — 2015-2026/5, no filter; ROADMAP text update deferred to Phase 9) documented in SUMMARY
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human inspection of the unified corpus (end-of-phase gate)</name>
  <files></files>
  <action>
    End-of-phase human verification gate. Per `workflow.human_verify_mode=end-of-phase`, the user reviews the unified corpus produced by Task 2 and the verification report captured in 06-03-SUMMARY.md, then approves or describes issues. This task performs no automated code changes — it is a blocking confirmation that the three ROADMAP success criteria are TRUE from the user's perspective.
  </action>
  <verify>
    <automated>echo "human-verify checkpoint — no automated verify; gating on resume-signal='approved'"</automated>
    <human-check>User runs `python -m pytest tests/ -q` (full suite green) and `python -c "import pyarrow.parquet as pq; print([(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet') if str(f.type)=='null'])"` (returns empty list); confirms odds_trifecta/payoff SHA-256 unchanged via the report in SUMMARY.</human-check>
  </verify>
  <done>User typed "approved" in response to the resume-signal prompt.</done>
  <what-built>
    Phase 6 has produced the unified `data/standard/{race,entry,result}.parquet` corpus (Kaggle 2015-2021 post-D-01/D-02 from Plan 06-01 + scraped 2022-2026/5 from D-06 pre-task, merged via the idempotent + transactional + FK-validating + column-set-equality-guarded integrate_standard_layer from Plan 06-02). All 9 integration tests pass, per-partition preflight is clean, per-period graded counts match grade-regex derivation, per-year race counts show no gaps, odds/payoff SHA-256 unchanged.
  </what-built>
  <how-to-verify>
    1. Review the verification report captured in 06-03-SUMMARY.md (per-partition preflight, per-period graded counts, per-year race counts, PK-set union sizes, odds/payoff SHA-256 + row counts pre/post, audit results, FK integrity).
    2. Run `python -m pytest tests/ -q` and confirm the FULL suite (Phase 1/2/3/4/6) is green — no regressions.
    3. Inspect `data/standard/race.parquet` schema via `python -c "import pyarrow.parquet as pq; [print(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet')]"`. Confirm every `race_flag_*` column is Arrow `bool` (not `null`); `race_date` is `string`; `distance` is `int64`.
    4. Spot-check that `data/standard/odds_trifecta.parquet` and `payoff.parquet` SHA-256 values match the pre-integration snapshot in SUMMARY (D-05 honor).
    5. Confirm the per-period graded counts in SUMMARY: Kaggle-period count matches Plan 06-01's derivation; scraped-period count is plausible (hundreds of graded races across 2022-2026/5).
    6. Note the scope decision: unified corpus is 2015-2026/5 per CONTEXT D-07 (ROADMAP text update deferred to Phase 9).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 6 integration → Phase 7/9 downstream | Unified corpus becomes input to model training + backtesting. Schema/dedup/FK integrity propagates downstream. |
| Integration write → existing Phase 2/4 outputs | Destructive overwrite of `data/standard/{race,entry,result}.parquet`. Atomic tmp-swap + regenerable sources are the controls. |
| D-06 pre-task output → Phase 6 input | Scraped corpus is externally produced. Per-partition preflight (HIGH #16) is the trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | D-06 smoke-only corpus mistakenly treated as complete (HIGH #15) | mitigate | Task 1 per-partition preflight: < 40 partitions OR > 20% invalid → exit 1, halt. Does NOT weaken the gate. |
| T-06-12 | Tampering | Empty/partial/corrupt scraped partition silently integrated (HIGH #16) | mitigate | Per-partition 3-file presence + non-empty + race_date-matches-dir-name check. > 20% invalid → halt. |
| T-06-13 | Tampering | Success criterion #1 (no dups) silently false | mitigate | integrate_standard_layer's FAIL-LOUD pre-dedup + validate_integrity. |
| T-06-14 | Tampering | Success criterion #2 (schema identical) silently false | mitigate | Plan 06-02 column-set equality assert (HIGH #9) + pq.read_schema check. |
| T-06-15 | Denial of Service | Phase 5 seed (odds/payoff) destroyed (HIGH #17) | mitigate | SHA-256 + row count snapshot in the actual verify.automated command; assertion of byte-identical pre/post. |
| T-06-16 | Tampering | Graded count compared to wrong baseline (HIGH #18) | mitigate | Per-period (Kaggle vs scraped) graded count compared to grade-regex derivation, not a fixed band. |
| T-06-17 | Tampering | Single-month corpus passes date-range check (HIGH #19) | mitigate | Robust date assertions: min in 2015-01..03, max in 2026-01..06, every year > 500 races. |
| T-06-18 | Tampering | PK-set union drift (MEDIUM #20) | mitigate | Output unique PK set == input partition PK set union, per table. |
| T-06-19 | Repudiation | Phase 6 closed without human confirming | mitigate | Task 3 blocking checkpoint:human-verify. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` full suite green
- `python -m pytest tests/pipeline/test_integration.py -q` green (9 tests, slow-path gates cleared)
- `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns; every race_flag_* is Arrow bool; race_date is string; distance is int64
- `pd.read_parquet('data/standard/race.parquet')['race_id'].nunique() == len(...)` (no duplicates)
- `race_date.min()` in 2015-01..03; `.max()` in 2026-01..06 (D-07); every year 2015..2025 > 500 races
- Per-period graded counts match grade-regex derivation
- PK-set union equality per table
- odds_trifecta/payoff SHA-256 + row counts unchanged (HIGH #17 — in verify.automated)
- Human checkpoint: approved
</verification>

<success_criteria>
- HIGH #15 resolved: halt-on-smoke-only gate kept; per-partition preflight robust; D-06 prerequisite documented
- HIGH #16 resolved: per-partition 3-file/non-empty/date-check preflight
- HIGH #17 resolved: odds/payoff SHA-256 in actual verify.automated command
- HIGH #18 resolved: per-period graded count verification (not fixed band)
- HIGH #19 resolved: robust min/max + per-year race count assertions
- MEDIUM #20 resolved: PK-set union equality
- MEDIUM #21 resolved: scope = 2015-2026/5 per CONTEXT D-07; no filter; ROADMAP text update deferred to Phase 9
- ROADMAP success criteria #1/#2/#3 TRUE; DATA-05 delivered
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-03-SUMMARY.md` when done. Include: per-partition preflight report, per-period graded counts, per-year race counts, PK-set union sizes, odds/payoff SHA-256 + row counts pre/post, audit_leakage results, FK integrity, scope decision note (D-07 → 2015-2026/5, ROADMAP text update deferred).
</output>

## Artifacts this phase produces

- `data/standard/race.parquet` (unified 2015-2026/5, post-integration — Kaggle 2015-2021 + scraped 2022-2026/5 merged via idempotent + transactional integration)
- `data/standard/entry.parquet` (unified 2015-2026/5)
- `data/standard/result.parquet` (unified 2015-2026/5)
- `data/standard/odds_trifecta.parquet` (UNCHANGED — D-05 honor, SHA-256-verified)
- `data/standard/payoff.parquet` (UNCHANGED — D-05 honor, SHA-256-verified)
- Verification report (captured in 06-03-SUMMARY.md): per-partition preflight, per-period graded counts, per-year race counts, PK-set union sizes, odds/payoff SHA-256, audit_leakage results, FK integrity, scope decision note
- No new source symbols (integrate_standard_layer was created in Plan 06-02; this plan only INVOKES it against the real data path + runs the verification gate)

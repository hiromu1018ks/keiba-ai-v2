---
phase: 06-data-integration
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - data/standard/race.parquet
  - data/standard/entry.parquet
  - data/standard/result.parquet
autonomous: false
requirements: [DATA-05]
must_haves:
  truths:
    - "Unified race.parquet exists on disk with Kaggle 2015-2021 rows + scraped 2022-2026/5 rows merged, zero duplicate race_ids (observable via pd.read_parquet + nunique == len)"
    - "ROADMAP success criterion #1: NO duplicate race_id exists between Kaggle and scraped sources in the unified race.parquet (asserted via integrate_standard_layer's FAIL-LOUD pre-dedup check returning cleanly)"
    - "ROADMAP success criterion #2: Schema is identical across the full date range — verified via pyarrow.parquet.read_schema and str(field.type) equality, adapted from Phase 4 TestSchemaCompatibility"
    - "ROADMAP success criterion #3: Combined dataset spans 2015-01-01 through 2024-12-31 (extended to 2026-05-31 per D-07) with expected volume (race >21,929 + scraped_races, entry/result >311,806 + scraped_entries/results)"
    - "Phase 1 audit_leakage() on the unified race table returns []; on entry table returns exactly {popularity, win_odds} (no other post-race columns leaked)"
    - "odds_trifecta.parquet and payoff.parquet (Phase 5 seed) are UNCHANGED by Phase 6 (D-05)"
    - "Human has inspected the unified corpus (row count, date range, schema report) and confirmed it matches the expected contract"
  artifacts:
    - path: "data/standard/race.parquet"
      provides: "Unified race table covering 2015-01-01 through 2026-05-31 (Kaggle + scraped merged, no duplicates)"
    - path: "data/standard/entry.parquet"
      provides: "Unified entry table covering 2015-2026/5"
    - path: "data/standard/result.parquet"
      provides: "Unified result table covering 2015-2026/5"
  key_links:
    - from: "integrate_standard_layer invocation"
      to: "data/standard/race.parquet"
      via: "_atomic_write_parquet overwrite (D-03)"
      pattern: "_atomic_write_parquet"
    - from: "Phase 6 verification"
      to: "tests/pipeline/test_integration.py::test_unified_race_date_range + test_row_counts_within_expected_bounds"
      via: "the previously-gated slow-path tests now run against the real unified corpus"
      pattern: "test_unified_race_date_range|test_row_counts_within_expected_bounds"
---

<objective>
Execute the Phase 6 integration end-to-end against the real `data/standard/` tree (Kaggle 2015-2021 regenerated by Plan 06-01 + scraped 2022-2026/5 produced by the D-06 pre-task), producing the unified `data/standard/{race,entry,result}.parquet` corpus required by DATA-05 and the three ROADMAP success criteria. Then run the full Phase 6 verification suite (the 8 tests from Plan 06-02 with their slow-path gates now cleared), reuse the Phase 4 `TestSchemaCompatibility` physical-type equality pattern on the integrated output, and pause for a human inspection checkpoint (per `workflow.human_verify_mode=end-of-phase`).

Purpose: This is the actual DATA-05 delivery contract — the three ROADMAP success criteria must be TRUE before the phase closes. Phase 7 (model) and Phase 9 (backtest) depend on this corpus existing.

Output: Three unified Parquet files under `data/standard/`, the 8-test integration suite green, the schema-invariant verification report captured, and a human-approved inspection result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md
@.planning/phases/06-data-integration/06-02-SUMMARY.md

# The integration module (built in Plan 06-02) and the Phase 4 schema-equality pattern to reuse
@src/pipeline/integration.py
@src/scraper/normalizer.py
@tests/scraper/test_end_to_end.py
@tests/pipeline/test_integration.py

# Phase 2 verification entry point (reused for the unified corpus)
@src/pipeline/validators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify D-06 pre-task status (scraped corpus present) — halt with a blocking exit code if smoke-only</name>
  <files></files>
  <read_first>
    - data/standard/scraped/ (directory listing — should contain month dirs 202201 .. 202605 if D-06 pre-task completed; only 202306 if smoke-only)
    - .planning/STATE.md (Blockers/Concerns section — D-06 pre-task status; "Phase 6 context gathered — 前提タスク: 本格スクレイプ(2022-2026/5)未実行")
    - .planning/phases/06-data-integration/06-CONTEXT.md (D-06 decision text — "Phase 6 は統合ロジックのみを担い、スクレイプ済みデータを入力とする")
  </read_first>
  <action>
    Inspect `data/standard/scraped/` directory contents. Per RESEARCH.md Open Question 1, Phase 6 should ASSERT that every expected `{YYYYMM}/` directory exists before merge, but TOLERATE missing months (log a warning — JRA does have off months). Decision matrix: (a) if `data/standard/scraped/` contains >= 12 month directories spanning 2022-01 through 2026-05 (or close to it — off months tolerated) → proceed to Task 2 with the full corpus. (b) if `data/standard/scraped/` contains only `202306` (smoke 5 races) → this is the SMOKE-ONLY state flagged in RESEARCH.md; STOP the plan and return a blocking message instructing the user to run the D-06 full scrape first via `/gsd-quick` or a Phase 4 extension (per CONTEXT.md D-06), because ROADMAP success criterion #3 (date coverage 2015-2024, extended to 2026-05 per D-07) cannot be verified against smoke-only data. Do NOT proceed to Task 2 in case (b) — invoking integrate_standard_layer() on smoke data would technically succeed but would fail success criterion #3 and produce a misleadingly small unified corpus. Document the directory listing (month count + earliest/latest YYYYMM) in the plan summary. Per RESEARCH.md: "tolerate missing months (just log a warning, since JRA does have off months)" — the threshold is structural coverage of the 2022-2026/5 window (>=40 of ~53 expected month dirs), not perfect monthly completeness.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && n=$(ls data/standard/scraped/ 2>/dev/null | wc -l | tr -d ' '); echo "scraped month dir count: $n"; test "$n" -ge 40 || { echo "BLOCKING: scraped corpus is smoke-only ($n dirs < 40); run D-06 pre-task first"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - If scraped corpus is structurally complete (>= ~40 of ~53 expected month dirs across 2022-01 .. 2026-05 — off months tolerated): proceed; document the directory count and earliest/latest month in the plan summary
    - If scraped corpus is smoke-only (fewer than 40 month dirs): the verify command exits non-zero with a BLOCKING message and the plan halts; do NOT invoke integrate_standard_layer() against the real data path
  </acceptance_criteria>
  <done>
    - Scraped corpus status documented (count, date range)
    - Decision (proceed vs halt) made and recorded; if halt, the verify gate exits non-zero and the user is informed that D-06 pre-task must run before Task 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Invoke integrate_standard_layer() against the real data/standard/ tree; capture the unified-corpus verification report</name>
  <files>data/standard/race.parquet, data/standard/entry.parquet, data/standard/result.parquet</files>
  <read_first>
    - src/pipeline/integration.py (the module from Plan 06-02; verify integrate_standard_layer signature and return contract)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Code Examples — Verified end-to-end integration recipe" lines 324-376 — the SPEC; section "Phase 4 TestSchemaCompatibility pattern (reuse for Phase 6)" lines 380-401; section "Phase 1 audit_leakage reuse" lines 404-417)
    - tests/scraper/test_end_to_end.py (lines 449-536 — the TestSchemaCompatibility pattern; adapt the body for the unified corpus output)
    - src/schemas/audit.py (audit_leakage signature)
  </read_first>
  <action>
    Invoke `integrate_standard_layer(standard_dir=Path("data/standard"))` via `poetry run python -c "from pathlib import Path; from src.pipeline.integration import integrate_standard_layer; result = integrate_standard_layer(Path('data/standard')); print(result)"`. This overwrites `data/standard/{race,entry,result}.parquet` atomically (D-03). Then capture a verification report by running: (1) `pyarrow.parquet.read_schema('data/standard/race.parquet')` and assert zero Arrow-null columns and every race_flag_* has str(type)=='bool' (Pattern 5 / Phase 4 TestSchemaCompatibility reuse). (2) pandas row counts for race/entry/result + race_date min/max (assert min starts with '2015' and max starts with '2026' or '2024' depending on D-07 actual coverage). (3) `race_id.nunique() == len(race_df)` (no duplicate race_ids — success criterion #1). (4) Referential integrity: every entry.race_id and result.race_id in set(race.race_id). (5) `audit_leakage([RaceSchema], race_df, 'post-integration race audit') == []` and `set(audit_leakage([EntrySchema], entry_df, 'post-integration entry audit')) == {'popularity', 'win_odds'}` (RESEARCH.md Open Question 2). (6) odds_trifecta.parquet and payoff.parquet row counts UNCHANGED from pre-integration snapshot (D-05 — use a pre-call snapshot via pandas.read_parquet for the row count, then assert equality post-call). Write the report (counts, schema types, audit results) to the plan summary.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && poetry run python -c "from pathlib import Path; from src.pipeline.integration import integrate_standard_layer; r = integrate_standard_layer(Path('data/standard')); print('wrote:', r)" && poetry run pytest tests/pipeline/test_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `integrate_standard_layer` returns a dict with keys {'race', 'entry', 'result'} mapping to existing Path objects
    - `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero columns with str(type)=='null'
    - `pd.read_parquet('data/standard/race.parquet')['race_id'].nunique() == pd.read_parquet('data/standard/race.parquet').shape[0]` (no duplicate race_ids)
    - `pd.read_parquet('data/standard/race.parquet')['race_date'].min()` starts with '2015' and `.max()` starts with '2024' or '2026' (per D-07 coverage)
    - `audit_leakage([RaceSchema], race_df)` returns []; `set(audit_leakage([EntrySchema], entry_df))` == {'popularity', 'win_odds'}
    - odds_trifecta.parquet and payoff.parquet row counts unchanged from pre-integration snapshot
    - `poetry run pytest tests/pipeline/test_integration.py -q` is green INCLUDING test_unified_race_date_range and test_row_counts_within_expected_bounds (slow-path gates now cleared)
  </acceptance_criteria>
  <done>
    - Unified corpus written; integrate_standard_layer returned 3 paths
    - All 8 tests in tests/pipeline/test_integration.py green
    - Verification report (counts, schema, audit, FK integrity, date range) captured in the plan summary
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human inspection of the unified corpus (end-of-phase gate)</name>
  <files></files>
  <action>
    End-of-phase human verification gate. Per `workflow.human_verify_mode=end-of-phase`, the user reviews the unified corpus produced by Task 2 and the verification report captured in 06-03-SUMMARY.md, then approves or describes issues. This task performs no automated code changes — it is a blocking confirmation that the three ROADMAP success criteria are TRUE from the user's perspective.
  </action>
  <verify>
    <automated>echo "human-verify checkpoint — no automated verify; gating on resume-signal='approved'"</automated>
    <human-check>User runs `poetry run pytest tests/ -q` (full suite green) and `poetry run python -c "import pyarrow.parquet as pq; print([(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet') if str(f.type)=='null'])"` (returns empty list); confirms odds_trifecta/payoff row counts unchanged.</human-check>
  </verify>
  <done>User typed "approved" in response to the resume-signal prompt.</done>
  <what-built>
    Phase 6 has produced the unified `data/standard/{race,entry,result}.parquet` corpus (Kaggle 2015-2021 regenerated with D-01/D-02 fixes + scraped 2022-2026/5 merged via integrate_standard_layer). All 8 integration tests pass, the schema is invariant across the full date range, the Phase 1 audit_leakage check passes, and odds_trifecta/payoff are preserved (D-05). This checkpoint verifies the result from the user's perspective.
  </what-built>
  <how-to-verify>
    1. Review the verification report captured in 06-03-SUMMARY.md (row counts, date range, schema types, audit results, FK integrity). Confirm the numbers match expectations: race table row count ~21,929 (Kaggle) + scraped race count (from D-06 pre-task); entry/result tables ~311,806 (Kaggle) + scraped counts. Race date range: min in 2015, max in 2026 (or 2024 if D-07 actual coverage stopped there).
    2. Run `poetry run pytest tests/ -q` and confirm the FULL suite (Phase 1/2/3/4/6) is green — no regressions from the Kaggle-side dtype changes.
    3. Inspect `data/standard/race.parquet` schema via `poetry run python -c "import pyarrow.parquet as pq; [print(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet')]"`. Confirm every race_flag_* column is Arrow `bool` (not `null`) — this is success criterion #2 (Kaggle/scraped indistinguishable).
    4. Spot-check that `data/standard/odds_trifecta.parquet` and `payoff.parquet` still exist with unchanged row counts (D-05 honor — these are the Phase 5 seed and must be preserved).
    5. Confirm `graded_stakes=True` count on the unified race.parquet is in the 780-880 range (D-01 effect propagated through integration; should match Plan 06-01's count since scraped data has very few graded-stakes-international-designation races that would shift the count).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 6 integration → Phase 7/9 downstream | The unified corpus becomes the input to model training and backtesting. Schema/dedup integrity here propagates to every downstream phase. |
| Integration write → existing Phase 2/4 outputs | Destructive overwrite of `data/standard/{race,entry,result}.parquet`. Atomic write + regenerable raw sources are the safety controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | D-06 pre-task smoke-only corpus mistakenly treated as complete | mitigate | Task 1 explicitly inspects `data/standard/scraped/` directory count and HALTS with exit code 1 if fewer than 40 month dirs (smoke-only); refuses to invoke integration on incomplete data (which would fail success criterion #3). |
| T-06-12 | Tampering | Success criterion #1 (no duplicate races) silently false | mitigate | integrate_standard_layer's FAIL-LOUD pre-dedup assertion (from Plan 06-02) refuses to write if any duplicate race_id exists post-concat. Verified by the integration succeeding cleanly (any duplicate would have raised). |
| T-06-13 | Tampering | Success criterion #2 (schema identical) silently false | mitigate | Task 2 reads `pyarrow.parquet.read_schema('data/standard/race.parquet')` and asserts zero Arrow-null columns + every race_flag_* is Arrow bool. Pattern 5 / Phase 4 TestSchemaCompatibility reuse. Verified by test_schema_invariant_post_integration. |
| T-06-14 | Denial of Service | Phase 5 seed (odds_trifecta/payoff) destroyed by destructive overwrite | mitigate | Task 2 snapshots odds/payoff row counts pre-integration and asserts unchanged post-integration. D-05 + Plan 06-02's hardcoded 3-table allowlist (SCHEMA_BY_TABLE = {race, entry, result}). Verified by test_odds_payoff_not_overwritten. |
| T-06-15 | Repudiation | Phase 6 closed without human confirming the corpus | mitigate | Task 3 is a blocking checkpoint:human-verify gate. Per `workflow.human_verify_mode=end-of-phase`, the user must approve before the phase closes. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies in this plan. |
</threat_model>

<verification>
- `poetry run pytest tests/ -q` full suite green (Phase 1/2/3/4/6 — no regressions)
- `poetry run pytest tests/pipeline/test_integration.py -q` green (8 tests, slow-path gates cleared)
- `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns; every race_flag_* is Arrow bool
- `pd.read_parquet('data/standard/race.parquet')['race_id'].nunique() == len(pd.read_parquet('data/standard/race.parquet'))` (no duplicates)
- `pd.read_parquet('data/standard/race.parquet')['race_date'].min()` starts with '2015'; `.max()` starts with '2024' or '2026'
- `audit_leakage([RaceSchema], race_df) == []` and `set(audit_leakage([EntrySchema], entry_df))` == {'popularity', 'win_odds'}
- `odds_trifecta.parquet` and `payoff.parquet` row counts unchanged
- Human checkpoint: approved
</verification>

<success_criteria>
- ROADMAP success criterion #1 TRUE: zero duplicate race_id between Kaggle and scraped sources (integrate_standard_layer returned cleanly; FAIL-LOUD did not fire)
- ROADMAP success criterion #2 TRUE: schema identical across full date range (zero Arrow-null columns, every race_flag_* is Arrow bool, pyarrow physical-type equality verified)
- ROADMAP success criterion #3 TRUE: unified corpus spans 2015-01-01 through 2024-12-31 (extended to 2026-05-31 per D-07) with expected volume (Kaggle + scraped)
- DATA-05 delivered: 2015-2024+ data in unified standard Parquet, ready for Phase 3 feature regeneration and Phase 7 model training
- D-05 honor verified: odds_trifecta/payoff preserved (Phase 5 seed intact)
- Human inspection: approved
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-03-SUMMARY.md` when done (include the verification report: row counts, date range, schema types, audit results, FK integrity, odds/payoff preservation proof)
</output>

## Artifacts this phase produces

- `data/standard/race.parquet` (unified 2015-2026/5, post-integration — Kaggle 2015-2021 + scraped 2022-2026/5 merged)
- `data/standard/entry.parquet` (unified 2015-2026/5)
- `data/standard/result.parquet` (unified 2015-2026/5)
- Verification report (captured in 06-03-SUMMARY.md): row counts, race_date min/max, schema types per column, audit_leakage results, FK integrity counts, odds/payoff preservation snapshot
- No new source symbols (integrate_standard_layer was created in Plan 06-02; this plan only INVOKES it against the real data path)
- New observable state: the three unified Parquet files exist on disk with the date range and row counts documented in the summary

---
phase: 06-data-integration
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - data/standard/race.parquet
  - data/standard/entry.parquet
  - data/standard/result.parquet
autonomous: false
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #15 (D-06 pre-task — KEPT from cycle 2): the full scrape (2022-2026/5) is documented as a hard prerequisite. The preflight (Task 1) verifies per-partition completeness. If the corpus is smoke-only, the plan HALTS with exit code 1. The plan does NOT weaken the halt gate."
    - "HIGH #16 (per-partition preflight — cycle-2 PARTIALLY RESOLVED, cycle-3 STRENGTHENED): every present scraped partition is checked for 3-file presence (race/entry/result.parquet) AND non-emptiness (row count > 0 for ALL THREE, via pyarrow.parquet.ParquetFile(path).metadata.num_rows) AND valid race_date in race.parquet whose YYYYMM matches the partition dir name. The cycle-2 verify swallowed exceptions and never asserted `n > 0` for entry/result; the cycle-3 verify asserts all 3 files non-empty via pyarrow metadata and re-raises (does NOT swallow) exceptions."
    - "HIGH #16 (preflight partition count — KEPT): assert partition count >= 40 (smoke-only halt gate)."
    - "HIGH #17 (odds/payoff snapshot IN verify.automated — KEPT from cycle 2): SHA-256 + row count of odds_trifecta.parquet and payoff.parquet captured BEFORE integration and asserted UNCHANGED AFTER, in the actual verify.automated command."
    - "HIGH #18 (graded count split by period — KEPT from cycle 2): graded_stakes count verified SEPARATELY for the Kaggle period (2015-2021 rows) and the scraped period (2022-2026/5 rows), each compared to derive_race_flags derivation. NOT a fixed 780-880 band."
    - "HIGH #19 (date-range robustness — cycle-2 PARTIALLY RESOLVED, cycle-3 STRENGTHENED): assert BOTH (a) `dmax == actual_scraped_max` (proves no data dropped DURING integration — the cycle-2 self-referential check, KEPT) AND (b) `actual_scraped_max >= EXPECTED_FLOOR` where EXPECTED_FLOOR = '2024-01-01' (the cycle-3 NEW floor — catches an incomplete D-06 scrape, NOT just integration drop). ALSO assert `dmin` is in early 2015 ('2015-01-01' <= dmin <= '2015-03-31'). Per-year race counts asserted (each present year 2015..end has > 500 races). The cycle-2 self-referential weakness is resolved: a scrape that stopped at 2023-12 now FAILS the floor assertion (real gap), rather than passing because dmax == actual_scraped_max."
    - "MEDIUM #20 (PK-set union equality — cycle-3 EXTENDED to entry + result): output unique PK set == union of input partition unique PK sets, PER TABLE (race AND entry AND result — NOT just race). The cycle-2 verify checked only race.parquet; the cycle-3 verify mirrors the check for entry.parquet (PK = horse_race_id) and result.parquet (PK = horse_race_id), catching entry/result PK drift during merge."
    - "MEDIUM #21 (scope resolution — KEPT from cycle 2): CONTEXT D-07 LOCKS the unified corpus to 2015-2026/5. ROADMAP text update DEFERRED to Phase 9. The plan HONORS D-07 (integrate all real data) and DOCUMENTS the ROADMAP-contract divergence in the SUMMARY. No period filter applied."
    - "HIGH #3 (8-point run_all_validations — MOVED here from 06-01-T3 per cycle-3 design): the FULL 8-point run_all_validations(raw_dir, parquet_dir=Path('data/standard'), source_counts, source_stats) runs HERE against the unified ROOT where all 5 tables coexist (unified race/entry/result + Phase 5 odds/payoff). source_counts + source_stats are supplied so all 8 checks run. overall_pass=True. This is the natural validation point: it validates the FINAL unified standard tree."
requirements_resolved:
  - "HIGH #11 (cycle 2 NOT RESOLVED → cycle 3 RESOLVED): preflight asserts each partition race/entry/result non-empty via pyarrow.parquet.ParquetFile(path).metadata.num_rows > 0; partition count >= 40; exceptions re-raised (not swallowed)"
  - "HIGH #14 (cycle 2 PARTIALLY RESOLVED → cycle 3 RESOLVED): dmax == actual_scraped_max (no drop during integration, KEPT) AND actual_scraped_max >= '2024-01-01' floor (NEW — catches incomplete D-06 scrape) AND dmin in 2015-01..03"
  - "NEW HIGH (cycle 2: PK-set union race-only → cycle 3 RESOLVED): PK-set union verify extended to entry.parquet AND result.parquet (not just race)"
  - "HIGH #3 (cycle 2 PARTIALLY RESOLVED → cycle 3 RESOLVED): full 8-point run_all_validations moved from 06-01-T3 to 06-03-T2 against unified root (5 tables present)"
  - "HIGH #15/#16 gate (cycle 2 RESOLVED — KEPT): halt-on-smoke-only gate; partition count >= 40"
  - "HIGH #17 (cycle 2 RESOLVED — KEPT): odds/payoff SHA-256 in verify.automated"
  - "HIGH #18 (cycle 2 RESOLVED — KEPT): per-period graded counts"
artifacts:
  - path: "data/standard/race.parquet"
    provides: "Unified race table covering 2015-2026/5 (Kaggle + scraped merged, no duplicates, per-partition verified, 8-point validated)"
  - path: "data/standard/entry.parquet"
    provides: "Unified entry table covering 2015-2026/5"
  - path: "data/standard/result.parquet"
    provides: "Unified result table covering 2015-2026/5"
key_links:
  - from: "integrate_standard_layer invocation"
    to: "data/standard/{race,entry,result}.parquet"
    via: "tempfile.mkdtemp validate-before-swap (HIGH #6 cycle-3 from Plan 06-02)"
    pattern: "integrate_standard_layer"
  - from: "Phase 6 full 8-point validation"
    to: "run_all_validations against data/standard/ root"
    via: "5 tables present (unified race/entry/result + Phase 5 odds/payoff); source_counts + source_stats supplied"
    pattern: "run_all_validations"
  - from: "Phase 6 verification"
    to: "odds/payoff SHA-256 snapshot/restore (HIGH #17)"
    via: "pre+post integration checksum equality"
    pattern: "sha256"
---

<objective>
Execute the Phase 6 integration end-to-end against the real `data/standard/` tree (Kaggle 2015-2021 from Plan 06-01 + scraped 2022-2026/5 from the D-06 pre-task), producing the unified `data/standard/{race,entry,result}.parquet` corpus required by DATA-05 and the three ROADMAP success criteria.

This plan resolves THREE cycle-2 HIGH defects (the remaining unresolved ones for 06-03) plus relocates the HIGH #3 8-point validation here:

- **HIGH #11 (preflight does not assert entry/result non-empty — NOT RESOLVED in cycle 2):** cycle 2's prose described per-partition 3-file presence + non-empty, but the ACTUAL `<verify><automated>` command only read `race.parquet` for non-empty + date; the entry/result non-empty check wrapped `pd.read_parquet` in a bare `except Exception: pass` and the `n` variable was computed but NEVER asserted `> 0`. Empty `entry.parquet`/`result.parquet` files passed the verify. **FIX (cycle 3):** the preflight verify uses `pyarrow.parquet.ParquetFile(path).metadata.num_rows > 0` on ALL THREE files per partition (race/entry/result), asserts explicitly, and re-raises (does NOT swallow) exceptions. The partition count gate `>= 40` is KEPT.

- **HIGH #14 (actual_scraped_max self-referential — PARTIALLY RESOLVED in cycle 2):** cycle 2 asserted `dmax == actual_scraped_max`, which proves only that no data was dropped DURING integration — it accepts an incomplete D-06 scrape as "complete" (if the scrape stopped at 2023-12, both dmax and actual_scraped_max are 2023-12-x and the assertion passes despite the 2026-05 target being unreached). **FIX (cycle 3):** assert BOTH (a) `dmax == actual_scraped_max` (no drop during integration — the cycle-2 check, KEPT as a necessary condition) AND (b) `actual_scraped_max >= EXPECTED_FLOOR` where `EXPECTED_FLOOR = '2024-01-01'` (the cycle-3 NEW floor — a defensible contract floor derived from the ROADMAP success criterion #3 "2015-2024" and CONTEXT D-07's "2015-2026/5" extension; if the scrape covers less than 2024-01-01, that's a real gap, not a self-referential pass). ALSO assert `dmin` is in early 2015 ('2015-01-01' <= dmin <= '2015-03-31'). The EXPECTED_FLOOR is documented explicitly so the boundary is visible.

- **NEW HIGH (PK-set union verify race-only — NEW in cycle 2):** cycle 2's prose said "per table" but the actual `<verify><automated>` PK-set equality block only read `race.parquet`. Entry/result PK drift (dropped horses during merge) was undetected. **FIX (cycle 3):** extend the PK-set union equality block to `entry.parquet` (PK = horse_race_id) AND `result.parquet` (PK = horse_race_id), mirroring the race block. Assert each table's output unique PK set == union of input partition unique PK sets for that table.

- **HIGH #3 relocation (8-point run_all_validations — moved here from 06-01-T3 per cycle-3 design):** the FULL 8-point `run_all_validations(raw_dir, parquet_dir=Path('data/standard'), source_counts, source_stats)` runs HERE against the unified ROOT, where all 5 tables coexist (unified race/entry/result + Phase 5 odds/payoff). `source_counts` + `source_stats` are supplied so all 8 checks run (none skipped). `overall_pass=True`. This is the natural validation point: 06-01-T3 validated the 3-table kaggle/ subdir specifically (could not run the 5-table run_all_validations there); 06-03-T2 validates the FINAL unified standard tree where the 5-table contract is honored.

Plus KEPT from cycle 2: HIGH #15 (halt-on-smoke-only gate), HIGH #16 (per-partition 3-file presence + race_date match), HIGH #17 (odds/payoff SHA-256 in verify.automated), HIGH #18 (per-period graded counts), MEDIUM #20 (PK-set union — now extended to entry/result), MEDIUM #21 (scope resolution per CONTEXT D-07).

Output: Three unified Parquet files, the 10-test integration suite green, the full 8-point validation green against the unified root, the verification report (per-partition preflight, per-period graded counts, per-year race counts, PK-set union for all 3 tables, odds/payoff SHA-256) captured, and a human-approved inspection result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md
@.planning/phases/06-data-integration/06-02-SUMMARY.md

# The integration module (built in Plan 06-02) and the Phase 4 schema-equality pattern
@src/pipeline/integration.py
@src/scraper/normalizer.py
@tests/scraper/test_end_to_end.py
@tests/pipeline/test_integration.py

# Phase 2 verification entry point (reused for the unified corpus — 5 tables present at root)
@src/pipeline/validators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-partition preflight (HIGH #11 cycle-3 + HIGH #16) — verify each scraped month dir has 3 NON-EMPTY tables (pyarrow metadata num_rows > 0 for ALL THREE) with valid race_date matching dir name; assert partition count >= 40; halt if smoke-only or structurally incomplete; re-raise (do NOT swallow) exceptions</name>
  <files></files>
  <read_first>
    - data/standard/scraped/ (directory listing — current state: only `202306` = smoke 5 races; the preflight must detect this and HALT)
    - .planning/STATE.md (Blockers/Concerns — "前提タスク: 本格スクレイプ(2022-2026/5)未実行")
    - .planning/phases/06-data-integration/06-CONTEXT.md (D-06 — separate pre-task; D-07 — 2015-2026/5 scope)
    - src/scraper/normalizer.py (lines 383-401 `_parse_race_date` — string race_date "YYYY-MM-DD" parsing helper)
    - pyarrow.parquet.ParquetFile(path).metadata.num_rows — the authoritative row count without reading the full file (fast, no exception swallowing)
  </read_first>
  <action>
    Implement a per-partition preflight as a Python one-shot. For each `data/standard/scraped/{YYYYMM}/` directory present:
    (1) HIGH #16 — 3-file presence: assert `race.parquet`, `entry.parquet`, `result.parquet` all exist.
    (2) HIGH #11 cycle-3 — NON-EMPTY for ALL THREE: for each of the 3 files, read `pyarrow.parquet.ParquetFile(path).metadata.num_rows` and assert `> 0`. Do NOT wrap in bare `except Exception: pass` — re-raise (a read error is a real failure, not a skip). Do NOT compute `n` and forget to assert it (the cycle-2 bug).
    (3) HIGH #16 — race_date validity: read `race.parquet` columns `["race_id", "race_date"]`; assert every `race_date` parses via `pd.to_datetime(..., errors="coerce")` AND `dt.strftime("%Y%m")` matches the partition dir name. Log mismatched race_id + race_date pairs (cap at 5 sample).
    Collect results: total partitions, valid partitions, invalid partitions (with reasons), missing months.

    HIGH #11 cycle-3 / HIGH #15 decision matrix:
    (a) If partition count < 40 OR smoke-only signature (only `202306` present): HALT exit code 1 with message: "BLOCKING: scraped corpus is smoke-only or incomplete (X partitions < 40); run D-06 pre-task (full scrape 2022-2026/5) before continuing Plan 06-03." Do NOT proceed to Task 2.
    (b) If > 20% of present partitions are invalid (3-file/empty/date failures): HALT exit code 1 with per-partition failure report.
    (c) Otherwise: proceed to Task 2. Tolerate small missing months (JRA off-months) but log them.

    Implement as `python -c "..."` inline OR a throwaway `scripts/preflight_scraped.py`. The verify command below is the authoritative gate.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
from pathlib import Path
import pandas as pd
import pyarrow.parquet as pq
import sys
root = Path('data/standard/scraped')
if not root.exists():
    print('BLOCKING: data/standard/scraped/ does not exist; run D-06 pre-task first'); sys.exit(1)
dirs = sorted(d for d in root.iterdir() if d.is_dir())
print(f'partition count: {len(dirs)}')
if len(dirs) < 40:
    print(f'BLOCKING: only {len(dirs)} scraped partitions (< 40); smoke-only or incomplete; run D-06 full scrape (2022-2026/5) first'); sys.exit(1)
invalid = []
for d in dirs:
    yyyymm = d.name
    # HIGH #16: 3-file presence + HIGH #11 cycle-3: non-empty for ALL THREE via pyarrow metadata
    for tbl in ('race', 'entry', 'result'):
        p = d / f'{tbl}.parquet'
        if not p.exists():
            invalid.append((yyyymm, tbl, 'missing file')); continue
        # HIGH #11 cycle-3: assert num_rows > 0; re-raise on read error (do NOT swallow)
        try:
            nrows = pq.ParquetFile(p).metadata.num_rows
        except Exception as e:
            # Re-raise — a read error is a real failure (cycle-2 bug was swallowing this)
            raise RuntimeError(f'partition {yyyymm}/{tbl}.parquet read error: {e}') from e
        if nrows <= 0:
            invalid.append((yyyymm, tbl, f'empty ({nrows} rows)'))
    # HIGH #16: race_date matches dir name (only if race.parquet exists + non-empty)
    rp = d / 'race.parquet'
    if rp.exists():
        try:
            nrows_race = pq.ParquetFile(rp).metadata.num_rows
        except Exception as e:
            raise RuntimeError(f'partition {yyyymm}/race.parquet read error: {e}') from e
        if nrows_race > 0:
            rdf = pd.read_parquet(rp, columns=['race_id','race_date'])
            parsed = pd.to_datetime(rdf['race_date'], errors='coerce')
            bad = rdf.loc[parsed.isna() | (parsed.dt.strftime('%Y%m') != yyyymm)]
            if len(bad) > 0:
                invalid.append((yyyymm, 'race', f'{len(bad)} rows with date != {yyyymm}'))
print(f'invalid partitions: {len(invalid)}')
for x in invalid[:10]:
    print('  INVALID:', x)
if len(invalid) > len(dirs) * 0.2:
    print(f'BLOCKING: >20% of scraped partitions invalid ({len(invalid)}/{len(dirs)}); re-run scrape for broken months'); sys.exit(1)
print('OK: preflight passed — all 3 tables non-empty per partition, race_date matches dir, partition count >= 40')
"</automated>
  </verify>
  <done>
    - Preflight runs against every `data/standard/scraped/{YYYYMM}/` partition
    - HIGH #11 cycle-3: each partition's race/entry/result asserted NON-EMPTY via pyarrow metadata num_rows > 0 (no swallowing)
    - HIGH #16: 3-file presence + race_date matches dir name
    - HIGH #15: if smoke-only (< 40 partitions) or structurally broken (> 20% invalid): verify exits non-zero; plan halts; Task 2 NOT attempted
    - If preflight passes: partition report documented in SUMMARY
  </done>
</task>

<task type="auto">
  <name>Task 2: Snapshot odds/payoff (HIGH #17) → invoke integrate_standard_layer → run FULL 8-point run_all_validations against unified root (HIGH #3 cycle-3 relocation) → assert per-period graded counts (HIGH #18), robust date range with EXPECTED_FLOOR (HIGH #14 cycle-3), PK-set union for ALL 3 tables (NEW HIGH cycle-3), odds/payoff SHA-256 unchanged (HIGH #17)</name>
  <files>data/standard/race.parquet, data/standard/entry.parquet, data/standard/result.parquet</files>
  <read_first>
    - src/pipeline/integration.py (the module from Plan 06-02; verify integrate_standard_layer signature + tempfile.mkdtemp validate-before-swap)
    - src/pipeline/validators.py (lines 764-894 run_all_validations — the FULL 8-point validator; takes raw_dir + parquet_dir + optional source_counts + source_stats; iterates 5 tables via TABLE_TO_SCHEMA; validate_referential_integrity at 352-403 checks child_tables entry/result/odds_trifecta/payoff. Against data/standard/ root where all 5 tables coexist, this returns overall_pass=True when source_counts + source_stats are supplied.)
    - src/pipeline/kaggle_converter.py (read_race_result_csv, split_race_entry_result — to compute source_counts/source_stats from the source CSV; mirror the filter at convert() lines 82-90)
    - .planning/phases/06-data-integration/06-RESEARCH.md (Code Examples — Verified end-to-end integration recipe)
    - src/scraper/flag_crosswalk.py (lines 124-130 _GRADE_REGEX, lines 135-215 derive_race_flags — to recompute expected graded count per period)
  </read_first>
  <action>
    STEP 0 (HIGH #17 — snapshot BEFORE integration): compute SHA-256 + row count of `data/standard/odds_trifecta.parquet` and `data/standard/payoff.parquet`. Store for post-integration comparison.

    STEP 1 (invoke integration): `python -c "from pathlib import Path; from src.pipeline.integration import integrate_standard_layer; r = integrate_standard_layer(Path('data/standard')); print('wrote:', r)"`. Overwrites `data/standard/{race,entry,result}.parquet` atomically via Plan-06-02 tempfile.mkdtemp validate-before-swap (HIGH #6 cycle-3).

    STEP 2 (HIGH #17 — assert odds/payoff UNCHANGED): recompute SHA-256 + row count of odds_trifecta.parquet and payoff.parquet AFTER integration; assert byte-identical to STEP 0. If any differ, STOP.

    STEP 3 (HIGH #3 cycle-3 — FULL 8-point run_all_validations against unified ROOT): compute `source_counts` + `source_stats` from the source Kaggle CSV (mirror 06-01-T3's computation: read `data/raw/kaggle/19860105-20210731_race_result.csv`, apply the same 2015+ flat filter as convert() lines 82-90, split via split_race_entry_result, count race/entry/result; for odds read `*_odds.csv`). Then add the scraped partition counts: for each table, sum `pq.ParquetFile(p).metadata.num_rows` across all `data/standard/scraped/*/{table}.parquet` partitions. Add Kaggle + scraped counts per table to get `source_counts`. For `source_stats`, compute null_rates + distributions from the merged frame (or skip detailed stats and rely on the 3-table-specific checks from 06-01-T3 + the per-period/per-year checks below; if skipping, pass a minimal source_stats covering race.distance + entry.popularity to exercise validate_null_rates/validate_distributions). Invoke `run_all_validations(raw_dir=Path('data/raw/kaggle'), parquet_dir=Path('data/standard'), source_counts=source_counts, source_stats=source_stats)`. Assert `overall_pass is True`. Document any non-True check key. This is the cycle-3 resolution of HIGH #3: the full 8-point runs against the unified ROOT where all 5 tables coexist.

    STEP 4 (HIGH #18 — per-period graded count): split unified race.parquet into Kaggle-period (race_date < '2022-01-01') and scraped-period (>= '2022-01-01'). For each: count `race_flag_graded_stakes == True` AND recompute expected via `derive_race_flags(grade_or_space, race_name)` (where `grade_or_space = str(grade) if pd.notna(grade) and str(grade).strip() != '' else ' '`). Assert actual == expected per period. Unified count = sum (NOT a fixed band).

    STEP 5 (HIGH #14 cycle-3 + HIGH #19 — robust date range with EXPECTED_FLOOR):
    (a) Assert `'2015-01-01' <= dmin <= '2015-03-31'` (dmin in early 2015).
    (b) Compute `actual_scraped_max = max(str(pd.read_parquet(p, columns=['race_date'])['race_date'].max()) for each scraped race.parquet partition)`.
    (c) HIGH #14 cycle-3 — assert BOTH:
        - `dmax == actual_scraped_max` (no data dropped DURING integration — the cycle-2 check, KEPT as necessary)
        - `actual_scraped_max >= EXPECTED_FLOOR` where `EXPECTED_FLOOR = '2024-01-01'` (the cycle-3 NEW floor — catches an incomplete D-06 scrape; if the scrape stopped at 2023-12, this FAILS, surfacing a real gap rather than passing self-referentially). Document EXPECTED_FLOOR explicitly.
    (d) Per-year race counts: `df.groupby(df['race_date'].str[:4])['race_id'].count()`; assert each PRESENT year 2015..end has > 500 races (WARNING-1 threshold). Log (do not fail on) a year missing from the index — Task 1's preflight already halts on structural gaps.

    STEP 6 (NEW HIGH cycle-3 — PK-set union for ALL 3 tables, not just race): for each of race/entry/result, compute `set(output PKs) == set(union of input partition PKs)`. PK_BY_TABLE = {race: race_id, entry: horse_race_id, result: horse_race_id}. Input partitions = Kaggle `data/standard/kaggle/{table}.parquet` PKs UNION each scraped partition's PKs. Output = unified `data/standard/{table}.parquet` PKs. Assert set equality per table (race AND entry AND result). The cycle-2 verify checked only race.parquet; the cycle-3 verify mirrors for entry + result.

    STEP 7 (full Phase 6 test suite): `python -m pytest tests/pipeline/test_integration.py -q` — all 10 tests green including slow-path.

    STEP 8 (capture verification report): write to SUMMARY — row counts per table, race_date min/max + per-year counts + EXPECTED_FLOOR, actual_scraped_max, per-period graded counts, PK-set union sizes (all 3 tables), odds/payoff SHA-256 + row counts pre/post, run_all_validations overall_pass + per-check results, audit_leakage results, FK integrity.

    SCOPE RESOLUTION (MEDIUM #21 — per CONTEXT D-07): NO period filter — unified corpus contains all real data the D-06 scrape produced (target 2015-2026/5). The expected race_date.max() equals actual_scraped_max (WARNING-3 robust to scrape coverage) AND must be >= EXPECTED_FLOOR '2024-01-01' (HIGH #14 cycle-3 — catches incomplete scrape). Document actual_scraped_max and the ROADMAP-text-vs-actual divergence for Phase 9.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -c "
import hashlib
from pathlib import Path
import pandas as pd
import pyarrow.parquet as pq

# STEP 0: snapshot odds/payoff BEFORE integration
def snapshot(p):
    p = Path(p)
    if not p.exists(): return None
    h = hashlib.sha256(p.read_bytes()).hexdigest()
    n = pq.ParquetFile(p).metadata.num_rows
    return (h, n)
pre_odds = snapshot('data/standard/odds_trifecta.parquet')
pre_payoff = snapshot('data/standard/payoff.parquet')
print('pre odds:', pre_odds); print('pre payoff:', pre_payoff)

# STEP 1: invoke integration
from src.pipeline.integration import integrate_standard_layer
r = integrate_standard_layer(Path('data/standard'))
print('wrote:', {k: str(v) for k,v in r.items() if k != 'audit'})

# STEP 2: odds/payoff UNCHANGED (HIGH #17)
post_odds = snapshot('data/standard/odds_trifecta.parquet')
post_payoff = snapshot('data/standard/payoff.parquet')
print('post odds:', post_odds); print('post payoff:', post_payoff)
assert pre_odds == post_odds, f'odds_trifecta.parquet CHANGED (D-05 violation)'
assert pre_payoff == post_payoff, f'payoff.parquet CHANGED (D-05 violation)'

# STEP 3: FULL 8-point run_all_validations against unified ROOT (HIGH #3 cycle-3)
from src.pipeline.kaggle_converter import read_race_result_csv, split_race_entry_result
from src.pipeline.validators import run_all_validations
raw = Path('data/raw/kaggle')
df = read_race_result_csv(raw / '19860105-20210731_race_result.csv')
df['レース日付'] = pd.to_datetime(df['レース日付'], errors='coerce')
df = df[df['レース日付'].notna()].copy()
df = df[df['レース日付'] >= '2015-01-01']
df = df[df['障害区分'] != '障害']
krace, kentry, kresult = split_race_entry_result(df)
# source_counts: Kaggle + scraped per table
def scraped_count(tbl):
    total = 0
    for d in sorted(Path('data/standard/scraped').iterdir()):
        p = d / f'{tbl}.parquet'
        if p.exists(): total += pq.ParquetFile(p).metadata.num_rows
    return total
source_counts = {
    'race': len(krace) + scraped_count('race'),
    'entry': len(kentry) + scraped_count('entry'),
    'result': len(kresult) + scraped_count('result'),
}
# odds/payoff source_counts: use existing root file row counts (Phase 5 seed, unchanged)
source_counts['odds_trifecta'] = pq.ParquetFile('data/standard/odds_trifecta.parquet').metadata.num_rows
source_counts['payoff'] = pq.ParquetFile('data/standard/payoff.parquet').metadata.num_rows
# Minimal source_stats to exercise validate_null_rates + validate_distributions
source_stats = {
    'race': {'null_rates': {'distance': float(krace['distance'].isna().mean())},
             'distributions': {'distance': {'min': float(krace['distance'].min()), 'max': float(krace['distance'].max()), 'mean': float(krace['distance'].mean())}}},
    'entry': {'null_rates': {'popularity': float(kentry['popularity'].isna().mean())},
              'distributions': {'popularity': {'min': float(kentry['popularity'].min()), 'max': float(kentry['popularity'].max()), 'mean': float(kentry['popularity'].mean())}}},
}
v = run_all_validations(raw, Path('data/standard'), source_counts=source_counts, source_stats=source_stats)
print('overall_pass=', v['overall_pass'])
print('per-check:', {k: v[k] for k in ['row_counts','schema_conformance','null_rates','distributions','referential_integrity','sample_rows','value_ranges']})
assert v['overall_pass'] is True, {k: val for k, val in v.items() if val is False and k != 'overall_pass'}

# STEP 4: per-period graded count (HIGH #18)
from src.scraper.flag_crosswalk import derive_race_flags
rdf = pd.read_parquet('data/standard/race.parquet')
rdf['_period'] = rdf['race_date'].apply(lambda s: 'kaggle' if str(s) < '2022-01-01' else 'scraped')
for period, sub in rdf.groupby('_period'):
    actual = int(sub['race_flag_graded_stakes'].fillna(False).sum())
    expected = sum(1 for _, rr in sub.iterrows() if derive_race_flags(race_condition=(str(rr.get('grade')) if pd.notna(rr.get('grade')) and str(rr.get('grade')).strip() != '' else ' '), race_name=(str(rr.get('race_name')) if pd.notna(rr.get('race_name')) else '')).get('race_flag_graded_stakes') is True)
    print(f'period={period}: graded actual={actual} expected={expected}')
    assert actual == expected, f'{period} graded mismatch'

# STEP 5: robust date range with EXPECTED_FLOOR (HIGH #14 cycle-3 + HIGH #19)
dmin = str(rdf['race_date'].min()); dmax = str(rdf['race_date'].max())
print(f'race_date min={dmin} max={dmax}')
assert '2015-01-01' <= dmin <= '2015-03-31', f'unexpected race_date.min={dmin}'
scraped_maxes = []
for d in sorted(Path('data/standard/scraped').iterdir()):
    rp = d / 'race.parquet'
    if rp.exists():
        sdf = pd.read_parquet(rp, columns=['race_date'])
        if len(sdf): scraped_maxes.append(str(sdf['race_date'].max()))
actual_scraped_max = max(scraped_maxes) if scraped_maxes else None
print(f'actual_scraped_max={actual_scraped_max}')
assert actual_scraped_max is not None
# HIGH #14 cycle-3: BOTH conditions
assert dmax == actual_scraped_max, f'unified max {dmax} != scraped max {actual_scraped_max} (data dropped/added in integration)'
EXPECTED_FLOOR = '2024-01-01'  # HIGH #14 cycle-3 — defensible floor from ROADMAP #3 (2015-2024) + D-07 extension
assert actual_scraped_max >= EXPECTED_FLOOR, f'INCOMPLETE SCRAPE: actual_scraped_max={actual_scraped_max} < EXPECTED_FLOOR={EXPECTED_FLOOR} (D-06 target not reached — real gap, not self-referential pass)'
per_year = rdf.groupby(rdf['race_date'].str[:4])['race_id'].count()
print('per-year race counts:'); print(per_year)
for yr in [str(y) for y in range(2015, 2027)]:
    if yr in per_year.index:
        assert per_year[yr] > 500, f'year {yr} has only {per_year[yr]} races (< 500)'

# STEP 6: PK-set union for ALL 3 tables (NEW HIGH cycle-3)
PK_BY_TABLE = {'race': 'race_id', 'entry': 'horse_race_id', 'result': 'horse_race_id'}
for tbl, pk in PK_BY_TABLE.items():
    out_pks = set(pd.read_parquet(f'data/standard/{tbl}.parquet', columns=[pk])[pk])
    in_pks = set()
    kp = Path(f'data/standard/kaggle/{tbl}.parquet')
    if kp.exists(): in_pks |= set(pd.read_parquet(kp, columns=[pk])[pk])
    for d in sorted(Path('data/standard/scraped').iterdir()):
        p = d / f'{tbl}.parquet'
        if p.exists(): in_pks |= set(pd.read_parquet(p, columns=[pk])[pk])
    print(f'{tbl}: input PK count={len(in_pks)} output PK count={len(out_pks)}')
    assert in_pks == out_pks, f'{tbl} PK-set mismatch: input={len(in_pks)} output={len(out_pks)}'

print('OK: integration verified — 8-point run_all_validations, per-period graded, robust date range with EXPECTED_FLOOR, PK-set union for all 3 tables, odds/payoff unchanged')
" && python -m pytest tests/pipeline/test_integration.py -q</automated>
  </verify>
  <done>
    - Unified corpus written; integrate_standard_layer returned 3 paths
    - HIGH #3 cycle-3: FULL 8-point run_all_validations against data/standard/ root returns overall_pass=True (source_counts + source_stats supplied; all 5 tables present)
    - HIGH #17: odds_trifecta/payoff SHA-256 + row counts IDENTICAL pre/post
    - HIGH #18: per-period graded counts match grade-regex derivation
    - HIGH #14 cycle-3: dmax == actual_scraped_max AND actual_scraped_max >= '2024-01-01' (EXPECTED_FLOOR); dmin in 2015-01..03
    - NEW HIGH cycle-3: PK-set union equality for race AND entry AND result
    - All 10 integration tests green (slow-path gates cleared)
    - Verification report captured in SUMMARY (incl. EXPECTED_FLOOR, actual_scraped_max, per-check run_all_validations results, PK-set union sizes for all 3 tables)
    - Scope decision (D-07 — 2015-2026/5, no filter; ROADMAP text update deferred to Phase 9) documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human inspection of the unified corpus (end-of-phase gate)</name>
  <files></files>
  <action>
    End-of-phase human verification gate. Per `workflow.human_verify_mode=end-of-phase`, the user reviews the unified corpus produced by Task 2 and the verification report captured in 06-03-SUMMARY.md, then approves or describes issues. This task performs no automated code changes — it is a blocking confirmation that the three ROADMAP success criteria are TRUE from the user's perspective.
  </action>
  <verify>
    <automated>echo "human-verify checkpoint — no automated verify; gating on resume-signal='approved'"</automated>
    <human-check>User runs `python -m pytest tests/ -q` (full suite green) and `python -c "import pyarrow.parquet as pq; print([(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet') if str(f.type)=='null'])"` (returns empty list); confirms odds_trifecta/payoff SHA-256 unchanged via the report in SUMMARY.</human-check>
  </verify>
  <done>User typed "approved" in response to the resume-signal prompt.</done>
  <what-built>
    Phase 6 has produced the unified `data/standard/{race,entry,result}.parquet` corpus (Kaggle 2015-2021 post-D-01/D-02 from Plan 06-01 + scraped 2022-2026/5 from D-06 pre-task, merged via the idempotent + tempfile.mkdtemp validate-before-swap + FK-validating + column-set-equality-guarded integrate_standard_layer from Plan 06-02). All 10 integration tests pass, the FULL 8-point run_all_validations passes against the unified root (HIGH #3 cycle-3), per-partition preflight is clean, per-period graded counts match grade-regex derivation, date range is robust with EXPECTED_FLOOR (HIGH #14 cycle-3), PK-set union verified for all 3 tables (NEW HIGH cycle-3), odds/payoff SHA-256 unchanged.
  </what-built>
  <how-to-verify>
    1. Review the verification report captured in 06-03-SUMMARY.md (per-partition preflight, per-period graded counts, per-year race counts, EXPECTED_FLOOR + actual_scraped_max, PK-set union sizes for all 3 tables, odds/payoff SHA-256 + row counts pre/post, run_all_validations per-check results, audit results, FK integrity).
    2. Run `python -m pytest tests/ -q` and confirm the FULL suite is green.
    3. Inspect `data/standard/race.parquet` schema via `python -c "import pyarrow.parquet as pq; [print(f.name, f.type) for f in pq.read_schema('data/standard/race.parquet')]"`. Confirm every `race_flag_*` column is Arrow `bool`; `race_date` is `string`; `distance` is `int64`.
    4. Spot-check that `data/standard/odds_trifecta.parquet` and `payoff.parquet` SHA-256 values match the pre-integration snapshot in SUMMARY (D-05 honor).
    5. Confirm EXPECTED_FLOOR assertion: actual_scraped_max >= '2024-01-01' (HIGH #14 cycle-3 — catches incomplete D-06 scrape).
    6. Confirm PK-set union equality for ALL 3 tables (race AND entry AND result), not just race (NEW HIGH cycle-3).
    7. Note the scope decision: unified corpus is 2015-2026/5 per CONTEXT D-07 (ROADMAP text update deferred to Phase 9).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 6 integration → Phase 7/9 downstream | Unified corpus becomes input to model training + backtesting. Schema/dedup/FK integrity propagates downstream. |
| Integration write → existing Phase 2/4/5 outputs | Destructive overwrite of `data/standard/{race,entry,result}.parquet`. tempfile.mkdtemp validate-before-swap + idempotent recovery + regenerable sources are the controls. |
| D-06 pre-task output → Phase 6 input | Scraped corpus is externally produced. Per-partition preflight (HIGH #11 cycle-3 + HIGH #16) is the trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | D-06 smoke-only corpus mistakenly treated as complete (HIGH #15) | mitigate | Task 1 preflight: < 40 partitions OR > 20% invalid → exit 1, halt. |
| T-06-12 | Tampering | Empty entry/result partition silently integrated (HIGH #11 cycle-3) | mitigate | Per-partition pyarrow metadata num_rows > 0 for ALL THREE files; exceptions re-raised (not swallowed). |
| T-06-13 | Tampering | Success criterion #1 (no dups) silently false | mitigate | integrate_standard_layer's FAIL-LOUD pre-dedup + validate_integrity. |
| T-06-14 | Tampering | Success criterion #2 (schema identical) silently false | mitigate | Plan 06-02 column-set equality assert (HIGH #9) + 8-point run_all_validations schema_conformance check against unified root (HIGH #3 cycle-3). |
| T-06-15 | Denial of Service | Phase 5 seed (odds/payoff) destroyed (HIGH #17) | mitigate | SHA-256 + row count snapshot in verify.automated; byte-identical pre/post assertion. |
| T-06-16 | Tampering | Graded count compared to wrong baseline (HIGH #18) | mitigate | Per-period graded count compared to grade-regex derivation. |
| T-06-17 | Tampering | Incomplete D-06 scrape accepted as complete (HIGH #14 cycle-3) | mitigate | `actual_scraped_max >= EXPECTED_FLOOR` ('2024-01-01') AND `dmax == actual_scraped_max` (both conditions). Catches incomplete scrape, not just integration drop. |
| T-06-18 | Tampering | PK-set union drift in entry/result undetected (NEW HIGH cycle-3) | mitigate | PK-set union equality checked for race AND entry AND result (PK = race_id for race, horse_race_id for entry/result). |
| T-06-19 | Tampering | 8-point verification skipped or run against wrong dir (HIGH #3 cycle-3) | mitigate | FULL 8-point run_all_validations against data/standard/ root where all 5 tables coexist; source_counts + source_stats supplied; overall_pass=True asserted. |
| T-06-20 | Repudiation | Phase 6 closed without human confirming | mitigate | Task 3 blocking checkpoint:human-verify. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` full suite green
- `python -m pytest tests/pipeline/test_integration.py -q` green (10 tests, slow-path gates cleared)
- HIGH #3 cycle-3: `run_all_validations(raw_dir, Path('data/standard'), source_counts=..., source_stats=...)` returns overall_pass=True (all 5 tables present at root; all 8 checks run)
- `pyarrow.parquet.read_schema('data/standard/race.parquet')` has zero Arrow-null columns; every race_flag_* Arrow bool; race_date string; distance int64
- `pd.read_parquet('data/standard/race.parquet')['race_id'].nunique() == len(...)` (no duplicates)
- HIGH #14 cycle-3: `'2015-01-01' <= dmin <= '2015-03-31'` AND `dmax == actual_scraped_max` AND `actual_scraped_max >= '2024-01-01'` (EXPECTED_FLOOR)
- Per-period graded counts match grade-regex derivation
- NEW HIGH cycle-3: PK-set union equality for race AND entry AND result
- odds_trifecta/payoff SHA-256 + row counts unchanged (HIGH #17)
- Human checkpoint: approved
</verification>

<success_criteria>
- HIGH #11 resolved (cycle 3): preflight asserts all 3 files non-empty per partition via pyarrow metadata; exceptions re-raised
- HIGH #14 resolved (cycle 3): dmax == actual_scraped_max AND actual_scraped_max >= '2024-01-01' (EXPECTED_FLOOR); dmin in early 2015
- NEW HIGH resolved (cycle 3): PK-set union verify extended to entry + result (not just race)
- HIGH #3 resolved (cycle 3): full 8-point run_all_validations moved here from 06-01-T3, runs against unified root (5 tables present)
- HIGH #15/#16 (KEPT): halt-on-smoke-only gate; per-partition 3-file presence + race_date match
- HIGH #17 (KEPT): odds/payoff SHA-256 in verify.automated
- HIGH #18 (KEPT): per-period graded counts
- MEDIUM #20 resolved (cycle 3): PK-set union equality per table (race + entry + result)
- MEDIUM #21 (KEPT): scope = 2015-2026/5 per CONTEXT D-07; no filter; ROADMAP text update deferred to Phase 9
- ROADMAP success criteria #1/#2/#3 TRUE; DATA-05 delivered
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-03-SUMMARY.md` when done. Include: per-partition preflight report, run_all_validations per-check results (overall_pass + all 8 check keys), per-period graded counts, per-year race counts, EXPECTED_FLOOR ('2024-01-01') + actual_scraped_max + dmax, PK-set union sizes (all 3 tables), odds/payoff SHA-256 + row counts pre/post, audit_leakage results, FK integrity, scope decision note (D-07 → 2015-2026/5, ROADMAP text update deferred).
</output>

## Artifacts this phase produces

- `data/standard/race.parquet` (unified 2015-2026/5, post-integration — Kaggle 2015-2021 + scraped 2022-2026/5 merged via idempotent + tempfile.mkdtemp validate-before-swap integration)
- `data/standard/entry.parquet` (unified 2015-2026/5)
- `data/standard/result.parquet` (unified 2015-2026/5)
- `data/standard/odds_trifecta.parquet` (UNCHANGED — D-05 honor, SHA-256-verified)
- `data/standard/payoff.parquet` (UNCHANGED — D-05 honor, SHA-256-verified)
- Verification report (captured in 06-03-SUMMARY.md): per-partition preflight, 8-point run_all_validations per-check results, per-period graded counts, per-year race counts, EXPECTED_FLOOR + actual_scraped_max, PK-set union sizes for all 3 tables, odds/payoff SHA-256, audit_leakage results, FK integrity, scope decision note
- No new source symbols (integrate_standard_layer created in Plan 06-02; this plan INVOKES it against the real data path + runs the verification gate)

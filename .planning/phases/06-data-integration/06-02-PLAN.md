---
phase: 06-data-integration
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - src/pipeline/integration.py
  - tests/pipeline/test_integration.py
  - tests/pipeline/conftest.py
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #5 (idempotency — KEPT from cycle 2): integrate_standard_layer reads Kaggle rows from a SEPARATE input path (data/standard/kaggle/{table}.parquet — written by Plan 06-01 Task 2 via convert(core_tables_subdir='kaggle')) and NEVER from the output path data/standard/{table}.parquet. A second invocation produces byte-identical output."
    - "BLOCKER-1 (cross-plan data contract — KEPT): Plan 06-01 writes the regenerated Kaggle race/entry/result to data/standard/kaggle/{table}.parquet. integrate_standard_layer defaults kaggle_input_dir to standard_dir / 'kaggle' and reads each Kaggle table from there. The data/standard/kaggle/ directory is created by Plan 06-01 BEFORE this plan runs (Wave 1 -> Wave 2 dependency)."
    - "HIGH #6 (corpus transactionality — cycle 3 + cycle 4 + cycle 5): integrate_standard_layer writes all 3 unified tables to a STAGING subdir (tempfile.mkdtemp() under standard_dir, prefix '.integration_staging_') FIRST via _atomic_write_parquet (3 calls to os.replace INSIDE staging — these are NOT patched), validates ALL 3 staged tables there (schema column-set, row counts > 0, PK integrity via validate_integrity on the staged DataFrames), THEN swaps each into root via a DEDICATED, PATCHABLE module-level function _commit_staging(staging_dir, standard_dir) that performs the 3 os.replace(staging_dir / f'{table}.parquet', standard_dir / f'{table}.parquet') calls. Transactionality model is ACCURATELY described as 'validate-before-swap with idempotent recovery' — NOT perfect atomicity. A mid-swap crash leaves at most a partial swap; because integration is IDEMPOTENT (reads Kaggle from data/standard/kaggle/, never from output) and reads scraped partitions (immutable inputs), re-running integrate_standard_layer produces IDENTICAL output, fully recovering. CYCLE-5 FIX (NEW HIGH #2): the transactionality test test_integration_partial_swap_recoverable patches _commit_staging (NOT global os.replace) so the monkeypatched OSError fires during the SWAP (2nd os.replace inside _commit_staging) — never during the staging writes. The test mutates an input between the clean run and the failing run (add a row to a scraped partition) so the new generation produces a DIFFERENT SHA-256 than canonical, making the 'post != canonical' assertion meaningful."
    - "HIGH #6 (staging dir uses tempfile.mkdtemp — cycle-3 fix for MEDIUM): the staging dir is created via tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir) — NOT a fixed '.integration_tmp' name — so concurrent runs and stale leftovers from prior crashes do not collide. The staging dir is removed in a finally block (best-effort cleanup)."
    - "HIGH #7 (autouse skip gate — KEPT from cycle 2): hermetic tests live in TestIntegrationHermetic with NO _require_scraped_data autouse fixture; real-corpus slow-path tests live in TestUnifiedCorpus with the autouse skip. Hermetic tests run unconditionally."
    - "HIGH #8 (FK test actually raises — KEPT from cycle 2): test_referential_integrity injects an entry row whose race_id is NOT in the race table and asserts that validate_integrity returns a list containing 'orphan entry race_id' AND that integrate_standard_layer RAISES ValueError."
    - "HIGH #8b (horse_race_id 1-to-1 mismatch RAISES — cycle 4 NEW HIGH FIX + cycle 5 TEST ISOLATION): the hard-violation filter in integrate_standard_layer matches the FULL set of validate_integrity hard-failure strings: 'duplicate' OR 'orphan' OR 'mismatch' (the last token catches the 'horse_race_id mismatch: entry/result are not 1-to-1' violation at normalizer.py:330-334 which contains NEITHER 'duplicate' NOR 'orphan'). Equivalently, the filter is 'duplicate' in v or 'orphan' in v or 'mismatch' in v or '1-to-1' in v. CYCLE-5 FIX (NEW HIGH #1): the regression test test_horse_race_id_mismatch_raises uses DISJOINT UNIQUE horse_race_ids in entry/result (e.g. entry horse_race_ids = ['E1'], result horse_race_ids = ['R1']) — NEITHER table has duplicates, NEITHER has an FK orphan (all race_ids present in race table). validate_integrity returns ONLY the 'horse_race_id mismatch' violation (check (c) finds no duplicates; checks (e)/(f) find no orphans). The test asserts validate_integrity returns a list whose ONLY entry contains 'mismatch' — PROVING the token is load-bearing for the 1-to-1-only case. It THEN asserts integrate_standard_layer RAISES ValueError (not just warns)."
    - "HIGH #9 (reindex guarded by column-set equality — KEPT from cycle 2): BEFORE any reindex, integrate_standard_layer asserts set(input_df.columns) == set(schema.model_fields.keys()) and raises ValueError on mismatch."
    - "integrate_standard_layer imports _atomic_write_parquet and SCHEMA_DTYPE_MAP from src.scraper.normalizer (NO re-implementation)"
    - "integrate_standard_layer hardcodes SCHEMA_BY_TABLE = {race, entry, result} — does NOT iterate TABLE_TO_SCHEMA.keys() (Pitfall 5)"
    - "Pre-dedup overlap raises ValueError (not silent drop_duplicates away — Pitfall 6)"
    - "integrate_standard_layer never adds a `source` column (D-05)"
    - "audit_leakage is CALLED inside integrate_standard_layer (not just imported) on the merged race_df and entry_df (MEDIUM #13)"
    - "Missing Kaggle files, missing scraped partitions, or missing table-within-a-month each raise an explicit error (MEDIUM #14)"
    - "Module-level logging uses loguru; logs per-table (NOT per-row)"
requirements_resolved:
  - "HIGH #5 (cycle 2 RESOLVED — KEPT): kaggle_input_dir separate path + idempotency test"
  - "HIGH #6 (cycle 2 PARTIALLY RESOLVED → cycle 3 RESOLVED → cycle 4 STRENGTHENED → cycle 5 TEST-ISOLATION FIX): validate-before-swap via tempfile.mkdtemp staging + idempotent recovery model; the swap is wrapped in a DEDICATED module-level function _commit_staging that the transactionality test patches directly (not global os.replace) — proving the recovery under the REAL mid-swap failure"
  - "HIGH #7 (cycle 2 RESOLVED — KEPT): two-class split"
  - "HIGH #8 (cycle 2 RESOLVED — KEPT): orphan FK injection + validate_integrity + ValueError"
  - "HIGH #8b (cycle 4 NEW HIGH RESOLVED → cycle 5 TEST-ISOLATION FIX): horse_race_id 1-to-1 mismatch RAISES — filter extended with 'mismatch' (or '1-to-1'); regression test uses DISJOINT unique horse_race_ids so the mismatch token is provably load-bearing"
  - "HIGH #9 (cycle 2 RESOLVED — KEPT): column-set equality assert before reindex"
artifacts:
  - path: "src/pipeline/integration.py"
    provides: "integrate_standard_layer(standard_dir, kaggle_input_dir=None) entry point + _recast_to_canonical + _assert_column_set_equality + _commit_staging (dedicated swap function) + validate-before-swap with idempotent recovery; hard-violation filter includes 'mismatch'"
    exports: ["integrate_standard_layer"]
    contains: "SCHEMA_BY_TABLE"
  - path: "tests/pipeline/test_integration.py"
    provides: "TestIntegrationHermetic (fast path, no skip gate — 9 tests incl. cycle-5 ISOLATED test_horse_race_id_mismatch_raises + cycle-5 ISOLATED test_integration_partial_swap_recoverable patching _commit_staging) + TestUnifiedCorpus (real corpus, gated). 11 tests total."
    contains: "class TestIntegrationHermetic"
  - path: "tests/pipeline/conftest.py"
    provides: "tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures"
    contains: "tmp_kaggle_input_dir"
key_links:
  - from: "src/pipeline/integration.py"
    to: "src/scraper/normalizer._atomic_write_parquet + validate_integrity"
    via: "from src.scraper.normalizer import _atomic_write_parquet, SCHEMA_DTYPE_MAP, validate_integrity"
    pattern: "from src\\.scraper\\.normalizer import.*(SCHEMA_DTYPE_MAP|_atomic_write_parquet|validate_integrity)"
  - from: "integrate_standard_layer"
    to: "src.schemas.audit.audit_leakage"
    via: "post-integration audit CALL (not just import)"
    pattern: "audit_leakage\\(\\[RaceSchema\\]"
  - from: "integrate_standard_layer hard-violation filter"
    to: "validate_integrity 'horse_race_id mismatch' string (normalizer.py:330-334)"
    via: "hard_violations filter includes 'mismatch' (or '1-to-1') in addition to 'duplicate'/'orphan'"
    pattern: "(mismatch|1-to-1)"
  - from: "integrate_standard_layer staging swap"
    to: "data/standard/{race,entry,result}.parquet"
    via: "tempfile.mkdtemp staging + _commit_staging(staging_dir, standard_dir) (DEDICATED patchable swap function — cycle-5 fix) + os.replace inside _commit_staging (HIGH #6 cycle-3 validate-before-swap + idempotent recovery; cycle-5 _commit_staging mid-swap failure injection test)"
    pattern: "_commit_staging"
---

<objective>
Build the Phase 6 integration module `src/pipeline/integration.py` and its test suite `tests/pipeline/test_integration.py`. The module merges Kaggle (post-D-01/D-02 from Plan 06-01) and scraped (post-D-06 pre-task) corpora into a unified `data/standard/{race,entry,result}.parquet` per D-03/D-04.

This plan is the cycle-5 convergence pass. The cycle-4 production-code mitigations were correct but the two load-bearing regression tests did not prove what they claimed. Cycle 5 fixes BOTH TESTS surgically without touching the production mitigation. The four cycle-2-resolved HIGHs are KEPT unchanged.

- **HIGH #8b (horse_race_id mismatch RAISES — cycle 4 NEW HIGH FIX, cycle 5 TEST ISOLATION FIX):** the cycle-4 production-code fix was CORRECT — the hard-violation filter was extended to `"duplicate" in v or "orphan" in v or "mismatch" in v or "1-to-1" in v` and this genuinely catches the `"horse_race_id mismatch: entry/result are not 1-to-1"` violation string (normalizer.py:330-334), which contains NEITHER `"duplicate"` NOR `"orphan"`. BUT the cycle-4 regression test data was `entry = ["X"], result = ["X", "X"]` — which triggers check (c) (duplicate horse_race_id in result, normalizer.py:301-307) FIRST, so the test passed via the `"duplicate"` token regardless of whether `"mismatch"` was in the filter. The token was NOT provably load-bearing. **FIX (cycle 5):** change the test data to DISJOINT UNIQUE horse_race_ids: `entry horse_race_ids = ["E1"], result horse_race_ids = ["R1"]` (both tables internally unique, no duplicates; all race_ids present in race table so no FK orphan). Then `validate_integrity` returns ONLY the mismatch violation: `Counter({"E1":1}) != Counter({"R1":1})` (multiset mismatch at normalizer.py:320-336); checks (b)/(c) find no duplicates; checks (e)/(f) find no orphans. The test FIRST asserts `validate_integrity(...)` returns a list whose ONLY entry contains `"mismatch"` (proving the token is the sole classifier), THEN asserts `integrate_standard_layer` RAISES ValueError. This makes the token load-bearing for the test — removing `"mismatch"` from the filter would let this test data pass `validate_integrity` entirely (no hard violation) and the integration would NOT raise.

- **HIGH #6 (corpus transactionality — cycle 3 RESOLVED, cycle 4 STRENGTHENED test, cycle 5 TEST ISOLATION FIX):** the validate-before-swap + idempotent recovery DESIGN is sound. BUT the cycle-4 test patched global `os.replace` (via `monkeypatch.setattr(integration_mod.os, "replace", ...)`) — and `normalizer.os` is the SAME module object as `integration.os` (both `import os`). `_atomic_write_parquet` (normalizer.py:643-653) calls os.replace to commit each STAGING file BEFORE the swap loop runs, so the 2nd os.replace call fired during the 2nd STAGING WRITE — the test never reached the swap loop it claimed to exercise. Additionally a clean re-run from identical inputs produces byte-identical output, so `post != canonical` was vacuously misleading. **FIX (cycle 5):** (a) extract the swap into a DEDICATED, PATCHABLE module-level function `_commit_staging(staging_dir, standard_dir)` that performs exactly the 3 `os.replace(staging_dir / f"{table}.parquet", standard_dir / f"{table}.parquet")` calls. The transactionality test patches THIS function (via `monkeypatch.setattr("src.pipeline.integration._commit_staging", failing_swap)`), so the failure fires during the actual SWAP, never during the staging writes. (b) Mutate an input between the clean run and the failing run (add a row to a scraped partition) so the new generation produces a DIFFERENT SHA-256 than canonical — making the `post != canonical` assertion meaningful. Then RESTORE `_commit_staging`, re-invoke `integrate_standard_layer`, and assert all 3 outputs are byte-identical to a fresh clean run. This proves idempotent recovery under the REAL mid-swap failure.

- **HIGH #5 (idempotency — RESOLVED in cycle 2, KEPT):** read Kaggle from `data/standard/kaggle/{table}.parquet` (a stable separate input), never from the output path. `test_integration_is_idempotent` proves byte-identical re-run.

- **HIGH #7 (autouse skip — RESOLVED in cycle 2, KEPT):** hermetic tests ungated; real-corpus tests gated.

- **HIGH #8 (FK orphan — RESOLVED in cycle 2, KEPT):** `test_referential_integrity_orphan_raises` injects orphan, asserts `validate_integrity` reports it, integration raises ValueError.

- **HIGH #9 (column-set equality — RESOLVED in cycle 2, KEPT):** `_assert_column_set_equality` before reindex.

MEDIUMs: #12 (test count: 11 — `test_horse_race_id_mismatch_raises` retained, cycle-5 isolated); #13 (audit_leakage actually called); #14 (explicit errors for missing Kaggle files, missing scraped partitions, missing table within month).

Output: New module `src/pipeline/integration.py` exposing `integrate_standard_layer()` with a DEDICATED `_commit_staging` swap function. New test file `tests/pipeline/test_integration.py` with 11 tests across two classes — the two cycle-5-isolated regression tests prove their load-bearing properties. Extended `tests/pipeline/conftest.py` with hermetic Kaggle/scraped fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md

# Phase 4 dtype authority + atomic write + FK validator (primitives to reuse)
@src/scraper/normalizer.py

# Phase 1 audit contract
@src/schemas/audit.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py

# Reference test pattern: Phase 4 TestSchemaCompatibility
@tests/scraper/test_end_to_end.py

# Conftest: extend the existing tmp_standard_dir fixture
@tests/pipeline/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hermetic fixtures to tests/pipeline/conftest.py (tmp_kaggle_input_dir + tmp_scraped_partitions_dir); create tests/pipeline/test_integration.py with TWO test classes (TestIntegrationHermetic ungated — 9 tests incl. cycle-5 ISOLATED test_horse_race_id_mismatch_raises + cycle-5 ISOLATED test_integration_partial_swap_recoverable patching _commit_staging + TestUnifiedCorpus gated — 2 tests)</name>
  <files>tests/pipeline/conftest.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - tests/pipeline/conftest.py (full file — tmp_standard_dir at line 291, sample_standard_race_df at 304, sample_standard_entry_df at 355, sample_standard_result_df at 436, combined standard-files fixture around 492-509; extend, do not duplicate)
    - tests/scraper/conftest.py (lines 17-47 — the canonical tmp_path-derived fixture pattern)
    - tests/scraper/test_end_to_end.py (lines 464-536 — TestSchemaCompatibility class shape AND the _require_kaggle_parquet autouse skip fixture at 467-472; mirror as `_require_scraped_data` for the GATED class only)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Phase Requirements → Test Map" — the test names/behaviors)
  </read_first>
  <behavior>
    - Test 1: `python -m pytest tests/pipeline/test_integration.py --collect-only -q` lists all 11 expected test names AND assigns them to the correct class (TestIntegrationHermetic: 9 tests; TestUnifiedCorpus: 2 tests)
    - Test 2: `tmp_scraped_partitions_dir` fixture creates `tmp_path/data/standard/scraped/202301/` and `tmp_path/data/standard/scraped/202302/` with one tiny synthetic Parquet per table per month (reindex to Schema.model_fields order)
    - Test 3: `tmp_kaggle_input_dir` fixture creates `tmp_path/data/standard/kaggle/{race,entry,result}.parquet` (the SEPARATE input path — HIGH #5). It does NOT touch `tmp_path/data/standard/{race,entry,result}.parquet` (the output path).
    - Test 4 (HIGH #7): TestIntegrationHermetic has NO autouse skip fixture; its tests COLLECT and RUN against synthetic tmp_path data. TestUnifiedCorpus has the `_require_scraped_data` autouse skip; its 2 tests skip when the real corpus is smoke-only.
  </behavior>
  <action>
    Extend tests/pipeline/conftest.py with TWO new fixtures:
    (1) `tmp_kaggle_input_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/kaggle/` and writes `{race,entry,result}.parquet` there (reindex each to `list(Schema.model_fields.keys())`, write via `to_parquet(engine='pyarrow', index=False)`). This is the HIGH #5 separate-input-path fixture.
    (2) `tmp_scraped_partitions_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/scraped/{202301,202302}/` and writes one synthetic Parquet per table per month (same reindex+write). Mirrors the Phase 4 partitioned layout.

    Then create tests/pipeline/test_integration.py with TWO classes:
    - `class TestIntegrationHermetic:` (NO autouse skip — runs unconditionally against tmp_path). 9 test method stubs:
      `test_no_duplicate_race_ids_fail_loud` (HIGH #8)
      `test_referential_integrity_orphan_raises` (HIGH #8)
      `test_horse_race_id_mismatch_raises` (HIGH #8b cycle-5 ISOLATED — DISJOINT unique horse_race_ids in entry/result, NO duplicates, NO FK orphans; proves the mismatch token is load-bearing)
      `test_column_set_mismatch_raises_before_reindex` (HIGH #9)
      `test_schema_invariant_post_integration` (every race_flag_* Arrow bool; distance int64; race_date string)
      `test_no_post_race_leakage_audit_called` (MEDIUM #13)
      `test_odds_payoff_not_overwritten` (sentinel files unchanged post-integration)
      `test_integration_is_idempotent` (HIGH #5: two consecutive runs produce byte-identical output)
      `test_integration_partial_swap_recoverable` (HIGH #6 cycle-5: patch `src.pipeline.integration._commit_staging` (NOT global os.replace) to raise OSError on the 2nd os.replace inside it; assert inconsistent state OR raise; RESTORE _commit_staging and re-invoke; assert all 3 outputs byte-identical to a fresh clean run)
    - `class TestUnifiedCorpus:` (autouse `_require_scraped_data` skip when real corpus is smoke-only). 2 test method stubs:
      `test_unified_race_date_range`
      `test_row_counts_within_expected_bounds`

    Each stub body is `pytest.skip("integration.py not yet implemented — Wave 0 stub")` for now (Task 2 replaces). Use try/except at module top with a sentinel for the `integrate_standard_layer` import so collection succeeds when integration.py does not yet exist.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py --collect-only -q 2>&1 | grep -E '^(tests/pipeline/test_integration\.py::|test_)' | wc -l | tr -d ' ' | grep -q '^11$'</automated>
  </verify>
  <done>
    - tests/pipeline/test_integration.py exists with 2 classes (Hermetic ungated + UnifiedCorpus gated) and 11 collected stubs
    - conftest.py has tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures
    - `pytest --collect-only` lists 11 items without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/pipeline/integration.py — separate Kaggle input path (HIGH #5), validate-before-swap with idempotent recovery via tempfile.mkdtemp staging + DEDICATED _commit_staging swap function (HIGH #6 cycle-3 + cycle-5 _commit_staging fix), hard-violation filter including 'mismatch' (HIGH #8b cycle 4), column-set equality assert (HIGH #9), real FK validation via validate_integrity (HIGH #8), audit_leakage CALLED (MEDIUM #13); replace test stubs with cycle-5 ISOLATED assertions</name>
  <files>src/pipeline/integration.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - src/scraper/normalizer.py (lines 67 `import os`; lines 95-192 SCHEMA_DTYPE_MAP; lines 226-255 _build_typed_dataframe; lines 263-374 validate_integrity — CONFIRM at lines 293-307 the per-table duplicate checks fire on entry AND result horse_race_id; CONFIRM at lines 330-334 the violation string is exactly `horse_race_id mismatch: entry/result are not 1-to-1 (only-in-entry=..., only-in-result=..., count-mismatch={...})` — contains NEITHER 'duplicate' NOR 'orphan'; CONFIRM at lines 338-372 the FK orphan checks; lines 643-653 _atomic_write_parquet ALSO calls os.replace at line 653 — this is WHY the cycle-4 global-monkeypatch test was wrong; lines 744-756 hard_violations raise ValueError pattern — mirror this with the EXTENDED token set that includes 'mismatch')
    - src/schemas/audit.py (audit_leakage signature — returns list[str] of leaked post-race column names; logs warning, does NOT raise per D-12)
    - src/schemas/race.py / entry.py / result.py (model_fields — canonical column-order source)
    - .planning/phases/06-data-integration/06-PATTERNS.md (section `src/pipeline/integration.py`)
    - .planning/phases/06-data-integration/06-RESEARCH.md (Code Examples — Verified end-to-end integration recipe)
    - tests/pipeline/test_integration.py (the stubs from Task 1)
  </read_first>
  <behavior>
    - Test 1 (TestIntegrationHermetic::test_no_duplicate_race_ids_fail_loud): construct Kaggle input + scraped partition with OVERLAPPING race_id; integrate_standard_layer RAISES ValueError mentioning "duplicate"
    - Test 2 (TestIntegrationHermetic::test_referential_integrity_orphan_raises — HIGH #8): construct synthetic data where entry.race_id contains an ID NOT in race.race_id; integrate_standard_layer calls validate_integrity, which returns a list containing "orphan entry race_id"; integrate_standard_layer then RAISES ValueError
    - Test 3 (TestIntegrationHermetic::test_horse_race_id_mismatch_raises — HIGH #8b cycle-5 ISOLATED): CYCLE-5 TEST DATA — construct entry_df with 1 row whose horse_race_id is "E1"; construct result_df with 1 row whose horse_race_id is "R1" (DISJOINT, both tables INTERNALLY UNIQUE). Construct race_df such that EVERY race_id referenced by entry.race_id ("E1"'s race_id) AND result.race_id ("R1"'s race_id) IS in race_df (NO FK orphan). With this data: validate_integrity returns a list whose ONLY entry is `"horse_race_id mismatch: entry/result are not 1-to-1 (only-in-entry=1, only-in-result=1, count-mismatch={})"` — the duplicate check at normalizer.py:293-307 finds nothing in either table; the FK checks at normalizer.py:338-372 find nothing. The test FIRST asserts `len(violations) == 1 and "mismatch" in violations[0]` (proving the mismatch token is the SOLE classifier — without it in the filter, this data has NO hard violation and integration would NOT raise). The test THEN asserts `pytest.raises(ValueError)` when calling integrate_standard_layer.
    - Test 4 (TestIntegrationHermetic::test_column_set_mismatch_raises_before_reindex — HIGH #9): construct an input DataFrame whose column set differs from set(Schema.model_fields.keys()); integrate_standard_layer RAISES ValueError BEFORE reaching reindex
    - Test 5 (TestIntegrationHermetic::test_schema_invariant_post_integration): integrated race.parquet via pq.read_schema — every race_flag_* Arrow bool, distance int64, race_date string
    - Test 6 (TestIntegrationHermetic::test_no_post_race_leakage_audit_called — MEDIUM #13): audit_leakage CALLED inside integrate_standard_layer (verify via return dict containing `audit` key OR monkeypatch spy). For race: []. For entry: ['popularity', 'win_odds'] (post-race per Phase 1 D-03).
    - Test 7 (TestIntegrationHermetic::test_odds_payoff_not_overwritten): sentinel odds_trifecta.parquet + payoff.parquet in standard_dir BEFORE integration; after integration their row counts AND SHA-256 IDENTICAL
    - Test 8 (TestIntegrationHermetic::test_integration_is_idempotent — HIGH #5): run integrate_standard_layer TWICE; assert second run's output SHA-256 IDENTICAL to first
    - Test 9 (TestIntegrationHermetic::test_integration_partial_swap_recoverable — HIGH #6 cycle-5 ISOLATED): the genuine mid-swap failure injection test. Steps:
      (a) Run integrate_standard_layer once cleanly against the hermetic fixtures; capture the canonical SHA-256 of all 3 outputs (race/entry/result).
      (b) MUTATE AN INPUT: append one extra synthetic row to a scraped partition file (e.g. a second entry in scraped/202301/entry.parquet with a NEW horse_race_id), so the new generation produces a DIFFERENT SHA-256 than canonical. This makes the `post != canonical` assertion meaningful (it would not be meaningful if the inputs were unchanged — a clean re-run would just reproduce canonical).
      (c) CYCLE-5 FIX: patch the DEDICATED swap function `_commit_staging` (NOT global os.replace). The patched wrapper delegates to the real _commit_staging but raises OSError on the 2nd os.replace INSIDE it (use a mutable call counter wrapping the real swap). `monkeypatch.setattr("src.pipeline.integration._commit_staging", failing_swap)` where failing_swap internally calls the real _commit_staging but intercepts the 2nd os.replace.
      (d) Invoke integrate_standard_layer again (with the mutated input). Assert it either (i) RAISES the OSError (acceptable — the swap propagated the mid-swap failure), OR (ii) returns but the corpus is in a detectable INCONSISTENT state (NOT all 3 match canonical — the mid-swap failure left at least one file from the new generation and at least one from the old). The point is that the integration did NOT silently succeed.
      (e) RESTORE _commit_staging (monkeypatch teardown) and re-invoke integrate_standard_layer. Assert all 3 output files EXIST and their SHA-256 matches a FRESH clean run of integrate_standard_layer against the (mutated) inputs — NOT the original canonical from step (a). This proves idempotent recovery: re-running the deterministic transform against the (mutated) immutable inputs fully recovers the corpus regardless of how the prior run was interrupted mid-swap.
    - Test 10 (TestUnifiedCorpus::test_unified_race_date_range): slow-path, gated
    - Test 11 (TestUnifiedCorpus::test_row_counts_within_expected_bounds): slow-path, gated; asserts PK-set union of input partitions == output unique PK count per table (MEDIUM #20)
  </behavior>
  <action>
    Create src/pipeline/integration.py following PATTERNS.md section `src/pipeline/integration.py` with the HIGH #6 cycle-3 transactionality fix + cycle-5 DEDICATED _commit_staging swap function + HIGH #8b cycle-4 hard-violation filter fix:

    Imports: `os`, `tempfile`, `from pathlib import Path`, `import pandas as pd`, `from loguru import logger`, `from src.schemas.entry import EntrySchema`, `from src.schemas.race import RaceSchema`, `from src.schemas.result import ResultSchema`, `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet, validate_integrity`, `from src.schemas.audit import audit_leakage`.

    Module constants:
    - `SCHEMA_BY_TABLE = {"race": RaceSchema, "entry": EntrySchema, "result": ResultSchema}` (3-table allowlist — Pitfall 5 / HIGH #2 odds/payoff protection)
    - `PK_BY_TABLE = {"race": "race_id", "entry": "horse_race_id", "result": "horse_race_id"}`

    Helper `_recast_to_canonical(df, schema)` (mirrors `_build_typed_dataframe` lines 242-255 — try/except TypeError re-raise, NO `errors='ignore'`).

    Helper `_assert_column_set_equality(df, schema, source_label)` (HIGH #9):
    ```
    expected = set(schema.model_fields.keys())
    actual = set(df.columns)
    if actual != expected:
        extra = actual - expected
        missing = expected - actual
        raise ValueError(f"{source_label}: column set mismatch (extra={sorted(extra)}, missing={sorted(missing)}); refusing to reindex")
    ```

    CYCLE-5 FIX (NEW HIGH #2): a DEDICATED, PATCHABLE module-level swap function:
    ```
    def _commit_staging(staging_dir: Path, standard_dir: Path) -> None:
        """Atomically-ish swap each staged table file into the standard root.

        Performs exactly 3 os.replace calls (one per table). This function exists
        as a SEPARATE, PATCHABLE boundary so the transactionality test can inject
        a mid-swap failure by patching THIS symbol — NOT global os.replace, which
        is also called by _atomic_write_parquet during the staging WRITES and would
        fire the monkeypatch at the wrong stage (cycle-4 NEW HIGH #2).
        """
        for table in ("race", "entry", "result"):
            os.replace(staging_dir / f"{table}.parquet", standard_dir / f"{table}.parquet")
    ```
    The transactionality test patches `src.pipeline.integration._commit_staging` (via `monkeypatch.setattr("src.pipeline.integration._commit_staging", failing_swap)`). The failing_swap wrapper intercepts the 2nd os.replace INSIDE _commit_staging and raises OSError. Because _atomic_write_parquet's os.replace calls happen during the STAGING writes (BEFORE the swap) and are NOT routed through _commit_staging, they are NOT affected by this patch.

    Entry point `integrate_standard_layer(standard_dir: Path = Path("data/standard"), kaggle_input_dir: Path | None = None) -> dict`:

    - HIGH #5 idempotency: if `kaggle_input_dir` is None, default to `standard_dir / "kaggle"`. READ Kaggle from `kaggle_input_dir / f"{table}.parquet"`, NEVER from `standard_dir / f"{table}.parquet"` (the output path). If missing, RAISE FileNotFoundError (MEDIUM #14).
    - MEDIUM #14: if `standard_dir / "scraped"` does not exist OR contains zero month dirs, RAISE ValueError. If a month dir lacks one of the 3 table files, RAISE FileNotFoundError.
    - For each table in SCHEMA_BY_TABLE:
      1. Read Kaggle from `kaggle_input_dir / f"{table}.parquet"`.
      2. Glob `standard_dir / "scraped" / "*" / f"{table}.parquet"`, concat into one scraped frame.
      3. HIGH #9: `_assert_column_set_equality` on BOTH kaggle_df and scraped_df BEFORE reindex.
      4. reindex BOTH to `list(schema.model_fields.keys())`
      5. recast BOTH via `_recast_to_canonical`
      6. `merged = pd.concat([kaggle_df, scraped_df], ignore_index=True)`
      7. per-table pre-dedup: assert `merged[pk].duplicated().sum() == 0`; if not, RAISE ValueError mentioning "duplicate" (Pitfall 6)
    - After all 3 merged frames built, call `validate_integrity(merged_race, merged_entry, merged_result)` (normalizer.py:263). HIGH #8b cycle-4: treat ANY violation whose string contains "duplicate", "orphan", "mismatch", OR "1-to-1" as HARD — RAISE ValueError. This extends the normalizer.py:744-756 token set (`duplicate`/`orphan`) with the `"horse_race_id mismatch: entry/result are not 1-to-1"` violation string (normalizer.py:330-334), which contains NEITHER `"duplicate"` NOR `"orphan"` and would otherwise be classified SOFT (warning only), silently writing a structurally inconsistent entry/result corpus. The exact filter is:
      ```
      hard_violations = [v for v in violations if (
          "duplicate" in v or "orphan" in v or "mismatch" in v or "1-to-1" in v
      )]
      if hard_violations:
          raise ValueError(f"integrate_standard_layer: {len(hard_violations)} hard integrity "
                           f"violation(s) detected (duplicate PK / FK orphan / entry-result 1-to-1 "
                           f"mismatch); refusing to write structurally inconsistent corpus. "
                           f"First 3: {hard_violations[:3]}")
      ```
      Adding BOTH `"mismatch"` and `"1-to-1"` is belt-and-braces — either substring matches the violation string at normalizer.py:330-334. The cycle-5 regression test (DISJOINT unique horse_race_ids) PROVES this token is load-bearing: without it, the test data has NO hard violation at all (no duplicates, no orphans) and integration would NOT raise.
    - MEDIUM #13: call `audit_leakage([RaceSchema], merged_race, "post-integration race audit")` — expect []; log WARNING if non-empty. Call `audit_leakage([EntrySchema], merged_entry, "post-integration entry audit")` — expect `['popularity', 'win_odds']`; if any OTHER column leaks, RAISE ValueError.

    HIGH #6 cycle-3 + cycle-5 transactionality (validate-before-swap with idempotent recovery via DEDICATED _commit_staging):
    - Create staging dir via `staging_dir = Path(tempfile.mkdtemp(prefix='.integration_staging_', dir=str(standard_dir)))`. The `tempfile.mkdtemp` resolves the cycle-2 MEDIUM about fixed `.integration_tmp` collisions (each run gets a unique staging dir; concurrent runs and stale leftovers do not collide).
    - Write all 3 merged frames to the staging dir via `_atomic_write_parquet(merged, staging_dir / f"{table}.parquet")`. NOTE: _atomic_write_parquet internally calls os.replace (normalizer.py:653) to commit each staging file — these are STAGING commits, NOT swaps. They are NOT routed through _commit_staging and are NOT patched by the cycle-5 transactionality test.
    - VALIDATE the staged files: re-read each staged parquet, assert row count > 0, assert schema column-set matches, assert PK uniqueness. If any staged-file validation fails, remove the staging dir (best-effort `shutil.rmtree(staging_dir, ignore_errors=True)`) and RAISE — the existing output files are untouched.
    - ONLY AFTER all staged files validate, call `_commit_staging(staging_dir, standard_dir)` to swap each into root. Wrap the swap call in try/finally that removes the staging dir. (The cycle-5 isolated test test_integration_partial_swap_recoverable patches _commit_staging to inject a mid-swap failure on the 2nd os.replace inside it — see Task 1 behavior.)
    - ACCURATE transactionality description (in the docstring + SUMMARY): "validate-before-swap with idempotent recovery." The 3 sequential `os.replace` calls INSIDE `_commit_staging` are NOT perfectly atomic — a failure between the 2nd and 3rd swap leaves a partial swap. HOWEVER, because integration reads only from immutable inputs (`data/standard/kaggle/` + scraped partitions) and NEVER from its own output, re-running `integrate_standard_layer` produces IDENTICAL output (against the same inputs), fully recovering from any partial swap. This is more robust than a backup-and-restore scheme (which depends on the backup surviving the crash) and is the genuinely correct guarantee for a deterministic, idempotent transform. The `test_integration_partial_swap_recoverable` test (cycle-5 isolated) documents this recovery path via _commit_staging monkeypatch failure injection.
    - Return `{"race": standard_dir / "race.parquet", "entry": ..., "result": ..., "audit": {...}}`. (Return type is `dict` — kept loose to accommodate the `audit` sub-dict; cycle-3 MEDIUM about strict typing is NOT load-bearing for HIGH resolution.)

    Then replace the 11 test stubs in tests/pipeline/test_integration.py with real assertions matching the behavior list. Hermetic tests (1-9) construct synthetic Kaggle+scraped Parquet in tmp_path; slow-path tests (10-11) read real data/standard/ and are gated.

    For Test 3 (test_horse_race_id_mismatch_raises — cycle-5 ISOLATED): construct race_df with at least 2 race rows (race_ids "R_E1" and "R_R1", or simply make entry/result both reference race_id "RACE1" if your synthetic fixture has it — the race_id value matters only for FK; what matters for the mismatch check is the horse_race_id column). Construct entry_df with 1 row: `horse_race_id="E1", race_id=<an id present in race_df>`. Construct result_df with 1 row: `horse_race_id="R1", race_id=<an id present in race_df>`. CRITICAL: ensure `entry_df.horse_race_id.is_unique` and `result_df.horse_race_id.is_unique` (no duplicates in either), and ensure every `race_id` in entry/result IS in race_df (no FK orphan). Then:
    ```
    from src.scraper.normalizer import validate_integrity
    violations = validate_integrity(race_df, entry_df, result_df)
    # CYCLE-5: prove the mismatch token is the SOLE classifier
    assert len(violations) == 1, f"expected exactly 1 violation (the mismatch), got {violations}"
    assert "mismatch" in violations[0] and "1-to-1" in violations[0], f"unexpected violation string: {violations[0]}"
    # Now prove integrate_standard_layer raises via the hard-violation filter
    import pytest
    with pytest.raises(ValueError, match="(mismatch|1-to-1|hard integrity)"):
        integrate_standard_layer(standard_dir, kaggle_input_dir=kaggle_input_dir)
    ```
    The `assert len(violations) == 1` is the load-bearing check: if "mismatch" were removed from the hard-violation filter, validate_integrity would STILL return this 1 violation, but the integration would NOT classify it as hard (no "duplicate"/"orphan" token present) and would NOT raise — so the `pytest.raises` would fail. Together the two assertions prove the token is load-bearing for BOTH the violation detection AND the integration's hard-classification.

    For Test 9 (test_integration_partial_swap_recoverable cycle-5 ISOLATED): use pytest's monkeypatch fixture. Pattern:
    ```
    import hashlib, os as _os
    from src.pipeline import integration as integration_mod
    from src.pipeline.integration import integrate_standard_layer, _commit_staging

    def test_integration_partial_swap_recoverable(self, monkeypatch, tmp_kaggle_input_dir, tmp_scraped_partitions_dir):
        standard_dir = tmp_kaggle_input_dir.parent  # tmp_path/data/standard
        tbls = ("race", "entry", "result")

        # (a) Clean run — capture canonical SHA-256 of all 3 outputs
        integrate_standard_layer(standard_dir)
        canonical = {t: hashlib.sha256((standard_dir / f"{t}.parquet").read_bytes()).hexdigest() for t in tbls}

        # (b) CYCLE-6: MUTATE THE RACE INPUT so the new-generation race differs from
        # canonical. Change a NON-KEY column on one scraped race row (race_id preserved
        # → referential integrity stays valid; only the race content hash changes).
        # entry/result inputs are LEFT UNCHANGED so their new-generation == canonical —
        # this is what makes the mid-swap mixed-generation state OBSERVABLE (race swapped
        # to new-gen, entry/result still canonical because not yet swapped).
        import pandas as pd
        srace_path = standard_dir / "scraped" / "202301" / "race.parquet"
        srace = pd.read_parquet(srace_path)
        non_key_col = next(c for c in srace.columns if c != "race_id" and srace[c].dtype == object)
        srace.loc[0, non_key_col] = str(srace.loc[0, non_key_col]) + "_MUTATED"
        srace.to_parquet(srace_path, engine="pyarrow", index=False)

        # (c) CYCLE-5/6: patch the DEDICATED _commit_staging — NOT global os.replace.
        # Raise on the 2nd os.replace (entry swap) so RACE is swapped (new-gen) but
        # entry/result are NOT — leaving a genuine mixed-generation corpus on disk.
        real_replace = _os.replace
        call_counter = {"n": 0}
        def failing_commit_staging(staging_dir, standard_dir):
            for table in tbls:
                call_counter["n"] += 1
                if call_counter["n"] == 2:
                    raise OSError(f"simulated mid-swap failure on {table} (race swapped, entry/result not)")
                real_replace(staging_dir / f"{table}.parquet", standard_dir / f"{table}.parquet")
        monkeypatch.setattr(integration_mod, "_commit_staging", failing_commit_staging)

        # (d) Re-invoke with mutated race input. _commit_staging raises mid-swap.
        # Handle EITHER outcome (propagate OR swallow) — then OBSERVE the on-disk state.
        raised = False
        try:
            integrate_standard_layer(standard_dir)
        except OSError:
            raised = True  # acceptable — _commit_staging propagated the mid-swap failure

        # (e) CYCLE-6: OBSERVE THE MIXED-GENERATION STATE. race was swapped (new-gen,
        # differs from canonical because its input was mutated); entry/result were NOT
        # swapped (still canonical). This proves a DETECTABLE mid-swap inconsistency —
        # the recovery guarantee must restore from exactly this state.
        post_race = hashlib.sha256((standard_dir / "race.parquet").read_bytes()).hexdigest()
        post_entry = hashlib.sha256((standard_dir / "entry.parquet").read_bytes()).hexdigest()
        post_result = hashlib.sha256((standard_dir / "result.parquet").read_bytes()).hexdigest()
        assert post_race != canonical["race"], \
            "race should be new-generation (swapped before failure) — mixed-generation state not observed (was the race input actually mutated?)"
        assert post_entry == canonical["entry"], \
            "entry should still be canonical (not yet swapped at failure) — mixed-generation state not observed"
        assert post_result == canonical["result"], \
            "result should still be canonical (not yet swapped at failure) — mixed-generation state not observed"
        # Corpus is now MIXED-GENERATION: race=new-gen, entry/result=canonical(old). Detectable.

        # (f) RECOVERY: restore _commit_staging and re-invoke. Idempotent integration
        # reads Kaggle from data/standard/kaggle/ (never its own output) + immutable
        # scraped partitions, so re-run restores a CONSISTENT new-generation corpus.
        monkeypatch.undo()
        integrate_standard_layer(standard_dir)
        recovered = {t: hashlib.sha256((standard_dir / f"{t}.parquet").read_bytes()).hexdigest() for t in tbls}
        for t in tbls:
            assert (standard_dir / f"{t}.parquet").exists(), f"{t}.parquet missing after recovery"
        # race recovered to NEW-gen (mutated input applied); entry/result consistent
        # (their new-gen == canonical since their inputs were unchanged).
        assert recovered["race"] != canonical["race"], "recovered race should reflect the mutated input"
        assert recovered["entry"] == canonical["entry"], "recovered entry should be consistent"
        assert recovered["result"] == canonical["result"], "recovered result should be consistent"
        # A 3rd invocation is byte-identical (idempotency on mutated inputs) — proves
        # the recovered state is a stable fixed point, not a transient.
        integrate_standard_layer(standard_dir)
        for t in tbls:
            h = hashlib.sha256((standard_dir / f"{t}.parquet").read_bytes()).hexdigest()
            assert h == recovered[t], f"{t}.parquet NOT byte-identical on idempotent re-run after recovery"
    ```
    Adapt the exact path navigation (`tmp_kaggle_input_dir.parent` vs explicit `tmp_path / "data" / "standard"`) to match the conftest fixture's actual return type. The patch target `monkeypatch.setattr(integration_mod, "_commit_staging", ...)` patches the symbol that integration.py itself references when calling `_commit_staging(staging_dir, standard_dir)` — i.e. the call inside integrate_standard_layer resolves to the patched function. This is exactly the boundary Codex demanded: the failure fires during the SWAP, not during the staging writes.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds' && ! grep -q 'errors=ignore' src/pipeline/integration.py && ! grep -qE 'odds_trifecta|payoff' src/pipeline/integration.py && grep -q 'SCHEMA_BY_TABLE' src/pipeline/integration.py && grep -q 'validate_integrity' src/pipeline/integration.py && grep -q '_assert_column_set_equality' src/pipeline/integration.py && grep -q 'audit_leakage' src/pipeline/integration.py && grep -q '_commit_staging' src/pipeline/integration.py && grep -q 'os.replace' src/pipeline/integration.py && grep -q 'tempfile.mkdtemp' src/pipeline/integration.py && grep -q 'idempotent' src/pipeline/integration.py && grep -qE '(mismatch|1-to-1)' src/pipeline/integration.py && grep -q 'test_horse_race_id_mismatch_raises' tests/pipeline/test_integration.py && grep -q '_commit_staging' tests/pipeline/test_integration.py</automated>
  </verify>
  <done>
    - src/pipeline/integration.py exists with integrate_standard_layer, 3-table allowlist, separate Kaggle input, tempfile.mkdtemp validate-before-swap (HIGH #6 cycle-3), DEDICATED _commit_staging swap function (cycle-5), hard-violation filter including "mismatch"/"1-to-1" (HIGH #8b cycle-4), column-set equality assert, validate_integrity call, audit_leakage call
    - Transactionality model ACCURATELY described as "validate-before-swap with idempotent recovery" (NOT falsely atomic); cycle-5 isolated test patches _commit_staging (NOT global os.replace) so the failure fires during the SWAP, never during staging writes
    - CYCLE-6: test_integration_partial_swap_recoverable MUTATES THE RACE INPUT (non-key column, race_id preserved) so new-generation race ≠ canonical; asserts the mid-swap failure leaves an OBSERVABLE MIXED-GENERATION state (race=new-gen, entry/result=canonical) and that re-run RECOVERS all 3 to a consistent new-generation corpus (3rd run byte-identical). This proves recovery from the actual mixed-generation failure mode, not just OSError propagation.
    - 2 slow-path tests collected; skipped or green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle + scraped Parquet → unified Parquet | Two independently-produced corpora cross into a single merged output. Schema reconciliation + PK dedup + FK validation + entry/result 1-to-1 integrity are the trust controls. |
| Integration write → existing data/standard/*.parquet | Destructive overwrite (D-03). tempfile.mkdtemp staging + validate-before-swap via DEDICATED _commit_staging + idempotent recovery are the safety controls (HIGH #6 cycle-3 + cycle-5 _commit_staging fix). |
| Integration output → Phase 3/7/9 downstream | Mixed-generation corpus OR structurally inconsistent entry/result would corrupt feature/model/backtest. Validate-before-swap (HIGH #6 cycle-3) + idempotent recovery + entry/result 1-to-1 mismatch RAISE (HIGH #8b cycle-4) are the controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Pre-dedup duplicate race_id (boundary overlap) | mitigate | Per-table `merged[pk].duplicated().sum() == 0` assertion + `validate_integrity` duplicate check RAISE ValueError. Verified by `test_no_duplicate_race_ids_fail_loud`. |
| T-06-06 | Tampering | `pd.concat` without column-set check inflates/masks schema | mitigate | HIGH #9: `_assert_column_set_equality(df, schema, label)` BEFORE reindex. Verified by `test_column_set_mismatch_raises_before_reindex`. |
| T-06-07 | Denial of Service | Half-written mixed-generation corpus on partial swap failure (HIGH #6 cycle-3 + cycle-5 _commit_staging) | mitigate | Validate-before-swap: write all 3 to `tempfile.mkdtemp` staging, validate staged files, swap via `_commit_staging(staging_dir, standard_dir)` (DEDICATED patchable function — cycle-5) only after validation passes. Mid-swap crash recoverable via idempotent re-run (integration reads from immutable inputs). Verified by `test_integration_is_idempotent` (clean re-run) + `test_integration_partial_swap_recoverable` (cycle-5 isolated: monkeypatch `_commit_staging` to raise OSError on the 2nd os.replace INSIDE it, mutate an input so new generation differs from canonical, assert inconsistent state OR raise, then RESTORE + re-invoke + assert byte-identical recovery against the mutated inputs). Transactionality model ACCURATELY described as "validate-before-swap with idempotent recovery." |
| T-06-08 | Information Disclosure | Provenance leak via `source` column | mitigate | D-05 guard: no `source` column. Verified by grep gate. |
| T-06-09 | Tampering | Silent FK orphan acceptance (HIGH #8) | mitigate | `validate_integrity` CALLED; orphan FKs raise ValueError. Verified by `test_referential_integrity_orphan_raises`. |
| T-06-09b | Tampering | Silent entry/result horse_race_id 1-to-1 mismatch (HIGH #8b cycle 4 + cycle 5) | mitigate | hard-violation filter EXTENDED with `"mismatch"` (and `"1-to-1"`); matches the violation string at `normalizer.py:330-334` which contains NEITHER `"duplicate"` NOR `"orphan"`. CYCLE-5 ISOLATED regression test `test_horse_race_id_mismatch_raises` uses DISJOINT UNIQUE horse_race_ids in entry/result (e.g. entry=["E1"], result=["R1"]) with NO duplicates in either table and NO FK orphans — so `validate_integrity` returns EXACTLY ONE violation (the mismatch), proving the token is the sole classifier and is load-bearing for the integration's hard-classification. Without "mismatch" in the filter, this data has NO hard violation and integration would NOT raise. |
| T-06-10 | Tampering | Overwriting odds_trifecta/payoff (Phase 5 seed) | mitigate | SCHEMA_BY_TABLE = {race, entry, result} allowlist; sentinel test. |
| T-06-11 | Tampering | Non-idempotent re-run double-merges (HIGH #5) | mitigate | Kaggle read from `kaggle_input_dir` (separate path). Verified by `test_integration_is_idempotent`. |
| T-06-12 | Repudiation | audit_leakage imported but never called (MEDIUM #13) | mitigate | audit_leakage CALLED inside integrate_standard_layer; results in return dict. Verified by `test_no_post_race_leakage_audit_called`. |
| T-06-13 | Denial of Service | Silent skip on missing input (MEDIUM #14) | mitigate | Explicit FileNotFoundError / ValueError for each missing-input case. |
| T-06-14 | Tampering | autouse skip swallows hermetic tests (HIGH #7) | mitigate | Two-class split: TestIntegrationHermetic (no autouse) + TestUnifiedCorpus (gated). |
| T-06-15 | Tampering | Fixed `.integration_tmp` staging dir collides on concurrent runs / stale leftovers (cycle-2 MEDIUM) | mitigate | `tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir)` — unique per run; finally-block cleanup. |
| T-06-16 | Tampering | Transactionality test patches global os.replace and fires during STAGING writes instead of SWAP (cycle-4 NEW HIGH #2) | mitigate | CYCLE-5: swap extracted into DEDICATED module-level `_commit_staging(staging_dir, standard_dir)`; the test patches THIS symbol so the monkeypatched OSError fires during the actual SWAP, never during the staging writes (_atomic_write_parquet's os.replace is NOT routed through _commit_staging). |
| T-06-17 | Tampering | Mismatch regression test passes via the duplicate path (cycle-4 NEW HIGH #1) | mitigate | CYCLE-5: test data uses DISJOINT UNIQUE horse_race_ids (entry=["E1"], result=["R1"]) so no duplicate check fires; the test asserts validate_integrity returns EXACTLY ONE violation containing "mismatch", proving the token is load-bearing. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds'` green (9 hermetic tests, including cycle-5 ISOLATED test_horse_race_id_mismatch_raises + cycle-5 ISOLATED test_integration_partial_swap_recoverable)
- `python -m pytest tests/ -q` full suite green
- `grep 'errors=.ignore.' src/pipeline/integration.py` returns nothing
- `grep 'odds_trifecta\|payoff' src/pipeline/integration.py` returns nothing
- `grep 'validate_integrity\|_assert_column_set_equality\|_commit_staging\|os.replace\|kaggle_input_dir\|tempfile.mkdtemp\|idempotent' src/pipeline/integration.py` returns matches for all
- HIGH #8b cycle-4 + cycle-5: `grep -E '(mismatch|1-to-1)' src/pipeline/integration.py` returns match (hard-violation filter extended)
- HIGH #8b cycle-5: `grep 'test_horse_race_id_mismatch_raises' tests/pipeline/test_integration.py` returns match
- HIGH #6 cycle-5: `grep '_commit_staging' src/pipeline/integration.py tests/pipeline/test_integration.py` returns matches in BOTH files
</verification>

<success_criteria>
- HIGH #5 resolved (KEPT): integration reads Kaggle from separate input path; idempotent
- HIGH #6 resolved (cycle 3 + cycle 4 + cycle 5): validate-before-swap via tempfile.mkdtemp staging + DEDICATED _commit_staging swap function; idempotent recovery model accurately described; cycle-5 isolated test patches _commit_staging (NOT global os.replace) and mutates an input so the mid-swap failure produces a DIFFERENT generation than canonical — proving recovery under the REAL mid-swap failure
- HIGH #7 resolved (KEPT): hermetic tests ungated; real-corpus tests gated
- HIGH #8 resolved (KEPT): FK orphan test injects orphan, asserts validate_integrity reports it
- HIGH #8b resolved (cycle 4 production fix + cycle 5 TEST ISOLATION): hard-violation filter extended with "mismatch"/"1-to-1"; cycle-5 isolated regression test uses DISJOINT UNIQUE horse_race_ids and asserts validate_integrity returns EXACTLY ONE violation containing "mismatch" — proving the token is load-bearing for BOTH violation detection AND integration hard-classification
- HIGH #9 resolved (KEPT): column-set equality assert before reindex
- MEDIUMs #12/#13/#14 resolved: 11 tests (test_horse_race_id_mismatch_raises retained + cycle-5 isolated; test_integration_partial_swap_recoverable cycle-5 isolated), audit_leakage called, explicit missing-input errors
- Module ready for Wave 3 invocation against real corpus
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-02-SUMMARY.md` when done. Record: the transactionality model ("validate-before-swap with idempotent recovery" via tempfile.mkdtemp staging + DEDICATED `_commit_staging` swap function + cycle-5 isolated monkeypatch mid-swap failure injection test), the hard-violation filter extension (now includes "mismatch"/"1-to-1" — HIGH #8b cycle-4) with cycle-5 PROOF that the token is load-bearing (DISJOINT unique horse_race_ids, validate_integrity returns exactly ONE mismatch violation), the test count (11, including cycle-5 isolated test_horse_race_id_mismatch_raises + cycle-5 isolated test_integration_partial_swap_recoverable), the audit_leakage call confirmation.
</output>

## Artifacts this phase produces

- `src/pipeline/integration.py` (NEW)
  - `integrate_standard_layer(standard_dir, kaggle_input_dir=None) -> dict` — entry point (HIGH #5 separate input)
  - `_recast_to_canonical(df, schema) -> pd.DataFrame` — raises TypeError
  - `_assert_column_set_equality(df, schema, source_label) -> None` — HIGH #9
  - `_commit_staging(staging_dir, standard_dir) -> None` — CYCLE-5 NEW dedicated, patchable swap function (3 os.replace calls inside); the transactionality test patches THIS symbol, not global os.replace
  - `SCHEMA_BY_TABLE` — 3-table allowlist
  - `PK_BY_TABLE` — PK column per table
  - validate-before-swap via `tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir)` (HIGH #6 cycle-3) + idempotent recovery via DEDICATED `_commit_staging` (cycle-5)
  - hard-violation filter EXTENDED with `"mismatch"`/`"1-to-1"` (HIGH #8b cycle-4) — matches `normalizer.py:330-334` violation string; cycle-5 test PROVES the token is load-bearing
  - `validate_integrity` call (HIGH #8)
  - `audit_leakage` call (MEDIUM #13)
- `tests/pipeline/test_integration.py` (NEW)
  - `class TestIntegrationHermetic:` (9 tests, NO autouse skip) — HIGH #7; includes cycle-5 ISOLATED `test_horse_race_id_mismatch_raises` (HIGH #8b cycle-4 + cycle-5: DISJOINT unique horse_race_ids, asserts validate_integrity returns EXACTLY ONE mismatch violation) + cycle-5 ISOLATED `test_integration_partial_swap_recoverable` (patches `_commit_staging`, mutates input, asserts inconsistent state OR raise, then RESTORE + re-invoke + byte-identical recovery)
  - `class TestUnifiedCorpus:` (2 tests, autouse `_require_scraped_data`)
  - 11 tests total: `test_no_duplicate_race_ids_fail_loud`, `test_referential_integrity_orphan_raises`, `test_horse_race_id_mismatch_raises` (cycle-5 isolated), `test_column_set_mismatch_raises_before_reindex`, `test_schema_invariant_post_integration`, `test_no_post_race_leakage_audit_called`, `test_odds_payoff_not_overwritten`, `test_integration_is_idempotent`, `test_integration_partial_swap_recoverable` (cycle-5 isolated), `test_unified_race_date_range`, `test_row_counts_within_expected_bounds`
- `tests/pipeline/conftest.py` (MODIFIED)
  - `tmp_kaggle_input_dir(tmp_path, ...)` fixture (HIGH #5)
  - `tmp_scraped_partitions_dir(tmp_path, ...)` fixture

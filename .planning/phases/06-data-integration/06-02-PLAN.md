---
phase: 06-data-integration
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - src/pipeline/integration.py
  - tests/pipeline/test_integration.py
  - tests/pipeline/conftest.py
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #5 (idempotency — KEPT from cycle 2): integrate_standard_layer reads Kaggle rows from a SEPARATE input path (data/standard/kaggle/{table}.parquet — written by Plan 06-01 Task 2 via convert(core_tables_subdir='kaggle')) and NEVER from the output path data/standard/{table}.parquet. A second invocation produces byte-identical output."
    - "BLOCKER-1 (cross-plan data contract — KEPT): Plan 06-01 writes the regenerated Kaggle race/entry/result to data/standard/kaggle/{table}.parquet. integrate_standard_layer defaults kaggle_input_dir to standard_dir / 'kaggle' and reads each Kaggle table from there. The data/standard/kaggle/ directory is created by Plan 06-01 BEFORE this plan runs (Wave 1 -> Wave 2 dependency)."
    - "HIGH #6 (corpus transactionality — cycle 3 + cycle 4): integrate_standard_layer writes all 3 unified tables to a STAGING subdir (tempfile.mkdtemp() under standard_dir, prefix '.integration_staging_') FIRST, validates ALL 3 there (schema column-set, row counts > 0, PK integrity via validate_integrity on the staged DataFrames), THEN swaps each into root via os.replace. Transactionality model is ACCURATELY described as 'validate-before-swap with idempotent recovery' — NOT perfect atomicity. A mid-swap crash leaves at most a partial swap; because integration is IDEMPOTENT (reads Kaggle from data/standard/kaggle/, never from output) and reads scraped partitions (immutable inputs), re-running integrate_standard_layer produces IDENTICAL output, fully recovering. Cycle 4 strengthens the test: test_integration_partial_swap_recoverable now INJECTS a mid-swap os.replace failure via monkeypatch (2nd os.replace raises OSError), proves the integration surfaces/raises OR leaves an inconsistent state, then re-invokes with os.replace RESTORED and asserts all 3 outputs are restored to canonical byte-identical content. This proves the idempotent recovery guarantee under the REAL mixed-generation failure mode."
    - "HIGH #6 (staging dir uses tempfile.mkdtemp — cycle-3 fix for MEDIUM): the staging dir is created via tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir) — NOT a fixed '.integration_tmp' name — so concurrent runs and stale leftovers from prior crashes do not collide. The staging dir is removed in a finally block (best-effort cleanup)."
    - "HIGH #7 (autouse skip gate — KEPT from cycle 2): hermetic tests live in TestIntegrationHermetic with NO _require_scraped_data autouse fixture; real-corpus slow-path tests live in TestUnifiedCorpus with the autouse skip. Hermetic tests run unconditionally."
    - "HIGH #8 (FK test actually raises — KEPT from cycle 2): test_referential_integrity injects an entry row whose race_id is NOT in the race table and asserts that validate_integrity returns a list containing 'orphan entry race_id' AND that integrate_standard_layer RAISES ValueError."
    - "HIGH #8b (horse_race_id 1-to-1 mismatch RAISES — cycle 4 NEW HIGH FIX): the hard-violation filter in integrate_standard_layer matches the FULL set of validate_integrity hard-failure strings: 'duplicate' OR 'orphan' OR 'mismatch' (the last token catches the 'horse_race_id mismatch: entry/result are not 1-to-1' violation at normalizer.py:330-334 which contains NEITHER 'duplicate' NOR 'orphan'). Equivalently, the filter is 'duplicate' in v or 'orphan' in v or 'mismatch' in v or '1-to-1' in v. A new regression test test_horse_race_id_mismatch_raises injects an entry/result horse_race_id cardinality mismatch (entry has 1 row 'X', result has 2 rows 'X') and asserts integrate_standard_layer RAISES ValueError (not just warns)."
    - "HIGH #9 (reindex guarded by column-set equality — KEPT from cycle 2): BEFORE any reindex, integrate_standard_layer asserts set(input_df.columns) == set(schema.model_fields.keys()) and raises ValueError on mismatch."
    - "integrate_standard_layer imports _atomic_write_parquet and SCHEMA_DTYPE_MAP from src.scraper.normalizer (NO re-implementation)"
    - "integrate_standard_layer hardcodes SCHEMA_BY_TABLE = {race, entry, result} — does NOT iterate TABLE_TO_SCHEMA.keys() (Pitfall 5)"
    - "Pre-dedup overlap raises ValueError (not silent drop_duplicates away — Pitfall 6)"
    - "integrate_standard_layer never adds a `source` column (D-05)"
    - "audit_leakage is CALLED inside integrate_standard_layer (not just imported) on the merged race_df and entry_df (MEDIUM #13)"
    - "Missing Kaggle files, missing scraped partitions, or missing table-within-a-month each raise an explicit error (MEDIUM #14)"
    - "Module-level logging uses loguru; logs per-table (NOT per-row)"
requirements_resolved:
  - "HIGH #5 (cycle 2 RESOLVED — KEPT): kaggle_input_dir separate path + idempotency test"
  - "HIGH #6 (cycle 2 PARTIALLY RESOLVED → cycle 3 RESOLVED → cycle 4 STRENGTHENED): validate-before-swap via tempfile.mkdtemp staging + idempotent recovery model; cycle 4 strengthens test_integration_partial_swap_recoverable to INJECT mid-swap os.replace failure via monkeypatch (2nd call raises OSError), then prove recovery on re-invocation with os.replace restored"
  - "HIGH #7 (cycle 2 RESOLVED — KEPT): two-class split"
  - "HIGH #8 (cycle 2 RESOLVED — KEPT): orphan FK injection + validate_integrity + ValueError"
  - "HIGH #8b (cycle 4 NEW HIGH RESOLVED): horse_race_id 1-to-1 mismatch RAISES — filter extended with 'mismatch' (or '1-to-1'); regression test test_horse_race_id_mismatch_raises"
  - "HIGH #9 (cycle 2 RESOLVED — KEPT): column-set equality assert before reindex"
artifacts:
  - path: "src/pipeline/integration.py"
    provides: "integrate_standard_layer(standard_dir, kaggle_input_dir=None) entry point + _recast_to_canonical + _assert_column_set_equality + validate-before-swap with idempotent recovery; hard-violation filter includes 'mismatch'"
    exports: ["integrate_standard_layer"]
    contains: "SCHEMA_BY_TABLE"
  - path: "tests/pipeline/test_integration.py"
    provides: "TestIntegrationHermetic (fast path, no skip gate — 9 tests incl. new test_horse_race_id_mismatch_raises + cycle-4 strengthened test_integration_partial_swap_recoverable with monkeypatch mid-swap injection) + TestUnifiedCorpus (real corpus, gated). 11 tests total."
    contains: "class TestIntegrationHermetic"
  - path: "tests/pipeline/conftest.py"
    provides: "tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures"
    contains: "tmp_kaggle_input_dir"
key_links:
  - from: "src/pipeline/integration.py"
    to: "src/scraper/normalizer._atomic_write_parquet + validate_integrity"
    via: "from src.scraper.normalizer import _atomic_write_parquet, SCHEMA_DTYPE_MAP, validate_integrity"
    pattern: "from src\\.scraper\\.normalizer import.*(SCHEMA_DTYPE_MAP|_atomic_write_parquet|validate_integrity)"
  - from: "integrate_standard_layer"
    to: "src.schemas.audit.audit_leakage"
    via: "post-integration audit CALL (not just import)"
    pattern: "audit_leakage\\(\\[RaceSchema\\]"
  - from: "integrate_standard_layer hard-violation filter"
    to: "validate_integrity 'horse_race_id mismatch' string (normalizer.py:330-334)"
    via: "hard_violations filter includes 'mismatch' (or '1-to-1') in addition to 'duplicate'/'orphan'"
    pattern: "(mismatch|1-to-1)"
  - from: "integrate_standard_layer staging swap"
    to: "data/standard/{race,entry,result}.parquet"
    via: "tempfile.mkdtemp staging + os.replace (HIGH #6 cycle-3 validate-before-swap + idempotent recovery; cycle-4 monkeypatch mid-swap failure injection test)"
    pattern: "os\\.replace"
---

<objective>
Build the Phase 6 integration module `src/pipeline/integration.py` and its test suite `tests/pipeline/test_integration.py`. The module merges Kaggle (post-D-01/D-02 from Plan 06-01) and scraped (post-D-06 pre-task) corpora into a unified `data/standard/{race,entry,result}.parquet` per D-03/D-04.

This plan resolves the cycle-3 residuals for 06-02 (the cycle-4 NEW HIGH on horse_race_id mismatch-as-soft + the cycle-3 PARTIAL on transactionality test injection) and KEEPS the four cycle-2-resolved HIGHs:

- **HIGH #8b (horse_race_id mismatch RAISES — cycle 4 NEW HIGH FIX):** the cycle-3 plan's hard-violation filter was `"duplicate" in v or "orphan" in v` (mirroring `normalizer.py:744-756`). But `validate_integrity` returns `"horse_race_id mismatch: entry/result are not 1-to-1 ..."` for the entry/result cardinality check (`normalizer.py:330-334`) — that string contains NEITHER `"duplicate"` NOR `"orphan"`, so a non-1-to-1 entry/result corpus was written as a warning only. **FIX (cycle 4):** extend the hard-violation filter to ALSO match `"mismatch"` (or `"1-to-1"`): `hard_violations = [v for v in violations if ("duplicate" in v or "orphan" in v or "mismatch" in v or "1-to-1" in v)]`. Add a regression test `test_horse_race_id_mismatch_raises` that injects an entry/result horse_race_id cardinality mismatch (entry has 1 row "X", result has 2 rows "X") and asserts `integrate_standard_layer` RAISES ValueError. The Phase 6 success criterion #1 (no duplicates) implicitly assumes entry/result integrity — a structurally inconsistent entry/result corpus must fail loud, exactly the kind of silent corruption the EV downstream (Phase 7/9) cannot tolerate.

- **HIGH #6 (corpus transactionality — cycle 3 RESOLVED, cycle 4 STRENGTHENED test):** cycle 3 used `tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir)` as the staging dir, validated ALL 3 staged tables BEFORE any swap, then swapped each via `os.replace`. The transactionality model is described ACCURATELY as "validate-before-swap with idempotent recovery" — NOT perfect atomicity. The idempotent recovery guarantee is real because integration reads only from immutable inputs (`data/standard/kaggle/` + scraped partitions). BUT the cycle-3 test `test_integration_partial_swap_recoverable` deleted ONE output file post-run and re-invoked — it did NOT inject a failure DURING the swap loop, so it did not prove recovery from the ACTUAL mixed-generation failure mode Codex flagged. **FIX (cycle 4):** replace the post-run file-delete test with a mid-swap injection: use `pytest`'s `monkeypatch.setattr` (or `unittest.mock.patch`) to make `os.replace` RAISE `OSError` on the 2nd call during the swap, assert the integration surfaces/raises OR that the output is left in a detectable inconsistent state (1 file new-generation, 2 files old/missing), then RESTORE `os.replace` and re-invoke `integrate_standard_layer` and assert ALL 3 output files are byte-identical to a clean canonical run. This proves the idempotent-recovery guarantee under the real mid-swap failure.

- **HIGH #5 (idempotency — RESOLVED in cycle 2, KEPT):** read Kaggle from `data/standard/kaggle/{table}.parquet` (a stable separate input), never from the output path. `test_integration_is_idempotent` proves byte-identical re-run.

- **HIGH #7 (autouse skip — RESOLVED in cycle 2, KEPT):** hermetic tests ungated; real-corpus tests gated.

- **HIGH #8 (FK orphan — RESOLVED in cycle 2, KEPT):** `test_referential_integrity_orphan_raises` injects orphan, asserts `validate_integrity` reports it, integration raises ValueError.

- **HIGH #9 (column-set equality — RESOLVED in cycle 2, KEPT):** `_assert_column_set_equality` before reindex.

MEDIUMs: #12 (test count: 11, not 10 — adds `test_horse_race_id_mismatch_raises` for HIGH #8b cycle 4); #13 (audit_leakage actually called); #14 (explicit errors for missing Kaggle files, missing scraped partitions, missing table within month).

Output: New module `src/pipeline/integration.py` exposing `integrate_standard_layer()`. New test file `tests/pipeline/test_integration.py` with 11 tests across two classes. Extended `tests/pipeline/conftest.py` with hermetic Kaggle/scraped fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md

# Phase 4 dtype authority + atomic write + FK validator (primitives to reuse)
@src/scraper/normalizer.py

# Phase 1 audit contract
@src/schemas/audit.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py

# Reference test pattern: Phase 4 TestSchemaCompatibility
@tests/scraper/test_end_to_end.py

# Conftest: extend the existing tmp_standard_dir fixture
@tests/pipeline/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hermetic fixtures to tests/pipeline/conftest.py (tmp_kaggle_input_dir + tmp_scraped_partitions_dir); create tests/pipeline/test_integration.py with TWO test classes (TestIntegrationHermetic ungated — 9 tests incl. cycle-4 NEW test_horse_race_id_mismatch_raises + cycle-4 strengthened test_integration_partial_swap_recoverable with monkeypatch mid-swap injection + TestUnifiedCorpus gated — 2 tests)</name>
  <files>tests/pipeline/conftest.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - tests/pipeline/conftest.py (full file — tmp_standard_dir at line 291, sample_standard_race_df at 304, sample_standard_entry_df at 355, sample_standard_result_df at 436, combined standard-files fixture around 492-509; extend, do not duplicate)
    - tests/scraper/conftest.py (lines 17-47 — the canonical tmp_path-derived fixture pattern)
    - tests/scraper/test_end_to_end.py (lines 464-536 — TestSchemaCompatibility class shape AND the _require_kaggle_parquet autouse skip fixture at 467-472; mirror as `_require_scraped_data` for the GATED class only)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Phase Requirements → Test Map" — the test names/behaviors)
  </read_first>
  <behavior>
    - Test 1: `python -m pytest tests/pipeline/test_integration.py --collect-only -q` lists all 11 expected test names AND assigns them to the correct class (TestIntegrationHermetic: 9 tests; TestUnifiedCorpus: 2 tests)
    - Test 2: `tmp_scraped_partitions_dir` fixture creates `tmp_path/data/standard/scraped/202301/` and `tmp_path/data/standard/scraped/202302/` with one tiny synthetic Parquet per table per month (reindex to Schema.model_fields order)
    - Test 3: `tmp_kaggle_input_dir` fixture creates `tmp_path/data/standard/kaggle/{race,entry,result}.parquet` (the SEPARATE input path — HIGH #5). It does NOT touch `tmp_path/data/standard/{race,entry,result}.parquet` (the output path).
    - Test 4 (HIGH #7): TestIntegrationHermetic has NO autouse skip fixture; its tests COLLECT and RUN against synthetic tmp_path data. TestUnifiedCorpus has the `_require_scraped_data` autouse skip; its 2 tests skip when the real corpus is smoke-only.
  </behavior>
  <action>
    Extend tests/pipeline/conftest.py with TWO new fixtures:
    (1) `tmp_kaggle_input_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/kaggle/` and writes `{race,entry,result}.parquet` there (reindex each to `list(Schema.model_fields.keys())`, write via `to_parquet(engine='pyarrow', index=False)`). This is the HIGH #5 separate-input-path fixture.
    (2) `tmp_scraped_partitions_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/scraped/{202301,202302}/` and writes one synthetic Parquet per table per month (same reindex+write). Mirrors the Phase 4 partitioned layout.

    Then create tests/pipeline/test_integration.py with TWO classes:
    - `class TestIntegrationHermetic:` (NO autouse skip — runs unconditionally against tmp_path). 9 test method stubs:
      `test_no_duplicate_race_ids_fail_loud` (HIGH #8)
      `test_referential_integrity_orphan_raises` (HIGH #8)
      `test_horse_race_id_mismatch_raises` (HIGH #8b cycle 4 NEW — injects entry/result horse_race_id cardinality mismatch, asserts ValueError)
      `test_column_set_mismatch_raises_before_reindex` (HIGH #9)
      `test_schema_invariant_post_integration` (every race_flag_* Arrow bool; distance int64; race_date string)
      `test_no_post_race_leakage_audit_called` (MEDIUM #13)
      `test_odds_payoff_not_overwritten` (sentinel files unchanged post-integration)
      `test_integration_is_idempotent` (HIGH #5: two consecutive runs produce byte-identical output)
      `test_integration_partial_swap_recoverable` (HIGH #6 cycle-4: monkeypatch `os.replace` to raise OSError on the 2nd call during the swap, assert integration surfaces/raises OR leaves inconsistent state, then RESTORE os.replace and re-invoke integrate_standard_layer, assert all 3 outputs restored to canonical byte-identical content)
    - `class TestUnifiedCorpus:` (autouse `_require_scraped_data` skip when real corpus is smoke-only). 2 test method stubs:
      `test_unified_race_date_range`
      `test_row_counts_within_expected_bounds`

    Each stub body is `pytest.skip("integration.py not yet implemented — Wave 0 stub")` for now (Task 2 replaces). Use try/except at module top with a sentinel for the `integrate_standard_layer` import so collection succeeds when integration.py does not yet exist.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py --collect-only -q 2>&1 | grep -E '^(tests/pipeline/test_integration\.py::|test_)' | wc -l | tr -d ' ' | grep -q '^11$'</automated>
  </verify>
  <done>
    - tests/pipeline/test_integration.py exists with 2 classes (Hermetic ungated + UnifiedCorpus gated) and 11 collected stubs
    - conftest.py has tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures
    - `pytest --collect-only` lists 11 items without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/pipeline/integration.py — separate Kaggle input path (HIGH #5), validate-before-swap with idempotent recovery via tempfile.mkdtemp staging (HIGH #6 cycle-3 + cycle-4 monkeypatch mid-swap test), hard-violation filter including 'mismatch' (HIGH #8b cycle 4), column-set equality assert (HIGH #9), real FK validation via validate_integrity (HIGH #8), audit_leakage CALLED (MEDIUM #13); replace test stubs with real assertions</name>
  <files>src/pipeline/integration.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - src/scraper/normalizer.py (lines 95-192 SCHEMA_DTYPE_MAP; lines 226-255 _build_typed_dataframe; lines 263-374 validate_integrity — CONFIRM at lines 330-334 the violation string is exactly `horse_race_id mismatch: entry/result are not 1-to-1 (only-in-entry=..., only-in-result=..., count-mismatch={...})` — contains NEITHER 'duplicate' NOR 'orphan'; lines 643-653 _atomic_write_parquet; lines 744-756 hard_violations raise ValueError pattern — mirror this with the EXTENDED token set that includes 'mismatch')
    - src/schemas/audit.py (audit_leakage signature — returns list[str] of leaked post-race column names; logs warning, does NOT raise per D-12)
    - src/schemas/race.py / entry.py / result.py (model_fields — canonical column-order source)
    - .planning/phases/06-data-integration/06-PATTERNS.md (section `src/pipeline/integration.py`)
    - .planning/phases/06-data-integration/06-RESEARCH.md (Code Examples — Verified end-to-end integration recipe)
    - tests/pipeline/test_integration.py (the stubs from Task 1)
  </read_first>
  <behavior>
    - Test 1 (TestIntegrationHermetic::test_no_duplicate_race_ids_fail_loud): construct Kaggle input + scraped partition with OVERLAPPING race_id; integrate_standard_layer RAISES ValueError mentioning "duplicate"
    - Test 2 (TestIntegrationHermetic::test_referential_integrity_orphan_raises — HIGH #8): construct synthetic data where entry.race_id contains an ID NOT in race.race_id; integrate_standard_layer calls validate_integrity, which returns a list containing "orphan entry race_id"; integrate_standard_layer then RAISES ValueError
    - Test 3 (TestIntegrationHermetic::test_horse_race_id_mismatch_raises — HIGH #8b cycle 4 NEW): construct synthetic Kaggle+scraped data where the entry and result tables have a horse_race_id CARDINALITY mismatch — e.g. entry has 1 row with horse_race_id "X", result has 2 rows both with horse_race_id "X" (set-equal but not multiset-equal). validate_integrity returns a list containing "horse_race_id mismatch: entry/result are not 1-to-1". integrate_standard_layer's hard-violation filter MUST classify this as HARD (the string contains "mismatch") and RAISE ValueError. This is the regression test Codex's cycle-3 NEW HIGH demanded.
    - Test 4 (TestIntegrationHermetic::test_column_set_mismatch_raises_before_reindex — HIGH #9): construct an input DataFrame whose column set differs from set(Schema.model_fields.keys()); integrate_standard_layer RAISES ValueError BEFORE reaching reindex
    - Test 5 (TestIntegrationHermetic::test_schema_invariant_post_integration): integrated race.parquet via pq.read_schema — every race_flag_* Arrow bool, distance int64, race_date string
    - Test 6 (TestIntegrationHermetic::test_no_post_race_leakage_audit_called — MEDIUM #13): audit_leakage CALLED inside integrate_standard_layer (verify via return dict containing `audit` key OR monkeypatch spy). For race: []. For entry: ['popularity', 'win_odds'] (post-race per Phase 1 D-03).
    - Test 7 (TestIntegrationHermetic::test_odds_payoff_not_overwritten): sentinel odds_trifecta.parquet + payoff.parquet in standard_dir BEFORE integration; after integration their row counts AND SHA-256 IDENTICAL
    - Test 8 (TestIntegrationHermetic::test_integration_is_idempotent — HIGH #5): run integrate_standard_layer TWICE; assert second run's output SHA-256 IDENTICAL to first
    - Test 9 (TestIntegrationHermetic::test_integration_partial_swap_recoverable — HIGH #6 cycle-4 STRENGTHENED): the genuine mid-swap failure injection test the cycle-3 reviewer demanded. Steps:
      (a) Run integrate_standard_layer once cleanly against the hermetic fixtures; capture the canonical SHA-256 of all 3 outputs (race/entry/result).
      (b) Use `monkeypatch.setattr("os.replace", failing_replace)` where `failing_replace` is a wrapper that delegates to the real os.replace on the 1st call and RAISES OSError on the 2nd call (use a mutable call counter or a closure capturing `itertools.count`). Alternative: `unittest.mock.patch("src.pipeline.integration.os.replace", side_effect=[None, OSError("simulated mid-swap failure"), None])`.
      (c) Invoke integrate_standard_layer again. Assert it either (i) RAISES the OSError (acceptable — the swap loop propagates), OR (ii) returns but the corpus is in a detectable INCONSISTENT state (1 file new-generation, 2 files old/missing — i.e. NOT all 3 match the canonical SHA-256). The point of this assertion is that the integration did NOT silently succeed as if nothing happened.
      (d) RESTORE os.replace (monkeypatch undo / stop the patcher) and re-invoke integrate_standard_layer. Assert all 3 output files EXIST and their SHA-256 matches the canonical captures from step (a). This proves the idempotent recovery model: integration reads only from immutable inputs (data/standard/kaggle/ + scraped partitions), never from its own output, so a re-run restores the canonical corpus regardless of how the prior run was interrupted mid-swap.
      This replaces the cycle-3 file-delete test, which only proved "delete-a-file-then-rerun" recovery, NOT mid-swap failure recovery.
    - Test 10 (TestUnifiedCorpus::test_unified_race_date_range): slow-path, gated
    - Test 11 (TestUnifiedCorpus::test_row_counts_within_expected_bounds): slow-path, gated; asserts PK-set union of input partitions == output unique PK count per table (MEDIUM #20)
  </behavior>
  <action>
    Create src/pipeline/integration.py following PATTERNS.md section `src/pipeline/integration.py` with the HIGH #6 cycle-3 transactionality fix + HIGH #8b cycle-4 hard-violation filter fix:

    Imports: `os`, `tempfile`, `from pathlib import Path`, `import pandas as pd`, `from loguru import logger`, `from src.schemas.entry import EntrySchema`, `from src.schemas.race import RaceSchema`, `from src.schemas.result import ResultSchema`, `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet, validate_integrity`, `from src.schemas.audit import audit_leakage`.

    Module constants:
    - `SCHEMA_BY_TABLE = {"race": RaceSchema, "entry": EntrySchema, "result": ResultSchema}` (3-table allowlist — Pitfall 5 / HIGH #2 odds/payoff protection)
    - `PK_BY_TABLE = {"race": "race_id", "entry": "horse_race_id", "result": "horse_race_id"}`

    Helper `_recast_to_canonical(df, schema)` (mirrors `_build_typed_dataframe` lines 242-255 — try/except TypeError re-raise, NO `errors='ignore'`).

    Helper `_assert_column_set_equality(df, schema, source_label)` (HIGH #9):
    ```
    expected = set(schema.model_fields.keys())
    actual = set(df.columns)
    if actual != expected:
        extra = actual - expected
        missing = expected - actual
        raise ValueError(f"{source_label}: column set mismatch (extra={sorted(extra)}, missing={sorted(missing)}); refusing to reindex")
    ```

    Entry point `integrate_standard_layer(standard_dir: Path = Path("data/standard"), kaggle_input_dir: Path | None = None) -> dict[str, Path]`:

    - HIGH #5 idempotency: if `kaggle_input_dir` is None, default to `standard_dir / "kaggle"`. READ Kaggle from `kaggle_input_dir / f"{table}.parquet"`, NEVER from `standard_dir / f"{table}.parquet"` (the output path). If missing, RAISE FileNotFoundError (MEDIUM #14).
    - MEDIUM #14: if `standard_dir / "scraped"` does not exist OR contains zero month dirs, RAISE ValueError. If a month dir lacks one of the 3 table files, RAISE FileNotFoundError.
    - For each table in SCHEMA_BY_TABLE:
      1. Read Kaggle from `kaggle_input_dir / f"{table}.parquet"`.
      2. Glob `standard_dir / "scraped" / "*" / f"{table}.parquet"`, concat into one scraped frame.
      3. HIGH #9: `_assert_column_set_equality` on BOTH kaggle_df and scraped_df BEFORE reindex.
      4. reindex BOTH to `list(schema.model_fields.keys())`
      5. recast BOTH via `_recast_to_canonical`
      6. `merged = pd.concat([kaggle_df, scraped_df], ignore_index=True)`
      7. per-table pre-dedup: assert `merged[pk].duplicated().sum() == 0`; if not, RAISE ValueError mentioning "duplicate" (Pitfall 6)
    - After all 3 merged frames built, call `validate_integrity(merged_race, merged_entry, merged_result)` (normalizer.py:263). HIGH #8b cycle-4: treat ANY violation whose string contains "duplicate", "orphan", "mismatch", OR "1-to-1" as HARD — RAISE ValueError. This extends the normalizer.py:744-756 token set (`duplicate`/`orphan`) with the `"horse_race_id mismatch: entry/result are not 1-to-1"` violation string (normalizer.py:330-334), which contains NEITHER `"duplicate"` NOR `"orphan"` and would otherwise be classified SOFT (warning only), silently writing a structurally inconsistent entry/result corpus. The exact filter is:
      ```
      hard_violations = [v for v in violations if (
          "duplicate" in v or "orphan" in v or "mismatch" in v or "1-to-1" in v
      )]
      if hard_violations:
          raise ValueError(f"integrate_standard_layer: {len(hard_violations)} hard integrity "
                           f"violation(s) detected (duplicate PK / FK orphan / entry-result 1-to-1 "
                           f"mismatch); refusing to write structurally inconsistent corpus. "
                           f"First 3: {hard_violations[:3]}")
      ```
      Adding BOTH `"mismatch"` and `"1-to-1"` is belt-and-braces — either substring matches the violation string at normalizer.py:330-334.
    - MEDIUM #13: call `audit_leakage([RaceSchema], merged_race, "post-integration race audit")` — expect []; log WARNING if non-empty. Call `audit_leakage([EntrySchema], merged_entry, "post-integration entry audit")` — expect `['popularity', 'win_odds']`; if any OTHER column leaks, RAISE ValueError.

    HIGH #6 cycle-3 transactionality (validate-before-swap with idempotent recovery):
    - Create staging dir via `staging_dir = Path(tempfile.mkdtemp(prefix='.integration_staging_', dir=str(standard_dir)))`. The `tempfile.mkdtemp` resolves the cycle-2 MEDIUM about fixed `.integration_tmp` collisions (each run gets a unique staging dir; concurrent runs and stale leftovers do not collide).
    - Write all 3 merged frames to the staging dir via `_atomic_write_parquet(merged, staging_dir / f"{table}.parquet")`.
    - VALIDATE the staged files: re-read each staged parquet, assert row count > 0, assert schema column-set matches, assert PK uniqueness. If any staged-file validation fails, remove the staging dir (best-effort `shutil.rmtree(staging_dir, ignore_errors=True)`) and RAISE — the existing output files are untouched.
    - ONLY AFTER all staged files validate, swap each into root via `os.replace(staging_dir / f"{table}.parquet", standard_dir / f"{table}.parquet")`. Wrap the swap loop in try/finally that removes the staging dir. (The cycle-4 strengthened test test_integration_partial_swap_recoverable proves recovery from a mid-swap os.replace failure on the 2nd call — see Task 1 behavior.)
    - ACCURATE transactionality description (in the docstring + SUMMARY): "validate-before-swap with idempotent recovery." The 3 sequential `os.replace` calls are NOT perfectly atomic — a failure between the 2nd and 3rd swap leaves a partial swap. HOWEVER, because integration reads only from immutable inputs (`data/standard/kaggle/` + scraped partitions) and NEVER from its own output, re-running `integrate_standard_layer` produces IDENTICAL output, fully recovering from any partial swap. This is more robust than a backup-and-restore scheme (which depends on the backup surviving the crash) and is the genuinely correct guarantee for a deterministic, idempotent transform. The `test_integration_partial_swap_recoverable` test (cycle-4 strengthened) documents this recovery path via mid-swap os.replace failure injection.
    - Return `{"race": standard_dir / "race.parquet", "entry": ..., "result": ..., "audit": {...}}`.

    Then replace the 11 test stubs in tests/pipeline/test_integration.py with real assertions matching the behavior list. Hermetic tests (1-9) construct synthetic Kaggle+scraped Parquet in tmp_path; slow-path tests (10-11) read real data/standard/ and are gated.

    For Test 3 (test_horse_race_id_mismatch_raises): construct entry_df with 1 row whose horse_race_id is "X"; construct result_df with 2 rows BOTH with horse_race_id "X". The Counter-based check at normalizer.py:314-336 detects the multiset mismatch and returns a violation string starting with "horse_race_id mismatch: entry/result are not 1-to-1". The integration's hard-violation filter (extended with "mismatch"/"1-to-1") classifies it HARD and RAISES ValueError. Assert pytest.raises(ValueError).

    For Test 9 (test_integration_partial_swap_recoverable cycle-4 strengthened): use pytest's monkeypatch fixture. Pattern:
    ```
    import itertools, hashlib
    from src.pipeline import integration as integration_mod

    def test_integration_partial_swap_recoverable(self, monkeypatch, tmp_kaggle_input_dir, tmp_scraped_partitions_dir):
        standard_dir = tmp_kaggle_input_dir.parent  # tmp_path/data/standard
        # (a) Clean run — capture canonical SHA-256 of all 3 outputs
        from src.pipeline.integration import integrate_standard_layer
        integrate_standard_layer(standard_dir)
        canonical = {tbl: hashlib.sha256((standard_dir / f"{tbl}.parquet").read_bytes()).hexdigest() for tbl in ("race","entry","result")}

        # (b) Inject mid-swap os.replace failure on the 2nd call
        real_replace = os.replace
        call_counter = {"n": 0}
        def failing_replace(src, dst):
            call_counter["n"] += 1
            if call_counter["n"] == 2:
                raise OSError("simulated mid-swap failure (2nd os.replace)")
            return real_replace(src, dst)
        monkeypatch.setattr(integration_mod.os, "replace", failing_replace)

        # (c) Re-invoke; either it raises OSError OR leaves inconsistent state
        try:
            integrate_standard_layer(standard_dir)
            # If it returned, the corpus MUST be in an inconsistent state
            # (NOT all 3 match canonical — the mid-swap failure left >=1 file old/missing)
            post = {tbl: hashlib.sha256((standard_dir / f"{tbl}.parquet").read_bytes()).hexdigest()
                    for tbl in ("race","entry","result") if (standard_dir / f"{tbl}.parquet").exists()}
            assert post != canonical, "integration silently succeeded despite mid-swap failure — idempotent recovery broken"
        except OSError:
            pass  # acceptable — the swap loop propagated the mid-swap failure

        # (d) RESTORE os.replace (monkeypatch teardown handles this) and re-invoke
        monkeypatch.undo()  # restore integration_mod.os.replace to real os.replace
        integrate_standard_layer(standard_dir)
        for tbl in ("race","entry","result"):
            assert (standard_dir / f"{tbl}.parquet").exists(), f"{tbl}.parquet missing after recovery"
            h = hashlib.sha256((standard_dir / f"{tbl}.parquet").read_bytes()).hexdigest()
            assert h == canonical[tbl], f"{tbl}.parquet NOT restored to canonical content after recovery"
    ```
    Adapt the exact path navigation (`tmp_kaggle_input_dir.parent` vs explicit `tmp_path / "data" / "standard"`) to match the conftest fixture's actual return type. If the monkeypatch on `integration_mod.os.replace` does not intercept (because integration.py imports `os.replace` indirectly), patch the symbol integration.py actually references — `monkeypatch.setattr("src.pipeline.integration.os.replace", failing_replace)`. Read the implementation's exact os reference after Task 2 starts to confirm.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds' && ! grep -q 'errors=ignore' src/pipeline/integration.py && ! grep -qE 'odds_trifecta|payoff' src/pipeline/integration.py && grep -q 'SCHEMA_BY_TABLE' src/pipeline/integration.py && grep -q 'validate_integrity' src/pipeline/integration.py && grep -q '_assert_column_set_equality' src/pipeline/integration.py && grep -q 'audit_leakage' src/pipeline/integration.py && grep -q 'os.replace' src/pipeline/integration.py && grep -q 'tempfile.mkdtemp' src/pipeline/integration.py && grep -q 'idempotent' src/pipeline/integration.py && grep -qE '(mismatch|1-to-1)' src/pipeline/integration.py && grep -q 'test_horse_race_id_mismatch_raises' tests/pipeline/test_integration.py</automated>
  </verify>
  <done>
    - src/pipeline/integration.py exists with integrate_standard_layer, 3-table allowlist, separate Kaggle input, tempfile.mkdtemp validate-before-swap (HIGH #6 cycle-3), hard-violation filter including "mismatch"/"1-to-1" (HIGH #8b cycle-4), column-set equality assert, validate_integrity call, audit_leakage call
    - Transactionality model ACCURATELY described as "validate-before-swap with idempotent recovery" (NOT falsely atomic); cycle-4 monkeypatch mid-swap test proves the recovery under the real failure mode
    - 9 hermetic tests green (including NEW test_horse_race_id_mismatch_raises + cycle-4 strengthened test_integration_partial_swap_recoverable)
    - 2 slow-path tests collected; skipped or green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle + scraped Parquet → unified Parquet | Two independently-produced corpora cross into a single merged output. Schema reconciliation + PK dedup + FK validation + entry/result 1-to-1 integrity are the trust controls. |
| Integration write → existing data/standard/*.parquet | Destructive overwrite (D-03). tempfile.mkdtemp staging + validate-before-swap + idempotent recovery are the safety controls (HIGH #6 cycle-3 + cycle-4 monkeypatch mid-swap test). |
| Integration output → Phase 3/7/9 downstream | Mixed-generation corpus OR structurally inconsistent entry/result would corrupt feature/model/backtest. Validate-before-swap (HIGH #6 cycle-3) + idempotent recovery + entry/result 1-to-1 mismatch RAISE (HIGH #8b cycle-4) are the controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Pre-dedup duplicate race_id (boundary overlap) | mitigate | Per-table `merged[pk].duplicated().sum() == 0` assertion + `validate_integrity` duplicate check RAISE ValueError. Verified by `test_no_duplicate_race_ids_fail_loud`. |
| T-06-06 | Tampering | `pd.concat` without column-set check inflates/masks schema | mitigate | HIGH #9: `_assert_column_set_equality(df, schema, label)` BEFORE reindex. Verified by `test_column_set_mismatch_raises_before_reindex`. |
| T-06-07 | Denial of Service | Half-written mixed-generation corpus on partial swap failure (HIGH #6 cycle-3 + cycle-4) | mitigate | Validate-before-swap: write all 3 to `tempfile.mkdtemp` staging, validate staged files, swap via `os.replace` only after validation passes. Mid-swap crash recoverable via idempotent re-run (integration reads from immutable inputs). Verified by `test_integration_is_idempotent` (clean re-run) + `test_integration_partial_swap_recoverable` (cycle-4 strengthened: monkeypatch the 2nd `os.replace` to raise OSError, assert inconsistent state OR raise, then RESTORE + re-invoke + assert byte-identical recovery). Transactionality model ACCURATELY described as "validate-before-swap with idempotent recovery." |
| T-06-08 | Information Disclosure | Provenance leak via `source` column | mitigate | D-05 guard: no `source` column. Verified by grep gate. |
| T-06-09 | Tampering | Silent FK orphan acceptance (HIGH #8) | mitigate | `validate_integrity` CALLED; orphan FKs raise ValueError. Verified by `test_referential_integrity_orphan_raises`. |
| T-06-09b | Tampering | Silent entry/result horse_race_id 1-to-1 mismatch (HIGH #8b cycle 4) | mitigate | hard-violation filter EXTENDED with `"mismatch"` (and `"1-to-1"`); matches the violation string at `normalizer.py:330-334` which contains NEITHER `"duplicate"` NOR `"orphan"`. Verified by NEW regression test `test_horse_race_id_mismatch_raises` (entry 1 row "X", result 2 rows "X" → ValueError). |
| T-06-10 | Tampering | Overwriting odds_trifecta/payoff (Phase 5 seed) | mitigate | SCHEMA_BY_TABLE = {race, entry, result} allowlist; sentinel test. |
| T-06-11 | Tampering | Non-idempotent re-run double-merges (HIGH #5) | mitigate | Kaggle read from `kaggle_input_dir` (separate path). Verified by `test_integration_is_idempotent`. |
| T-06-12 | Repudiation | audit_leakage imported but never called (MEDIUM #13) | mitigate | audit_leakage CALLED inside integrate_standard_layer; results in return dict. Verified by `test_no_post_race_leakage_audit_called`. |
| T-06-13 | Denial of Service | Silent skip on missing input (MEDIUM #14) | mitigate | Explicit FileNotFoundError / ValueError for each missing-input case. |
| T-06-14 | Tampering | autouse skip swallows hermetic tests (HIGH #7) | mitigate | Two-class split: TestIntegrationHermetic (no autouse) + TestUnifiedCorpus (gated). |
| T-06-15 | Tampering | Fixed `.integration_tmp` staging dir collides on concurrent runs / stale leftovers (cycle-2 MEDIUM) | mitigate | `tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir)` — unique per run; finally-block cleanup. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds'` green (9 hermetic tests, including NEW test_horse_race_id_mismatch_raises + cycle-4 strengthened test_integration_partial_swap_recoverable)
- `python -m pytest tests/ -q` full suite green
- `grep 'errors=.ignore.' src/pipeline/integration.py` returns nothing
- `grep 'odds_trifecta\|payoff' src/pipeline/integration.py` returns nothing
- `grep 'validate_integrity\|_assert_column_set_equality\|os.replace\|kaggle_input_dir\|tempfile.mkdtemp\|idempotent' src/pipeline/integration.py` returns matches for all
- HIGH #8b cycle-4: `grep -E '(mismatch|1-to-1)' src/pipeline/integration.py` returns match (hard-violation filter extended)
- HIGH #8b cycle-4: `grep 'test_horse_race_id_mismatch_raises' tests/pipeline/test_integration.py` returns match
</verification>

<success_criteria>
- HIGH #5 resolved (KEPT): integration reads Kaggle from separate input path; idempotent
- HIGH #6 resolved (cycle 3 + cycle 4): validate-before-swap via tempfile.mkdtemp staging; idempotent recovery model accurately described; cycle-4 strengthened test_integration_partial_swap_recoverable proves recovery under real mid-swap os.replace failure (monkeypatch injection + restore + byte-identical re-run)
- HIGH #7 resolved (KEPT): hermetic tests ungated; real-corpus tests gated
- HIGH #8 resolved (KEPT): FK orphan test injects orphan, asserts validate_integrity reports it
- HIGH #8b resolved (cycle 4 NEW HIGH): hard-violation filter extended with "mismatch"/"1-to-1"; regression test test_horse_race_id_mismatch_raises proves entry/result 1-to-1 cardinality mismatch RAISES ValueError
- HIGH #9 resolved (KEPT): column-set equality assert before reindex
- MEDIUMs #12/#13/#14 resolved: 11 tests (adds test_horse_race_id_mismatch_raises; cycle-4 strengthens test_integration_partial_swap_recoverable), audit_leakage called, explicit missing-input errors
- Module ready for Wave 3 invocation against real corpus
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-02-SUMMARY.md` when done. Record: the transactionality model ("validate-before-swap with idempotent recovery" via tempfile.mkdtemp staging + cycle-4 monkeypatch mid-swap os.replace failure injection test), the hard-violation filter extension (now includes "mismatch"/"1-to-1" — HIGH #8b cycle-4), the test count (11, including NEW test_horse_race_id_mismatch_raises + cycle-4 strengthened test_integration_partial_swap_recoverable), the audit_leakage call confirmation.
</output>

## Artifacts this phase produces

- `src/pipeline/integration.py` (NEW)
  - `integrate_standard_layer(standard_dir, kaggle_input_dir=None) -> dict[str, Path]` — entry point (HIGH #5 separate input)
  - `_recast_to_canonical(df, schema) -> pd.DataFrame` — raises TypeError
  - `_assert_column_set_equality(df, schema, source_label) -> None` — HIGH #9
  - `SCHEMA_BY_TABLE` — 3-table allowlist
  - `PK_BY_TABLE` — PK column per table
  - validate-before-swap via `tempfile.mkdtemp(prefix='.integration_staging_', dir=standard_dir)` (HIGH #6 cycle-3) + idempotent recovery
  - hard-violation filter EXTENDED with `"mismatch"`/`"1-to-1"` (HIGH #8b cycle-4) — matches `normalizer.py:330-334` violation string
  - `validate_integrity` call (HIGH #8)
  - `audit_leakage` call (MEDIUM #13)
- `tests/pipeline/test_integration.py` (NEW)
  - `class TestIntegrationHermetic:` (9 tests, NO autouse skip) — HIGH #7; includes NEW `test_horse_race_id_mismatch_raises` (HIGH #8b cycle-4) + cycle-4 strengthened `test_integration_partial_swap_recoverable` (monkeypatch mid-swap os.replace failure injection + recovery)
  - `class TestUnifiedCorpus:` (2 tests, autouse `_require_scraped_data`)
  - 11 tests total: `test_no_duplicate_race_ids_fail_loud`, `test_referential_integrity_orphan_raises`, `test_horse_race_id_mismatch_raises` (NEW), `test_column_set_mismatch_raises_before_reindex`, `test_schema_invariant_post_integration`, `test_no_post_race_leakage_audit_called`, `test_odds_payoff_not_overwritten`, `test_integration_is_idempotent`, `test_integration_partial_swap_recoverable` (cycle-4 strengthened), `test_unified_race_date_range`, `test_row_counts_within_expected_bounds`
- `tests/pipeline/conftest.py` (MODIFIED)
  - `tmp_kaggle_input_dir(tmp_path, ...)` fixture (HIGH #5)
  - `tmp_scraped_partitions_dir(tmp_path, ...)` fixture

---
phase: 06-data-integration
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - src/pipeline/integration.py
  - tests/pipeline/test_integration.py
  - tests/pipeline/conftest.py
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "HIGH #5 (idempotency): integrate_standard_layer reads Kaggle rows from a SEPARATE input path (data/standard/kaggle/{table}.parquet — written by Plan 06-01 Task 2 via convert(core_tables_subdir='kaggle')) and NEVER from the output path data/standard/{table}.parquet. A second invocation of integrate_standard_layer produces byte-identical output (verified by idempotency test)."
    - "BLOCKER-1 (cross-plan data contract — resolved by Plan 06-01 Task 2): Plan 06-01 writes the regenerated Kaggle race/entry/result to data/standard/kaggle/{table}.parquet via convert(core_tables_subdir='kaggle'). integrate_standard_layer defaults kaggle_input_dir to standard_dir / 'kaggle' and reads each Kaggle table from there. NO task in any plan reads Kaggle input from data/standard/{table}.parquet (the integration output path). The data/standard/kaggle/ directory is created by Plan 06-01 BEFORE this plan runs (Wave 1 -> Wave 2 dependency)."
    - "HIGH #6 (corpus-level transactionality): integrate_standard_layer writes all 3 tables to a tmp dir FIRST, runs validate_integrity + the pre-dedup overlap check + schema-column-set equality on the in-memory merged frames, and ONLY THEN swaps all 3 files into place via os.replace. A failure in any table's validation leaves all 3 output files at their pre-call state."
    - "HIGH #7 (autouse skip gate): hermetic tests live in a SEPARATE class (TestIntegrationHermetic) with NO _require_scraped_data autouse fixture; real-corpus slow-path tests live in TestUnifiedCorpus with the autouse skip. Hermetic tests run unconditionally."
    - "HIGH #8 (FK test actually raises): test_referential_integrity injects an entry row whose race_id is NOT in the race table and asserts that validate_integrity (from src/scraper/normalizer.py:263) returns a list containing 'orphan entry race_id'. The test fails if the integration module silently accepts orphan FKs."
    - "HIGH #9 (reindex guarded by column-set equality): BEFORE any reindex, integrate_standard_layer asserts `set(input_df.columns) == set(schema.model_fields.keys())` and raises ValueError on mismatch. reindex can no longer silently drop extra columns or fill missing columns with NA."
    - "integrate_standard_layer imports _atomic_write_parquet and SCHEMA_DTYPE_MAP from src.scraper.normalizer (NO re-implementation)"
    - "integrate_standard_layer hardcodes SCHEMA_BY_TABLE = {race, entry, result} — does NOT iterate TABLE_TO_SCHEMA.keys() (Pitfall 5)"
    - "Pre-dedup overlap raises ValueError (not silent drop_duplicates away — Pitfall 6)"
    - "integrate_standard_layer never adds a `source` column (D-05)"
    - "audit_leakage is CALLED inside integrate_standard_layer (not just imported) on the merged race_df and entry_df (MEDIUM #13)"
    - "Missing Kaggle files, missing scraped partitions, or missing table-within-a-month each raise an explicit error (MEDIUM #14)"
    - "Module-level logging uses loguru; logs per-table (NOT per-row)"
  artifacts:
    - path: "src/pipeline/integration.py"
      provides: "integrate_standard_layer(standard_dir, kaggle_input_dir=None) entry point + _recast_to_canonical + _assert_column_set_equality"
      exports: ["integrate_standard_layer"]
      contains: "SCHEMA_BY_TABLE"
    - path: "tests/pipeline/test_integration.py"
      provides: "TestIntegrationHermetic (fast path, no skip gate) + TestUnifiedCorpus (real corpus, gated). 9 tests total."
      contains: "class TestIntegrationHermetic"
    - path: "tests/pipeline/conftest.py"
      provides: "tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures"
      contains: "tmp_kaggle_input_dir"
  key_links:
    - from: "src/pipeline/integration.py"
      to: "src/scraper/normalizer._atomic_write_parquet + validate_integrity"
      via: "from src.scraper.normalizer import _atomic_write_parquet, SCHEMA_DTYPE_MAP, validate_integrity"
      pattern: "from src\\.scraper\\.normalizer import.*(SCHEMA_DTYPE_MAP|_atomic_write_parquet|validate_integrity)"
    - from: "integrate_standard_layer"
      to: "src.schemas.audit.audit_leakage"
      via: "post-integration audit CALL (not just import)"
      pattern: "audit_leakage\\(\\[RaceSchema\\]"
    - from: "integrate_standard_layer"
      to: "data/standard/{race,entry,result}.parquet"
      via: "tmp-dir swap (HIGH #6 transactionality)"
      pattern: "os\\.replace"
---

<objective>
Build the Phase 6 integration module `src/pipeline/integration.py` and its test suite `tests/pipeline/test_integration.py`. The module merges Kaggle (post-D-01/D-02 from Plan 06-01) and scraped (post-D-06 pre-task) corpora into a unified `data/standard/{race,entry,result}.parquet` per D-03/D-04.

BLOCKER-1 (cross-plan data contract — depends on Plan 06-01 Task 2): Plan 06-01 writes the regenerated Kaggle race/entry/result to `data/standard/kaggle/{table}.parquet` via `convert(core_tables_subdir='kaggle')`. This plan's `integrate_standard_layer` reads Kaggle input from that directory (default `kaggle_input_dir = standard_dir / 'kaggle'`) and writes the unified corpus to `data/standard/{table}.parquet`. Because this plan is Wave 2 and Plan 06-01 is Wave 1, the `data/standard/kaggle/` directory EXISTS by the time this plan's real-corpus invocation (Plan 06-03) runs. The hermetic tests in Task 1 create their OWN `tmp_path/data/standard/kaggle/` fixture (tmp_kaggle_input_dir) so they do not depend on 06-01's real output.

This plan addresses FIVE HIGH defects from the cross-AI review (06-REVIEWS.md):
- HIGH #5 (non-idempotent integration): read+write same path double-merges. FIX: read Kaggle from `data/standard/kaggle/{table}.parquet` (a stable separate input), never from the output path.
- HIGH #6 (no corpus-level transactionality): per-file atomic write leaves mixed generations. FIX: write all 3 tables to a tmp dir, validate, then swap all 3 via os.replace.
- HIGH #7 (autouse skip swallows hermetic tests): class-level autouse skips fast-path tests too. FIX: split into TestIntegrationHermetic (no skip) + TestUnifiedCorpus (gated).
- HIGH #8 (FK test is a no-op): "does not raise" passes without FK validation. FIX: call `validate_integrity` (src/scraper/normalizer.py:263), inject an orphan FK, assert the violation is reported.
- HIGH #9 (reindex masks schema drift): extra cols dropped, missing cols filled with NA. FIX: column-set equality assert BEFORE reindex.

Plus MEDIUMs: #12 (test count: 9, not 8 — D-01/D-02 grade/dtype tests live in Plan 06-01's test_kaggle_converter.py, so test_integration.py covers 9 distinct integration behaviors); #13 (audit_leakage actually called, not just imported); #14 (explicit errors for missing Kaggle files, missing scraped partitions, missing table within month).

Output: New module `src/pipeline/integration.py` exposing `integrate_standard_layer()`. New test file `tests/pipeline/test_integration.py` with 9 tests across two classes. Extended `tests/pipeline/conftest.py` with hermetic Kaggle/scraped fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-data-integration/06-CONTEXT.md
@.planning/phases/06-data-integration/06-RESEARCH.md
@.planning/phases/06-data-integration/06-PATTERNS.md
@.planning/phases/06-data-integration/06-01-SUMMARY.md

# Phase 4 dtype authority + atomic write + FK validator (primitives to reuse)
@src/scraper/normalizer.py

# Phase 1 audit contract
@src/schemas/audit.py
@src/schemas/race.py
@src/schemas/entry.py
@src/schemas/result.py

# Reference test pattern: Phase 4 TestSchemaCompatibility
@tests/scraper/test_end_to_end.py

# Conftest: extend the existing tmp_standard_dir fixture
@tests/pipeline/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hermetic fixtures to tests/pipeline/conftest.py (tmp_kaggle_input_dir + tmp_scraped_partitions_dir); create tests/pipeline/test_integration.py with TWO test classes (TestIntegrationHermetic ungated + TestUnifiedCorpus gated)</name>
  <files>tests/pipeline/conftest.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - tests/pipeline/conftest.py (full file — tmp_standard_dir at line 291, sample_standard_race_df at 304, sample_standard_entry_df at 355, sample_standard_result_df at 436, and the combined standard-files fixture around lines 492-509; extend, do not duplicate)
    - tests/scraper/conftest.py (lines 17-47 — the canonical tmp_path-derived fixture pattern)
    - tests/scraper/test_end_to_end.py (lines 464-536 — TestSchemaCompatibility class shape AND the _require_kaggle_parquet autouse skip fixture at 467-472; mirror as `_require_scraped_data` for the GATED class only)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Phase Requirements → Test Map" — the 9 test names/behaviors)
  </read_first>
  <behavior>
    - Test 1: `python -m pytest tests/pipeline/test_integration.py --collect-only -q` lists all 9 expected test names AND assigns them to the correct class (TestIntegrationHermetic: 7 tests; TestUnifiedCorpus: 2 tests)
    - Test 2: `tmp_scraped_partitions_dir` fixture creates `tmp_path/data/standard/scraped/202301/` and `tmp_path/data/standard/scraped/202302/` with one tiny synthetic Parquet per table per month (reindex to Schema.model_fields order)
    - Test 3: `tmp_kaggle_input_dir` fixture creates `tmp_path/data/standard/kaggle/{race,entry,result}.parquet` (the SEPARATE input path — HIGH #5 idempotency fix). It does NOT touch `tmp_path/data/standard/{race,entry,result}.parquet` (the output path).
    - Test 4 (HIGH #7): TestIntegrationHermetic has NO autouse skip fixture; its tests COLLECT and RUN against the synthetic tmp_path data without requiring real scraped data. TestUnifiedCorpus has the `_require_scraped_data` autouse skip; its 2 tests skip when the real corpus is smoke-only.
  </behavior>
  <action>
    Extend tests/pipeline/conftest.py with TWO new fixtures:
    (1) `tmp_kaggle_input_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/kaggle/` and writes `{race,entry,result}.parquet` there (reindex each to `list(Schema.model_fields.keys())`, write via `to_parquet(engine='pyarrow', index=False)`). This is the HIGH #5 separate-input-path fixture.
    (2) `tmp_scraped_partitions_dir(tmp_path, sample_standard_race_df, sample_standard_entry_df, sample_standard_result_df)` — creates `tmp_path/data/standard/scraped/{202301,202302}/` and writes one synthetic Parquet per table per month (same reindex+write). This mirrors the Phase 4 partitioned layout.

    Then create tests/pipeline/test_integration.py with TWO classes:
    - `class TestIntegrationHermetic:` (NO autouse skip — runs unconditionally against tmp_path). 7 test method stubs:
      `test_no_duplicate_race_ids_fail_loud` (HIGH #8: inject overlapping race_id, expect ValueError)
      `test_referential_integrity_orphan_raises` (HIGH #8: inject orphan entry race_id, expect validate_integrity reports it)
      `test_column_set_mismatch_raises_before_reindex` (HIGH #9: input with extra/missing column, expect ValueError)
      `test_schema_invariant_post_integration` (every race_flag_* is Arrow bool; distance is int64; race_date is string)
      `test_no_post_race_leakage_audit_called` (MEDIUM #13: audit_leakage is CALLED, returns expected sets)
      `test_odds_payoff_not_overwritten` (sentinel files unchanged post-integration)
      `test_integration_is_idempotent` (HIGH #5: two consecutive runs produce byte-identical output; SHA-256 equality)
    - `class TestUnifiedCorpus:` (autouse `_require_scraped_data` skip when real corpus is smoke-only — mirror `tests/scraper/test_end_to_end.py:467-472`). 2 test method stubs:
      `test_unified_race_date_range`
      `test_row_counts_within_expected_bounds`

    Each stub body is `pytest.skip("integration.py not yet implemented — Wave 0 stub")` for now (Task 2 replaces). The import of `integrate_standard_layer` may be at module top inside a try/except OR inside each test body — pick the one that makes collection succeed when integration.py does not yet exist (try/except at module top with a sentinel is preferred).
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py --collect-only -q 2>&1 | grep -E '^(tests/pipeline/test_integration\.py::|test_)' | wc -l | tr -d ' ' | grep -q '^9$' && python -m pytest tests/pipeline/test_integration.py --collect-only -q 2>&1 | grep -c 'TestIntegrationHermetic' | head -1 && python -m pytest tests/pipeline/test_integration.py --collect-only -q 2>&1 | grep -c 'TestUnifiedCorpus' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/pipeline/test_integration.py` exists; `pytest --collect-only` lists exactly 9 test items
    - 7 tests collected under `TestIntegrationHermetic::`; 2 under `TestUnifiedCorpus::`
    - `grep 'tmp_kaggle_input_dir' tests/pipeline/conftest.py` returns a match (HIGH #5 separate input path)
    - `grep 'tmp_scraped_partitions_dir' tests/pipeline/conftest.py` returns a match
    - All 9 stubs collect without import error
  </acceptance_criteria>
  <done>
    - tests/pipeline/test_integration.py exists with 2 classes (Hermetic ungated + UnifiedCorpus gated) and 9 collected stubs
    - conftest.py has tmp_kaggle_input_dir + tmp_scraped_partitions_dir fixtures
    - `pytest --collect-only` lists 9 items without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/pipeline/integration.py — separate Kaggle input path (HIGH #5), corpus-level tmp-swap (HIGH #6), column-set equality assert (HIGH #9), real FK validation via validate_integrity (HIGH #8), audit_leakage CALLED (MEDIUM #13); replace test stubs with real assertions</name>
  <files>src/pipeline/integration.py, tests/pipeline/test_integration.py</files>
  <read_first>
    - src/scraper/normalizer.py (lines 95-192 SCHEMA_DTYPE_MAP; lines 226-255 _build_typed_dataframe; lines 263-380 validate_integrity — the FK/duplicate validator returning a list of violation strings; lines 643-653 _atomic_write_parquet; lines 744-756 hard_violations raise ValueError pattern)
    - src/schemas/audit.py (audit_leakage signature — returns list[str] of leaked post-race column names; logs warning, does NOT raise per D-12)
    - src/schemas/race.py / entry.py / result.py (model_fields — canonical column-order source)
    - .planning/phases/06-data-integration/06-PATTERNS.md (section `src/pipeline/integration.py` — exact imports, entry-point signature, SCHEMA_BY_TABLE allowlist)
    - .planning/phases/06-data-integration/06-RESEARCH.md (section "Code Examples — Verified end-to-end integration recipe" lines 324-376 — the SPEC body; section "Phase 4 TestSchemaCompatibility pattern (reuse for Phase 6)" lines 380-401; section "Phase 1 audit_leakage reuse" lines 404-417)
    - tests/pipeline/test_integration.py (the stubs from Task 1)
  </read_first>
  <behavior>
    - Test 1 (TestIntegrationHermetic::test_no_duplicate_race_ids_fail_loud): construct Kaggle input + scraped partition with OVERLAPPING race_id; integrate_standard_layer RAISES ValueError mentioning "duplicate"
    - Test 2 (TestIntegrationHermetic::test_referential_integrity_orphan_raises — HIGH #8): construct synthetic data where entry.race_id contains an ID NOT in race.race_id; integrate_standard_layer calls validate_integrity, which returns a list containing "orphan entry race_id"; integrate_standard_layer then RAISES ValueError (because the integration treats orphan FKs as hard violations, mirroring normalizer.py:744-756 hard_violations pattern)
    - Test 3 (TestIntegrationHermetic::test_column_set_mismatch_raises_before_reindex — HIGH #9): construct an input DataFrame whose column set differs from `set(Schema.model_fields.keys())` (extra column OR missing column); integrate_standard_layer RAISES ValueError BEFORE reaching reindex
    - Test 4 (TestIntegrationHermetic::test_schema_invariant_post_integration): integrated race.parquet read via pq.read_schema — every race_flag_* is Arrow bool, distance is int64, race_date is string
    - Test 5 (TestIntegrationHermetic::test_no_post_race_leakage_audit_called — MEDIUM #13): audit_leakage is CALLED inside integrate_standard_layer (verify via the returned dict containing an `audit` key, OR via monkeypatch/spy on audit_leakage). For race: returns []. For entry: returns ['popularity', 'win_odds'] (post-race per Phase 1 D-03).
    - Test 6 (TestIntegrationHermetic::test_odds_payoff_not_overwritten): sentinel odds_trifecta.parquet + payoff.parquet placed in standard_dir BEFORE integration; after integration their row counts AND SHA-256 checksums are IDENTICAL (proves SCHEMA_BY_TABLE allowlist excludes odds/payoff)
    - Test 7 (TestIntegrationHermetic::test_integration_is_idempotent — HIGH #5): run integrate_standard_layer TWICE in succession on the same tmp_path tree; assert the second run's output race.parquet/entry.parquet/result.parquet have IDENTICAL SHA-256 to the first run's output (idempotency: second run does NOT double-merge because it reads from the separate kaggle/ input path, not the output path)
    - Test 8 (TestUnifiedCorpus::test_unified_race_date_range): slow-path, gated; runs when real scraped corpus present
    - Test 9 (TestUnifiedCorpus::test_row_counts_within_expected_bounds): slow-path, gated; asserts PK-set union of input partitions == output unique PK count (MEDIUM #20 — not just a lower bound)
  </behavior>
  <action>
    Create src/pipeline/integration.py following PATTERNS.md section `src/pipeline/integration.py` with these HIGH-fix modifications:

    Imports: `os`, `from pathlib import Path`, `import pandas as pd`, `from loguru import logger`, `from src.schemas.entry import EntrySchema`, `from src.schemas.race import RaceSchema`, `from src.schemas.result import ResultSchema`, `from src.scraper.normalizer import SCHEMA_DTYPE_MAP, _atomic_write_parquet, validate_integrity`, `from src.schemas.audit import audit_leakage`.

    Module constants:
    - `SCHEMA_BY_TABLE = {"race": RaceSchema, "entry": EntrySchema, "result": ResultSchema}` (3-table allowlist — Pitfall 5 / HIGH #2 odds/payoff protection)
    - `PK_BY_TABLE = {"race": "race_id", "entry": "horse_race_id", "result": "horse_race_id"}`

    Helper `_recast_to_canonical(df, schema)` (mirrors `_build_typed_dataframe` lines 242-255 — try/except TypeError re-raise, NO `errors='ignore'`).

    Helper `_assert_column_set_equality(df, schema, source_label)` (HIGH #9):
    ```
    expected = set(schema.model_fields.keys())
    actual = set(df.columns)
    if actual != expected:
        extra = actual - expected
        missing = expected - actual
        raise ValueError(f"{source_label}: column set mismatch (extra={sorted(extra)}, missing={sorted(missing)}); refusing to reindex — schema drift would be silently masked")
    ```

    Entry point `integrate_standard_layer(standard_dir: Path = Path("data/standard"), kaggle_input_dir: Path | None = None) -> dict[str, Path]`:
    - HIGH #5 idempotency: if `kaggle_input_dir` is None, default to `standard_dir / "kaggle"`. READ Kaggle rows from `kaggle_input_dir / f"{table}.parquet"`, NEVER from `standard_dir / f"{table}.parquet"` (the output path). If the kaggle input file is missing for any of the 3 tables, RAISE FileNotFoundError with a clear message (MEDIUM #14).
    - MEDIUM #14 (explicit errors): if `standard_dir / "scraped"` does not exist OR contains zero month dirs, RAISE ValueError("no scraped partitions found"). If a month dir lacks one of the 3 table files, RAISE FileNotFoundError naming the missing file. (Tolerate a month dir being entirely absent — JRA off months — but require the 3-table contract within each present dir.)
    - For each table in SCHEMA_BY_TABLE:
      1. Read Kaggle from `kaggle_input_dir / f"{table}.parquet"`.
      2. Glob `standard_dir / "scraped" / "*" / f"{table}.parquet"`, concat into one scraped frame. If a month dir is missing this table file, RAISE (MEDIUM #14).
      3. HIGH #9: call `_assert_column_set_equality(kaggle_df, schema, f"kaggle {table}")` and `_assert_column_set_equality(scraped_df, schema, f"scraped {table}")` BEFORE any reindex.
      4. reindex BOTH to `list(schema.model_fields.keys())` (now safe — column sets already verified equal)
      5. recast BOTH via `_recast_to_canonical`
      6. `merged = pd.concat([kaggle_df, scraped_df], ignore_index=True)`
      7. HIGH #8 / Pitfall 6: call `validate_integrity(race_df, entry_df, result_df)` — but validate_integrity needs all 3 tables. So: build all 3 merged frames first, THEN call `validate_integrity(merged_race, merged_entry, merged_result)` once, and treat any violation containing "duplicate" or "orphan" as hard (raise ValueError, mirror normalizer.py:744-756). Also separately assert `merged[pk].duplicated().sum() == 0` per-table pre-dedup.
      8. MEDIUM #13: call `audit_leakage([RaceSchema], merged_race, "post-integration race audit")` — for race, expect `[]`; log a WARNING if non-empty. Call `audit_leakage([EntrySchema], merged_entry, "post-integration entry audit")` — for entry, expect exactly `['popularity', 'win_odds']` (these are post-race per Phase 1 D-03); if any OTHER column leaks, raise ValueError.
    - HIGH #6 corpus-level transactionality: write all 3 merged frames to a tmp dir (`standard_dir / ".integration_tmp"` or `tempfile.mkdtemp()`). Run `_atomic_write_parquet(merged, tmp_dir / f"{table}.parquet")` for each. THEN, after all 3 are written and validated, swap each into place via `os.replace(tmp_dir / f"{table}.parquet", standard_dir / f"{table}.parquet")`. If any validation fails BEFORE the swap, the tmp dir is cleaned up and the existing output files are untouched (mixed-generation-safe). Wrap the swap in a try/finally that removes the tmp dir.
    - Return `{"race": standard_dir / "race.parquet", "entry": ..., "result": ...}`.

    Then replace the 9 test stubs in tests/pipeline/test_integration.py with real assertions matching the behavior list above. Hermetic tests (1-7) construct synthetic Kaggle+scraped Parquet in tmp_path via the new fixtures; slow-path tests (8-9) read real data/standard/ and are gated.
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v2 && python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds' && ! grep -q 'errors=ignore' src/pipeline/integration.py && ! grep -qE 'odds_trifecta|payoff' src/pipeline/integration.py && grep -q 'SCHEMA_BY_TABLE' src/pipeline/integration.py && grep -q 'validate_integrity' src/pipeline/integration.py && grep -q '_assert_column_set_equality' src/pipeline/integration.py && grep -q 'audit_leakage' src/pipeline/integration.py && grep -q 'os.replace' src/pipeline/integration.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'def integrate_standard_layer' src/pipeline/integration.py` returns a match
    - `grep -c 'SCHEMA_BY_TABLE' src/pipeline/integration.py` >= 1 (allowlist constant)
    - `grep 'errors=.ignore.' src/pipeline/integration.py` returns NO matches
    - `grep 'odds_trifecta\|payoff' src/pipeline/integration.py` returns NO matches (D-05)
    - `grep 'validate_integrity' src/pipeline/integration.py` returns a match (HIGH #8 — real FK validation, not "does not raise")
    - `grep '_assert_column_set_equality' src/pipeline/integration.py` returns a match (HIGH #9)
    - `grep 'audit_leakage' src/pipeline/integration.py` returns >= 2 matches (import + CALL — MEDIUM #13)
    - `grep 'os.replace' src/pipeline/integration.py` returns >= 1 match (HIGH #6 tmp-swap)
    - `grep 'kaggle_input_dir' src/pipeline/integration.py` returns a match (HIGH #5 idempotency)
    - `grep 'raise ValueError' src/pipeline/integration.py` returns >= 2 matches (pre-dedup overlap + column-set mismatch + orphan FK)
    - The 7 hermetic tests pass against synthetic tmp_path data WITHOUT requiring the real scraped corpus
    - The 2 slow-path tests are collected but skipped (smoke-only) OR pass (if D-06 pre-task done)
    - `test_integration_is_idempotent`: two consecutive runs produce byte-identical SHA-256 output
  </acceptance_criteria>
  <done>
    - src/pipeline/integration.py exists with integrate_standard_layer, 3-table allowlist, separate Kaggle input, tmp-swap transactionality, column-set equality assert, validate_integrity call, audit_leakage call
    - 7 hermetic tests green (including idempotency, FK orphan raises, column-set mismatch raises)
    - 2 slow-path tests collected; skipped or green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle + scraped Parquet → unified Parquet | Two independently-produced corpora cross into a single merged output. Schema reconciliation + PK dedup + FK validation are the trust controls. |
| Integration write → existing data/standard/*.parquet | Destructive overwrite (D-03). Atomic write + tmp-swap + regenerable sources are the safety controls. |
| Integration output → Phase 3/7/9 downstream | Mixed-generation corpus would corrupt feature/model/backtest. Tmp-swap (HIGH #6) is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Pre-dedup duplicate race_id (boundary overlap) | mitigate | Per-table `merged[pk].duplicated().sum() == 0` assertion + `validate_integrity` duplicate check RAISE ValueError. Verified by `test_no_duplicate_race_ids_fail_loud`. |
| T-06-06 | Tampering | `pd.concat` without column-set check inflates/masks schema | mitigate | HIGH #9: `_assert_column_set_equality(df, schema, label)` BEFORE reindex. Extra cols / missing cols raise. Verified by `test_column_set_mismatch_raises_before_reindex`. |
| T-06-07 | Denial of Service | Half-written mixed-generation corpus on partial failure | mitigate | HIGH #6: write all 3 to tmp dir, validate, swap via os.replace. Verified by `test_integration_is_idempotent` (and implicit in the transactional structure). |
| T-06-08 | Information Disclosure | Provenance leak via `source` column | mitigate | D-05 anti-pattern guard: no `source` column. Verified by grep gate. |
| T-06-09 | Tampering | Silent FK orphan acceptance (HIGH #8) | mitigate | `validate_integrity` (normalizer.py:263) CALLED; orphan FKs raise ValueError. Verified by `test_referential_integrity_orphan_raises`. |
| T-06-10 | Tampering | Overwriting odds_trifecta/payoff (Phase 5 seed) | mitigate | SCHEMA_BY_TABLE = {race, entry, result} allowlist; sentinel test. |
| T-06-11 | Tampering | Non-idempotent re-run double-merges (HIGH #5) | mitigate | Kaggle read from `kaggle_input_dir` (separate path), not the output path. Verified by `test_integration_is_idempotent` (two runs → identical SHA-256). |
| T-06-12 | Repudiation | audit_leakage imported but never called (MEDIUM #13) | mitigate | audit_leakage CALLED inside integrate_standard_layer; results included in return dict / logged. Verified by `test_no_post_race_leakage_audit_called` (spy or return-dict key). |
| T-06-13 | Denial of Service | Silent skip on missing Kaggle file / scraped partition / table-within-month (MEDIUM #14) | mitigate | Explicit FileNotFoundError / ValueError for each missing-input case. |
| T-06-14 | Tampering | autouse skip swallows hermetic tests (HIGH #7) | mitigate | Two-class split: TestIntegrationHermetic (no autouse) + TestUnifiedCorpus (gated). Hermetic tests run unconditionally. |
| T-06-SC | Tampering | No package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/pipeline/test_integration.py -q -k 'not unified_race_date_range and not row_counts_within_expected_bounds'` green (7 hermetic tests)
- `python -m pytest tests/ -q` full suite green
- `grep 'errors=.ignore.' src/pipeline/integration.py` returns nothing
- `grep 'odds_trifecta\|payoff' src/pipeline/integration.py` returns nothing
- `grep 'validate_integrity\|_assert_column_set_equality\|os.replace\|kaggle_input_dir' src/pipeline/integration.py` returns matches for all four
</verification>

<success_criteria>
- HIGH #5 resolved: integration reads Kaggle from separate input path; idempotent (test proves byte-identical re-run)
- HIGH #6 resolved: corpus-level tmp-swap transactionality
- HIGH #7 resolved: hermetic tests ungated; real-corpus tests gated
- HIGH #8 resolved: FK orphan test injects an orphan and asserts validate_integrity reports it
- HIGH #9 resolved: column-set equality assert BEFORE reindex
- MEDIUMs #12/#13/#14 resolved: 9 tests, audit_leakage called, explicit missing-input errors
- Module ready for Wave 3 invocation against real corpus
</success_criteria>

<output>
Create `.planning/phases/06-data-integration/06-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/pipeline/integration.py` (NEW)
  - `integrate_standard_layer(standard_dir, kaggle_input_dir=None) -> dict[str, Path]` — entry point (HIGH #5 separate input)
  - `_recast_to_canonical(df, schema) -> pd.DataFrame` — raises TypeError
  - `_assert_column_set_equality(df, schema, source_label) -> None` — HIGH #9 (raises ValueError on mismatch)
  - `SCHEMA_BY_TABLE` — 3-table allowlist
  - `PK_BY_TABLE` — PK column per table
  - corpus-level tmp-swap (HIGH #6) via `os.replace`
  - `validate_integrity` call (HIGH #8)
  - `audit_leakage` call (MEDIUM #13)
- `tests/pipeline/test_integration.py` (NEW)
  - `class TestIntegrationHermetic:` (7 tests, NO autouse skip) — HIGH #7
  - `class TestUnifiedCorpus:` (2 tests, autouse `_require_scraped_data`)
  - 9 tests total: `test_no_duplicate_race_ids_fail_loud`, `test_referential_integrity_orphan_raises`, `test_column_set_mismatch_raises_before_reindex`, `test_schema_invariant_post_integration`, `test_no_post_race_leakage_audit_called`, `test_odds_payoff_not_overwritten`, `test_integration_is_idempotent`, `test_unified_race_date_range`, `test_row_counts_within_expected_bounds`
- `tests/pipeline/conftest.py` (MODIFIED)
  - `tmp_kaggle_input_dir(tmp_path, ...)` fixture (HIGH #5 separate input path)
  - `tmp_scraped_partitions_dir(tmp_path, ...)` fixture
